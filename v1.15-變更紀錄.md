# v1.15 變更紀錄

---

## 遊戲大綱（給下一位 AI 接手用）

**遊戲名稱：** CPBL 第七隊擴張計畫  
**類型：** 瀏覽器單機棒球 GM 模擬（純前端，無後端）  
**檔案結構：**
- `index.html` — 主畫面與所有 UI 場景（約 870 行）
- `game.js` — 遊戲核心邏輯（約 3150 行，含所有 Class 與 Event Handler）
- `data.js` — 球員資料（CPBL 2025 本地野手、投手、國際球員、傳奇英雄、教練、開局陣容）
- `style.css` — 全部樣式（約 1950 行）
- `open-game.bat` — 雙擊在瀏覽器開啟遊戲

**主要 Class（game.js 內）：**
| Class | 職責 |
|---|---|
| `Game` | 主遊戲狀態機，管理比賽流程、存檔、UI 更新 |
| `Player` | 球員物件（野手/投手/二刀流），含能力值、體力、特能 |
| `Roster` | 球員名單管理（一軍/二軍切換） |
| `GachaSystem` | 抽卡/球探報告邏輯 |
| `SeasonManager` | 賽季流程（30 場比賽、結算、賽季事件） |
| `CardManager` | 戰術卡片（影分身等） |
| `MedicalCenter` | 防護中心（物理治療、傷病預防） |
| `SaveManager` | localStorage 存讀檔（v3 格式） |
| `CommentaryGenerator` | 實況轉播字串生成 |
| `OpponentAI` | v1.15 新增：對手策略 AI（打擊策略/投球策略/換投） |

**核心比賽流程：**
1. 玩家在「賽前準備」設定先發投手、打線、守位、教練
2. 進入比賽後，逐打席手動操作（正常投球 / 燃燒生命 / 模擬打席）
3. 9 局結束 → 彈出結算 → 回主畫面
4. 球員成長、SP（球探點數）管理、抽卡擴充陣容

**存檔格式（localStorage, key: cpblExpansion_save_v3）：**  
包含：currency、playerShards、roster players 全欄位、pitcherRole、daysOfRest、碎片商店、一軍名單、打序、防守指派、輪值順序、戰績。

---

## v1.15 修正的 8 個問題

### 1. ✅ 球員管理介面改為橫列模式
- 原本：卡片式 grid，在窄螢幕變成單欄豎排，不直覺
- 現在：每位球員佔一橫列（名字、守位、關鍵能力值、體力條、調子、按鈕），一目瞭然
- 一軍/二軍各自一個區塊，列內有「詳細」「守位」「升/降軍」三個按鈕
- CSS 新增：`.player-row`, `.player-row-*`, `.mini-bar`，舊卡片樣式保留（擴張中心仍用卡片）

### 2. ✅ 投手體力條加高
- `.meter { height }` 從 `8px` 改為 `16px`
- 同時加上漸層色：綠色（健康）/ 黃色（疲勞）/ 紅色（危險）

### 3. ✅ 防守位置可以正常變更
- **根本原因：** `cycleDefensePosition()` 原本只找「空的守位」，但 `autoAssignDefense()` 把所有守位都填滿後，就永遠找不到空位，導致點「守位」按鈕沒有反應
- **修法：** 直接移到下一格守位，若目標已有人 → 兩人互換（swap），不再跳過有人的格子
- 球員管理頁的「守位」按鈕也直接呼叫 `cycleDefense()`，與賽前準備頁相同邏輯

### 4. ✅ 結算畫面關閉後回主畫面
- `closeSummary()` 關閉 modal 後呼叫 `changeScene('lobby')`
- 避免結算後停留在比賽畫面造成混亂

### 5. ✅ 對手球隊加入 AI 策略
- 新增 `OpponentAI` class，每次打席前呼叫 `decide()`
- **對手進攻時（top half）：**
  - 落後 3+ 分 → 積極揮擊（pow↑, discipline↓）
  - 領先 2+ 分且 7 局後 → 消極等球（discipline↑）
  - 有跑壘員且 2 出局 → 積極揮擊
  - 其餘 → 普通
- **對手守備時（bottom half）：**
  - 領先且壘上有人且未滿 2 出 → 變化球誘騙
  - 落後 2+ 分 → 速球強攻
  - 8 局後且不落後 → 引誘出棒
  - 其餘 → 速球/均衡/變化球隨機
- **自動換投：** 對手先發體力 < 25% 且超過 5 局 → 自動換上牛棚（找體力 > 25 的後援投手）
- AI 策略會在實況轉播顯示「⚙ 對手策略：...」

### 6. ✅ 燃燒生命消耗大幅降低
- **原因：** 一次打席最多 9 球，每球體力扣 2 × 3倍 = 每球扣 6，一場打席最多扣 54，實際平均 4-5 球 = 約 24-30 點，確實太高
- **修法：**
  - `consumeStamina()` 的 burnLifeActive 乘數：`3` → `1.5`
  - 每球基礎消耗統一為 `1`（原本燃燒生命時傳入 `2`，現在統一 `1`）
  - 效果：燃燒生命每球扣 `1 × 1.5 = 1.5`，一次打席約 6-8 點（原本 24-30 點）

### 7. ✅ 比賽操作介面增大、減少需要捲動的情況
- `.match-grid` 從 `minmax` 彈性欄位改為固定欄位（`400px / 1fr / 240px`）
- `.control-row` 從 3 欄改為 **4 欄**，按鈕 `min-height` 提高至 `48px`，字體更大
- 棒次列（`#player-lineup p`、`#opponent-lineup p`）文字縮小至 12px、間距壓縮

### 8. ✅ UI 全面升級
- **HUD bar：** 更深的玻璃擬態、綠色邊框光暈、投影增強
- **操作按鈕（control-row）：** 更大、有陰影、更明顯的 active 狀態（綠色光暈）
- **策略按鈕：** hover 時微縮放效果
- **體力條：** 加漸層（綠 → 淺綠 / 黃 → 淡黃 / 紅 → 粉紅）
- **實況轉播：** 文字縮至 12.5px 提高密度
- **賽後結算 modal：** 更大圓角（14px）、更深陰影
- **大廳按鈕：** hover 時向上位移更多 + 綠色光暈
- **分數板：** 字體再放大（52px）
- **球員卡片（card grid）：** 圓角、懸停上浮效果

---

## 給下一位 AI 的工作建議

1. **新增守位選單：** 目前「守位」按鈕是循環式，可考慮改成下拉選單讓玩家直接選守位，更直覺
2. **對手 AI 加強：** `OpponentAI.decide()` 目前每打席呼叫一次，未來可加入更多場上判斷（滿壘故意四壞球、盜壘反應等）
3. **對手換投 UI：** AI 換投目前只改 `opponentTeam.pitcher`，若對手的 bullpen 陣列結構與 player side 不同，需確認 `g.opponentTeam.bullpen` 是否存在
4. **橫列模式再優化：** 小螢幕時 `.player-row` 6 欄 grid 可能過擠，需加 `@media` 折疊
5. **存檔版本號：** v1.15 沒有新增存檔欄位，版本號維持 v3 不需升版

---

## 驗證狀態

- ✅ `game.js` 語法正確（OpponentAI class 定義在 resolveAtBat 之前）
- ✅ `game.opponentAI = new OpponentAI(game)` 在 `let game = new Game()` 之後初始化
- ✅ `closeSummary()` 結束後會呼叫 `changeScene('lobby')`（changeScene 在 index.html 中定義，game.js 後面才載入，所以用 `typeof` 檢查）
- ✅ `cycleDefensePosition` 修正允許互換，不再因滿員卡住
- ✅ 燃燒生命乘數從 3× 降為 1.5×，基礎消耗統一為 1
- ✅ 球員管理橫列模式完整渲染（含守位按鈕）
- ✅ 體力條高度 8px → 16px，加漸層色

— 這份說明由 Claude 在 v1.15 開發完成後撰寫。
