# v1.16 變更紀錄

---

## 版本摘要

v1.16 主要針對 `v1.15` 之後玩家回報的操作問題與畫面體驗進行修正，重點放在：

- 修復進攻時會被錯誤切換成對手投球策略的問題
- 讓防守位置調整重新可用，而且更直覺
- 把球員管理頁改回卡牌式介面
- 強化對手 AI，讓比賽不再像只有單邊在操作
- 重新整理比賽主畫面 UI，讓主要操作區更集中

---

## 本次更新重點

### 1. 修正進攻流程錯誤

- 修正玩家進攻時，對手投球策略介入後造成打席流程異常的問題
- 修正打席內策略計算順序，避免因變數初始化時機錯誤而影響進攻
- 實測後，進攻按鈕與強攻按鈕皆可正常觸發，不會再卡住

### 2. 對手 AI 強化

- 新增對手進攻策略判斷
  - 落後時會提高積極度
  - 關鍵局數會偏向高風險高報酬打法
- 新增對手投球策略判斷
  - 會依局數、出局數、壘上狀況切換速球、變化球、引誘球等配球思路
- 新增對手技能使用
  - AI 會在高張力局面判斷是否使用燃燒生命
- 新增對手牛棚調度
  - 對手先發體力過低時，會自動換上後援投手

### 3. 防守位置系統修正

- 修正原本「看得到守位但無法真正變更」的問題
- 現在球員卡上可直接透過下拉選單指定守位
- 若目標守位已有球員，系統會自動互換位置，而不是直接失敗
- `輪換守位` 也重新可用，會優先依球員原生守位循環
- 非先發打線中的球員不允許直接指定守位，避免資料狀態混亂

### 4. 球員管理頁重做

- 將 v1.15 的橫列文字名單改回卡牌式顯示
- 每張卡片現在會顯示：
  - 球員等級
  - 球隊與定位
  - 主要能力
  - 體力條
  - 特性標籤
  - 防守位置控制
  - 詳細資訊 / 登板 / 指定打者 / 升降級按鈕
- 一軍與二軍重新分區顯示，辨識更直觀

### 5. 燃燒生命平衡調整

- 降低燃燒生命的體力消耗倍率
  - 從 `1.5x` 調整為 `1.2x`
- 降低單球投球體力消耗
  - 從 `1` 調整為 `0.85`
- 保留技能的爆發感，但不再出現「一個打席燒掉過量體力」的誇張情況

### 6. 比賽 UI 視覺重整

- 重新整理 HUD 版面
  - 資源、戰績、賽程、對手資訊集中成同一排
- 放大並加厚投手體力條
  - 顏色更清楚
  - 警戒與危險狀態更容易辨識
- 整理主操作區按鈕尺寸與排列
  - 守備投球 / 燃燒生命 / 模擬守備 / 盜壘 / 自動模擬等操作更集中
- 壓縮次要資訊區塊高度
  - 讓主要操作內容更容易留在首屏
- 更新樣式載入版本參數
  - 避免瀏覽器快取舊 CSS，導致看不到新介面

### 7. 結算畫面返回流程修正

- 結算視窗關閉後，現在會強制回到主畫面
- 即使場景狀態殘留，也會重新指定 `Lobby` 為當前畫面

---

## 實際覆蓋的檔案

這次已直接覆蓋原始檔案：

- `game.js`
- `index.html`
- `style.css`

新增檔案：

- `v1.16-變更紀錄.md`

---

## 測試與確認

本次已完成以下檢查：

- `game.js` 語法檢查通過
- `data.js` 語法檢查通過
- `index.html` 內嵌 script 解析通過
- 本機頁面已實際打開檢查
- 球員卡管理頁已確認可顯示卡牌與守位控制
- 進攻按鈕已確認不再報錯
- 對手 AI 策略切換已確認可正常運作

---

## 備註

如果後續要做 `v1.17`，我會建議接著補兩塊：

- 進一步優化比賽畫面在小螢幕下的資訊密度
- 補上更完整的 AI 換人、守備與跑壘判斷
