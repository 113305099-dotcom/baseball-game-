# v1.17 更新手冊

---

## 版本摘要

v1.17 主要把比賽視覺改成像素風，並把賽前守位、球員頭像、記分板與自動模擬流程重新整理成比較像「棒球遊戲」的操作方式。

本版重點：

- 全站視覺改成像素球場風格
- 球員頭像優先使用中華職棒官方網站球員照，並套用像素化顯示
- 取消單打席的「模擬進攻 / 模擬守備」操作
- 「全場自動」會一路跑完整場比賽，不再因關鍵時刻暫停
- 比賽頁新增復古記分板，顯示 B / S / O、逐局比分、R / H / E
- 賽前守位改成球場圖拖放，可把球員拖到指定守備位置

---

## 操作方式

### 1. 賽前守位

- 進入 `PLAY 開始賽程`
- 在賽前準備頁找到守備位置圖
- 可把打線中的球員拖曳到球場上的 `C / 1B / 2B / 3B / SS / LF / CF / RF / DH`
- 可把板凳球員拖到守位上，系統會自動替換該守位原本球員所在的棒次
- 也可以拖曳兩個守位互換球員
- 如果球員不適合該守位，位置卡會顯示守位懲罰

### 2. 比賽頁記分板

- 比賽開始後，上方會出現像素風記分板
- `BATTER` 顯示目前打者
- `BALL / STRIKE / OUT` 顯示目前球數與出局數
- 下方逐局表顯示第 1 到第 9 局得分
- `R / H / E` 顯示總得分、安打、失誤

### 3. 全場自動

- 舊的單打席 `模擬進攻 / 模擬守備` 已移除
- 按下 `全場自動` 後，系統會自動跑完完整比賽
- 不會因為得點圈、後段局數、比分接近或投手體力低而停下
- 比賽結束後仍會跳出結算畫面，關閉後回到主畫面

### 4. 球員頭像

- 本地 CPBL 球員會優先讀取中華職棒官方網站的球員照
- 顯示時會使用像素化濾鏡與像素邊框
- 如果官方圖片無法載入，會自動退回遊戲內建像素頭像

---

## 本次更新重點

### 1. 像素風視覺重做

- 全站背景、面板、按鈕、卡牌改成像素風材質
- 加入像素格線、硬邊框、低解析度遊戲感陰影
- 主要標題改用像素字體
- 球員卡、HUD、比賽操作按鈕都統一成像素介面

### 2. 官方球員照像素化

- 於 `game.js` 新增官方球員照對照表
- 目前已對接多名本地 CPBL 球員官方頁面的大頭照
- 支援未來在資料中直接加入 `officialImage` 或 `cpblImage`
- 圖片載入失敗時會自動 fallback，不會讓卡牌破圖

### 3. 全場自動模擬

- 移除關鍵時刻暫停條件
- 自動模擬現在只會在比賽結束或玩家手動停止時停下
- 賽季結算時會主動關閉自動模擬，避免一路跑到下一場
- 舊版 `auto-sim` 單打席按鈕已從畫面移除

### 4. 復古記分板

- 新增 `classic-scoreboard`
- 支援逐局比分紀錄
- 新增安打數統計
- 預留失誤欄位
- 比賽流程中得分會寫入當局，並同步更新總分

### 5. 守位球場圖拖放

- 賽前守位從文字格子改成球場位置圖
- 支援從打線拖曳到守位
- 支援從板凳拖曳到守位並替換棒次
- 支援守位互換
- `DH` 以獨立區塊呈現，避免和實際場上守位混在一起

---

## 實際覆蓋的檔案

本次已直接覆蓋原始檔案：

- `game.js`
- `index.html`
- `style.css`

新增檔案：

- `v1.17-變更紀錄.md`

---

## 測試與確認

已完成以下檢查：

- `game.js` 語法檢查通過
- `data.js` 語法檢查通過
- `index.html` 內嵌 script 解析通過
- 本機頁面已打開檢查
- 賽前守位圖已確認可渲染
- 比賽頁記分板已確認可渲染
- `全場自動` 按鈕已確認存在
- 舊的 `模擬進攻 / 模擬守備 / 模擬打席` 按鈕已移除
- 瀏覽器 console 未出現錯誤

---

## 備註

官方球員照路徑來自中華職棒官方網站球員頁。若官方日後更換圖片位置，遊戲仍會自動退回像素頭像；之後可再補一個圖片更新工具，把新官方路徑批次寫回資料檔。
