# v1.20 變更紀錄

本版針對使用者回饋修正兩件事：球員管理頁可讀性、球員立繪從「照片像素化」改為「依照片特徵重繪成像素小人」。主畫面暫時沿用 v1.19 原始主視覺，不再套用 v1.20 重繪或局部修正版。

## 1. 主畫面球場圖狀態

- 暫時沿用原本 v1.19 主畫面圖：`art-assets/production/backgrounds/pc-2000s-lobby-ballpark.png`
- 不再引用 `pc-2000s-lobby-ballpark-v120.png`。
- 不再引用 `pc-2000s-lobby-ballpark-batterbox-fix.png`。
- 打擊區方向修正延後，之後只做局部修補，不重繪整張主視覺。

![沿用原始主畫面球場圖](art-assets/production/backgrounds/pc-2000s-lobby-ballpark.png)

## 2. 球員管理頁可讀性修正

- 修正球員一句話介紹、體力列、能力格、球員 meta 標籤與特能標籤的文字顏色。
- 將深色半透明資訊框改成復古米黃色高對比面板。
- 保留 v1.19 的 2000 年日系 PC 像素風，但降低「文字與背景混在一起」的問題。

## 3. 球員立繪改為重繪式像素小人

- 新增輸出資料夾：`art-assets/production/portraits/redrawn/`
- 新增 manifest：`art-assets/production/portraits/portrait-redrawn-manifest.js`
- 新增檢查用 contact sheet：`art-assets/production/portraits/redrawn-portrait-contact-sheet.png`
- 目前已產生 43 位球員的重繪式像素小人。
- 新流程會先讀取官網照片來源，抽取膚色、髮色、球帽 / 球衣主色，再套用日系復古 chibi baseball 模板重畫。
- 遊戲會優先使用 `LOCAL_REDRAWN_PIXEL_PORTRAITS`，沒有重繪圖時才退回 v1.19 的照片像素化圖，再退回即時遠端照片像素化。

![v1.20 重繪式球員像素小人樣張](art-assets/production/portraits/redrawn-portrait-contact-sheet.png)

## 4. 程式與工具

- `index.html`
  - CSS cache version 更新為 `style.css?v=1.20`。
  - 新增載入 `portrait-redrawn-manifest.js`。
- `game.js`
  - `getLocalPixelPortraitUrl()` 會優先讀取重繪式頭像 manifest。
  - 本地重繪頭像加入 `redrawn-pixel-portrait` class，避免被舊照片裁切樣式影響。
- `style.css`
  - 新增 v1.20 override block。
  - 主畫面維持原始 v1.19 球場圖。
  - 修正球員管理頁與詳情 modal 的文字對比。
- `tools/build-redrawn-pixel-portraits.ps1`
  - 產生依照片特徵重繪的球員像素小人、manifest 與 contact sheet。

備註：`tools/build-v120-loading-art.ps1` 與 `pc-2000s-lobby-ballpark-v120.png` 保留在檔案中作為失敗草稿紀錄，但目前遊戲不引用它們。

## 5. 後續建議

- 這批重繪式小人已經不再是單純馬賽克，但仍屬於自動化第一版。
- 下一步建議挑 5 至 8 位你最在意的球員手修：臉型、髮型、鬍子、膚色、球衣配色會更像本人。
- 若你確認這個方向，我再把同樣流程擴展到教練、商店卡、賽前先發、醫療頁與比賽 HUD。
