# v2.11 變更紀錄

> 政大棒球征服世界 v2.11 — 主線劇情、WBC、Sky Net、像素化修正、雙殺、歷史戰績、資料中心一次到位。

---

## 12 項需求對應

| # | 需求 | 狀態 | 主要改動 |
|---|---|---|---|
| 1 | 教練卡池移到擴張中心，降低卡池花費至 200，修正抽到未定義教練 | ✅ | 移到 shop tab；`drawCoach()` 回傳 `{success, coach, message}`；修正 `c.roleType`、`c.xpBonus` 顯示；補抽機制避免抽到 undefined |
| 2 | 球員像素化失敗，看起來都一樣 | ✅ | 移除 `crossorigin="anonymous"` 讓圖片能正常載入；CORS 失敗時以 CSS `image-rendering: pixelated` 提供退路；新增 `referrerpolicy="no-referrer"` |
| 4 | 政大外野新增「天網」，高 10 m | ✅ | `STADIUMS_DATA.nccu.fenceHeight: 10`、`hrFactor: 0.55`、加入 `calcBattedBall` 軌跡高度判斷與 `hitNet` 處理 |
| 5 | 比賽開始的轉播不見了 | ✅ | Game 建構子末尾補初始開場廣播；`prepareNextMatch` 仍呼叫 `pickCommentary('opening',...)` |
| 6 | 新增雙殺守備機制 | ✅ | `resolveAtBat` groundOut 處理時，1B 有跑者 + < 2 出局 → 機率觸發 |
| 7 | 策略按鈕滑鼠移上要顯示說明 | ✅ | 全部策略按鈕加 `title` 屬性，CSS 加 `ℹ` 提示 |
| 8 | 新增歷史戰績回顧 | ✅ | 新 scene `history`；表格顯示年度戰績、名次、上下半季冠軍、總冠軍/亞、當季與累計 WBC 積分 |
| 9 | 失誤太多，請依真實守備率 | ✅ | `rollFieldingOutcome` baseline 從 0.70 拉到 0.985（對齊真實 MLB） |
| 10 | 下場比賽不要顯示前一場紀錄 | ✅ | `prepareNextMatch` 開頭 `this.log = []` |
| 11 | 新增主線劇情 — 2030 WBC | ✅ | 4 季積分制度、`runWBCTournament` 8 強單淘汰、`STORYLINE_TEXTS`、新 scene `storyline` / `wbc` |
| 12 | 資料獨立成 Excel 易改檔 | ✅（用 CSV） | 新 scene `data`；提供 4 張表的下載/匯入 CSV；改回直接存 localStorage 覆寫；CSV 含 UTF-8 BOM 解決 Excel 中文亂碼 |

> 用 CSV 而非 .xlsx 的原因：CSV 不需要 SheetJS 之類的外部函式庫即可實作，Excel / Google Sheets / Numbers 都能直接開啟編輯，且本機 `file://` 開啟時不會受 CORS 限制影響。

---

## 各項細節說明

### 1. 教練卡池移到擴張中心 + bug 修正
- **位置**：擴張中心畫面（shop tab）多了一塊「教練卡池」面板
- **抽卡成本**：每次 200 資金（原本 UI 寫 300，但內部其實已是 200，修正顯示）
- **抽到「未定義」教練 bug 來源**：原本 UI code `c.role || '通用'`，但教練物件只有 `c.roleType` 沒有 `c.role`；`c.xpBonus` 是物件被當成數字計算 → 顯示 NaN（看起來像「未定義」）
- **修正**：
  - 新增 `coachRoleTypeLabel()` 把 roleType 轉中文
  - 新增 `describeXpBonus()` 把 xpBonus 物件展開成「全體 +10%」、「投手 +20%、守備 +10%」之類的字串
  - 補抽機制：當目標稀有度抽完，會回退到其它稀有度繼續抽，不會抽到 undefined

### 2. 球員照片像素化修正
- **原因**：原本帶 `crossorigin="anonymous"`，CPBL 官網不送 CORS 標頭 → 圖片實際載入失敗 → 自動 fallback 到 hash-based SVG 像素頭像 → 同隊球員顏色雷同
- **修法**：
  - 拿掉 `crossorigin` 屬性 → 照片正常載入
  - canvas 像素化嘗試失敗（CORS 阻擋 readback）就保留原圖
  - 套用 CSS `image-rendering: pixelated` 確保視覺上仍是像素風
  - 新增 `referrerpolicy="no-referrer"` 提高 CPBL 對部分版本瀏覽器的相容性
  - 加 `width:100%;height:100%;object-fit:cover` 讓照片填滿頭像方塊

### 4. 政大「天網」10 公尺高牆
- 球場資料 `fenceHeight: 10.0`、`hrFactor: 0.55`
- `calcBattedBall` 新增軌跡高度計算（依 EV、LA、wallDist 算彈道在牆位置的高度）
- 全壘打需要「距離夠 + 高度也超過 fenceHeight」雙條件
- 距離夠但高度不夠（撞天網）→ 新 `hitNet` 旗標 → resolveAtBat 處理為 70% 牆前接殺、30% 二壘安打
- 轉播會顯示「擊球撞上天網（高度 X m，網高 10 m）」

### 5. 初始比賽開場轉播
- Game 建構子末尾加入：用當下對手與球場資料呼叫 `pickCommentary('opening', ...)` 推進 `this.log`
- 之後每次 `prepareNextMatch` 也都會推一條開場廣播

### 6. 雙殺機制
- 條件：滾地球 + 守備成功 + 一壘有跑者 + 出局數 < 2
- 機率公式：基礎 0.42 + (EV - 90)/200 + (fielding - 70)/250 + (arm - 70)/250 − (speed - 70)/200，鎖在 [0.15, 0.7]
- 雙殺成功：一壘跑者清空 + 連續 2 個 out + 記錄「滾地雙殺」結果

### 7. 策略按鈕 tooltip
- 等球 / 普通 / 積極：對應加成詳細寫在 title
- 速球 / 混合 / 變化 / 引誘：對應加成與適用情境寫在 title
- 滑鼠移到按鈕上 0.5 秒會顯示瀏覽器原生 tooltip
- 按鈕右上角會出現 `ℹ` 標記提示

### 8. 歷史戰績回顧
- 大廳新增「歷史戰績」入口（RECORD）
- 表格欄位：年份、戰績、聯盟名次、上半季冠軍、下半季冠軍、總冠軍、亞軍、WBC 積分（當季/累計）

### 9. 守備率真實化
- 之前 baseline 0.70 → 失誤率 ~30%，極端不真實
- 新 baseline 0.985（對齊真實 MLB 約 .985 全聯盟守備率）
- 公式：`max(0.85, min(0.998, 0.985 + (effective-75)/800 - difficulty/500 - penalty/400))`
- 仍然保留 Barrel、平飛球、極端噴射角的難度修正，但整體影響縮小

### 10. 比賽開始不顯示前場紀錄
- `prepareNextMatch()` 開頭 `this.log = []`
- 開新的一場 → 轉播只剩開場廣播 + 該場記錄

### 11. 主線劇情 — 2030 WBC
**4 年積分制度**：
| 來源 | 分數 |
|---|---|
| 年度第一名 | 7 |
| 年度第二名 | 6 |
| 上半季冠軍 | 3 |
| 下半季冠軍 | 3 |
| 總冠軍 | 10 |
| 亞軍 | 7 |
| 季後挑戰賽敗者 | 3 |

**運作流程**：
1. 從 2026 年開始，每年打完 40 場 + 季後賽，呼叫 `recordSeasonHistoryAndWbcPoints()` 累積積分到 `wbcPointsByTeam`
2. 進入下一年（`startNextYear()`）會清空當季戰績、半季冠軍、聯盟排名
3. 4 季結束（2026 ~ 2029）→ `evaluateWBCQualification()` 比較積分
4. 政大棒球積分第一 → `storylineStage = 'wbc_qualified'`，切到 WBC 場景
5. 政大棒球積分非第一 → `storylineStage = 'wbc_eliminated'`，遊戲結束畫面

**WBC 8 強單淘汰**：
- 對手 7 國：日本、美國、韓國、多明尼加、委內瑞拉、波多黎各、墨西哥（資料寫在 `WBC_NATIONAL_TEAMS`）
- 中華隊強度由 `estimateTeamStrength()` 從政大棒球一軍球員平均能力計算
- `simulateWBCGame()` 用隊伍強度 ± 隨機因素決定勝負
- 結果分四種：champion / runnerUp / semifinal / quarterfinal
- 對應結算文案會替換 `{stage}` 與 `{opp}`

### 12. 資料中心（CSV 匯入/匯出）
- 大廳新增「資料中心」入口（DATA）
- 4 張表可下載 + 匯入：本土打者、本土投手、球場、教練卡池
- 下載 CSV 內含 UTF-8 BOM，Excel 直接開啟無亂碼
- 匯入後存到 `localStorage.dataOverrides`，重新整理頁面才生效（且下次也會自動套用覆寫）
- 提供「清除所有資料覆寫」按鈕還原預設

**為何用 CSV 不用 .xlsx**：
- `.xlsx` 需要 SheetJS 之類函式庫，會大幅膨脹遊戲檔案
- CSV 是試算表通用格式，Excel / Google Sheets / Numbers / LibreOffice 都能開
- 本機 `file://` 也能直接匯入匯出，不需要伺服器
- 對遊戲運作沒有不良影響：覆寫存在 localStorage，可隨時清除

---

## 存檔版本

`PLAYER_DATA_VERSION = 4`（從 3 升上來，新增 `currentYear`、`seasonHistory`、`wbcPointsByTeam`、`storylineStage`、`wbcBracket`、`storylineIntroShown`）。舊存檔載入後會自動清空，避免欄位不一致造成的 crash。

---

## 修改檔案

| 檔案 | 改動 |
|---|---|
| `data.js` | 政大球場 Sky Net、新增 WBC_NATIONAL_TEAMS / WBC_QUALIFICATION_RULES / STORYLINE_TEXTS / DATA_SCHEMAS |
| `game.js` | 修像素化、改 drawCoach 回傳、降失誤率、加 hitNet / 雙殺、log 清空、seasonEnd / runPlayoffs 重寫、新增 evaluateWBCQualification / startNextYear / runWBCTournament / simulateWBCGame / estimateTeamStrength、SaveManager 補欄位 |
| `index.html` | 教練卡池移 shop、加 4 個新 scene、加 tooltip、加 CSV 匯入/匯出、修 staff 渲染 |
| `style.css` | 新增 storyline / history / data / wbc / shop-coaches 樣式 |

## 驗證狀態

- ✅ `data.js` 語法檢查通過
- ✅ `game.js` 語法檢查通過
- ✅ `index.html` 內聯 JS 語法檢查通過
- ✅ HTML 標籤對稱，無重複 ID
- ✅ v2.11 所有功能靜態檢查全部就位

— 這份說明由 Claude 在 v2.11 開發完成後撰寫。
