# v3.11 更新書

更新日期：2026-05-17

## 本週期目標

本週期的核心目標是把投打對決從大型單檔流程，整理成更容易維護的模組化架構。原則是：後台先決定結果，前端只負責演出結果。

## 已完成事項

### 1. 投球與打擊模型強化

- `pitch-engine.js` 已成為單球判定核心，負責控球落點、揮棒決策、接觸判定、界外球閥門與進場物理種子。
- 打者 heat map 已實際影響接觸與力量修正，不再只是資料欄位。
- 進場球會產生 `inPlay` 資料，供守備、跑壘與動畫後續使用。

### 2. 場內球守備引擎

- 新增 `fielding-engine.js`，負責擊球落點、飛行時間、守備員候選、守備成功率、失誤、雙殺、安打與出局判定。
- `visualTimeline.ball_arrives` 已改為結果感知：出局與失誤會指向實際處理點，乾淨安打才指向落點。
- 修正守備候選排序，當多名守備員成功率相同時，會以守備範圍差距與移動距離決定較合理的人選。

### 3. 防守狀態建構

- 新增 `defense-state-builder.js`，負責把球員名單、打序、守備位置、對手打序與球場資料轉成守備引擎可讀格式。
- 修正舊存檔或未排守位時只剩投手可選的問題。
- 即使守位資料缺失或指向不存在的球員，也會從打序自動補齊守備員。

### 4. 跑壘引擎

- 新增 `baserunning-engine.js`，負責保送、安打、長打、全壘打與失誤上壘的跑者推進。
- `game.js` 的 `advanceRunners()` 已改為委派給跑壘引擎。
- 失誤上壘不會被記為安打。

### 5. 結果套用層拆分

- 新增 `pitch-result-applier.js`，負責未進場結果：壞球、看著好球、暴投、揮空、界外、保送、三振。
- 新增 `in-play-result-applier.js`，負責進場結果：安打、出局、失誤、雙殺、牆面球、全壘打、得分、XP、旁白與球數重置。
- `game.js` 現在更像協調器：負責 stamina、狀態保存、UI 更新與模組串接。

### 6. 守備動畫與結果同步

- `battle-scene.js` 的守備畫面已改用 `FieldingEngine.POSITIONS`，避免前端與後台座標不同步。
- 球的動畫目標改吃 `visualTimeline.ball_arrives`，不再自己根據方向文字猜落點。
- 修正「文字顯示中外野方向，但實際投手處理」造成視覺矛盾的問題。
- 修正右外野球可能錯選左外野追球的候選排序問題。

### 7. 未來素材接點

- `battle-scene.js` 新增 `window.GameAnimationAssets.drawFielder(ctx, actor)` hook。
- 未來可用這個 hook 替換目前陽春方塊守備員。
- `actor` 會帶入 `x`、`y`、`scale`、`state`、`team`、`position`、`label`、`player`、`selected`。
- 目前預期狀態有 `idle`、`run`、`field`、`miss`。

### 8. 交接文件

- 更新 `ENGINE_HANDOFF.md`，記錄目前模組邊界、資料預設值與下一步安全重構目標。
- 新增本檔作為 v3.11 週期紀錄。
- 新增 `PROJECT_OVERVIEW.md` 作為下一位 AI 的專案快速導覽。

## 已驗證項目

- `node --check` 已通過：
  - `game.js`
  - `pitch-engine.js`
  - `pitch-result-applier.js`
  - `fielding-engine.js`
  - `defense-state-builder.js`
  - `baserunning-engine.js`
  - `in-play-result-applier.js`
  - `battle-scene.js`
  - `game-renderer.js`
- 煙霧測試已確認：
  - 保送可正確推進跑者。
  - 兩好球界外不增加好球數。
  - 失誤上壘不記安打。
  - 缺守位資料時可補滿 9 人防守。
  - 右外野方向球會選 `RF`。
  - 三游方向滾地球會選 `SS`。
  - 中外野飛球會選 `CF`。
  - 短飛球由投手處理時，動畫目標與結果一致。

## 尚未解決事項

### 1. 實際球員資料庫

- 目前仍依賴既有 `data.js` 與通用能力轉換。
- 尚未完成完整球員資料庫、資料更新流程與手動覆寫規格。
- 未來要決定資料來源、欄位格式、版本控管與更新頻率。

### 2. 美術與動畫

- 目前守備員仍是 canvas 方塊 fallback。
- 尚未建立正式 PNG sprite manifest。
- 尚未完成投球、打擊、跑壘、接球、傳球、滑壘、漏接、撲接等動畫資產。
- `GameAnimationAssets.drawFielder()` hook 已準備好，但尚未接入實際素材。

### 3. 傳球與跑者時間軸

- `visualTimeline` 目前已能同步球與守備員處理點。
- 尚未完整表示傳球、跑者起跑、進壘、滑壘、夾殺、外野回傳。
- 跑壘目前仍是結果式簡化推進，尚未完全時間軸化。

### 4. 守備細節

- 守備模型已可用，但仍是通用演算法。
- 尚未細分守備員起步方向、轉身、接球姿勢、牆邊球、內野反彈球、外野落地後處理。
- 失誤類型目前有傳球、接捕、判斷，但尚未接完整動畫。

### 5. `game.js` 仍然偏大

- 核心對決已拆出多個模組，但 `game.js` 仍包含管理、賽季、旁白、資料、UI 協調等許多責任。
- 下一步應繼續拆：
  - 賽季與 WBC 流程。
  - 管理/陣容操作。
  - 旁白資料與文字 mapping。
  - helper 常數與舊資料轉換。

### 6. 瀏覽器端完整回歸測試

- 已做語法與引擎煙霧測試。
- 尚未建立自動化瀏覽器測試。
- 建議之後用 Playwright 或簡易 debug panel 記錄每次 `lastPitchContext`、`lastInPlayContext`、`playResult`、`visualTimeline`。

## 架構判斷

目前基礎架構方向是正確的。

原因：

- 結果判定已集中在引擎與 applier，不再散落於畫面。
- 動畫只讀 `lastPitchContext` 與 `lastInPlayContext`，不決定勝負。
- 守備、跑壘、投球、結果套用都已拆成單一責任模組。
- 未來換 PNG 或動畫，應優先接 `GameAnimationAssets`，不需要修改投打或守備判定。

## 下一步建議

1. 建立 `animation-assets.js` 與 sprite manifest，先支援守備員 `idle/run/field/miss`。
2. 擴充 `visualTimeline`，加入 `throw_start`、`throw_arrives`、`runner_start`、`runner_arrives`。
3. 把賽季、WBC、管理操作從 `game.js` 拆出去。
4. 建立 debug overlay，顯示每次場內球的 `selected.position`、`playResult.code`、`ball_arrives.point`。
5. 規劃正式球員資料庫格式，避免未來美術、數據與遊戲邏輯互相卡住。
