# v3.12 更新書

更新日期：2026-05-17

## 本週期目標

本週期依照使用者要求，暫時不處理完整球員資料庫與正式美術動畫，改補齊其他基礎架構缺口：

- 場內球傳球與跑者時間軸。
- 可讀取引擎狀態的 debug panel。
- 可重複執行的核心引擎煙霧測試。
- 文件同步更新。

## 已完成事項

### 1. `visualTimeline` 擴充

`fielding-engine.js` 的 `visualTimeline.events` 已新增下列事件：

- `runner_start`
- `runner_arrives`
- `throw_start`
- `throw_arrives`

這代表場內球不再只有球與守備員移動，現在也有資料層的跑者與傳球事件。這些事件目前是簡化時間軸，不等同最終完整跑壘 AI，但已足以作為後續動畫與 debug 的資料來源。

### 2. 跑者事件

目前會輸出：

- 打者從本壘跑向一壘、二壘或三壘。
- 雙殺時一壘跑者往二壘。
- 失誤時壘上跑者強制推進。

跑者時間估算改用棒球壘間距，不再用畫面斜角座標計算，避免本壘到一壘時間過長。

### 3. 傳球事件

目前會輸出：

- 滾地出局往一壘傳球。
- 雙殺情境往二壘與一壘的傳球事件。

傳球時間會依守備員 arm/velocity 能力估算。

### 4. 守備畫面讀取新時間軸

`battle-scene.js` 已支援讀取：

- `runner_start`
- `runner_arrives`
- `throw_start`
- `throw_arrives`

目前以簡化線條與小球表示跑者路徑與傳球路徑。這不是正式美術動畫，但已把資料流與畫面層接起來。

### 5. Debug Panel

新增 `engine-debug-panel.js`。

功能：

- 可顯示最近一次投球結果。
- 可顯示最近一次場內球結果。
- 可顯示守備員、擊球資訊與 `visualTimeline` 事件。
- 可用 `Ctrl+Shift+D` 開關。
- 也可用網址參數 `?debug=1` 或 localStorage 啟用。

這能讓下一位 AI 或開發者不用讀 console，也能在遊戲畫面中確認引擎資料是否合理。

### 6. 回歸煙霧測試

新增 `tools/smoke-test-engines.js`。

目前測試：

- `DefenseStateBuilder` 可補滿 9 人防守。
- 三游方向滾地球會選 `SS`。
- 右外野方向平飛球會選 `RF`。
- 場內球時間軸包含跑者與傳球事件。
- 失誤上壘不記安打。
- 保送由 `PitchResultApplier` 正確處理。

執行方式：

```bash
node tools/smoke-test-engines.js
```

### 7. 文件更新

- 更新 `PROJECT_OVERVIEW.md`。
- 更新 `ENGINE_HANDOFF.md`。
- 新增本檔作為 v3.12 週期紀錄。

## 已驗證項目

已通過語法檢查：

- `game.js`
- `pitch-engine.js`
- `fielding-engine.js`
- `defense-state-builder.js`
- `pitch-result-applier.js`
- `in-play-result-applier.js`
- `battle-scene.js`
- `engine-debug-panel.js`
- `tools/smoke-test-engines.js`

已執行：

```bash
node tools/smoke-test-engines.js
```

結果：

```text
engine smoke tests passed
```

## 尚未解決事項

### 1. 完整球員資料庫

本週期依使用者要求暫不處理。

### 2. 正式美術動畫

本週期依使用者要求暫不處理。

目前僅補好資料 hook 與簡化視覺提示。

### 3. 跑壘仍未完全物理化

目前 `runner_start/runner_arrives` 是視覺時間軸提示。

尚未完成：

- 依外野手傳球能力決定跑者是否多推進。
- 跑者速度、起跑判斷、教練策略與傳球路線的完整對抗。
- 夾殺、滑壘、衝本壘、回壘等細節。

### 4. 雙殺傳球仍是簡化表示

目前雙殺會輸出往二壘與一壘的傳球事件，但尚未精準區分：

- 先傳二壘再傳一壘。
- 游擊/二壘手補位。
- 一壘手接球腳踩壘包。

這是下一輪守備時間軸細化的重點。

### 5. Debug Panel 仍是開發工具

目前 panel 只顯示引擎摘要，不提供互動調參。

未來可加入：

- 固定一顆球的亂數 seed。
- 顯示所有守備候選。
- 顯示每個候選的 `routeDistanceM`、`successScore`、`rangeMarginM`。
- 將最近幾球匯出為 JSON。

### 6. 尚未做瀏覽器自動化測試

目前已新增 Node 層煙霧測試，但尚未做 Playwright 或瀏覽器畫面回歸測試。

## 架構判斷

v3.12 後，除了完整資料庫與美術動畫以外，核心架構已進一步穩定。

現在專案具備：

- 投球判定模組。
- 早期投球結果套用模組。
- 防守狀態建構模組。
- 場內球守備引擎。
- 跑壘引擎。
- 場內球結果套用模組。
- 場內球視覺時間軸。
- debug panel。
- 回歸煙霧測試。

下一位 AI 可以先跑 `node tools/smoke-test-engines.js`，再開始修改。

## 下一步建議

1. 進一步細化雙殺與傳球時間軸。
2. 把所有守備候選資訊加入 debug panel。
3. 建立 Playwright 瀏覽器 smoke test。
4. 將 `game.js` 的賽季/WBC/管理系統繼續拆出。
5. 等素材準備好後，新增 `animation-assets.js` 接 `GameAnimationAssets.drawFielder()`。
