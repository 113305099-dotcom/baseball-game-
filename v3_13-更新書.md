# v3.13 更新書

更新日期：2026-05-17

## 本週期目標

本週期延續 v3.12，依照使用者要求仍暫時不處理完整球員資料庫與正式美術動畫，優先把程式端基礎架構補到更容易交接與擴充的狀態。

主要目標：

- 修正雙殺視覺時間軸過於粗略的問題。
- 讓動畫可讀到跑者出局事件，而不是只靠抵達壘包事件。
- 強化 debug panel，使動畫與實際結果不一致時更容易查。
- 強化 smoke test，讓下一位 AI 修改核心引擎前有基本防線。
- 同步更新 `PROJECT_OVERVIEW.md` 與 `ENGINE_HANDOFF.md`。

## 已完成事項

### 1. 雙殺時間軸細化

`fielding-engine.js` 的雙殺事件已從「兩個傳球大略同時存在」改成明確序列：

1. 打者從本壘跑向一壘。
2. 一壘跑者從一壘跑向二壘。
3. 第一位守備員接球後先傳二壘。
4. `runner_out` 標記一壘跑者在二壘被封殺。
5. 補位守備員再轉傳一壘。
6. `runner_out` 標記打者在一壘出局。
7. 最後輸出 `double_play` 終局事件。

新增的雙殺傳球會帶有：

- `sequence: "force"`：第一段封殺二壘。
- `sequence: "relay"`：第二段轉傳一壘。

這讓資料層可以表達「先二壘、再一壘」，也讓未來素材動畫可以依事件順序播放接球、轉身、傳球與踩壘。

### 2. 跑者出局事件

`visualTimeline.events` 現在包含：

- `runner_start`
- `runner_arrives`
- `runner_out`
- `throw_start`
- `throw_arrives`

`runner_out` 是 v3.13 的重要補強。它讓畫面層能知道跑者是在目標壘包被殺出局，而不是把所有跑者都當作安全抵達。

### 3. 守備畫面支援 `runner_out`

`battle-scene.js` 已改為讀取 `runner_arrives` 或 `runner_out` 作為跑者路徑終點。

目前視覺仍是簡化版：

- 跑者路徑用藍色虛線。
- 跑者位置用小點。
- 出局跑者抵達出局點後會以紅色圈與叉號提示。

這不是正式美術動畫，但可以確認資料層與畫面層是否對齊。

### 4. Debug Panel 強化

`engine-debug-panel.js` 現在除原本摘要外，額外顯示：

- 守備候選摘要。
- 傳球序列摘要。
- 跑者出局摘要。
- 完整 `visualTimeline` 簡表。

也新增：

```js
GameDebugPanel.snapshot()
```

這會回傳最近一次投球與場內球資料，包含守備候選、選定守備員、擊球資訊與時間軸事件。未來若再遇到「畫面顯示中外野飛球，但實際是投手接」這類問題，可以直接比對 snapshot，不必先翻大量 console。

### 5. Smoke Test 強化

`tools/smoke-test-engines.js` 已新增驗證：

- 雙殺必須先有 `force` 傳二壘。
- 雙殺必須再有 `relay` 傳一壘。
- 一壘跑者必須在二壘被 `runner_out`。
- 打者必須在一壘被 `runner_out`。
- 二壘封殺必須早於一壘出局。
- 一般滾地出局也必須有打者在一壘的 `runner_out`。
- `index.html` 必須載入主要引擎腳本。
- `engine-debug-panel.js` 必須在 `battle-scene.js` 之後載入。

這些檢查能降低日後模組拆分時把載入順序或雙殺事件弄壞的機率。

### 6. 文件更新

已更新：

- `PROJECT_OVERVIEW.md`
- `ENGINE_HANDOFF.md`

已新增：

- `v3_13-更新書.md`

## 已驗證項目

已通過語法檢查：

- `fielding-engine.js`
- `battle-scene.js`
- `engine-debug-panel.js`
- `tools/smoke-test-engines.js`

已執行：

```bash
node tools/smoke-test-engines.js
```

結果：

```text
engine smoke tests passed
```

## 尚未解決事項

### 1. 完整球員資料庫

本週期依使用者要求暫不處理。

目前仍可用通用能力值與現有球員資料運作，但還沒有完整真實球員資料庫。

### 2. 正式美術動畫

本週期依使用者要求暫不處理。

目前已保留 `window.GameAnimationAssets.drawFielder(ctx, actor)` 作為未來 PNG / sprite 接點。之後要替換守備員圖像時，建議新增 `animation-assets.js` 與 sprite manifest，不需要改 `FieldingEngine`。

### 3. 跑壘 AI 仍是簡化版

v3.13 已讓雙殺有基本序列，但跑壘還不是完整 AI。

尚未完成：

- 跑者是否多推進的即時計算。
- 外野回傳與跑者速度的對抗。
- 衝本壘、滑壘、回壘、夾殺。
- 依局面、分差、跑者能力決定冒險程度。

### 4. 補位守備仍是規則化簡化

目前雙殺補位邏輯是簡化規則：

- `SS` 接球時多半由 `2B` 補位轉傳。
- `2B` 接球時多半由 `SS` 補位轉傳。
- `1B` 接球時預設 `SS` 參與轉傳。

未來若要更真實，應把補位守備員也納入候選評分，而不是只靠固定規則。

### 5. 尚未做真正瀏覽器自動化測試

目前新增的是 Node smoke test 與 `index.html` 靜態腳本順序檢查。

尚未完成：

- Playwright 開啟 `index.html`。
- 實際跑一局並截圖確認畫面沒有空白。
- 檢查 debug panel 在瀏覽器內是否正常開關。
- 檢查 Canvas 動畫是否有畫出正確守備員與跑者。

### 6. `game.js` 仍可繼續拆分

投打與場內球核心已經抽出，但 `game.js` 仍保留不少大型責任：

- 賽季推進。
- WBC 目標。
- 管理系統。
- 旁白文字資料。
- 存檔與 UI 更新協調。

這些不是 v3.13 的主要風險，但未來若繼續長期開發，仍建議逐步拆模組。

## 架構判斷

v3.13 後，除了完整球員資料庫與正式美術動畫外，基礎程式架構已經更接近可交接狀態。

目前最重要的原則仍是：

- `FieldingEngine` 決定結果。
- `visualTimeline` 描述結果如何演出。
- `BattleScene` 只讀時間軸，不重新判定結果。
- `GameDebugPanel` 協助檢查資料是否合理。
- `tools/smoke-test-engines.js` 作為修改前後的最低限度回歸測試。

## 下一步建議

1. 建立 Playwright 瀏覽器 smoke test。
2. 把 `game.js` 的賽季、WBC、管理與文字資料逐步拆分。
3. 讓補位守備員選擇改為資料驅動。
4. 擴充跑壘 AI，加入跑者速度、壘包距離、傳球時間與策略。
5. 等素材準備好後，新增 `animation-assets.js` 與 sprite manifest 接上正式 PNG / 動畫。
