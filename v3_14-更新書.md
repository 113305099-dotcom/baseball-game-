# v3.14 更新書

更新日期：2026-05-17

## 本週期目標

本週期依照使用者要求，暫時不處理完整球員資料庫與正式美術動畫，優先補強非美術、非資料庫的核心一致性。

主要目標：

- 讓跑壘結果與守備視覺時間軸共用同一份推進資料。
- 讓安打、失誤與長打能在 `visualTimeline` 顯示壘上跑者推進。
- 將雙殺轉傳補位從固定字串改成候選評分。
- 強化 smoke test，避免後續修改破壞跑壘與轉傳資料。
- 同步更新 `PROJECT_OVERVIEW.md` 與 `ENGINE_HANDOFF.md`。

## 已完成事項

### 1. 跑壘引擎輸出 movement plan

`baserunning-engine.js` 的 `resolveAdvance()` 現在除了回傳壘包、得分與是否安打，也會回傳：

- `movements`

每個 movement 包含：

- `runnerId`
- `runner`
- `fromBase`
- `toBase`
- `scored`

這讓其他模組可以知道每位跑者從哪個壘包移動到哪裡，而不是只看到最後壘包狀態。

### 2. 場內球結果帶出 `advanceResult`

`fielding-engine.js` 會在下列結果上附加同一份跑壘計畫：

- `hit`
- `error`
- `home_run`
- `net_double`

欄位位置：

```js
playResult.advanceResult
```

這份資料會同時提供給：

- `visualTimeline` 產生跑者動畫事件。
- `InPlayResultApplier` 套用實際比分與壘包狀態。

### 3. 安打與失誤跑者時間軸

`visualTimeline.events` 現在不只在雙殺與失誤時有跑者事件。

安打、二壘安打、三壘安打、全壘打、牆面二壘安打與失誤上壘，都能依 `advanceResult.movements` 產生：

- `runner_start`
- `runner_arrives`

得分跑者會帶有：

```js
scored: true
```

這讓守備俯視畫面可以畫出壘上跑者推進，不再只有打者跑壘。

### 4. 狀態套用避免二次抽隨機

`game.advanceRunners()` 現在接受第四個參數：

```js
advanceRunners(outcome, team, batter, precomputedAdvance)
```

`in-play-result-applier.js` 會把 `playResult.advanceResult` 傳進去。

因此同一顆場內球只會抽一次跑壘推進結果，避免出現：

- 畫面顯示跑者得分。
- 實際壘包狀態卻沒有得分。

### 5. 雙殺補位改成候選評分

`fielding-engine.js` 的雙殺第二段轉傳不再只靠固定規則。

目前補位轉傳會依下列資料評分：

- 合理補位守位範圍。
- 守備能力。
- 肩力。
- 到二壘補位距離。
- 守位責任偏好。

仍保留棒球責任限制，例如：

- `SS` 接球時優先由 `2B` 或 `P` 補位。
- `2B` 接球時優先由 `SS` 或 `P` 補位。
- `1B`、`3B`、`P` 接球時才在多個內野補位候選中評分。

輸出位置：

```js
playResult.relay
```

傳球事件的 `from` 也會使用實際評分選出的補位守備員。

### 6. Smoke Test 強化

`tools/smoke-test-engines.js` 新增驗證：

- 小寫 `single/double/triple/home_run` 能正確正規化。
- 二壘安打的 `movements` 會讓打者上二壘、二三壘跑者得分。
- 場內球二壘安打會在 `visualTimeline` 顯示 R2、R3 回本壘。
- 右側滾地雙殺會依補位候選評分選擇轉傳守備員。
- 轉傳事件 `throw_start.from` 必須等於評分選出的補位守備員。

## 已驗證項目

已執行：

```bash
node tools/smoke-test-engines.js
node tools/browser-smoke-test.js
```

結果：

```text
engine smoke tests passed
browser smoke tests passed
```

## 尚未解決事項

### 1. 完整球員資料庫

依使用者要求，本週期不處理。

### 2. 正式美術動畫

依使用者要求，本週期不處理。

目前仍使用既有 Canvas / placeholder 視覺，正式 sprite manifest 與 PNG 動畫留待後續。

### 3. 跑壘 AI 仍是簡化版

v3.14 已讓跑壘結果與視覺時間軸共用同一份資料，但跑壘判斷仍是簡化模型。

尚未完成：

- 外野回傳與跑者速度的即時對抗。
- 滑壘、回壘、夾殺。
- 依局面、分差、跑者能力決定冒險程度。
- 犧牲飛球的視覺時間軸仍未完全資料化。

### 4. 傳球模型仍可擴充

雙殺補位已改為候選評分，但外野回傳、轉傳員位置、長傳衰減與傳球失誤仍尚未完整模型化。

### 5. `game.js` 仍可繼續拆分

投打、守備、跑壘與結果套用已繼續分層，但 `game.js` 仍保留：

- 賽季推進。
- WBC 目標。
- 管理系統。
- 旁白文字資料。
- 存檔與 UI 更新協調。

## 下一步建議

1. 把犧牲飛球、外野回傳與跑者挑戰三壘/本壘資料化。
2. 擴充 `visualTimeline` 的傳球失誤、外野回傳與轉傳事件。
3. 把 `game.js` 的賽季、WBC、管理與文字資料逐步拆分。
4. 把瀏覽器 smoke test 擴充成完整 UI 操作與整局流程測試。
5. 等素材準備好後，再新增 `animation-assets.js` 與 sprite manifest。
