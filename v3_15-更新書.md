# v3.15 更新書

更新日期：2026-05-17

## 本週期目標

本週期依照使用者要求，暫時不處理完整球員資料庫與正式美術動畫，優先處理三個核心問題：

1. 轉播欄顯示外野手處理，但守備動畫卻顯示投手處理。
2. 滾地球模型過度簡化，沒有考慮彈跳、減速、反應時間與打穿防線。
3. 確認目前模組設計未來接球員立繪與動畫時是否會造成困擾。

## 已完成事項

### 1. 修正轉播與動畫不一致

問題來源：

- 舊模型只有一個 `fielding.selected`。
- 當球穿越內野時，第一位嘗試處理的內野手與最後撿球的外野手沒有分開。
- 視覺層有機會拿到第一位嘗試者，造成轉播與動畫顯示不同守備員。

v3.15 改為分兩層：

- `fielding.primaryAttempt`：第一位嘗試攔球的守備員，例如 `P`。
- `fielding.selected`：最後負責處理球的守備員，例如 `CF`。

當強襲球穿越內野時，轉播會顯示類似：

```text
投手前方，P 來不及攔下，最後由 CF 處理。
```

`BattleScene` 也改為優先讀取最新的 `game.lastInPlayContext`，讓動畫使用最後處理者，降低舊資料殘留造成的錯位。

### 2. 重設滾地球物理模型

`fielding-engine.js` 現在對滾地球建立簡化物理資料：

- 第一彈跳時間。
- 第一彈跳距離。
- 彈性保留速度。
- 內野土區摩擦。
- 外野草地摩擦。
- 停球距離。
- 停球時間。

新增資料位置：

```js
ballInfo.groundProfile
```

每位守備候選人現在會依球到達該處理點時的狀態評估：

- `ballArrivalSpeedKmh`
- `ballPathPhase`
- `ballReachedPoint`
- `arrivalSec`
- `reactionSec`
- `routeDistanceM`
- `reachChance`
- `handleChance`
- `successScore`

這讓模型可以判斷：

- 球雖然經過投手附近，但太快、時間太短，投手無法處理。
- 球滾到游擊或二壘防區時速度下降，內野手才有處理機會。
- 球速太快或反應時間不足時，可能穿越內野。

### 3. 打穿防線後改由外野手處理

滾地球現在先評估內野第一反應。

若第一位內野手來不及或不可及，且不是合理失誤，結果會改成：

- `playResult.code = "hit"`
- `fielding.primaryAttempt = 第一位內野手`
- `fielding.selected = 補位處理的外野手`
- `fielding.result.reason = "through_infield"`

因此畫面上會看到外野手處理球，而不是投手或游擊手在不合理位置追球。

### 4. 失誤與來不及分開

舊規則中，部分「追不到」的球可能被記成失誤。

v3.15 加入強襲球條件：

- 球到達速度過快。
- 球抵達時間接近或早於反應窗口。

這種情況不會直接記成守備失誤，而會判定為打穿防線或安打。

### 5. Debug Panel 強化

`engine-debug-panel.js` 現在會顯示：

- `Attempt`：第一位嘗試守備員與球到達速度。
- snapshot 內的 `primaryAttempt`。
- snapshot 內的 `groundProfile`。
- 每位候選人的 `ballSpeedKmh`。

這可以直接排查：

- 轉播與動畫是否使用同一位最後處理者。
- 投手為什麼沒有處理強襲球。
- 球穿越內野時是哪位守備員第一反應。

### 6. Smoke Test 強化

`tools/smoke-test-engines.js` 新增：

- 強襲投手前滾地球會打穿內野。
- 投手會記錄為 `primaryAttempt`。
- 最後處理者會變成 `CF`。
- `fielder_arrives.fielder` 必須等於最後處理者。

這直接防止「轉播外野手、動畫投手」的錯位回歸。

## 對三個問題的結論

### 問題 1：轉播外野手，動畫投手

判斷：這是資料模型責任沒有分清楚。

改善：

- 已分離 `primaryAttempt` 與 `selected`。
- 轉播描述會同時說明第一反應與最後處理。
- 動畫使用 `fielding.selected` 作為最後處理者。
- Debug Panel 可檢查兩者是否一致。

### 問題 2：滾地球應考慮彈跳、減速、反應時間與打穿防線

判斷：使用者指出的是核心模型問題，不能只靠調參。

改善：

- 已新增第一版彈跳與摩擦模型。
- 已將候選守備員評估從單純幾何距離，改為球到達處理點時的時間與速度。
- 已加入強襲球反應懲罰。
- 已加入打穿內野後外野手接手處理。

仍未完成：

- 更精準的空氣阻力曲線。
- 不同球場土質、草皮、水分與彈性係數資料化。
- 多次彈跳高度與不規則彈跳。
- 外野回傳與跑者挑戰壘包的完整時間競賽。

### 問題 3：未來接球員立繪和動畫會不會造成困擾

判斷：目前架構方向正確，不會造成大困擾，但要守住資料邊界。

原因：

- 判定結果仍在 `FieldingEngine`。
- 動畫資料仍走 `visualTimeline`。
- `BattleScene` 不重新判定結果。
- 既有 `window.GameAnimationAssets.drawFielder(ctx, actor)` hook 可替換守備員繪製。
- v3.15 後 `actor.state` 可以更穩定對應 `idle`、`run`、`field`、`miss`。

建議：

- 未來接 PNG / sprite 時，不要把圖片邏輯放進 `FieldingEngine`。
- 新增 `animation-assets.js` 與 sprite manifest。
- 讓 manifest 負責角色、動作、幀數與路徑。
- `BattleScene` 只把 `actor` 傳給 `GameAnimationAssets.drawFielder()`。
- 若要更完整動畫，再擴充 `visualTimeline` 事件，不要在畫面層硬寫判定。

## 已驗證項目

已執行：

```bash
node --check fielding-engine.js
node --check in-play-result-applier.js
node --check battle-scene.js
node --check engine-debug-panel.js
node --check tools/smoke-test-engines.js
node tools/smoke-test-engines.js
node tools/browser-smoke-test.js
```

結果：

```text
engine smoke tests passed
browser smoke tests passed
```

## 尚未解決事項

### 1. 完整球員資料庫

依使用者要求，本週期不處理。

### 2. 正式美術動畫

依使用者要求，本週期不處理。

### 3. 更完整的球場物理

v3.15 是第一版滾地球物理，不是完整物理模擬。

後續可加入：

- 球場地面材質參數。
- 草地長度與內野土硬度。
- 多次彈跳與彈跳高度。
- 空氣阻力、旋轉、濕度與雨天修正。

### 4. 外野回傳與跑者時間競賽

目前打穿防線後可以由外野手處理，但外野手回傳、跑者挑戰二壘/三壘/本壘仍未完整時間軸化。

## 下一步建議

1. 把球場材質參數加入 `STADIUMS_DATA` 或獨立 `FIELD_SURFACES`。
2. 擴充外野回傳、轉傳與跑者挑戰壘包模型。
3. 將犧牲飛球、滑壘、回壘、夾殺納入 `visualTimeline`。
4. 建立 `animation-assets.js` 與 sprite manifest 空殼，等美術素材到位後直接接上。
5. 把瀏覽器 smoke test 擴充成完整 UI 操作與整局流程測試。
