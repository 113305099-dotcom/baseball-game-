# v3.16 更新書

更新日期：2026-05-17  
主題：完整物理引擎第一版、外野傳球決策、跑者與傳球時間線

## 本次完成

- 建立 `FIELD_SURFACE_PHYSICS`，讓球場材質能影響彈跳係數、內野摩擦、外野草皮摩擦、滾動速度與空氣阻力。
- `FieldingEngine.calcBattedBall()` 現在會輸出 `ballInfo.surface`、`ballInfo.groundProfile.surfaceKey` 與非滾地球用的 `ballInfo.flightProfile`。
- 滾地球不再只用固定摩擦；天然草、人工草、混合紅土與室內場地可以得到不同停球距離與到點速度。
- 外野手處理安打後，`FieldingEngine` 會評估直傳與中繼回傳，並建立 `playResult.throwDecision`。
- 跑者推進與回傳挑戰共用 `BaserunningEngine.runnerTravelSec()`，避免動畫、比分與跑者結果各算各的。
- 外野回傳若刺殺跑者，會寫入 `advanceResult.outsOnBases`，`game.advanceRunners()` 會同步增加出局數。
- `visualTimeline` 會把外野直傳/中繼傳球與跑者出局寫成 `throw_start`、`throw_arrives`、`runner_out` 事件。
- `in-play-result-applier.js` 會在轉播欄補上外野回傳摘要。
- `engine-debug-panel.js` 新增 surface 與 throwDecision 摘要，方便檢查每球物理與傳跑判定。

## 主要檔案

- `data.js`：新增 `FIELD_SURFACE_PHYSICS` 並掛到 `window`。
- `fielding-engine.js`：加入材質物理、飛球阻力 profile、外野直傳/中繼決策、跑者挑戰判定。
- `baserunning-engine.js`：新增跑者跑壘時間估算與 challenged runner 移除工具。
- `game.js`：套用 `advanceResult.outsOnBases` 的出局數。
- `in-play-result-applier.js`：顯示外野回傳轉播文字。
- `engine-debug-panel.js`：顯示材質、傳球決策與跑者出局。
- `tools/smoke-test-engines.js`：新增材質滾動差異與外野回傳刺殺跑者測試。

## 架構說明

v3.16 的核心是讓「球怎麼動」、「誰處理球」、「跑者跑多久」、「球傳多久」使用同一份結果資料。

資料流為：

1. `FieldingEngine.calcBattedBall()` 依擊球初速、角度、球場與材質產生球路資料。
2. `FieldingEngine.chooseFielder()` 選出第一反應與最後處理守備員。
3. `BaserunningEngine.resolveAdvance()` 先建立跑者推進計畫。
4. 若外野手處理安打，`FieldingEngine` 用跑者時間與傳球時間決定是否挑戰壘包。
5. 結果寫回 `playResult.advanceResult`、`playResult.throwDecision` 與 `visualTimeline`。
6. `InPlayResultApplier` 與 `Game.advanceRunners()` 只套用既定結果，不再二次抽籤。

## 已驗證

```bash
node --check fielding-engine.js
node --check baserunning-engine.js
node --check in-play-result-applier.js
node --check engine-debug-panel.js
node --check game.js
node --check data.js
node --check tools/smoke-test-engines.js
node tools/smoke-test-engines.js
```

## 仍未處理

- 完整球員資料庫。
- 正式美術、立繪、sprite manifest 與動畫素材。
- 犧牲飛球的細緻跑者判定。
- 滑壘、夾殺、煞車回壘、三壘指導員 hold/send 決策。
- 雨天、濕度、風向、球場維護狀態與更多材質細節。

## 下一步建議

- 把 `throwDecision` 延伸到犧牲飛球與內野趨前守備。
- 為跑者新增 aggressiveness、baserunning、slide 等能力值，讓傳跑判定不只看 speed。
- 將目前簡化座標改成更完整的球場區域模型，支援深遠外野、牆邊反彈與界外區追球。
