# v3.17 更新書

更新日期：2026-05-17  
主題：守備追球動畫、球路終點、傳球時序與轉播文字修正

## 本次優先修正

- 修正滾地球打穿內野後，畫面仍把球停在內野、外野手像瞬移到內野接球的問題。
- `FieldingEngine` 現在會把安打的 `ball_arrives` 指向最後處理點；打穿內野時會顯示在外野接球/撿球位置。
- `visualTimeline` 新增 `fielder_start` 事件，守備員即使追不到球，也會有跑動路徑。
- `BattleScene` 改為讀取多個守備員路徑，可同時呈現第一反應守備員撲球失敗與外野手補位。
- 放慢場內球視覺節奏，讓球飛行、落地與守備員起跑有更明顯的時間感。
- 修正外野回傳時序：如果跑者已經先到壘，不能再因為後續傳球動畫判出局。
- 壘上出局時間改為跑者抵達壘包時觸殺，不再發生球先到壘、跑者還在路上卻先顯示出局。
- 轉播欄改成一到兩句主播式描述，移除「處理成功率、初速、仰角、預估距離」等工程偵錯文字。

## 主要檔案

- `fielding-engine.js`
  - 地面球不再套用球場 `hrFactor`，避免強勁滾地球距離被全壘打係數錯誤縮短。
  - 新增 `firstBouncePoint`，並讓外野處理的滾地安打使用最後 pickup point。
  - 新增 `fielder_start` / `fielder_arrives` 路徑事件。
  - 修正 `throwDecision` 的 safe/out 判斷，跑者先到壘即安全。
- `battle-scene.js`
  - 改為動畫化多位守備員路徑。
  - 放慢場內球視覺球速與守備畫面停留時間。
- `in-play-result-applier.js`
  - 重寫為乾淨 UTF-8 轉播文字。
  - 安打結果以主播語氣描述，不再輸出 debug 數據。
- `engine-debug-panel.js`
  - 新增 fielder route 摘要。
- `tools/smoke-test-engines.js`
  - 新增打穿內野最後點、第一反應/最後處理路徑、跑者先到壘安全的回歸測試。

## 現在的時間線重點

`visualTimeline.events` 目前可包含：

- `contact`
- `ball_arrives`
- `fielder_start`
- `fielder_arrives`
- `runner_start`
- `runner_arrives`
- `runner_out`
- `throw_start`
- `throw_arrives`

動畫層只讀這些事件，不自行判斷誰接球、誰追球、誰出局。

## 已驗證

```bash
node --check fielding-engine.js
node --check battle-scene.js
node --check in-play-result-applier.js
node --check tools/smoke-test-engines.js
node tools/smoke-test-engines.js
node tools/browser-smoke-test.js
```

## 仍未處理

- 完整球員資料庫。
- 正式美術、立繪、sprite manifest 與動畫素材。
- 犧牲飛球、滑壘、夾殺與三壘指導員 hold/send 決策。
- 更完整的外野牆邊反彈、界外區追球與天氣球場係數。
