# v3.18 更新書

更新日期：2026-05-18  
主題：犧牲飛球、tag-up 跑壘計畫與高飛球補傳挑戰

## 本次完成

- `fly_out` 不再只是單純記一個出局。
- 深遠飛球現在可以在 `FieldingEngine` 內直接產生 tag-up 跑壘計畫。
- 三壘跑者可以在接殺後回本壘，二壘跑者也可以視深度與滯空時間推進三壘。
- 高飛球的跑者起跑時間會寫入 `advanceResult.movements[].startAtSec`，所以跑者會在接殺後才起跑。
- 高飛球 tag-up 後的補傳挑戰，改用和安打一樣的 `throwDecision` / `outsOnBases` 流程。
- `InPlayResultApplier` 的 `fly_out` 分支已改成吃 `playResult.advanceResult`，不再依賴舊的 `game.trySacrificeFly()` 隨機邏輯。
- 轉播欄現在能描述：
  - 犧牲飛球得分
  - 二壘跑者 tag-up 推進
  - 本壘或三壘補傳刺殺

## 主要檔案

- `fielding-engine.js`
  - 新增空中出局跑壘結果 `emptyAdvanceResult()` / `applyAdvanceMovement()`
  - 新增 `resolveAirOutAdvance()`
  - `challengePriority()` 與 `resolveRunnerThrowChallenge()` 現在支援 `fly_out`
- `in-play-result-applier.js`
  - `fly_out` 改用 `game.advanceRunners(..., playResult.advanceResult)`
  - 新增高飛球推進轉播摘要
- `tools/smoke-test-engines.js`
  - 新增犧牲飛球得分
  - 新增高飛球補傳刺殺
  - 新增 `InPlayResultApplier` 不再使用舊 `trySacrificeFly()` 的回歸測試

## 目前資料流

1. `FieldingEngine` 判定高飛球接殺。
2. 若是外野深遠飛球，`resolveAirOutAdvance()` 會產生：
   - `advanceResult`
   - `advanceResult.movements[].startAtSec`
   - `airOutAdvance`
   - 視情況產生 `throwDecision`
3. `visualTimeline` 直接使用同一份 movement / throw 資料。
4. `InPlayResultApplier` 套用該結果，再記錄打者的飛球出局。

## 已驗證

```bash
node --check fielding-engine.js
node --check in-play-result-applier.js
node --check tools/smoke-test-engines.js
node tools/smoke-test-engines.js
node tools/browser-smoke-test.js
```

## 仍未處理

- 完整球員資料庫。
- 正式美術、立繪、sprite manifest 與動畫素材。
- 滑壘、夾殺、三壘指導員 hold/send 與更完整跑者 AI。
- 更完整的牆邊反彈、界外區追球與天氣球場係數。
