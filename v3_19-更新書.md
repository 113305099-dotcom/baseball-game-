# v3.19 更新書

更新日期：2026-05-18  
主題：跑壘指導員決策、近距離滑壘事件與 debug 可視化

## 本次完成

- 不處理完整球員資料庫與正式美術動畫，延續核心引擎整理。
- `BaserunningEngine` 現在會在 `advanceResult.decisions` 記錄跑壘指導員的 `send` / `hold` 判斷。
- 一壘安打的二壘跑者、二壘安打的一壘跑者，會依速度、侵略性、跑壘策略與亂數決定是否多搶一個壘包。
- `FieldingEngine` 的高飛球 tag-up 也會記錄停壘或放行決策，即使最後沒有跑者推進，也能在 debug 與轉播中看見原因。
- 外野回傳形成近距離攻防時，`throwDecision.slide` 與 `visualTimeline.runner_slide` 會記錄滑壘提示。
- `InPlayResultApplier` 補上壘指導員放行/擋下、飛球深度不足停壘等轉播文字。
- `GameDebugPanel` 新增 `RunnerAI` 與 `Slides` 欄位，方便檢查跑壘 AI、補傳與滑壘事件是否一致。
- 玩家進攻時，`game.baserunningMode` 會透過 `advanceBonus` 傳入場內球引擎，影響預先決定的跑壘計畫。

## 主要檔案

- `baserunning-engine.js`
  - 新增 `decisions` 輸出。
  - 新增速度、侵略性、跑壘風格與全隊跑壘策略對多進壘的影響。
  - `movements[]` 可攜帶該次跑壘的 `decision`。
- `fielding-engine.js`
  - 高飛球 tag-up 會保留 hold/send 決策。
  - 近距離外野補傳會產生 `slide` 與 `runner_slide` timeline 事件。
  - `resolveInPlay()` 可接收 `advanceBonus`。
- `game.js`
  - 玩家進攻場內球會把目前跑壘策略傳入 `FieldingEngine`。
- `in-play-result-applier.js`
  - 新增跑壘指導員與 tag-up 停壘轉播文字。
- `engine-debug-panel.js`
  - 新增 `RunnerAI` / `Slides` 摘要。
- `tools/smoke-test-engines.js`
  - 新增保守停壘、激進放行、高飛球停壘與滑壘事件回歸測試。

## 目前資料流

1. `BaserunningEngine.resolveAdvance()` 產生 `advanceResult.movements` 與 `advanceResult.decisions`。
2. `FieldingEngine.resolveInPlay()` 對安打、外野補傳與高飛球 tag-up 共用同一份跑壘計畫。
3. 若外野回傳挑戰跑者，`throwDecision` 會記錄目標跑者、壘包、傳球路徑、安全/出局與滑壘資訊。
4. `visualTimeline` 依 `advanceResult.movements` 產生跑者起跑、到壘、出局與滑壘事件。
5. `InPlayResultApplier` 套用同一份結果，不再二次抽跑壘亂數。

## 已驗證

```bash
node --check baserunning-engine.js
node --check fielding-engine.js
node --check in-play-result-applier.js
node --check engine-debug-panel.js
node --check game.js
node --check tools/smoke-test-engines.js
node --check tools/browser-smoke-test.js
node tools/smoke-test-engines.js
node tools/browser-smoke-test.js --screenshot=browser-smoke-fielding.png
```

## 仍未處理

- 完整球員資料庫。
- 正式 PNG sprite sheet、逐幀動畫與滑壘實際動作素材。
- 夾殺、折返跑壘、三壘指導員更細緻的 hold/send UI。
- 更完整的牆邊反彈、界外區追球與天氣球場係數。
- 瀏覽器測試尚未擴展到完整多局 UI 互動。
