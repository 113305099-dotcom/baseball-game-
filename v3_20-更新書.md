# v3.20 更新書

更新日期：2026-05-18  
主題：守備佈陣系統（Defensive Alignment）、save/load 擴充、引擎回歸測試補強

---

## 本次完成

### 1. 守備佈陣系統（Defensive Alignment）

規則書第 11.9.1 節定義了六種守備佈陣，v3.20 正式實作到引擎層。

**新增佈陣：**

| 識別碼 | 名稱 | 用途 |
|---|---|---|
| `standard` | 標準 | 預設站位，適合一般情況 |
| `bunt_defense` | 防短打 | 全內野前移 9 公尺，縮短滾地球與短打的處理距離 |
| `double_play` | 雙殺佈陣 | 2B/SS 各向二壘靠近 5 公尺並後退 6 公尺，加快雙殺轉傳銜接 |
| `infield_in` | 內野趨前 | 全內野極端前移 14-16 公尺，防止三壘跑者搶本壘 |
| `outfield_deep` | 外野深守 | 全外野後退 18 公尺，優先防止長打與全壘打 |
| `outfield_in` | 外野前守 | 全外野前移 18 公尺，防短安並限制跑者多進壘 |

**實作細節：**

- `defense-state-builder.js` 新增 `BASE_POSITIONS`（守備員基準座標）與 `DEFENSIVE_ALIGNMENTS`（佈陣偏移表）。
- `buildDefenseState(gameRef, battingTeam, shiftKey)` 新增第三個參數 `shiftKey`；每個 `assignment` 現在帶有 `start` 欄位，儲存套用偏移後的實際起始座標。
- `fielding-engine.js` 原本就讀取 `entry?.start`，此改動完全相容，不需修改守備引擎本體。
- `game.js` 新增 `game.defensiveShift`（預設 `'standard'`），在玩家防守（`battingTeam === 'opponent'`）時傳入 `buildDefenseState`；對手防守時固定使用 `'standard'`（等待 AI 佈陣邏輯）。
- 無效的 `shiftKey` 值自動退回 `'standard'`，不會導致引擎出錯。

### 2. save / load 擴充

- `game.defensiveShift` 納入 `SaveManager.save()` 與 `SaveManager.load()`。
- 讀檔時驗證 `shiftKey` 是否在 `DEFENSIVE_ALIGNMENTS` 中，非法值自動退回 `'standard'`，確保舊存檔不會出錯。

### 3. `DefenseStateBuilder` 公開新介面

`DefenseStateBuilder` 現在額外公開：

- `DefenseStateBuilder.BASE_POSITIONS`：各守位基準座標，供 UI 或調校工具讀取。
- `DefenseStateBuilder.DEFENSIVE_ALIGNMENTS`：完整佈陣定義表，供 UI 顯示按鈕 label 與 description。

### 4. 回歸測試補強（`tools/smoke-test-engines.js`）

新增 `testDefensiveAlignments()`，涵蓋：

- `DEFENSIVE_ALIGNMENTS` 與 `BASE_POSITIONS` 是否正確公開。
- 所有六種佈陣是否存在且帶有 label 欄位。
- `buildDefenseState` 回傳正確 `shiftKey`。
- 無效 `shiftKey` 退回 `standard`。
- `bunt_defense`：SS 的 `start.y` 比 `standard` 小（往前移）。
- `infield_in`：1B 的 `start.y` 比 `standard` 小（極端前移）。
- `outfield_deep`：CF 的 `start.y` 比 `standard` 大（後退）。
- `outfield_in`：LF 的 `start.y` 比 `standard` 小（前移）。
- `double_play`：2B 的 `start.x` 比 `standard` 小，SS 的 `start.x` 比 `standard` 大（向中靠攏）。
- `standard`：所有守位的 `start` 與 `BASE_POSITIONS` 完全吻合。

---

## 主要異動檔案

- `defense-state-builder.js`
  - 新增 `BASE_POSITIONS` 常數。
  - 新增 `DEFENSIVE_ALIGNMENTS` 常數（6 種佈陣）。
  - 新增 `shiftedStart(position, alignment)` 輔助函式。
  - `buildDefenseState()` 新增 `shiftKey` 第三參數，每個 `assignment` 帶有 `start` 座標。
  - 公開 `BASE_POSITIONS` 與 `DEFENSIVE_ALIGNMENTS`。
- `game.js`
  - `Game` 建構子新增 `this.defensiveShift = 'standard'`。
  - `SaveManager.save()` 新增 `defensiveShift` 欄位。
  - `SaveManager.load()` 新增 `defensiveShift` 讀取與驗證。
  - `resolveAtBatWithContext()` 中 `buildDefenseState` 呼叫新增 `shiftKey` 傳入。
- `tools/smoke-test-engines.js`
  - 新增 `testDefensiveAlignments()` 函式與在 `run()` 中的呼叫。

---

## 已驗證

```bash
node --check defense-state-builder.js
node --check tools/smoke-test-engines.js
node tools/smoke-test-engines.js
```

所有既有測試與新增測試均通過。

---

## 尚未處理

- 對手佈陣 AI：目前對手固定使用 `standard`，未來可依比分、壘況、局數自動選擇佈陣。
- 玩家投球畫面的佈陣切換按鈕 UI（等美術方向確定後接入 `game.defensiveShift`）。
- 佈陣與球場/地面材質的聯動（例如人工草球場是否加強外野前守效益）。
- 完整球員資料庫。
- 正式 PNG sprite sheet 與逐幀動畫素材。
- 夾殺、折返跑壘與三壘指導員 UI。
