# v3.21 更新書

更新日期：2026-05-18  
主題：旁白模組拆分、守備佈陣 UI、對手佈陣 AI

---

## 本次完成

### 1. 旁白模組拆分（Commentary Module Extraction）

`CommentaryGenerator` class 從 `game.js` 移出，獨立成 `commentary.js`。

**實作細節：**

- 新建 `commentary.js`，以 IIFE 包裝並掛到 `globalThis.CommentaryGenerator`，與其他引擎模組風格一致。
- `game.js` 原來的 `CommentaryGenerator` class（約 65 行）已移除，改以 `// CommentaryGenerator 已移至 commentary.js（v3.21）` 標記。
- `index.html` 在 `game-renderer.js` 與 `game.js` 之間插入 `<script src="commentary.js"></script>`，確保 `game.js` 初始化 `new CommentaryGenerator()` 時類別已存在。

**為什麼要拆：**
`game.js` 已超過 4800 行，將靜態旁白模板獨立出去是第一步拆分，也讓後續可以直接替換 `commentary.js` 做多語言或更豐富的旁白，不需碰核心遊戲邏輯。

---

### 2. 守備佈陣 UI 按鈕

`panel-defense`（守備配球面板）新增「守備佈陣」控制區塊，讓玩家在投球半局可即時切換六種守備佈陣。

**HTML 變更（`index.html`）：**

在 `strategy-group-pitch-effort` 後新增：

```html
<div class="ctrl-group" id="strategy-group-defensive-shift">
  <span class="ctrl-label">守備佈陣</span>
  <div class="ctrl-chips ctrl-chips-shift">
    <button type="button" data-defensive-shift="standard"      title="...">標準</button>
    <button type="button" data-defensive-shift="bunt_defense"  title="...">防短打</button>
    <button type="button" data-defensive-shift="double_play"   title="...">雙殺</button>
    <button type="button" data-defensive-shift="infield_in"    title="...">趨前</button>
    <button type="button" data-defensive-shift="outfield_deep" title="...">深守</button>
    <button type="button" data-defensive-shift="outfield_in"   title="...">前守</button>
  </div>
</div>
```

**Click handler 新增：**

```js
const defensiveShift = event.target.closest('[data-defensive-shift]');
if (defensiveShift) {
  game.setDefensiveShift(defensiveShift.dataset.defensiveShift);
  return;
}
```

**Active state 同步：**

```js
document.querySelectorAll('[data-defensive-shift]').forEach((button) => {
  button.classList.toggle('active', button.dataset.defensiveShift === game.defensiveShift);
});
```

**攻守切換聯動：** `strategy-group-defensive-shift` 在進攻半局（`isOffensive`）時自動禁用所有按鈕，邏輯與 `strategy-group-pitch-effort` 相同。

**`game.js` 新增方法 `setDefensiveShift(shiftKey)`：**

```js
setDefensiveShift(shiftKey) {
  // 驗證 shiftKey 是否在 DEFENSIVE_ALIGNMENTS
  // 進攻半局拒絕操作
  // 更新 this.defensiveShift，寫 managementLog，存檔，更新 UI
}
```

---

### 3. 對手佈陣 AI

`OpponentAI.decide()` 在對手守備半局（`currentHalf === 'bottom'`）新增依局勢自動選擇 `game.opponentDefensiveShift` 的邏輯。

**決策邏輯：**

| 情況 | 選擇佈陣 |
|------|---------|
| 壘上無人 | `standard` |
| 三壘有人 + 出局數 < 2 + 比分差距 ≤ 1 | `infield_in`（防三壘跑者搶本） |
| 領先 4 分以上且 7 局後 | `outfield_deep`（保護大幅優勢，防長打） |
| 多名跑者上壘，或 1 壘有人且無出局 | `double_play`（爭取雙殺） |
| 其他 | `standard` |

**`resolveAtBatWithContext` 更新：**

原本玩家進攻時守備引擎固定使用 `'standard'`，現改為傳入 `game.opponentDefensiveShift`：

```js
// 修改前
battingTeam === 'opponent' ? (game.defensiveShift || 'standard') : 'standard'

// 修改後
battingTeam === 'opponent' ? (game.defensiveShift || 'standard') : (game.opponentDefensiveShift || 'standard')
```

**新增狀態 `game.opponentDefensiveShift`（預設 `'standard'`）：**
- 加入 `Game` 建構子。
- 加入 `SaveManager.save()` / `SaveManager.load()`（讀取時驗證合法值，非法回落 `'standard'`）。

---

### 4. smoke-test-engines.js 補強

新增 `testV321Features()`，涵蓋：

- `commentary.js` 存在且包含 `CommentaryGenerator` class。
- `commentary.js` 有正確的 `global.CommentaryGenerator = CommentaryGenerator` 掛載。
- `game.js` 不再包含 `CommentaryGenerator` class。
- `index.html` 載入 `commentary.js` 且在 `game.js` 之前。
- `index.html` 存在 `strategy-group-defensive-shift` 與六個 `data-defensive-shift` 按鈕。
- `game.js` 有 `setDefensiveShift(shiftKey)` 方法。
- `game.js` 有 `opponentDefensiveShift` 狀態。
- `OpponentAI` 內含 `infield_in`、`double_play`、`outfield_deep` 佈陣判斷。

`testBrowserScriptOrder()` 新增 `commentary.js` 到必要腳本清單，並新增排序斷言。

---

## 主要異動檔案

| 檔案 | 異動類型 | 說明 |
|------|---------|------|
| `commentary.js` | **新增** | CommentaryGenerator 獨立模組 |
| `game.js` | 修改 | 移除 CommentaryGenerator class；新增 `opponentDefensiveShift` 狀態、`setDefensiveShift()` 方法；更新 save/load；OpponentAI 佈陣 AI；resolveAtBatWithContext 傳入 opponentDefensiveShift |
| `index.html` | 修改 | 加入 `<script src="commentary.js">`；panel-defense 新增守備佈陣按鈕；click handler、active 切換、show/hide 聯動 |
| `tools/smoke-test-engines.js` | 修改 | 新增 `testV321Features()`；更新 `testBrowserScriptOrder()` |
| `PROJECT_OVERVIEW.md` | 修改 | 更新版本、啟動順序、尚未完成清單、接手順序 |

---

## 已驗證

```bash
node tools/smoke-test-engines.js
```

所有既有測試與新增 v3.21 測試均通過（engine smoke tests passed）。

---

## 尚未處理

- 旁白模板仍是靜態字串，未獨立成可熱替換的文字資料檔。
- 對手佈陣 AI 使用固定規則，未來可接上打者熱區 / 投手疲勞度做更精細判斷。
- 正式 PNG sprite sheet 與逐幀動畫素材。
- 真實球員資料庫。
- 賽季、WBC、管理系統進一步模組化。
