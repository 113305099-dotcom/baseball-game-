# v3.22 更新書

更新日期：2026-05-18  
主題：守備動畫修正、先發投手保護、賽後回顧增強、對手投球傾向、UI 佈局改善

---

## 本次完成

### 1. 守備動畫 Bug 修正（外野手穿越內野問題）

**問題：** 當滾地球穿越內野滾到外野時，外野手的動畫顯示其跑到內野接球，而非停留在外野。

**根本原因：** `fielding-engine.js` 的 `playPointFor` 函式只在 `landingDistanceM >= 52` 時才為外野守備員計算外野接球位置；若球的「第一彈跳點」在內野（< 52m），外野手的 `playPoint` 被計算成內野落點，導致動畫跑到內野。

**修正（`fielding-engine.js`）：**

```js
// 修改前
if (isOutfieldPosition(entry.position) && landingDistanceM >= 52) {

// 修改後（移除 landingDistanceM >= 52 條件）
if (isOutfieldPosition(entry.position)) {
  const infieldLimit = ballInfo.groundProfile?.infieldLimitM || 52;
  const outfieldPickupM = Math.max(infieldLimit, 52) + 8;
```

外野手現在一律在外野接球，不管球從哪個位置穿越到外野。

---

### 2. 先發投手保護（比賽中禁止更換先發）

**問題：** 比賽開始後，玩家可從名單管理頁面直接更換先發投手，違反棒球規則。

**修正（`game.js`）：**

新增 `isMatchInProgress()` 方法：
```js
isMatchInProgress() {
  return this.inning > 1 || this.playerScore > 0 || this.opponentScore > 0 ||
         this.playerHits > 0 || this.opponentHits > 0;
}
```

在 `selectStartingPitcher()` 中加入防護：
```js
if (this.isMatchInProgress()) {
  this.addManagementLog('比賽進行中無法更換先發投手，請使用「換投」功能。');
  return false;
}
```

比賽進行中必須使用 `bringInReliever()` 換投（已存在的後援換投功能），不能重新指定先發。

---

### 3. 賽後回顧增強

**原本：** 賽後彈窗只顯示勝負、比分、資金、聯盟戰績。

**新增內容：**

#### 3a. 賽事統計追蹤（`matchStats`）

`Game` constructor 加入：
```js
this.matchStats = { playerK: 0, playerBB: 0, playerHR: 0, opponentK: 0, opponentBB: 0, opponentHR: 0, keyEvents: [] };
```

`addToLog()` 中自動掃描事件文字，記錄三振、保送、本壘打，以及安打/全壘打等重要事件（最多 12 條）。

每場比賽前在 `prepareNextMatch()` 中重置。

#### 3b. 賽後摘要 Modal 擴充

新增 HTML 區塊：
- `#summary-linescore`：逐局記分板（9 局 + R/H/E）
- `#summary-stats`：雙方 HR/K/BB 對照
- `#summary-events`：最多 8 條關鍵事件

`showMatchSummary()` 函式新增參數接收上述資料，並動態填入 DOM。

---

### 4. 對手投球傾向顯示

進攻面板（`panel-offense`）新增「對手配球傾向」區塊，依照目前球數、壘況、投手球種即時顯示速球/變化球/指叉系的預期機率。

**計算邏輯（`computeOpponentPitchTendency()`）：**

- 基礎機率來自 `opponentPitchPlan`（fastball / breaking / waste / balanced）
- 依球數調整：3 壞球傾向速球（避免保送）、2 好球傾向變化球（三振球）
- 依投手球種過濾：若投手無滑球/曲球，breaking 機率轉移至其他球種
- 依出力檔位調整：全力 → 速球加重

**UI 呈現：**
```
速球系 ████████░░ 62%
變化球 ████░░░░░░ 28%
指叉系 █░░░░░░░░░ 10%
```

隨每次球數更新動態調整，守備半局時自動隱藏。

---

### 5. 比賽介面 UI 佈局改善

#### 5a. 轉播日誌加高
- 從 `max-height: 80px` 改為 `flex: 0 0 160px; min-height: 120px; max-height: 200px`
- 字體從 `.62rem` 調整為 `.64rem`
- 行高從 1.5 調整為 1.6
- 使轉播欄更接近正方形，而非細長條

#### 5b. 右側控制欄加寬
- 從 `grid-template-columns: 1fr 320px` 改為 `1fr 360px`
- 1440px 以上螢幕進一步加寬至 `1fr 420px`（加入媒體查詢）

---

## 主要異動檔案

| 檔案 | 異動說明 |
|------|---------|
| `fielding-engine.js` | 移除外野接球距離條件，修正穿越內野動畫 bug |
| `game.js` | 新增 `isMatchInProgress()`、`computeOpponentPitchTendency()`；`addToLog` 追蹤 matchStats；`selectStartingPitcher` 防護；`showMatchSummary` 增強 |
| `index.html` | 賽後摘要 modal 新增逐局記分板/統計/關鍵事件；進攻面板新增對手投球傾向 UI |
| `style.css` | 轉播日誌加高；右欄加寬；投球傾向 bar 樣式；賽後摘要 modal 樣式 |
| `tools/smoke-test-engines.js` | 新增 `testV322Features()` |
| `PROJECT_OVERVIEW.md` | 更新版本與尚未完成清單 |

---

## 已驗證

```bash
node tools/smoke-test-engines.js
```

所有既有測試與新增 v3.22 測試均通過（engine smoke tests passed）。

---

## 尚未處理

- 打者個人數據追蹤（per-batter 安打/打點/HR 統計）
- 更精細的 MVP 評選（目前無個人上場統計）
- 對手投球傾向的球種名稱本地化（依投手真實球種代碼顯示中文名）
- 手機板面（768px 以下）的 UI 進一步最佳化
- 夾殺、折返跑壘等進階跑壘情境
