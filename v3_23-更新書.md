# v3.23 更新書

更新日期：2026-05-19
主題：主線劇情序章、旺來體育報紙頭版、後勤管理系統、球員養成三軌、賽後獎勵、轉播豐富化

---

## 本次完成

### 1. 主線劇情序章（首次進入遊戲）

新檔案：

- `story-data.js` — 劇情文本資料檔（與旁白文案放同層）
  - `INTRO_CHAPTERS`：序章 4 段（邁阿密的雨 → 失望的島嶼 → 政大棒球隊的誕生 → 你的接任）
  - `SEASON_INTERLUDES`：2026~2029 賽季中段穿插
  - `WBC_CHAPTERS`：晉級 / 淘汰 / 冠軍 / 亞軍 4 種結局文本
- `story-intro.js` — 序章 UI 模組（投影片式 modal）
  - `StoryIntro.autoPlayIfNeeded(game)` 在 `initializeGameRuntime` 結尾自動觸發
  - 已看過會跳過（`game.storylineIntroShown` 為 true）
  - 可「跳過序章」或逐段「繼續」
  - 結束時設定 `storylineIntroShown = true` 並存檔

### 2. 旺來體育報紙頭版（賽後摘要）

新檔案：

- `newspaper-summary.js` — 像素風報紙頭版
  - 報頭：「旺來體育」+ 英文副題 + 期數 + 日期（依 `game.currentYear`）
  - 結果條（勝 / 敗 / 平的不同色塊）
  - 隨機頭版大標（依比分差使用 `GAME_PARAMS.newspaper.headlines.bigWin/win/tie/close/loss/blowout/walkoff/comeback`）
  - 副標 lead 來自 `COMMENTARY_LIBRARY.newspaperLead`
  - 逐局記分板（9 局 + R/H/E）
  - 雙方 HR/K/BB 數據
  - 關鍵事件清單（最近 6 條）
  - MVP 區塊（含特性顯示）
  - 獎勵清單（資金 / 教練證 / 球員卡）
  - 聯盟戰績表
  - 像素字體（DotGothic16 + JetBrains Mono）

`SeasonManager.endMatch()` 改為優先呼叫 `NewspaperSummary.showNewspaperSummary()`，舊 `showMatchSummary` 留作 fallback。

### 3. 轉播文案豐富化（網路梗）

新檔案：

- `commentary-data.js` — 旁白資料庫獨立檔，含網路用語 / PTT 梗
  - homeRun：default / walkoff（再見全壘打）/ grandSlam（滿貫）/ powerHitter / tape（超大號）
  - strikeout：default / lookingStrike（看著三振）/ elitePitcher / crisis（危機三振）
  - single：default / clutchHitter / bunt / seeingEyeSingle
  - double / triple / groundOut / doublePlay / weakContact
  - flyOut / deepFly / sacrificeFly / shallow
  - popupOut / walk / disciplined / intentional
  - hitByPitch / error / throwingError / runScored / leadChange / tiedGame
  - opening / greatPlay / bigSituation
  - newspaperLead（報紙副標各情境）
  - 經典語句：「這球就像你的前女友 — 再也回不來啦！」「飛球 飛球 飛球⋯⋯阿出去啦！」「K！K！K！」「不可思議！這位野手剛剛飛起來！」

- `commentary.js` — 改造
  - `CommentaryGenerator.generateCommentary(outcome, player, cardActive, extra)` 新增 `extra` 上下文參數（walkoff / grandSlam / doublePlay / sacrificeFly / crisis / tape / looking 等）
  - `pickSubcategory()` 統一決定 subcategory（特性 + 情境）
  - `generateHeadline()` 從 `GAME_PARAMS.newspaper.headlines` 抽取（與報紙共用）

### 4. 後勤管理（取代防護中心）

新檔案：

- `logistics-system.js` — `LogisticsCenter` 取代 `MedicalCenter`（保留 `medicalCenter` 別名以維持相容）
  - 部門結構：
    - `farmTraining`：二軍訓練中心（每場給二軍球員 XP）
    - `majorTraining`：一軍訓練中心，含 `pitcher / batter / fielder` 三子部門（XP 倍率）
    - `recovery`：恢復中心（多回體力 + 傷病風險下降）
    - `marketing`：行銷部門，含 `events / cheer` 兩子部門（觀眾 / 主場士氣）
  - `assignCoach(coachId, deptKey, subKey)` 部署、`unassignCoach()` 解任、`upgradeCoach()` 升級
  - 教練升級：資金 + 教練證（費氏序列 1, 2, 3, 5, 8, 13, 21, 34）；最高 Lv9
  - `applyBetweenGamesEffects()` 場間自動發放二軍 XP + 多回體力 + 倒計時保護

`index.html`：

- 主畫面按鈕「防護中心」→「後勤管理」（OPS）
- 場景 ID `medical-tab` → `logistics-tab`（舊 SCENE_ID `medical` 保留作為 logistics 別名以兼容舊路由）
- 新增 `.logistics-tabs` 側邊頁籤：farm / major / recovery / marketing / coaches
- `renderLogisticsScene()` 取代 `renderMedicalScene()`，提供：
  - 二軍訓練中心：顯示二軍球員清單 + XP 增益值 + 教練部署
  - 一軍訓練中心：投手 / 打擊 / 守備 三子部門卡片，各自部署教練與顯示倍率
  - 恢復中心：個別球員治療 / 預防按鈕（原醫療功能保留）+ 教練部署
  - 行銷部門：活動 / 啦啦隊 兩子部門
  - 教練團：列出已聘教練、Lv、加成、升級按鈕（含資源檢查）

### 5. 球員養成三軌

新檔案：

- `player-growth-system.js`
  - `PlayerGrowth.ensureGrowthFields(p)`：補齊 `playerXP / playerLevel / cardLevel / cardCards / playerFragments / playerRank / growthLog`
  - 軌道一：經驗升級
    - `gainXP(player, amount, source)` / `checkLevelUp()` / `applyLevelUpBonus()`
    - 升級隨機 +2 屬性，由 `GAME_PARAMS.experience.statPointsPerLevel` 控制
    - 一軍訓練中心倍率自動套用（投/打/守）
  - 軌道二：球員卡升級
    - 抽到該球員的卡 → `addCardCopy(p, count)`
    - `upgradeCardLevel(p, game)` 消耗 1 張卡 + 資金，提升該球員能力
    - 上限受品階限制（`getAbilityCeiling`）
  - 軌道三：碎片品階
    - 該球員碎片 `playerFragments` → `upgradeRank(p)` 提升品階
    - 8 階（銅、銀、金、白金、鑽石、大師、宗師、傳奇）
    - 升階消耗碎片數為費氏序列：3 / 6 / 12 / 24 / 48 / 96 / 192
    - 每階 +5 能力上限、+10 潛力

`index.html`：

- `renderPlayerDetail()` 新增「球員養成（三軌）」區塊
- 每軌顯示目前數值 + 升級成本 + 操作按鈕（資源不足會 disable）
- 新增 `upgradeCardFromDetail(index)` / `upgradeRankFromDetail(index)` 全域函式

`game.js`：

- 抽到重複球員時，除了通用 `playerShards` 外，同步把碎片加到該球員的 `playerFragments`（用於品階提升）

### 6. 賽後獎勵系統

新檔案：

- `rewards-system.js`
  - `pickMVP(game, result)`：從上場球員找最高貢獻者
  - `generateRewards(game, result, summary)`：
    - 資金（依勝/平/敗 + MVP + 大勝 + 每安 / 每全壘打獎金）
    - 教練證（依勝負 + MVP / 完封加成；隨機 1~2 張）
    - 球員卡（依貢獻加權挑掉落對象；勝場 30% / 敗場 6%）
  - `applyRewards(game, rewards)` 套用到 `game.currency / coachCerts / 球員的 cardCards`

`game.js`：

- `Game.constructor` 新增 `coachCerts`、`playerBatterStats`
- `SaveManager.save` / `.load` 持久化 `coachCerts / playerBatterStats / logisticsData / playerGrowthData`
- `SeasonManager.endMatch()` 流程：先給 XP → 套用後勤效果 → 生成獎勵 → 套用獎勵 → 顯示報紙
- HUD 加上「教練證」chip（`#coach-cert-count`）

### 7. 打者個人數據追蹤（per-batter stats）

`game.js`：

- 新增 `_trackPerPlayerStats(txt, isOffense)`，在 `addToLog` 內依當前 `batter` / `pitcher` 累加：
  - 打者：`ab / h / hr / k / bb`
  - 投手：`k / bb`
- `prepareNextMatch()` 重置 `playerBatterStats`
- 賽後 MVP 評選使用此資料

### 8. 對手佈陣 AI 細化（依打者特性）

`game.js` `OpponentAI.decide()`：

- 力量型打者（怪力 / 力量打者 / 傳奇打者）→ 即使無人在壘也偏 `outfield_deep`
- 觸擊型打者（觸擊專家）+ 壘上有人 + 無人出局 → `bunt_defense`
- 巧打 / 紀律 + 三壘有人 → 強化 `infield_in`

### 9. 集中參數表

新檔案：

- `game-params.js` — `GAME_PARAMS` 全域物件，集中所有可調數值：
  - `experience`：每事件 XP、升級曲線、能力點數
  - `cardLevel`：每級資金成本、每級能力加成（打/投分別）
  - `rank`：品階名稱、晉升所需碎片、能力上限/潛力加成
  - `coaches`：費氏升級成本、每級加成、特性顯示
  - `logistics`：各部門 base / per-coach-level 數值
  - `rewards`：勝/敗/平資金、教練證機率、球員卡機率
  - `newspaper`：報紙標題各情境的隨機池

> 修改數值只需動 `game-params.js`，不需動任何引擎或 UI 程式碼。

---

## 主要異動檔案

| 檔案 | 異動 |
|------|------|
| `game-params.js` | 新檔。集中所有可調參數 |
| `story-data.js` | 新檔。主線劇情文本 |
| `commentary-data.js` | 新檔。豐富化轉播旁白資料庫 |
| `commentary.js` | 改寫。串接 commentary-data.js，新增情境 subcategory |
| `logistics-system.js` | 新檔。`LogisticsCenter` 取代 `MedicalCenter` |
| `player-growth-system.js` | 新檔。三軌養成（經驗 / 卡片 / 品階） |
| `rewards-system.js` | 新檔。賽後獎勵生成（資金 / 教練證 / 球員卡） |
| `newspaper-summary.js` | 新檔。旺來體育報紙頭版渲染 |
| `story-intro.js` | 新檔。序章 modal |
| `game.js` | 整合：LogisticsCenter、coachCerts、playerBatterStats、per-batter 追蹤、佈陣 AI 細化、save/load 擴充、endMatch 改用報紙與獎勵、storyIntro 自動播放 |
| `index.html` | 載入新模組、後勤管理 UI、教練證 HUD、球員養成三軌 UI、`renderLogisticsScene` 取代 `renderMedicalScene` |
| `style.css` | 報紙樣式、序章 modal、後勤頁籤 / 部門 / 教練卡、球員養成卡 |
| `PROJECT_OVERVIEW.md` | 同步更新到 v3.23 |
| `v3_23-更新書.md` | 本檔 |

---

## 資料結構新增 / 變更

### Game 新欄位

```js
this.coachCerts = 0;              // 教練證庫存
this.playerBatterStats = {};      // per-batter 即時統計
this.logisticsCenter = new LogisticsCenter(this); // 取代 medicalCenter
this.medicalCenter = this.logisticsCenter; // 別名相容
```

### Player 新欄位（由 PlayerGrowth.ensureGrowthFields 自動補齊）

```js
player.playerLevel       // 經驗等級
player.playerXP          // 累積 XP
player.cardLevel         // 球員卡等級（0~10）
player.cardCards         // 該球員的卡片庫存
player.playerFragments   // 該球員碎片
player.playerRank        // 0=銅 ~ 7=傳奇
player.growthLog         // 成長紀錄
```

### LogisticsCenter

```js
{
  departments: {
    farmTraining: { assignedCoachId },
    majorTraining: { sub: { pitcher, batter, fielder } },
    recovery: { assignedCoachId },
    marketing: { sub: { events, cheer } }
  },
  coachUpgrades: { [coachId]: { level, certInvested, moneyInvested } },
  protectionBuffs: {}
}
```

---

## 已驗證

- 手動讀過所有模組依賴關係：載入順序 `data.js → game-params.js → story-data.js → commentary-data.js → logistics-system.js → player-growth-system.js → rewards-system.js → newspaper-summary.js → story-intro.js → engine.js → ... → commentary.js → game.js`
- `index.html` 中 SCENE_ID 加入 `logistics`，舊 `medical` 名稱仍會路由到同一個畫面
- 所有新模組以 IIFE 封裝避免污染全域
- 保留 `MedicalCenter` 類別 + `game.medicalCenter` 別名，舊存檔可繼續使用

---

## 後續追加（2026-05-20）

### 10. CPBL 資料整合工具（tools/stat-mapper.html）

新檔案：

- `tools/stat-mapper.html` — 瀏覽器版 CPBL 資料整合工具（不需 Node.js / Python）
  - 上傳兩個檔案：`cpbl_players_2024-2026.json`（7.8 MB，Google Drive 下載）+ `data.js`
  - 加權規則：2025 × 1.00 / 2024 × 0.60 / 2026 × 0.80，打者以 PA 加權，投手以 BF 加權
  - 每位球員保留三季快照（`seasons.2024/2025/2026`）供賽季記錄與對比
  - 加權後數據直接寫回原 `CPBL_BATTER_STATS_2025` / `CPBL_PITCHER_STATS_2025` 陣列
  - 新增 `woba / iso`（打者）欄位
  - 每位球員注入完整 `advancedStats`：
    - `heatMap`（3×3，PA 加權平均）
    - `gbRate / ldRate / fbRate / popupRate / avgLaunchAngle`（from battedBall 分布）
    - `plateDiscipline / swingByCount / timing / pitchTypeMatchup / foulSplit`（取 2025-2026 合計切片）
    - `situational / splits / spray / ballQuality / highlights`
  - 彩色 log 輸出（命中 / 未命中 / 錯誤）
  - 「下載新的 data.js」按鈕，直接覆蓋遊戲根目錄
  - **使用步驟**：開啟 `tools/stat-mapper.html` → 上傳 JSON + data.js → 開始整合 → 下載 → 覆蓋 data.js

### 11. pitch-engine.js v1.1 — 四維進階數據接入

改造 `pitch-engine.js`，讓引擎讀取 `advancedStats` 的四個新維度：

**新增輔助函式（純函式，已掛到 `PitchEngine` 公開 API）：**
- `classifyPitchTypeCode(pitchName)` — 中文球種名稱 → FF / SI / FC / SL / CU / CH / FS
- `classifyPlateZone(pos)` — 最終位置 → core / edge / chase / invalid
  - core ≤ 11.25 cm（好球帶內半）
  - edge ≤ 22.5 cm（好球帶外緣）
  - chase ≤ 33.75 cm（追打區）
  - invalid > 33.75 cm（遠離好球帶）

**BatterJudgmentModule 改動：**
- 新增 `balls` 提取（來自 `battingConfig.balls`）
- `swingByCount`：依 `advancedStats.swingByCount["balls-strikes"].swingRate` 調整 `chaseProb`
  - 公式：`chaseProb += (swingRate - 0.47) * 0.30`（基準 47%，最大 ±0.09）
- `timing`：`_resolveTimingMod()` 新增第三參數 `timingData`
  - 讀取 `fastLatePct / fastEarlyPct`（或 slow 版本）計算 `badPct`
  - `timingAdj = -(badPct - 0.50) * 12`（最大 ±6 分，疊加在原 +8 / −14 基礎上）
  - 輸出範圍限縮：matched clamped to [−18, 12]

**ContactResolutionModule 改動：**
- `plateDiscipline` 混合（30%，需 ≥ 20 球樣本）：
  - 用 `classifyPlateZone(finalPosition)` 定位四區
  - `whiff / foul / inPlay` = 公式值 × 0.70 + 歷史值 × 0.30
- `pitchTypeMatchup` 混合（25%，需 ≥ 15 球樣本）：
  - 用 `classifyPitchTypeCode()` 取得球種代碼，查 `advancedStats.pitchTypeMatchup[code]`
  - `whiff / foul / inPlay` = 上步結果 × 0.75 + 歷史值 × 0.25
- 兩段混合均在兩好球修正**之前**套用；`_normalizeProbabilities()` 最後確保三路總和 = 1
- 無 `advancedStats` 或樣本不足時自動 fallback 到原公式，**無 breaking change**

**PitchEngineOrchestrator 改動：**
- 自動計算 `pitchTypeCode` 並注入 `battingConfig`（呼叫方無需手動傳）
- `balls` 傳入 `BatterJudgmentModule.decide()`

---

## 尚未處理

- **data.js 尚未實際更新**：`tools/stat-mapper.html` 工具已就緒，需使用者手動操作（上傳 JSON + data.js → 下載覆蓋）
- **補球員 meta**：守位 / bats / 身高體重 / 生日 / 照片 URL — 需從 CPBL 球員頁爬取，供 player-appearance.js 使用
- **補守備統計**：FLD% / E / A — 需從 CPBL 守備頁爬取，補 `abilities.fielding / arm`
- **newspaper-summary.js 接 highlights**：目前 `advancedStats.highlights`（home_runs / top_ev / clutch_pas）尚未被報紙摘要讀取
- **投打對決規則書 §17.x 更新**：四區（core / edge / chase / invalid）系統已在引擎實作，但規則書 §17.6 / §17.9 仍用舊 9×9 catch zone 描述，需同步更新文字
- **引擎改造（討論中）**：foul 升一等公民（子類型 + 獨立公式）、timing window 維度、球種對應 contact / foul 修正
- Sprite 美術整合（球員 / 背景）仍待 sprite-generation-prompts.md 流程生成
- 旁白資料的更細分情境（如「投手 6 局無失分 + 領先」場景）
- 報紙頭版要不要做成 Canvas 像素繪而非 HTML？目前先用 HTML + 像素字體，視覺已接近報紙
- 球員養成升級成本與能力提升量是初版，請依實際 playtest 調整 `game-params.js`
- 賽後旺來體育生成圖檔可另開「分享圖卡」功能（後續再做）
