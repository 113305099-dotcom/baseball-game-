# v3.24 更新書

更新日期：2026-05-20  
版本：v3.24

---

## 核心目標

建立 NBA 2K 式的球員能力值體系，讓玩家可以直觀地看到每位球員的強弱，並且讓數值對投打對決實際產生影響。

---

## 1. 設計原則：分段線性 CPBL 校準

### 標尺定義

| 球員水準 | 能力值 |
|----------|--------|
| 中職弱勢球員（出賽邊緣） | ~45 |
| **中職聯盟平均** | **60** |
| **中職頂尖（賽季 MVP 級）** | **~82** |
| 保留升級 / 更強聯盟空間 | 83–100 |

設計動機：
- 若用傳統線性縮放，成績較差的中職球員可能只有 10–20 分，玩起來毫無樂趣。
- 頂級中職球員不設 100 分，是為了讓玩家有升級的動力，也為未來 MLB 球隊、WBC 他國球員留下空間。

### 分段線性公式

```js
piecewise(x, lo, mid, hi, lo_s=45, mid_s=60, hi_s=82)
// x ≤ lo  → 45（底線）
// x = mid → 60（聯盟平均）
// x ≥ hi  → 82（聯盟頂尖）
// 中間兩段各自線性插值
```

數值越低越好的統計（ERA、BB%、WHIP）使用 `piecewiseInv()`（反向版本）。

---

## 2. 打者能力值

| 屬性 | 計算來源 | 參考區間（lo / avg / hi） |
|------|----------|--------------------------|
| `contact` | AVG×65% + OPS+×35% | AVG: .230/.275/.340；OPS+: 65/100/165 |
| `power` | ISO×60% + HR(per 600PA)×40% | ISO: .04/.11/.23；HR: 0/8/28 |
| `speed` | SB + 位置加成（CF+7、SS/OF+4、2B/3B+2） | SB: 0/12/45 |
| `discipline` | BB%×50% + 反向K%×50% | BB%: 3/7.5/14；K%（反）: 9/19/30 |
| `fielding` | 守位固定值（暫缺個人守備統計） | C=68、SS/2B=66、OF=63、1B=58 |
| `arm` | 守位固定值 | C=70、SS/3B=65、OF=63、2B=62、1B=57 |
| `clutch` | OPS+ 直接換算 | 同 contact 中的 OPS+ 區間 |
| `vsLeft` | contact×0.55 + discipline×0.25 + 左右打加成 | |
| `vsRight` | contact×0.55 + power×0.20 + 左右打加成 | |
| `scoringPosition` | = clutch | |
| `pinchHitter` | power×0.40 + discipline×0.35 + contact×0.25 | |

---

## 3. 投手能力值

| 屬性 | 計算來源 | 參考區間（lo / avg / hi） |
|------|----------|--------------------------|
| `velocity` | K%×55% + K/9×45% | K%: 12/20/32；K/9: 4/7/11 |
| `control` | 反向BB%×45% + 反向WHIP×55% | BB%（反）: 3/8/14；WHIP（反）: .85/1.22/1.55 |
| `breaking` | ERA+×55% + 反向FIP×45% | ERA+: 60/100/170；FIP（反）: 2.0/3.8/5.5 |
| `stamina` | IP（先發：85/150/210；後援：25/55/80） | |
| `fielding` | 68 + 先發場次修正（+0~7） | |
| `discipline` | control×0.70 + breaking×0.30 | |
| `stuff` | velocity×0.48 + breaking×0.36 + control×0.16 | |
| `vsLeft` | breaking×0.45 + control×0.35 + 左投加成 | 左投+8，右投+2 |
| `crisis` | control×0.40 + breaking×0.35 + 反向WHIP×0.25 | |
| `pickoff` | control×0.45 + fielding×0.35 + starts×0.30 | |
| `quickDelivery` | control×0.35 + velocity×0.25 + fielding×0.40 | |
| `recovery` | 後援基礎72 / 先發基礎57 + IP補正 | |

---

## 4. 儲存方式

每位球員的 `abilities` 物件由 `tools/stat-mapper.html` 在輸出 `data.js` 時預先計算並嵌入：

```js
{
  name: '陳傑憲',
  // ... 其他統計欄位 ...
  abilities: {
    contact: 72,
    power: 58,
    speed: 64,
    fielding: 63,
    arm: 63,
    discipline: 66,
    clutch: 70,
    vsLeft: 70,
    vsRight: 68,
    scoringPosition: 70,
    pinchHitter: 63
  }
}
```

---

## 5. game.js 讀取邏輯

`StatMapper.getBatterAbilities()` / `getPitcherAbilities()` 改為「讀取優先」：

```js
getBatterAbilities(stats) {
  // 優先使用 stat-mapper 預計算值
  if (stats.abilities && stats.abilities.contact != null) return stats.abilities;
  // fallback：動態公式（相容無 abilities 欄位的舊資料）
  // ...
}
```

投手同理（檢查 `stats.abilities.velocity != null`）。

---

## 6. 升級系統命名修正

**問題**：`player-growth-system.js` 的打者升級屬性用 `hitting / eye / defense`，但 `StatMapper` 回傳的是 `contact / discipline / fielding`，導致升級時找不到對應欄位、升級無效。

**修正（三處）**：

| 檔案 | 修改內容 |
|------|----------|
| `game-params.js` | `batterAttrs: ['hitting',...,'eye',...,'defense']` → `['contact','power','discipline','speed','fielding']` |
| `game-params.js` | `batterBonusPerLevel: {hitting:1,eye:1,defense:1,...}` → `{contact:1,power:1,discipline:1,speed:1,fielding:1}` |
| `player-growth-system.js` | 兩處 fallback 預設值同步更新 |

投手屬性（`velocity / control / breaking / stuff / stamina`）命名原本就一致，無需修改。

---

## 7. 操作方式（如何讓新能力值生效）

1. 打開 `tools/stat-mapper.html`（瀏覽器）
2. 上傳 `cpbl_players_2024-2026.json`（下載資料夾）
3. 上傳 `data.js`（遊戲根目錄）
4. 點「匯出全部球員」（模式二）
5. 下載新版 `data.js` → 覆蓋遊戲根目錄的 `data.js`
6. 重新整理 `index.html`

每位球員現在會有 `abilities` 物件，投打對決與升級系統都會正確使用。

---

## 8. 修改過的檔案

| 檔案 | 變更摘要 |
|------|----------|
| `tools/stat-mapper.html` | 新增 `piecewise()`、`piecewiseInv()`、`posBaseDefense()`、`posBaseArm()`、`computeBatterAbilities()`、`computePitcherAbilities()` 函數；`buildBatterEntry()` / `buildPitcherEntry()` 輸出加入 `abilities:` 欄位 |
| `game.js` | `StatMapper.getBatterAbilities()` / `getPitcherAbilities()` 加入 early-return 讀取優先邏輯 |
| `game-params.js` | 打者升級屬性名稱修正（3 個欄位） |
| `player-growth-system.js` | 打者升級屬性名稱 fallback 修正（2 處） |
| `PROJECT_OVERVIEW.md` | 版本升至 v3.24，更新文字說明與接手順序 |

**未新增任何新 .js 模組，無需修改 `index.html` 的 `<script>` 載入順序。**

---

## 9. 尚未完成（v3.24 後的下一步）

1. **執行 stat-mapper.html 更新 data.js**：工具已就緒，需手動操作（重跑一次以產生含 `abilities` 的新版 data.js）。
2. **補守備統計**：`fielding` / `arm` 目前是守位固定值，需從 CPBL 守備頁爬取個人數據才能更精準。
3. **補球員 meta**：守位、打擊側、身高體重，讓 `computeBatterAbilities` 的位置加成生效。
4. **投打對決規則書 §17.x 更新**：四區（core/edge/chase/invalid）文字說明仍用舊版，需同步。
5. **newspaper-summary.js 接 highlights**：advancedStats.highlights 尚未讀取。
