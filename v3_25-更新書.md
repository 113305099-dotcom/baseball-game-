# v3.25 更新書

更新日期：2026-05-21  
版本：v3.25 — 升級體系統合 + 天賦/特質拆分 + UI/體力/投手特化補強

> 本檔涵蓋 v3.25.0（A→B 組）和 v3.25.1～4（C/D/E 組）四個小批次的所有變更。

---

## 核心目標

1. **統合雙升級系統**：v3.23 的 PlayerGrowth + v1.18 的 awardPlayerXP 內部 levelUp 並存衝突，本版本合併為單一升級流水線
2. **天賦 / 特質徹底拆分**：用戶提出的關鍵設計修正——從統計推出的標籤不應該再回頭加成
3. **字母評級體系**：能力值旁顯示 S/A/B/C/D 字母，玩家一眼掌握強弱
4. **能力 tooltip**：滑鼠 hover 巧打/長打/變化球等顯示說明

---

## 1. 雙升級系統合併（A1）

### 修改前的問題

```
game.js: awardPlayerXP()
  ├─ 累積 XP
  ├─ 達門檻 → 內部 levelUp + 加屬性
  ├─ 屬性上限 = RATING_STAT_CAP（S=99, A=92, B=84, C=70, D=58）

player-growth-system.js: PlayerGrowth.gainXP()
  ├─ 累積 XP
  ├─ 達門檻 → applyLevelUpBonus 隨機 +1 兩個屬性
  ├─ 屬性上限 = getAbilityCeiling = 95 + rank × 5
```

兩者並存，**game.js 那條**才是真實運作的路徑；PlayerGrowth 那條從未被觸發。
結果：D 評等球員永遠不能突破 58（卡 RATING_STAT_CAP），即使升品階也沒用（PlayerGrowth.upgradeRank 的 ceiling 邏輯被旁路）。

### 修改後

```
game.js: awardPlayerXP(player, baseXP, category, game)
  ├─ 計算教練 xpBonus + RATING_GROWTH_MULTIPLIER（S 1.5x ... D 0.6x）
  └─ 委派給 PlayerGrowth.gainXP(player, gain, category)
       ├─ 累積 XP
       ├─ 達門檻 → applyLevelUpBonus(player, category)
       │    └─ 依 category 升對應屬性（batting → contact/power/discipline/clutch）
       └─ 屬性上限 = getAbilityCeiling(player) = 95 + playerRank × 5
```

**單一真理**：所有 XP/level 流水線走 PlayerGrowth；rating 只影響 XP 累積速度；rank 統一管 ceiling。
RATING_STAT_CAP 保留但標記 `deprecated`，可在後續版本移除。

### 補修：投手保送 XP 懲罰

`pitch-result-applier.js` 原本當 battingTeam === 'opponent' 時給投手 +3 XP（保送對手反而獎勵投手，明顯錯誤）。改為 -1 XP，與 `PARAMS.experience.xpPerWalkPitcher` 設定一致。

---

## 2. 字母評級顯示（A2）

### 字母評級表（GAME_PARAMS.abilityGrades）

| 分數區間 | 字母 | 顏色 | 標籤 |
|---------|------|------|------|
| 90+ | **S** | 金黃 | 超凡 |
| 80–89 | **A** | 紫色 | 頂尖 |
| 70–79 | **B** | 藍色 | 優良 |
| 60–69 | **C** | 綠色 | 中庸 |
| 50–59 | **D** | 灰色 | 尚可 |
| 0–49 | **E** | 深灰 | 不足 |

### 新增 helper

```js
window.getAbilityGrade(value) → { min, grade, color, label }
window.abilityWithGrade(value) → '<span class="ability-val">82</span><span class="ability-grade grade-A">A</span>'
```

### UI 套用位置

- **球員卡（gacha 中央位置）**：六大屬性旁加上 chip
- **球員詳細頁 (renderPlayerDetail)**：abilityGrid 套用 `abilityWithGrade()`
- 樣式定義在 `style.css` `.ability-grade.grade-*`

---

## 3. 天賦 vs 特質拆分（B 組核心）

### 設計哲學（用戶提案）

> **天賦（talent）** = 從球員真實數據（abilities）推出的標籤  
> 純顯示，告訴玩家「這球員選球特別好」「打很遠」  
> 已反映在 abilities 數值上，不會二次加成

> **特質（trait）** = 透過品階提升解鎖的能力  
> 真正會在投打對決中觸發 buff  
> 兩者名稱必須不同，避免混淆

### 完整對應表（GAME_PARAMS.talents + traits）

| 能力門檻 | 天賦（純顯示） | 特質（解鎖加成） | 特質效果 |
|---------|---------------|-----------------|---------|
| contact ≥ 80 | 安打製造機 | **神之巧手** | finalContactScore +6 |
| power ≥ 80 | 重砲手 | **巨人之力** | effectivePower +7 |
| power ≥ 90 | 怪力 | — | （怪力本身已是高門檻天賦） |
| discipline ≥ 80 | 選球眼 | **綠繡眼** | eye +7（影響追打率） |
| speed ≥ 80 | 快腿 | **閃電俠** | 盜壘成功率 +0.10 |
| fielding ≥ 80 | 鐵手套 | **絕對守備** | catchRange +8% |
| arm ≥ 80 | 強肩 | **神鵰俠侶** | throwVelocity +10% |
| clutch ≥ 80 | 大心臟 | **最終兵器** | 得點圈 contact +8、power +5 |
| vsLeft ≥ 80 | 剋左投 | **剋左狂魔** | 面對左投 contact +7、power +5 |
| velocity ≥ 82 | 火球男 | **光速球** | velocity +6 |
| control ≥ 82 | 精準投手 | **縫紉機** | control +5 |
| breaking ≥ 82 | 變化球大師 | **魔球師** | breaking +5 |
| stamina ≥ 82 | 鐵人 | **無限發動機** | 體力下降 -15%（v3.25.x 接入） |
| stuff ≥ 86 & crisis ≥ 82 | 尖牙利齒 | **絕對王牌**（需鑽石階） | 高 leverage ctrl +5、break +4 |
| any (品階 6+) | — | **覺醒** | 全能力 +2（必殺技預留） |

負面天賦（純顯示，不再額外懲罰）：
- **控球不穩**（control ≤ 58）—— abilities.control 低分已直接懲罰，毋須再扣
- **恐左**（vsLeft ≤ 60）—— vsLeft 低分已反映在 contact 修正中

### 程式碼變更

**game.js StatMapper：**
- `inferTraits` → `inferTalents`（保留同名別名向後相容）
- `createPlayerFromStats`：將結果存到 `player.talents`，`player.traits` 改為 `[]`

**game.js resolveAtBatWithContext：**
舊：`if (batter.traits.includes('選球眼')) eye += 7;`
新：`if (batter.traits.includes('綠繡眼')) eye += 7;`（13 處全部更新）

**game.js OpponentAI：**
讀 `upcomingBatter.talents` 來判斷打者特性（不再讀 traits）。

**player-growth-system.js：**
新增 `unlockTraitsByRank(player)`，每次 `upgradeRank` 自動呼叫。檢查 `GAME_PARAMS.traits` 中每筆，若 `abilities[unlocker] ≥ min` 且 `playerRank ≥ minRank`，push 對應特質名到 `player.traits` 並記錄到 growthLog。

**commentary.js：**
旁白觸發條件同時檢查 talents（天賦）+ traits（特質）。

**Save/Load migration：**
舊存檔載入時，把 `player.traits` 中的天賦字串自動移到 `player.talents`，真正的特質（光速球、巨人之力等）留在 traits。

---

## 4. 能力 tooltip（C 組部分完成）

`window.ABILITY_DESCRIPTIONS` 表，覆蓋打者九項 + 投手九項。在 `abilityGrid()` 套上 `title` 屬性，hover 即顯示說明。

範例：
- 巧打 → 「接觸打擊能力。影響進場率（一壘安打、二壘安打）」
- 變化球 → 「變化球尾勁。增加揮空率、減少擊出強勁球」
- 危機處理 → 「得點圈時的修正。壘上有人時 control 加成」

---

## 5. v3.25.1 後勤中心 UI 修正（C7）

使用者反映「後勤中心很多字體跟背景都是黑色根本看不到」。修正方式：

在 `style.css` 加入 `#logistics-tab` scope 的廣譜選擇器：
- 預設所有後勤頁子元素 `color: var(--text)`（防止 missing color 被瀏覽器套黑）
- 個別 amber/teal 背景的元素（coach-level、logistics-btn、growth-btn）強制深色文字 `#0a0e1a`
- `.logistics-select option` 補上深色背景 + 亮色文字（解決瀏覽器原生 option 樣式對比差）
- 補強 `input[type=text/number]`、`select` 在後勤頁中的對比度
- `dept-player-row` 內 `strong` / `muted` 明確指定顏色

→ 整體後勤頁不會再有黑底黑字問題。

## 6. v3.25.2 投手體力條改文字狀態 + 非線性下降（D9）

### 5 級狀態（GAME_PARAMS.staminaStates）

| 階段 | 範圍 | 名稱 | 圖示 | 能力修正 | 每球扣血倍率 |
|------|------|------|------|---------|------------|
| 5 | 80–100 | 龍精虎猛 | 🐉 | vel+2 / ctrl+2 / break+2 | 0.50x |
| 4 | 60–80 | 微微出汗 | 💧 | 不變 | 0.85x |
| 3 | 35–60 | 略顯疲憊 | 😮‍💨 | vel-3 / ctrl-2 / break-2 | 1.20x |
| 2 | 15–35 | 累死了 | 😩 | vel-8 / ctrl-6 / break-4 | 1.70x |
| 1 | 0–15 | 懷疑人生 | 💀 | vel-15 / ctrl-12 / break-10 | 2.20x |

**非線性下降**：從滿血到 60% 約 50 球（緩慢），60→35% 約 30 球（中），35→15% 約 17 球（崩潰），15→0 約 10 球（瀕死）。

**能力影響**：`resolveAtBatWithContext` 在計算有效 vel/ctrl/breaking 時自動套上 `staminaState.mods`。

### 程式改動

- **`game-params.js`**：新增 `staminaStates`
- **`game.js`**：新增全域 `getStaminaState(player)` helper；`Player.consumeStamina` 套上 `state.decayMul`；`Player.getStaminaStateInfo()` 提供 UI 使用
- **`index.html`**：投手體力區（`#pitcher-stamina`）改為文字狀態 + 圖示 + 顏色 chip，數字進度條移到 `title` tooltip；球員詳細頁和 pregame 投手選擇列同步顯示文字狀態

## 7. v3.25.3 球路選擇三框拖拉 UI（D12）

### 設計

```
┌─────────────────────────┐ ← 無效區 ±38 cm（紅虛線）
│                         │
│  ┌───────────────────┐  │
│  │                   │  │ ← 追打區 ±33.75 cm（黃虛線）
│  │   ┌───────────┐   │  │
│  │   │ 3×3 micro │   │  │ ← 好球帶 ±22.5 cm（綠實線）
│  │   │  ⊙ ⊙ ⊙   │   │  │
│  │   │  ⊙ ◎ ⊙   │   │  │   ◎ = 中央
│  │   │  ⊙ ⊙ ⊙   │   │  │
│  │   └───────────┘   │  │
│  └───────────────────┘  │
│                         │
└─────────────────────────┘
       準星 🟠 可拖拉
```

容器 200×200 px 對應 76×76 cm（1 cm ≈ 2.63 px）。
玩家可以：
- **拖拉**：在容器任意位置拖拉準星到精確座標（連續 cm 座標）
- **點擊微格**：點 3×3 內任一格快速跳到該格中心
- **支援觸控**：mobile 也能拖拉

### 引擎整合

`pitch-engine.js: PitchPhysicsModule.generate()` 接受 `pitchConfig.aimPosition`（優先於 `aimCellIndex`）：

```js
const originalTarget = (pitchConfig.aimPosition && Number.isFinite(pitchConfig.aimPosition.x))
  ? { x: pitchConfig.aimPosition.x, y: pitchConfig.aimPosition.y }
  : getGridCenter(aimIndex, GEOMETRY.pitcherAimSize);
```

`game.js: resolveAtBatWithContext()` 當玩家投球時把 `game.pitchAimPosition` 傳給引擎；對手 AI 仍走舊的 `aimCellIndex` 流程。

`Game.setPitchAimCell()` 同步更新 `pitchAimPosition` 為該格中心，舊 API 仍可用。

## 8. v3.25.4 下勾/側投投手特殊加成（E13）

### 資料

`GAME_PARAMS.specialPitcherArmSlot` 表（手動維護）：

| 球員 | armSlot |
|------|---------|
| 黃子鵬 | sidearm（低肩側投，2022 防禦率王） |
| 宋家豪 | submarine（前旅日下勾） |
| 林岳谷 | submarine（味全龍下勾救援） |
| 陳鴻文 | sidearm（中信兄弟前低肩） |
| 倪福德 | sidearm（前 MLB 老虎隊） |
| 鄭凱文 | sidearm（富邦悍將救援） |
| 江國謙 | submarine（統一獅下勾） |

`GAME_PARAMS.armSlotBonus`：

| 出手點 | stuffScore | movementScale | vsRight contact | vsLeft contact |
|--------|-----------|---------------|-----------------|---------------|
| submarine | +6 | +0.05 | -4 | +2 |
| sidearm | +4 | +0.03 | -3 | +1 |

### 引擎整合

- `createPlayerFromStats`：投手若在 `specialPitcherArmSlot` 表中，自動標記 `player.armSlot` 且推為「天賦」（下勾投手 / 低肩側投）
- `estimateStuffScore`：套上 armSlot bonus 的 `stuffScore` 加值（球速不快也能壓制）
- `resolveAtBatWithContext`：依據打者左右手套上 `vsRightContact` / `vsLeftContact`，並在比賽 log 中報「下勾出手讓打者難以判斷球路」

**設計動機**：黃子鵬等球員真實 ERA+ 普遍偏高，但 abilities.velocity 因球速不到 135 km/h 會被算低。加上出手點修正後，stuff 重回頂級水準，符合「球質難打」的真實表現。

---

## 9. 修改的檔案清單（v3.25 全部）

| 檔案 | 變更摘要 |
|------|----------|
| `game-params.js` | 新增 `abilityGrades`、`talents`、`traits`、`staminaStates`、`specialPitcherArmSlot`、`armSlotBonus` 六大資料表 |
| `game.js` | `inferTraits` → `inferTalents`；移除舊 trait 加成、改為新特質名；新增 `getAbilityGrade` / `abilityWithGrade` / `getStaminaState` helper；`awardPlayerXP` 委派 PlayerGrowth；存檔 migration；OpponentAI 改讀 talents；`Player.consumeStamina` 非線性下降；`estimateStuffScore` 加 armSlot 加成；`setPitchAimCell` 同步 `pitchAimPosition`；`createPlayerFromStats` 自動標記 armSlot |
| `player-growth-system.js` | `applyLevelUpBonus` 支援 category；新增 `unlockTraitsByRank`；`upgradeRank` 觸發特質解鎖 |
| `pitch-engine.js` | `PitchPhysicsModule.generate` 支援 `aimPosition` 連續座標 |
| `pitch-result-applier.js` | 投手保送從 +3 改為 -1 XP |
| `commentary.js` | 旁白觸發條件加入新天賦+特質名 |
| `index.html` | `abilityGrid` 套用字母評級；`renderPlayerDetail` 新增天賦/特質兩區；投手體力條改文字狀態；五宮格瞄準 UI 改為三框拖拉式；新增 `bindPitchAimZone`、`setPitchAimPositionCm`、`updateAimCursorDisplay` |
| `style.css` | 新增 `.ability-grade.grade-*`、`.detail-talents`、`.detail-traits-v2`、`.talent-pill`、`.trait-pill-v2`、`.pitch-aim-zone`、`.aim-frame.*`、`.aim-grid-3x3`、`.aim-cursor`；強化後勤頁 `#logistics-tab` scope 對比 |

---

## 10. 尚未完成（延後到 v3.26）

### 從 v3.24 延續
- 補球員 meta（守位、bats、身高體重）— 許多新球員預設 DH / R
- 補守備個人統計（fielding/arm 個人數據）取代守位固定值

> **註**：data.js 已經有 v3.24 預計算的 abilities（371 處），不需要為 v3.25 重跑 stat-mapper.html。只有換新球員 / 加新球季資料時才需要重跑。

### v3.25 過程中發現的新項目
- 擴充 `specialPitcherArmSlot` 名單（目前只有 7 位，CPBL 還有更多下勾/側投投手）
- 後勤中心其他可能的視覺問題（待 playtest 反饋）
- 球路三框拖拉 UI 的 mobile 觸控體驗 fine-tune
- 體力 5 級狀態的數值平衡（依 playtest 調整）

### 引擎面
- 投打對決規則書 §17.x 更新：四區（core/edge/chase/invalid）系統已實作但規則書文字尚未同步
- newspaper-summary.js 接 `highlights.home_runs / top_ev / clutch_pas`
- foul 升一等公民、timing window 維度、球種對應 contact/foul 修正（討論中）

---

## 11. 用戶可立即測試的事項

### 球員管理
1. 進入「我的球員」→ 任一球員詳細頁
2. 確認可看到：**🌱 天賦** + **✨ 特質** 兩區、字母評級 chip、hover 能力 tooltip

### 比賽中（投手體力）
3. 開始比賽 → 看到投手「龍精虎猛」chip 在右上角
4. 投 20-30 球後切到「微微出汗」→ 50-60 球進「略顯疲憊」→ 球速明顯下降

### 比賽中（球路瞄準）
5. 進攻換守備時看右側面板「瞄準位置」
6. 拖拉橘色準星 → 看到下方讀數變化「(15, -8) cm」
7. 點 3×3 微格 → 準星跳到該格中心
8. 試圖拖拉到無效區（紅虛線外）→ 暴投機率明顯增加

### 比賽中（下勾投手）
9. 投手位置試用「黃子鵬」「林岳谷」（如有）
10. 看 log 顯示「下勾出手讓打者難以判斷球路」
11. 對右打者 contact 明顯下降

### 升級系統
12. 後勤管理 → 球員養成 → 試升一個球員的品階
13. 若該球員 abilities 達門檻，會看到「🌟 解鎖特質：xxx」訊息
14. 解鎖後該球員上場，比賽中該特質生效（例如「最終兵器啟動！」log）

### 後勤中心
15. 進入後勤管理 → 所有頁籤 → 確認沒有黑底黑字、文字都可讀
