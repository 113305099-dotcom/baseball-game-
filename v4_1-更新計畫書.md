# 政大棒球征服世界 — v4.1 更新計畫書

> 建立日期：2026-05-29
> 來源需求：`C:\Users\user\Downloads\v4.1.docx`（8 項需求 + 圖1~圖4）
> 性質：本版幾乎全是 **UI/UX + 系統面**（經濟、養成、引導、比賽介面），只有第 7 項「失控」會碰引擎數值。
> **與投手引擎校準的關係**：v4.1 為更急的任務，原排程的「投手體系架構失衡修正（修正書 §15.8）」**暫時擱置**，待 v4.1 完成後再續。

---

## 0. 確定的設計決策（2026-05-29 與使用者確認）

| 議題 | 決策 |
|------|------|
| **執行順序** | 由淺入深四階段：Phase1 輕量UI → Phase2 系統 → Phase3 比賽UI → Phase4 新手教學 |
| **黃金棒球來源** | 任務成就 + 賽後/封王表現獎勵 + 賽季里程碑；**不開放資金兌換**。（前期保底待定：可選「每場固定小額」防卡關） |
| **黃金棒球用途** | 抽 **教練池** + **海外池（國際球星＋台灣傳奇球員）**；不開放資金兌換 |
| **資金用途** | 經營（訓練/聘僱/升級）+ **本土現役球員池**（本土池用資金抽，非黃金棒球） |
| **教練模型** | 專長對應位置：教練有領域（打擊/投手/守備/跑壘），放一軍時對「對應族群球員」加對應能力；教練分 SABCD，等級越高每次升級加越多 |
| **比賽UI** | 映像管 CRT 風格 + 旋鈕（懸停展開扇形）+ 四段出力條 + 對決按鈕，全做 |
| **瞄準方式** | **投手保留現有「三框拖拉」**；**打者保留/回到「九宮格」** |
| **失控機制** | 引擎一併更新，紅區出力真的有失控風險、控球差更易失控 |

> ⚠️ 下方所有 `檔案:行號` 來自 2026-05-29 的程式碼勘查，是「概略錨點」，施工前請以當下檔案實際內容為準（程式碼會變動）。

---

## Phase 1 — 輕量 UI（需求 #4、#5）

目標：快速見效、低風險、不動數值與引擎。先把畫面整理乾淨。

### 1A. 縮小頂部狀態欄（#4）
- **現況**：`index.html` 約 15–48 行 `<header class="hud-bar">`，內含 6 個 `.hud-chip`（資金/碎片/教練證/戰績/賽程/對手）。CSS `style.css` 約 63–155 行：`.hud-bar` sticky、`.hud-stats` grid 4 欄、`.hud-chip` min-height 52px、padding 偏大（即圖2 那條很佔空間的欄）。
- **改法**：
  1. 壓縮 `.hud-chip` 的 `min-height`、`padding`、字級；標題與數值改單行或更小的兩行。
  2. `.hud-bar` 整體高度下修，sticky 保留。
  3. 考慮把次要 chip（碎片、教練證）收進可展開的小區，或縮成 icon + 數字。
- **檔案**：`index.html`（HUD 區塊）、`style.css`（`.hud-bar` / `.hud-stats` / `.hud-chip`）。
- **驗收**：頂欄高度明顯下降、不遮擋主內容；桌面/窄螢幕都不破版。
- **風險**：低。注意 v4.1 #6 要求「比賽中刪掉狀態欄」——Phase 1 先縮小全域 HUD，比賽中的隱藏留到 Phase 3 一起做（避免兩次改同一段）。

### 1B. 準備階段一頁預覽 + 移除「教練團」（#5）
- **現況**：`index.html` 約 121–156 行 `id="pregame-tab"`，`.pregame-grid` 內含多個 side-panel，其中約 151 行 `<div id="pregame-coaches">` 為教練團面板；渲染在 `game-renderer*.js`。
- **改法**：
  1. 把 pregame 重排成「**單一畫面可一次預覽完**」（先發打序、先發投手、對手、賽程、關鍵狀態）。
  2. **移除** pregame 的教練團面板（`#pregame-coaches` 及其渲染呼叫）。教練管理移到後勤中心（與 Phase 2 #3 教練重做整合）。
- **檔案**：`index.html`（pregame-tab）、`game-renderer*.js`（pregame 渲染、移除 coaches render）。
- **驗收**：準備頁一螢幕看完不需捲動；教練團不再出現在準備頁、但仍能在後勤中心管理。
- **風險**：低-中。需確認移除教練團面板後，相關 render 函式不報錯（找 `pregame-coaches` 所有引用）。

---

## Phase 2 — 系統面：貨幣分離 + 教練重做（需求 #1、#3）

目標：打通經濟與養成迴圈。中風險，動 `game.js` / `data.js` / `game-params.js` / `logistics-system.js`，**需處理存檔 migration**。

### 2A. 貨幣分離：新增「黃金棒球」（#1）
- **現況**：單一貨幣 `currency`（`game.js` 約 1816 行初始 1000；load 約 1535 行）。抽卡花 `currency`：抽球員 −100（約 3230 行）、抽教練 −200（約 2740 行）；聘僱 −cost（約 2706 行）、訓練 −cost（約 2682 行）、升級 −cost.money（`logistics-system.js` 約 100 行）。獎勵表在 `game-params.js` 約 114–134 行。
- **改法**：
  1. 新增欄位 `goldBaseball`（暫名，UI 顯示「黃金棒球」），初始值待定（建議給新手起手包，足夠跑完教學的數抽）。
  2. **黃金棒球抽**：教練池、海外池（國際球星＋台灣傳奇）。**本土現役球員池維持用資金抽**（不改）。
  3. **資金保留**用於訓練 / 聘僱 / 升級 / 本土池（不動）。
  4. **黃金棒球發放來源**（依決策，不開放資金兌換）：
     - 賽後/封王表現獎勵 → 接 `game-params.js` 獎勵表 + 賽後結算（`RewardsSystem` / `SeasonManager.endMatch`）。
     - 任務成就 → 需與 Phase 4 教學/成就系統對接（先預留發放 API `grantGoldBaseball(n, reason)`）。
     - 賽季里程碑 → 賽季進度節點發放。
     - **前期無保底**（pity 關閉）；黃金棒球**只能靠任務取得**（不開放資金兌換、不每場固定發放）。
  5. **HUD**：頂欄新增「黃金棒球」chip（與 Phase 1 縮小後的版型一致）。
  6. **save/load migration**：舊存檔沒有 `goldBaseball` → 載入時補預設值（沿用既有 `data.currency || 1000` 的防呆模式）。
- **檔案**：`game.js`（currency 初始/load/save、抽卡扣費、發放）、`game-params.js`（獎勵表）、`index.html`+`style.css`（HUD chip）、`rewards-system.js`（賽後發放）。
- **驗收**：抽卡只扣黃金棒球；資金只動訓練/聘僱/升級；舊存檔載入不歸零；HUD 兩種貨幣分明。

#### 2A-bis. 卡池規格（2026-05-29 使用者確認）

**三個 banner：**
| 卡池 | 貨幣 | 內容 |
|------|------|------|
| 教練池 | 黃金棒球 | 教練（SABCD，見 2B） |
| **海外池** | 黃金棒球 | 國際球星 + **台灣傳奇球員** + 虛構一般海外球員（稀釋） |
| 本土池 | **資金** | CPBL 現役本土球員（沿用既有，改吃資金即可） |

**海外池規格：**
- **抽法**：單抽 **100 黃金棒球**；**10+1（1000 抽 11 次）**。
- **機率分級**：
  - **SSR 巨星 ~1.5%**（90 年代以來國際傳奇 + 台灣傳奇，例：大谷翔平、Pedro Martínez、王建民、陳金鋒）
  - **SR 次級名星 ~13%**（穩健名星）
  - **R 稀釋 ~85%**（**虛構一般海外球員**，程式生成、中低能力）
- **巨星/名星名單**：目前 data 沒這批球員，需新建能力值與 meta（沿用 abilities 體系，傳奇值落在顯示 88–99）。名單由 Claude 草擬 ~25 人 → 使用者審核增刪 → 寫入 data。草案檔：`海外卡池-球星名單草案.md`（待建）。
- **虛構海外球員生成**：需要一個 generator（隨機姓名 + R 級能力），可程式化大量產生當填充。
- **法律/授權**：真實球星姓名屬個人 fan 專案範疇，依使用者意願實作。
- **風險**：中。所有「花 currency 抽卡」的點都要改全；漏改會出現「免費抽」或「扣錯貨幣」。施工前先 grep 全部 `currency -=` / `currency >=` / `currency <`。

### 2B. 教練系統重做：專長對應位置 + SABCD（#3）
- **現況**：`data.js` 約 75–95 行 `COACHES_POOL`（19 位），欄位 `id/name/roleType(head|batting|pitching|baserunning)/rarity/specialty/xpBonus/statBonus/desc`。聘僱存 `hiredCoaches`（字串 ID 陣列，`game.js` 約 1900 行）。加成目前只走 XP/恢復/行銷（`game-params.js` 約 82–107、`logistics-system.js`）。升級費氏序列（`game-params.js` 約 59–67）。**問題：不分位置、放哪效果都一樣、不加球員能力值。**
- **改法**：
  1. **教練資料擴充**：每位教練明確 `domain`（打擊/投手/守備/跑壘）+ `grade`（S/A/B/C/D）+ 每級的 `statBonusPerLevel`（依 grade 不同）。
  2. **一軍加能力值**：聘為一軍教練時，依 `domain` 對「對應族群一軍球員」加對應能力：
     - 打擊教練 → contact / power（打者群）
     - 投手教練 → velocity / control（投手群）
     - 守備教練 → fielding / arm（野手群）
     - 跑壘教練 → speed / 跑壘相關（野手群）
     - 總教練(head) → 全隊小幅 or 經驗加成（待定）
  3. **SABCD 成長曲線**：等級越高、每次升級加的能力值越多（grade 決定基數與斜率）。沿用既有費氏升級費，但「升級回報」改成能力值增量表。
  4. 加成套用時機：能力值加成是「掛在球員身上的 buff」還是「即時計算」？→ 建議用**即時計算層**（讀能力時疊加教練 buff），避免污染 data.js 基礎值、也好回滾（與校準工作流偏好一致：不污染 baseline）。
  5. 移除「放哪都一樣」：加成只對 `domain` 對應的一軍球員生效。
- **檔案**：`data.js`（COACHES_POOL 欄位）、`logistics-system.js`（加成計算 + 等級）、`game.js`（讀能力時疊加教練 buff、一軍判定）、`game-params.js`（grade→成長表）、`game-renderer*.js`（教練卡顯示 domain/grade/效果）。
- **驗收**：不同 domain 教練放一軍，對對應球員能力有可見且不同的加成；同 domain 不同 grade 教練升級回報不同；非一軍教練不加能力。
- **風險**：中-高。能力值疊加層若沒做好「顯示 vs 引擎」分離，會影響投打對決校準（呼應檢討書 §7 的 `toEngineAbility` 思路）。**強烈建議教練 buff 走獨立疊加函式，不寫回基礎能力**。

---

## Phase 3 — 比賽 UI 大改：映像管 CRT（需求 #6、#7、#8 + 引擎失控）

目標：圖1/3/4 的視覺與互動。最大工程、且 #7 失控碰引擎。建議再細分 3 個子里程碑。

### 3A. 比賽版面 + CRT 風格 + 對決按鈕（#6）
- **現況**：比賽區 `battle-canvas-container`（`index.html` 約 186 行）+ 操作面板（守備 290–350、進攻 353–402）；`battle-scene.js` Canvas 主迴圈 `_draw`（約 1365–1376）依視角 `drawPitchingView/drawBattingView/drawFieldingView`。
- **改法**（依圖1）：
  1. 版面：左大「遊戲主畫面」+ 右上「特播欄」+ 右側「玩家操作欄」。
  2. **CRT 風格**：映像管外框、掃描線/微弧度/暈影、像素感（CSS overlay + 既有像素美術風格延續）。
  3. **比賽中刪掉狀態欄**：比賽進行時隱藏頂部 HUD（接 Phase 1 縮小後的 hud-bar，加比賽態 class 隱藏）；局數/比數/球數/壘包/出局數改放主畫面內的「比賽狀態欄」（圖1 紅框，比賽態獨立顯示）。
  4. **對決按鈕**：放在比賽狀態欄上方。玩家設定完（出力/球種/瞄準等）→ 按「對決開始」才執行投球；**若鎖定牽制則執行牽制**。即把「設定」與「執行」分離（目前可能是選完即投）。
- **檔案**：`index.html`（比賽區版面）、`style.css`（CRT overlay、比賽態隱藏 HUD、版面 grid）、`battle-scene.js`（主畫面內狀態欄）、`game.js`（對決按鈕觸發投球/牽制的流程閘門）。
- **驗收**：比賽畫面符合圖1 三區版面、有 CRT 質感、頂欄消失、按對決才投球、鎖定牽制按了走牽制。
- **風險**：中-高。「設定 vs 執行」分離會改動投球觸發流程，需確認與 visualTimeline 動畫、自動模式相容。

### 3B. 投手操作欄：旋鈕 + 四段出力條（#7）
- **現況**：守備面板（`index.html` 290–350）含投球計畫 chips、瞄準 `.pitch-aim-zone`（三框拖拉 + 九宮格按鈕，310–327，拖拉事件 850–887）、出力 3 按鈕（全力/普通/輕鬆，331–336，`data-pitch-effort`）、守備佈陣 6 種（338–348）。
- **改法**（依圖3，**保留三框拖拉瞄準**）：
  1. **旋鈕化**：球種、牽制、守備種類、技能 做成「電視旋鈕」；滑鼠懸停 → 跑出圓圈，選項是扇形（radial menu）。鍵盤/觸控也要有對應操作。
  2. **四段出力條**（取代 3 顆出力按鈕）：藍→綠→黃→紅 四段。
     - 越紅：球速↑、變化量↑、體力消耗↑。
     - 紅區：**失控風險**（控球越差越易失控）。
  3. 保留**三框拖拉**瞄準（strike/chase/invalid + 連續 aimPosition，引擎已支援）。
  4. 「自動按鈕」（圖3 右上綠鈕）：一鍵交給 AI 設定/快速對決。
- **引擎（失控）**：`pitch-engine.js` 接 4 段出力 → 映射到 velocity/break 增益 + 體力消耗倍率（沿用 v3.25 D 組 decayMul 概念）；紅區依 `control` 算失控機率，失控時 aimPosition 大幅偏移/暴投。**動引擎前先跑打者 baseline，改完重跑確認沒崩（AVG R²~0.26、SLG R²~0.43）**（校準工作流偏好）。
- **檔案**：`index.html`（投手面板）、`style.css`（旋鈕/扇形/出力條）、`battle-scene.js`（可能畫旋鈕回饋）、`pitch-engine.js`（出力→數值 + 失控）、`game.js`（面板狀態→引擎參數）。
- **驗收**：四段出力條可選、越紅越強越耗體力、紅區會失控且爛控球更常失控；旋鈕懸停展開扇形可選球種/牽制/守備/技能；三框拖拉瞄準仍可用。
- **風險**：高（碰引擎 + 大量新互動）。失控數值要物理/統計檢查，不要憑直覺。

### 3C. 打者操作欄：旋鈕 + 鎖定條 + 九宮格（#8）
- **現況**：進攻面板（`index.html` 353–402）含對手配球傾向、打擊策略 6 種、九宮格 `.zone-grid-3`（380–392）、鎖定球速 `data-velocity-lock`。
- **改法**（依圖4，**保留/回到九宮格**）：
  1. **旋鈕化**：打擊策略、盜壘、戰術、技能 做成電視旋鈕（扇形展開），對應投手版。
  2. **鎖定條**（取代出力條位置）：鎖定 快 / 中 / 慢（接既有 `velocity-lock`，擴成三段條狀 UI）。
  3. 瞄準維持**九宮格**（`zone-grid-3` 保留/強化）。
  4. 「自動按鈕」同投手。
- **引擎**：鎖定快/中/慢 已有 `velocityLock`，確認三段都對 timing 判斷生效（記憶提到 sim 中位 batter `velocityLock='none'` 對 timingMod 無效，需確認真人操作路徑有接上）。
- **檔案**：`index.html`（打者面板）、`style.css`、`game.js`（鎖定條/旋鈕→引擎）、必要時 `pitch-engine.js`（確認 velocityLock 生效）。
- **驗收**：旋鈕可選策略/盜壘/戰術/技能；鎖定條三段可選且影響對決；九宮格瞄準可用。
- **風險**：中。與 3B 共用旋鈕/CRT 元件，可複用。

---

## Phase 4 — 新手教學（需求 #2）

目標：帶新玩家跑完 故事 → 抽卡 → 養成 → 比賽 完整一輪。放最後，因為要等上述系統都到位才能教。

- **現況**：序章 `story-intro.js`（首次進入自動播放劇情），但**沒有互動式操作引導**。
- **改法**：
  1. 設計引導腳本：序章劇情 → 教學抽卡（給保底黃金棒球，抽到指定卡）→ 養成（升級/教練配置示範）→ 一場引導比賽（教操作欄：對決按鈕、出力/鎖定條、旋鈕、九宮格）。
  2. 引導 UI：高亮遮罩 + 步驟提示 + 「下一步」鎖定（只能照步驟點）。
  3. 與成就/任務系統對接：完成教學發黃金棒球（呼應 Phase 2 的 `grantGoldBaseball`）。
  4. 旗標 `tutorialCompleted`（save/load），跑過不再強制。
- **檔案**：新模組 `tutorial-system.js`（IIFE，game.js 之前載入）、`index.html`（遮罩/提示 DOM）、`style.css`、`game.js`（步驟掛鉤、發獎）、`story-intro.js`（銜接）。
- **驗收**：全新存檔能被一步步帶完四段流程、中途不卡、完成後正常進入自由遊玩。
- **風險**：中。依賴 Phase 1–3 的最終 UI/系統，故必須最後做；任何前面改 DOM/流程都可能要回頭調教學步驟。

---

## 跨階段：存檔 Migration 與相容性

- 每個 Phase 新增的存檔欄位都要在 load 時防呆補預設（沿用 `data.X || 預設` 模式）：
  - Phase 2：`goldBaseball`、教練 `domain/grade/level`、教練能力 buff 狀態。
  - Phase 3：操作面板新狀態（出力段位、鎖定段位）多為臨場態，通常不需持久化。
  - Phase 4：`tutorialCompleted`。
- 舊存檔（v3.25）載入後不可崩、不可歸零資源。

---

## 驗收總清單（逐 Phase 勾選）

- [~] **P1-4** 頂部狀態欄縮小、不破版 — **程式完成（2026-05-29）**：`style.css` `.hud-bar` padding 12→6px·margin 18→12px、`.hud-chip` min-height 52→34px·字級 18→15px、`.brand-mark` 44→34px、`.hud-stats` 改 `auto-fit` 單列。**待目視確認**。
- [~] **P1-5** 準備頁預覽 + 移除教練團 — **程式完成（2026-05-29）**：移除 `index.html` `#pregame-coaches` div(151) + `renderPregameScene` 內的變數/null 檢查/innerHTML 區塊。`selectCoach()` 函式保留未用（Phase 2 重做）。**待目視確認**。
- [~] **P2-1** 黃金棒球上線 — **Step 1 程式完成（2026-05-29，待目視）**：`game.js` 加 `goldBaseball`(init 300/save/load migration)、`drawCoach` 與 `drawPlayer('international')` 改扣黃金棒球(各 100)、`drawPlayer('local')` 維持資金、`grantGoldBaseball(n,reason)` API、勝場 +20 黃金棒球；`game-renderer-modules.js` HUD 加 `gold-baseball` 更新；`index.html` HUD 加黃金棒球 chip + 抽卡/教練標籤與 disable 分流；`style.css` HUD 改 7 欄(?v=4.3)。
  - **Step 2 程式完成（2026-05-29，待目視）**：
    - `game.js` GachaSystem 新增 `drawInternationalByRarity()`（SSR 1.5%←rating S／SR 13%←rating A,B／R 85%←虛構填充）+ `createFictionalFillerCandidate()`（虛構一般海外球員生成器，低能力 R 級）。
    - `game.js` 抽卡核心抽成 `_acquireDrawnPlayer()` 共用；新增 `drawInternationalEleven()`（11 連抽 1000 黃金棒球）。
    - `engine.js` 加 `game.draw.international11` action；`index.html` 加「11 連抽」按鈕 + disable；`game.js` bindEngineControl 綁定。
    - `data.js` `INTERNATIONAL_STAR_CANDIDATES` 併入 45 位（草案傳奇 + 台灣傳奇）→ 現 71 名（36 S / 34 A / 1 B）+ 生成 R。陣列括號平衡已驗。
    - ⚠️ 仍未 in-browser 驗證（本機無 node）。
- [~] **P2-3** 教練 SABCD + 專長加成 — **程式完成（2026-05-29，待目視）**：
  - `data.js` COACHES_POOL 19 位加 `domain`(pitching/batting/fielding/baserunning/head)+ `grade`(逐位評 SABCD)。
  - `logistics-system.js` 加 `GRADE_BASE`(S.55/A.45/B.35/C.27/D.18，保守級)+ `getMajorAbilityContributions()`；`getMajorTrainingMultiplier` 納入專長相符倍率(相符1.0/總教練0.5/非專長0.3)；assign/unassign/upgrade 觸發重算。
  - `game.js` 加 `applyCoachAbilityBonuses()`（可逆 delta，**不污染基礎值**，夾 99、記實際套用量）；Player 加 `coachAbilityDelta`、save/load 序列化；load 後 + 每場 prepareNextMatch 重算。
  - 加成接在後勤「**一軍訓練中心**」投手/打擊/守備指導員槽位（語意=一軍教練）；UI 顯示 grade·domain·一軍加成(+CSS `.coach-effect`，?v=4.4)。
  - ⚠️ **已知**：教練有兩套部署 — shop 的 `coachingStaff`(舊 XP 角色)與 logistics 的 `LogisticsCenter.departments`(一軍訓練中心)。能力加成只接後者。未來可考慮整併兩套(超出 v4.1)。
  - ⚠️ 仍未 in-browser 驗證（本機無 node）。
- [x] **P3-6** 比賽版面=圖1 三區、CRT 質感、比賽中隱藏頂欄、對決按鈕才投球、鎖定牽制走牽制 — **程式完成 + 瀏覽器驗證通過（2026-05-29）**：
  - **圖1 三區**：`index.html` 比賽區重構 — 左主畫面（CRT）｜右上「特播欄」`.arena-broadcast`（比賽資訊 matchup-bar + 轉播日誌 #play-log 從左欄搬上）｜右操作欄。
  - **比賽狀態欄 overlay**：頂部 count 橫條移除，局/比數/B/S/O/壘 + 比分移入 `.battle-canvas-wrap` 內的 `.match-status-overlay`（右下紅框，z-index 8、pointer-events:none）。內層 id（#inning/#balls/#strikes/#outs/#runners/#score-player/#score-opponent）全保留，updateUI 不受影響。
  - **CRT 質感**：既有掃描線/暈影/玻璃反光（style.css 5781-5803）之上，append 映像管 bezel 外框（`.battle-canvas-wrap` border + 多層 box-shadow + 圓角）。強度可調 → `:root` 的 `--crt-bezel`/`--crt-radius`。
  - **比賽中隱藏頂欄**：本來就已實作（`index.html:729` toggle `in-match` + style.css `body.in-match .hud-bar{display:none}`），驗證生效。
  - **對決按鈕（設定 vs 執行分離）**：新增 `#duel-start`「⚾ 對決開始」（唯一執行鈕）。`game.js` 加 `duelMode`（'pitch'/'pickoff'）+ `setDuelMode()`/`updateDuelModeUI()`；「牽制」鈕改為鎖定開關（不再立即執行），按下後 #duel-start 走 `game.pickoff` 並自動解鎖。`#normal-pitch` 保留為「快投」相容。
  - ⚠️ **CSS 全部 append 於 style.css 檔末**（遵守多層主題覆蓋慣例），`index.html` `style.css?v=4.5→4.6`。
  - ✅ **node 已可用**：engine smoke test + browser smoke test（真實 Chrome/Edge via CDP）皆通過；截圖目視確認三區/紅框/特播欄/綠對決鈕/CRT外框/頂欄隱藏皆正確、無破版。
  - 🐞 **連帶修掉一個既有 game-breaking bug**：`data.js`(一句話介紹字串) 與 `player-bios.js`(身高/守位 meta 物件) 都宣告 `const PLAYER_BIOS` → 重複宣告 SyntaxError 讓 player-bios.js 整個無法載入（以前無 node 沒被自動測出）。已將 player-bios.js 改名為 `PLAYER_META` + `window.PLAYER_META` 匯出；現有消費端（用 data.js 描述字串）不受影響，meta 物件保留供未來補守位/身高用。
- [x] **P3-7** 投手：四段出力條 + 旋鈕扇形 + 紅區失控 — **程式完成 + 瀏覽器驗證（2026-05-29）**：
  - **引擎（pitch-engine.js，全部加性、normal 一行不動）**：`EFFORT_MOVE_SCALE` 加 max:1.10；新增 `EFFORT_VELO_SCALE`(easy.97/normal1.0/full1.04/max1.08) 套入 `_estimateSpeedKmh`；新增 `EFFORT_WILD_FACTOR`(easy0/normal0/full0.4/max1.0)，`generate()` 內做高出力失控 → `wildChance=clamp((0.05+(80−ctrl)×0.007)×factor,0,0.5)`，失控時加 11–27cm 暴投級偏移；回傳 `isControlLapse`/`wildChance`。兩個 `normalizeEffortKey` 加 'max'/'爆發'。
  - **game.js**：每球體力消耗乘 `effortStaminaMul`(easy.9/normal1.0/full1.15/max1.5)；`lastPitchContext` 加 isControlLapse；失控寫 addToLog。
  - **打者 baseline 未崩（已驗證）**：normal 路徑邏輯上 bit-identical（wildFactor=0 跳過、倍率=1.0、RNG 呼叫序不變）。pre/post 各跑 99×1000 PA：mean AVG 穩定 .253→.252；R² 差異(AVG .587→.508~.540)經兩次 post 重跑證實為無 seed RNG 雜訊(§14.4)，非回歸。baseline 檔 `tools/_baseline-batter-pre3B.json`/`post3B.json`，runner `tools/run-batter-baseline.js`。
  - **UI（圖3 投手操作欄重排）**：`panel-defense` 重組為 `.pitcher-console` — 上排 5 旋鈕(球種/牽制/守備/技能佔位/綠自動)，下排 三框拖拉瞄準(左)+直立四段出力條(右,紅黃綠藍)。旋鈕 hover/點擊展開扇形(JS `layoutKnobFan` 弧排列)、鍵盤方向鍵+Esc、觸控走點擊。球種/守備扇形選項沿用既有 `data-pitch-plan`/`data-defensive-shift`(委派 handler 自動生效)；牽制旋鈕接 setDuelMode；自動鈕 `gameEngine.performPitch({auto:true})`。`style.css?v=4.8`。
  - 🔎 **失控數值物理 sanity check 已做**（單調遞減於控球、紅檔風險最大、偏移量暴投級），未做完整 sim 統計研究 → 後續 playtest 微調。
  - ✅ **2026-05-29 CRT 改版（使用者新指令，已解上述拋光項）**：
    - **木紋映像管電視機殼**取代深色 bezel（`.arena-canvas-col` 木理漸層 + `.battle-canvas-wrap` 圓角木殼邊框）。坑：勿用 `margin:auto`（flex 塌陷）→ 用 `align-self:stretch`。
    - **記分板移出右欄 → `#inning-scoreboard-overlay`**（CRT 螢幕上）：`switchHalf()` 換局自動彈（autoSim 不彈）+「記分板」鈕叫出 +「繼續比賽」關。保留全部 board-*/line-* id。
    - **🔴 比賽畫面一頁看完、不滑動**：打序/牛棚/卡片→`#lineup-overlay`、只顯示 active 面板、大量壓縮、`body.in-match{overflow:hidden}`+arena 100vh。1366×768 實測 `.arena-controls-col` 溢出 = 0。`style.css?v=4.9`。
    - ⚠️ aim zone 固定 px 幾何（綁拖拉座標），不可縮放。
  - ✅ **2026-05-29 投手操作區 4 項調整（style.css?v=5.0，已驗證）**：
    1. **出力 → throttle quadrant 搖桿**（拖拉桿吸附 爆發/全力/普通/輕鬆 四檔；`initThrottle`/`syncThrottle`）。
    2. **球種 → 實際球路**（依投手 arsenal 動態：四縫線/二縫線/滑球/曲球/指叉…+「自動」）。引擎 `playerPitchChoice`/`setPitchTypeChoice`，我方投球時覆寫 selectedPitch。**不再是配球策略**。
    3. **全自動開關**（`#knob-auto` switch 樣式，接 `game.auto.toggle` 全場自動 on/off）。
    4. **特播放大 + 操作區下移**（broadcast flex:1 吃上方剩餘空間），colOverflow 仍 = 0。
- [x] **P3-8** 打者操作欄（圖4）— **程式完成 + 瀏覽器驗證（2026-05-29，style.css?v=5.1）**：
  - `panel-offense` 重排成 `.pitcher-console.batter-console`（共用投手 console 樣式），與圖3 對稱。
  - **上排旋鈕**：打擊策略(fan ×6)、盜壘(動作鈕→game.steal)、戰術(跑壘風格 fan：保守/普通/激進，新增 `game.setBaserunningMode`)、技能(佔位 disabled)、全自動(switch)。
  - **下排**：九宮格(`zone-grid-3` 保留，左) + **三段鎖定條**(右；快 fast/中 none/慢 slow 拖拉桿，`initLockbar`/`syncLockbar`，接既有 `setBattingVelocityLock`)。
  - **全自動開關共用**：投手/打者兩個 `.auto-switch` 都用 `data-auto-toggle` 委派 → `game.auto.toggle`（全場自動），`syncAutoSwitch` 迴圈兩者。`initPitcherKnobs` 改迴圈所有 `.knob-row`。
  - velocityLock 走既有人類對戰路徑（UI 只是重新樣式化既有控制），未動引擎。colOverflow 實測 = 0（一頁無捲動維持）。
  - ⚠️ 對手配球傾向 `#pitch-tendency-display` 暫隱藏(display:none，DOM 保留避免 JS 報錯)，未來可放回特播欄。

> **🎉 Phase 3（比賽 UI 大改）全部完成：3A 版面+CRT+對決鈕、3B 投手 console+四段出力(後改 throttle)+失控、3C 打者 console。木紋映像管電視機殼 + 一頁無捲動 + 換局記分板 overlay。下一步 Phase 4 新手教學（最後一階段）。**
- [x] **P4-2** 新手教學 — **程式完成 + 瀏覽器驗證（2026-05-29）**：
  - 新模組 `tutorial-system.js`（IIFE，index.html 於 story-intro.js 後、game.js 前載入）→ `window.TutorialSystem.{start,isCompleted,autoStartIfNeeded}`。
  - **互動導覽**：聚光遮罩（box-shadow 9999px 暗化 + 綠框 spotlight 對準目標）+ 提示卡（標題/說明/進度/跳過/下一步），自含 style 注入。12 步：歡迎→資金→黃金棒球→擴張中心→球隊管理→準備出賽→比賽主畫面→對決開始→出力搖桿→球種旋鈕→全自動開關→完成。步驟可指定 `scene`（自動 changeScene）與 `sel`（高亮），目標不可見時自動置中後備。
  - **銜接**：`story-intro.js autoPlayIfNeeded` 改為序章關閉後 onFinish 啟動教學；序章已看過則直接視需要啟動。
  - **完成發獎 + 旗標**：完成（非跳過）發 100 黃金棒球（`grantGoldBaseball`），設 `game.tutorialCompleted=true` 並存檔。save/load/init 已序列化（game.js 1517/1663/1997 旁）。
  - ⚠️ 既有存檔（序章已看過、tutorialCompleted 未定）下次載入會跑一次此導覽（教 v4.1 新 UI），可跳過。

> **🎉🎉 v4.1 全部 8 項需求完成！** ①貨幣分離 ②新手教學 ③教練 SABCD ④縮 HUD ⑤準備頁+移除教練團 ⑥比賽版面CRT+對決鈕 ⑦投手出力(throttle)+旋鈕+失控 ⑧打者旋鈕+鎖定條+九宮格。木紋映像管電視機殼、一頁無捲動、換局記分板 overlay 全達成。`style.css?v=5.1`。
> **下一步（v4.1 後）**：可回到擱置的「投手體系架構失衡修正」（修正書 §15.8），或 playtest 微調失控數值/教練平衡。

---

## 待你回覆 / 施工前要再確認的小決策

1. ~~「黃金棒球」前期保底~~ → **已定：前期無保底、只能靠任務取得**。
2. 黃金棒球**初始持有量**（單抽已定 100 / 11 抽 1000）。海外/本土/教練池單抽費是否都 100？
3. 教練 **head（總教練）** 的加成定位：全隊小 buff 還是純經驗加成？
4. 教練能力 buff 是否要在球員卡上「分開顯示基礎值 vs 教練加成」（建議顯示，玩家才有感）。
5. CRT 風格強度（掃描線/弧度/暈影要明顯還是淡淡的）。

> 接手指南：本計畫由淺入深，Phase 之間可獨立驗收與 commit。每完成一個 Phase 建議寫對應 `v4.x-更新書.md`，殘留問題寫 issue tracker（沿用專案慣例）。動引擎（P3-7 失控）前務必先存打者 baseline。
