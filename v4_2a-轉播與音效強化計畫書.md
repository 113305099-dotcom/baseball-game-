# v4.2a — 轉播欄豐富化 + 背景音樂 計畫書

> 建立日期：2026-05-31
> 需求來源：使用者指示「轉播欄太單調」+「需要背景音樂」
> 性質：純 UI/內容 + 新音效模組，不碰引擎數值
> 預計工期：2-3 個工作階段

---

## 現況診斷

### 轉播欄（#play-log）目前做什麼

- `game.addToLog(message)` 把純文字 push 到 `game.log[]`
- `game-renderer.js` 取最後 20 筆，`reverse()` 後塞進 `#play-log`，每筆一個 `<p>` 標籤
- 所有訊息長一樣：深色背景 + 左側綠邊框 + 藍白字
- `commentary-data.js` 有 10+ 事件類別、20+ 情境子類別的文案庫，但**渲染層沒利用這些分類**——旁白跟系統訊息（「攻守交換」「換投」）混在一起，視覺上無法一秒分辨

### 缺什麼

| 現況 | 市面遊戲標配 |
|------|-------------|
| 純文字，無分類 | 事件類型用**顏色+圖示**區分 |
| 只顯示最新 20 條，全部反序 | 重要事件**高亮定格** + 一般事件滾動 |
| 無 pitch-by-pitch | 逐球紀錄條（⚫⚫🔴🟢 球數可視化） |
| 無局數/比分上下文 | 每局分隔線 + 得分時顯示新比數 |
| 換局只有一行字 | 換局時**轉播欄清空重置** + 局標題 |
| 無動畫 | 新訊息**滑入/淡入**效果 |
| 無音效 | 事件音效 + 背景 BGM |

### 音效現況

**完全沒有。** 沒有任何 `<audio>` 元素、Web Audio API、或音效管理模組。`game.js` 裡 `audio`/`sound`/`music`/`bgm` 搜尋結果為 0。

---

## 計畫總覽

分三塊施工，依賴關係：**BGM（獨立）→ 轉播內容豐富化 → 轉播互動/動畫**。

---

## Part 1 — 背景音樂系統（最獨立，先做）

### 1A. 音效管理模組 `sound-manager.js`

新建 IIFE 模組，掛 `window.SoundManager`，**game.js 前載入**。

```js
// 公開 API
SoundManager.init({ musicVolume: 0.3, sfxVolume: 0.5 })  // 初始化
SoundManager.playBGM('lobby' | 'pregame' | 'match' | 'tension' | 'victory' | 'menu')
SoundManager.crossfadeBGM(newTrack, durationMs)            // 淡入淡出切換
SoundManager.playSFX('hit' | 'strikeout' | 'homerun' | 'cheer' | 'catch' | ...)
SoundManager.setMusicVolume(v)                             // 0-1
SoundManager.setSFXVolume(v)                               // 0-1
SoundManager.toggleMute()
```

**技術方案**：Web Audio API（`AudioContext` + `GainNode` 做音量控制）

### 1B. BGM 來源策略

**瀏覽器限制**：本機 `file://` 無法用 `<audio>` 播本機檔案（CORS），但 `index.html` 開在瀏覽器時可以播遠端 URL。

**方案 A（推薦，零成本快速上線）**：使用程式生成的簡單旋律
- 用 Web Audio API 的 OscillatorNode 生成 8-bit 像素風 BGM
- 優點：無外部依賴、無授權問題、完全契合 CRT/像素風格
- 實作一個簡單的 `chiptune-bgm.js` 子模組，內建 4-5 首像素風 loop：
  - `lobby`：輕快 8-bit，大廳/管理場景
  - `pregame`：漸強節奏，準備頁
  - `match_defense`：緊湊鼓點，守備半局
  - `match_offense`：高昂旋律，進攻半局
  - `tension`：心跳 bass，滿壘/關鍵時刻（疊加而非切換）
  - `victory`：勝利號角，賽後
- 每首 8-16 小節 loop，用簡單波形（方波=像素感、三角波=bass）
- 生成邏輯：用音符頻率表 + 節奏序列 → GainNode 包絡控制

**方案 B（後期升級）**：遠端 PNG/MP3 sprite → fetch 解碼
- 找 royalty-free 8-bit baseball BGM 放 GitHub Pages 或 CDN
- 需要授權確認，建議 Phase A 先用方案 A，Phase E 再升級

### 1C. SFX 音效（事件觸發）

同樣用 Web Audio API 合成（免外部檔案）：
- **打擊聲**：短白噪 burst（模擬球棒擊球）
- **手套接球**：低頻 thump（sine 波快速衰減）
- **三振**：尖銳 sweep down
- **全壘打**：上升 sweep + 歡呼模擬（多頻 noise）
- **界外**：短促 click
- **UI 點擊**：極短 sine click

全部 < 0.3 秒，不干擾 BGM。

### 1D. UI 控制

- 大廳右上角加入喇叭 icon（🔊/🔇 toggle）
- 展開可調兩個滑桿：音樂音量 / 音效音量
- 設定存入 localStorage
- 比賽中 HUD 隱藏，但快捷鍵 `M` = mute toggle

**檔案**：`sound-manager.js`（新）、`chiptune-bgm.js`（新）、`index.html`（script 標籤 + speaker icon）、`style.css`（speaker 樣式）、`game.js`（場景切換時觸發 BGM 切換、addToLog 時觸發 SFX）
**風險**：低。純新增模組，不影響既有邏輯。唯一注意點：使用者瀏覽器需使用者互動過才能啟動 AudioContext（瀏覽器 autoplay 政策），解法是在第一次點擊時呼叫 `SoundManager.init()`。

---

## Part 2 — 轉播內容豐富化

### 2A. 事件分類與 color coding

**修改 `addToLog()`**，讓每條訊息攜帶分類標籤：

```js
// 現況
game.addToLog('全壘打！出去了！')

// v4.2a：第二參數帶分類
game.addToLog('全壘打！出去了！', { type: 'hr', level: 'highlight' })
```

**事件類型與視覺風格**：

| 類型 | icon | 左邊框色 | 字色 | 備註 |
|------|------|---------|------|------|
| `hr` 全壘打 | 💣 | 紅 `#f87171` | 金黃 | 特大字+閃爍進場 |
| `hit` 安打 | 🏏 | 綠 `#4ade80` | 白 | |
| `double` 二安 | ⚡ | 橙 `#fb923c` | 白 | |
| `triple` 三安 | 🔥 | 橘紅 | 白 | |
| `k` 三振 | ❌ | 藍 `#60a5fa` | 淺藍 | |
| `bb` 保送 | 🟡 | 黃 | 白 | |
| `out` 出局 | ⬛ | 灰 `#64748b` | 灰 | 小字 |
| `error` 失誤 | 💥 | 紫紅 | 淺紅 | |
| `sb` 盜壘 | 🏃 | 青 | 白 | |
| `run` 得分 | ⬆ | 亮綠 | 金黃 | 顯示新比數 |
| `system` 系統 | 📢 | 暗灰 | 灰 | 換投/換局/攻守交換 |
| `tension` 關鍵 | ⚠ | 閃黃 | 亮白 | 滿壘/得點圈/2out |

### 2B. 逐球紀錄條（Pitch Tracker）

在 `.arena-matchup-bar` 與 `.arena-log` 之間插入 `.pitch-tracker`：

```
⚫⚫🔴⚫🟢  ← 本打席球數：1-2（好球=紅/綠S, 壞球=黃點, 界外=灰點）
```

- 每球結束更新（不只看結果、而是看每顆球的 outcome）
- 使用 `game.lastPitchContext` 的資料：
  - 好球（called strike / swinging strike）→ 🔴
  - 壞球 → 🟡
  - 界外 → ⚪（灰色小點）
  - 進場球（contact → in-play）→ ⚾（該球最終結果用文字補）
- 打席結束（出局/上壘）→ tracker 清空、下一位打者重新開始
- **這是目前最缺的「逐球轉播感」核心 UI**

### 2C. 局分隔線與比數戳

- 換局時（`switchHalf` / 換局手動觸發）：插入分隔線
  ```
  ─── 3局上 結束｜政大 2 : 1 對手 ───
  ```
- 得分時：訊息自動附加當前比數
  - 現況：`跑者回本壘 — 得分！`
  - 改為：`⬆ 跑者回本壘 — 得分！【政大 3 : 1 對手】`

### 2D. 轉播欄即時摘要（Matchup Context Line）

在 `.arena-log` 頂部加入一條「當前狀態摘要列」，內容在每球後更新：

```
🏟 3局上｜2出局｜⚫⚫🟡 1-2｜👤 王威晨 vs 🥎 黃子鵬 (側投)
```

這行讓轉播有「電視轉播下方資訊條」的感覺，且不佔額外空間（可做成 `.broadcast-head` 的一部分或緊貼其下）。

---

## Part 3 — 轉播互動/動畫

### 3A. 新訊息進場動畫

- **一般訊息**：從右滑入 + 淡入（`@keyframes log-slide-in`，~300ms）
- **highlight 訊息**（HR/關鍵得分/滿貫）：先放大閃一下再回正常大小（`@keyframes log-pop`）
- 用 CSS animation，JS 只需加 class（`log-entry--slide` / `log-entry--highlight`）

### 3B. 換局轉播重置 + 摘要

- 換局時：轉播欄清空（保留局分隔線）+ pitch tracker 清空
- 換局後第一條訊息是局標題：
  ```
  ═══ 3局下 開始 ═══
  打者：陳傑憲 ｜ 投手：德保拉
  ```

### 3C. 重要事件定格

- HR / 滿貫 / 再見安打 / 關鍵三振：在轉播欄**置頂定格 3 秒**（該條訊息不隨自動捲動消失），背景加微光 pulsate
- 其他訊息正常捲動

### 3D. 手動展開轉播欄

- 轉播欄右上角加一個小小的「展開 ⤢」按鈕
- 點擊後轉播欄 overlay 展開為半透明大面板（佔 CRT 螢幕面積 70%），顯示最近 50 條完整轉播記錄
- 適合想回顧比賽過程的玩家
- 點背景或「收合」關閉

---

## Part 4 — commentary-data.js 擴充

### 4A. 補缺漏事件類別

目前 commentary-data.js 的事件覆蓋：

| 已有 | 缺漏 |
|------|------|
| homeRun, strikeout, single, double, triple | **暴投 wildPitch** |
| groundOut, flyOut, popupOut | **捕逸 passedBall** |
| walk, hitByPitch | **野手選擇 fielderChoice** |
| error, runScored | **投手犯規 balk** |
| opening, bigSituation | **再見（非全壘打）walkoffHit** |
| newspaperLead | **挑戰/重播輔助判決 replay** |
| | **投手受傷/驅逐出場 ejection** |
| | **雨中暫停 rainDelay** |
| | **投手教練上丘 moundVisit** |

### 4B. 新增情境標籤

每類文案補情境標籤（`tags` 陣列），讓 `generateCommentary` 可以更精準選擇，也可以避免短時間內重複同一句：

```js
homeRun: {
  default: [
    { text: '這顆飛得又高又遠 — 出去啦！全壘打！', tags: ['power','farfar'] },
    ...
  ]
}
```

### 4C. 稀有的特殊事件專屬文案

- **完全打擊達成**：`cycle` 事件
- **無安打比賽警報**：`noHitWatch`（6 局後每局觸發一次）
- **單局三轟**：`threeHrInning`
- **投手連續三振**：`strikeoutStreak`（連續 3K+ 觸發）

---

## 施工順序與依賴

```
Part 1（BGM）           ← 獨立，最先做
  ↓
Part 4（文案補漏）       ← 獨立，與 Part 2 並行
  ↓
Part 2A（color coding）  ← 需要 addToLog 改介面
  ↓
Part 2B（pitch tracker） ← 需要 lastPitchContext 資料
  ↓
Part 2C+D（分隔線/摘要） ← 需要 2A 的分類體系
  ↓
Part 3（動畫+定格+展開） ← 需要 2A 的分類體系
```

建議分 **2-3 個工作階段**：
- **Sprint 1**：Part 1 BGM + Part 4 文案補漏（不碰 render 層，獨立）
- **Sprint 2**：Part 2 全線（轉播內容豐富化，這是核心）
- **Sprint 3**：Part 3 動畫互動（最後拋光）

---

## 檔案異動清單

| 檔案 | 操作 | 說明 |
|------|------|------|
| `sound-manager.js` | **新建** | Web Audio API 音效管理核心 |
| `chiptune-bgm.js` | **新建** | 像素風 BGM 旋律生成器 |
| `commentary-data.js` | 擴充 | 補 10+ 缺漏事件 + 標籤系統 |
| `commentary.js` | 微調 | 支援 tags 防重複、新事件類別 |
| `game.js` | 改 `addToLog()` | 加 type/level 參數、換局邏輯、SFX 觸發點 |
| `game-renderer.js` | 改 `renderPlayLog()` | 分類渲染、pitch tracker、color coding、動畫 class |
| `index.html` | 加 DOM | 喇叭 icon、pitch tracker 區塊、展開按鈕、sound-manager/chiptune script 標籤 |
| `style.css` | append | 事件顏色、tracker、動畫、speaker、展開面板、`style.css?v=` bump |
| `game-params.js` | 擴充 | BGM 曲目設定、SFX 事件對照表 |

---

## 驗收清單

### Part 1（BGM）
- [ ] 進入大廳聽到 lobby BGM
- [ ] 進入準備頁 BGM 切換（或無縫淡入淡出）
- [ ] 比賽中有比賽 BGM（進攻/守備可不同）
- [ ] 關鍵時刻（滿壘/得點圈）BGM 變緊張
- [ ] 喇叭 icon 可 mute/unmute
- [ ] 音量滑桿可獨立調 BGM/SFX
- [ ] 瀏覽器 autoplay 政策不卡（首次點擊後啟動）

### Part 2（轉播內容）
- [ ] HR 訊息紅色左邊框 + 💣 icon
- [ ] 三振、安打、保送、失誤各有不同顏色
- [ ] 系統訊息（換投/換局）灰色小字不搶眼
- [ ] pitch tracker 每球更新、打席結束清空
- [ ] 局分隔線顯示比數
- [ ] 得分訊息附帶當前比數
- [ ] 狀態摘要列顯示局數/出局/球數/對戰

### Part 3（動畫）
- [ ] 新訊息滑入動畫流暢
- [ ] HR/關鍵事件進場有 pop 效果
- [ ] 換局清空轉播欄 + 顯示局標題
- [ ] 展開按鈕可查看完整轉播記錄
- [ ] 一頁無捲動鐵律維持（colOverflow = 0）

### Part 4（文案）
- [ ] 暴投、捕逸、野手選擇、投手犯規有新文案
- [ ] 完全打擊/無安打警報/連續三振有特殊文案
- [ ] 短時間內不重複同一句（tags 防重）

---

## 注意事項

1. **瀏覽器 autoplay 政策**：`AudioContext` 必須在用戶手勢（click/tap）之後才能 resume。解法：`SoundManager.init()` 綁定在第一次點擊事件（大廳任一按鈕），之後才能播。
2. **`file://` 限制**：方案 A 用 Web Audio 合成，不需要外部音檔，無 CORS 問題。
3. **一頁無捲動鐵律**：pitch tracker 和狀態摘要列都很小（一排高度），不可讓 `colOverflow > 0`。
4. **addToLog 介面改動**：從 `(message)` 改成 `(message, opts?)`，要 grep 所有呼叫點（約 30 處），逐一確認無報錯。建議用預設參數保持向下相容：`addToLog(message, { type: 'system', level: 'normal' } = {})`。
5. **CSS append 慣例**：所有新 CSS 寫在 `style.css` 最末，bump `style.css?v=` 版號。
6. **效能**：逐球 tracker 更新頻率高（每球一次），DOM 操作要輕量（只改 textContent，不重建元素）。
