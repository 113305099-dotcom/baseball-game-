# v4.2b — 轉播語氣大改造：雙人播報 + 閒聊 + 冷笑話

> 建立日期：2026-05-31
> 需求：轉播要像真實比賽——雙人對話、閒話家常、講冷笑話
> 性質：純內容 + 渲染層改造，不碰引擎
> 搭配計畫：v4_2a（轉播欄 UI 豐富化）——本計畫聚焦「語氣與內容」，與 4a 的「視覺與互動」互補

---

## 現況問題

目前 `commentary.js` 的產出是**單行字串**：

```js
generateCommentary('homeRun', player) → '碰！這球就像你的前女友 — 再也回不來啦！'
```

然後 `addToLog()` 直接塞進 `game.log[]`，渲染時每行一個 `<p>`。

**缺的不是文案量（commentary-data.js 已有 200+ 句），而是「形式」——真實轉播不是一個人從頭念到尾，而是兩個人在對話。**

---

## 改造目標

三層語氣，依比賽節奏切換：

| 層級 | 觸發時機 | 語氣 | 範例 |
|------|---------|------|------|
| **實況層** | 每球結果（必發） | 主播喊動作 + 球評補分析 | 「主播：外角低球被打穿二游！球評：王威晨的反向攻擊能力真的是一絕⋯⋯」 |
| **閒聊層** | 打席之間（機率觸發） | 球評講古 / 聊數據 / 聊天氣 | 「球評：你知道嗎，今天濕度 78%，縫線球特別會跑。」 |
| **冷笑話層** | 低張力時刻（低機率觸發） | 球評講笑話 → 主播吐槽 | 「球評：棒球跟咖啡有什麼共通點？⋯⋯都需要 good pitch（投球/濾掛）！」 |

---

## Part 1 — 雙人播報角色設計

### 1A. 兩個角色定位

| | 主播（實況）蔡兄 | 球評（分析）鍾 sir |
|---|------------------|---------------------|
| **風格** | 熱血、快嘴、擅長把場上動作喊成一句話 | 冷靜、講數據、會突然歪題講冷笑話 |
| **台詞比例** | ~60%（主call動作） | ~40%（分析+閒聊+笑話） |
| **視覺** | 粗體白字 | 斜體灰藍字（像素風字體） |
| **口頭禪** | 「這球——」「出去啦！」「判得好！」 | 「我跟你講⋯⋯」「你知道嗎⋯⋯」「從數據上看⋯⋯」 |

### 1B. 對話結構

每段播報不是隨機單句，而是有結構的**短對話**（1-3 輪）：

```
結構 A：主播獨白（普通 play，球評不插話）
  【蔡兄】游擊方向滾地球，穩穩傳一壘，出局！

結構 B：主播喊 + 球評補（精彩 play）
  【蔡兄】這球咬中了！左外野方向非常深遠——
  【鍾sir】林安可的揮棒軌跡真的漂亮，他今年 .580 的長打率不是開玩笑的。
  【蔡兄】——出去啦！全壘打！

結構 C：球評開話題 + 主播接（鬆散時刻）
  【鍾sir】欸蔡兄，你有沒有發現今天黃子鵬的側投，打者一直打滾地球？
  【蔡兄】真的欸，目前 8 個出局有 6 個是滾地。這下沉的角度打者完全對不到。
```

每段播報是一個陣列 `[{speaker, text}, ...]`，渲染時依 speaker 上不同樣式。

---

## Part 2 — 資料結構改造

### 2A. `commentary-data.js` 擴充格式

從單一字串陣列 → 結構化對話模板：

```js
// 舊格式（保留向下相容）
homeRun: {
  powerHitter: ['怪力打者上場 — 球場根本關不住他！']
}

// 新格式：對話模板
broadcast: {
  homeRun: {
    powerHitter: [
      // 模板 1：主播雙句 + 球評分析
      {
        lines: [
          { speaker: 'caster', text: '這球打到甜蜜點！${batter}大棒一揮——' },
          { speaker: 'color', text: '${batter}今年的 ${hrCount} 轟就是這樣來的，他只要咬到甜蜜點，沒有球場關得住。' },
          { speaker: 'caster', text: '——出去啦！${distance}公尺的大號全壘打！' }
        ]
      },
      // 模板 2：球評先驚呼 + 主播收尾
      {
        lines: [
          { speaker: 'color', text: '噢——這聲音！' },
          { speaker: 'caster', text: '不用看了！${batter}一棒掃出全壘打牆！' }
        ]
      },
      // 舊格式字串也相容（自動轉成單行主播台詞）
      '怪力打者上場 — 球場根本關不住他！'
    ]
  }
}
```

### 2B. 模板變數插值

```js
// 可用變數（從 game 狀態推）
${batter}          // 打者姓名
${pitcher}         // 投手姓名
${distance}        // 飛行距離（HR 時）
${hrCount}         // 本季/本場 HR 數
${inning}          // 局數
${score}           // 目前比數
${count}           // 球數（如 2-1）
${outs}            // 出局數
${team}            // 球隊名
${stadium}         // 球場名
${hitStreak}       // 連續安打場次
${avg}             // 打擊率
${era}             // 防禦率
${weather}         // 天氣
${runners}         // 壘上狀況中文（滿壘/一二壘有人/壘上無人）
```

---

## Part 3 — 閒聊系統（Filler Banter）

### 3A. 觸發邏輯

**閒聊在「低張力」時刻觸發**，不干擾比賽關鍵時刻：

| 情境 | 張力 | 閒聊機率 |
|------|------|---------|
| 投手剛上丘、第一球之前 | 低 | 15% |
| 打者走上打擊區、第一球之前 | 低 | 15% |
| 普通出局（滾地/飛球無跑者）後 | 低 | 10% |
| 保送後、下一打者上場前 | 低 | 10% |
| 換投熱身時 | 中低 | 30% |
| 大比分差（5+分）任一時刻 | 低 | 25% |
| 滿壘 / 得點圈 / 一分差 | **高** | **0%**（不閒聊） |
| 九局下半平手 | **極高** | **0%** |

### 3B. 閒聊話題池

```js
// commentary-data.js 新增 BANTER 區塊
const BANTER = {

  // ── 球員故事 ──
  playerStory: [
    { lines: [
      { speaker: 'color', text: '你知道嗎，${batter} 大學時期其實是投手。後來手臂受傷才轉打者，結果反而打出名堂。' },
      { speaker: 'caster', text: '真的假的？那他現在能投能打，根本二刀流啊！' }
    ]},
    // ...更多
  ],

  // ── 數據趣味 ──
  statFun: [
    { lines: [
      { speaker: 'color', text: '欸我剛查了一下，${batter} 在第七局之後的打擊率高達 .340，越晚越猛。' },
      { speaker: 'caster', text: '這就是所謂的 clutch hitter 啊，關鍵時刻特別專注。' }
    ]},
    // ...更多
  ],

  // ── 天氣/球場閒聊 ──
  ballparkChat: [
    { lines: [
      { speaker: 'color', text: '今天 ${stadium} 的風向是順風，外野手可能要往後站兩步。' },
      { speaker: 'caster', text: '而且濕度偏高，變化球的轉速會比平常更好。' }
    ]},
    { lines: [
      { speaker: 'color', text: '講到 ${stadium}，我上次來這裡的時候，他們的熱狗是我吃過最⋯⋯特別的。' },
      { speaker: 'caster', text: '哈！你是來轉播還是來吃的？' }
    ]},
    // ...更多
  ],

  // ── 對手介紹 ──
  opponentTalk: [
    { lines: [
      { speaker: 'color', text: '對手 ${pitcher} 今年對左打的被打擊率只有 .210，今天政大排了五個左打，會是關鍵。' },
      { speaker: 'caster', text: '鍾sir 犀利，這個對戰組合確實不好打。' }
    ]},
    // ...更多
  ],

  // ── 懷舊 / CPBL 歷史 ──
  nostalgia: [
    { lines: [
      { speaker: 'color', text: '看到這場景我想到 2003 年亞錦賽，同樣的滿場觀眾、同樣的緊張氣氛⋯⋯' },
      { speaker: 'caster', text: '那場真的是經典啊，到現在球迷還在講。' }
    ]},
    // ...更多
  ],

  // ── 食物閒聊 ──
  foodChat: [
    { lines: [
      { speaker: 'color', text: '說到棒球，我覺得球場的便當比外面好吃三倍。是心理作用還是真的？' },
      { speaker: 'caster', text: '絕對是心理作用，但我也覺得比較好吃⋯⋯尤其是配著比賽吃。' }
    ]},
    // ...更多
  ]
};
```

---

## Part 4 — 冷笑話系統

### 4A. 棒球冷笑話池

全部用雙人對話格式（球評講 → 主播吐槽）：

```js
const DAD_JOKES = [
  {
    lines: [
      { speaker: 'color', text: '為什麼棒球員不玩撲克牌？' },
      { speaker: 'caster', text: '⋯⋯為什麼？' },
      { speaker: 'color', text: '因為怕被發「王牌」啊。' },
      { speaker: 'caster', text: '⋯⋯（我到底為什麼要接這個梗）' }
    ]
  },
  {
    lines: [
      { speaker: 'color', text: '投手跟咖啡有什麼共通點？' },
      { speaker: 'caster', text: '我不知道，但我直覺這不會是好笑話。' },
      { speaker: 'color', text: '都需要好的 pitch。' },
      { speaker: 'caster', text: '導播，下一則請切掉謝謝。' }
    ]
  },
  {
    lines: [
      { speaker: 'color', text: '你知道為什麼棒球場的草總是特別綠嗎？' },
      { speaker: 'caster', text: '因為澆水澆得多？' },
      { speaker: 'color', text: '因為每天都有人在外野「飛」來「飛」去⋯⋯灌溉得很均勻。' },
      { speaker: 'caster', text: '這個笑話的水平跟你的打擊率一樣低。' }
    ]
  },
  {
    lines: [
      { speaker: 'color', text: '為什麼野手不會迷路？' },
      { speaker: 'caster', text: '？？？' },
      { speaker: 'color', text: '因為他們永遠知道自己的「守備位置」。' },
      { speaker: 'caster', text: '⋯⋯今天轉播就到這裡，謝謝大家收看。' }
    ]
  },
  // ... 目標 30+ 則（中英雙關都可以）
];
```

### 4B. 笑話觸發規則

- 只在**低張力**時刻觸發（同閒聊規則）
- 機率 **5-8%**（比閒聊低，才不會太頻繁）
- 同一場比賽最多 3 則笑話（不氾濫）
- 同一個笑話跨場不重複（用 `toldJokes` Set 記錄）

---

## Part 5 — 渲染層改造

### 5A. 雙人對話的 CSS 樣式

```css
/* 主播台詞 */
.broadcast-caster {
  font-weight: 700;
  color: #f8fafc;
  border-left: 3px solid #f87171;
  /* 紅邊 = 主播 */
}

/* 球評台詞 */
.broadcast-color {
  font-weight: 400;
  font-style: italic;
  color: #94a3b8;
  border-left: 3px solid #60a5fa;
  /* 藍邊 = 球評 */
  padding-left: 16px;  /* 內縮表示回應 */
}

/* 冷笑話特殊樣式（球評講笑話時字體微抖/emoji 提示）*/
.broadcast-joke {
  /* 左側加 🤡 或 🎤 emoji 裝飾 */
}
```

### 5B. 渲染邏輯改造

修改 `game-renderer.js` 的 `#play-log` 渲染，辨識訊息格式：

- **舊格式**（純字串）：直接 `<p>` 顯示（向下相容）
- **新格式**（`{speaker, text}` 陣列）：每行依 speaker 上不同 class
- **閒聊/笑話與實況**：用輕微分隔線分開（`<hr class="broadcast-sep">`）

### 5C. 轉播欄內的自動捲動

- 新訊息進來 → 自動 scroll 到最新
- 但玩家手動往上滑時，暫停自動捲動（讓玩家可以回顧）
- 10 秒沒手動滑 → 恢復自動捲動
- 換局時強制捲到底

---

## Part 6 — 情境感知的旁白深度

### 6A. 追蹤「本場故事線」

在 `game.matchStats` 中新增 `storyThreads`：追蹤本場比賽的敘事線索。

```js
storyThreads: {
  pitcherDominant: true/false,     // 投手壓制中？
  batterHot: ['王威晨', '陳傑憲'], // 哪些打者今天手感好
  streak: { type: 'K', count: 4 }, // 連續 4K？
  comebackChance: true/false,      // 有逆轉機會？
}
```

這些線索讓旁白可以說出類似：
> 「鍾sir：王威晨今天三支三，對方投手完全拿他沒辦法。」

而不是每次都只能報單一事件的泛用句。

### 6B. 跨場記憶（call-back）

`game.seasonHistory` 已記錄每場結果。可以在閒聊時 call-back：

> 「鍾sir：上次政大對這隊的時候，九局下半才用再見安打贏球。今天又打到膠著，歷史會重演嗎？」

---

## Part 7 — 施工清單

### 檔案異動

| 檔案 | 操作 | 重點 |
|------|------|------|
| `commentary-data.js` | **大擴充** | 所有事件類別補 `broadcast.*` 對話模板（結構化 lines 陣列）、新增 `BANTER`（閒聊池 5 大類各 10+ 模板）、新增 `DAD_JOKES`（30+ 冷笑話）、新增 `fillTemplate` 變數系統 |
| `commentary.js` | **重寫** | `BroadcastGenerator` class：`generateCall(outcome, player, extra)` 回傳 `[{speaker,text}]`、`maybeBanter(tension)` 回傳閒聊/null、`maybeDadJoke(tension)` 回傳笑話/null、`buildContext(game)` 收集模板變數 |
| `game.js` | **改 `addToLog`/`addCommentary`** | `addCommentary()` 改接 `BroadcastGenerator`、新增 `maybeAddBanter()` 在低張力時機觸發、`matchStats` 加 `storyThreads` + `toldJokes` Set |
| `game-renderer.js` | **改 `#play-log` 渲染** | 辨識陣列格式 → 逐行依 speaker 上 class、自動捲動邏輯（手動暫停/恢復）、分隔線 |
| `style.css` | **append** | `.broadcast-caster` / `.broadcast-color` / `.broadcast-joke` / `.broadcast-sep` 樣式，`style.css?v=` bump |

### 施工順序

```
Step 1: 先改渲染層（Part 5）——讓雙人格式可以顯示
  ↓
Step 2: 改 commentary.js → BroadcastGenerator（Part 2A 資料結構 + Part 6 情境追蹤）
  ↓
Step 3: 補 commentary-data.js 對話模板（Part 2B 變數插值）——核心事件先做
  ↓
Step 4: 補閒聊池 BANTER + 冷笑話池 DAD_JOKES（Part 3 + 4）
  ↓
Step 5: game.js 串接（Part 3A 觸發邏輯 + Part 6 故事線）
  ↓
Step 6: 拋光（自動捲動、分隔線、笑話不重複）
```

### 保守策略（最小可行版）

如果不想一次改全部，可以先做 MVP：**只改 `commentary.js` 和 `commentary-data.js`，不改 game.js/addToLog 介面**。

做法：`generateCommentary()` 回傳值從字串改為 HTML 字串（內含 `<span class="caster">` / `<span class="color">` 標籤），直接塞進既有 `addToLog`。這樣渲染層幾乎不用動，但雙人對話感就有了。

---

## 對比：改造前後

### 改造前
```
📢 全壘打！出去了！
📢 三振出局。
📢 二壘安打。
📢 攻守交換，3局上，政大守備。
```

### 改造後
```
【蔡兄】這球咬中！左外野方向——非常深遠！
【鍾sir】林安可的擊球初速 168 公里，聯盟前三的暴力揮棒。
【蔡兄】——出去啦！三分砲！政大逆轉比數 5:3！
──
【鍾sir】說到這個，你知道棒球跟咖啡有什麼共通點嗎？
【蔡兄】不要。
【鍾sir】都需要好的 pitch。
【蔡兄】⋯⋯導播我們進廣告。
──
═══════ 3局上 結束 ═══════ 政大 5 : 3 對手
【蔡兄】三局上半結束，政大逆轉領先兩分。鍾sir，這場你怎麼看？
【鍾sir】黃子鵬的側投開始讓對手的滾地球變多，下半局守備要多注意。
```

---

## 驗收清單

- [ ] 全壘打/三振/安打有雙人對話感（主播喊＋球評補）
- [ ] 普通滾地/飛球可以只有主播單句（球評不搶話）
- [ ] 閒聊在低張力時機觸發（不是每球都講）
- [ ] 冷笑話偶爾出現，觸發率 < 8%
- [ ] 同一場比賽冷笑話不超過 3 則、不重複
- [ ] 高張力時刻（滿壘/一分差/九局）不觸發閒聊或笑話
- [ ] 主播文字為粗體白字紅邊，球評為斜體灰藍字藍邊
- [ ] 向下相容：既有純字串 addToLog 仍可正常顯示
- [ ] 轉播欄自動捲動，手動上滑暫停、10 秒後恢復
- [ ] colOverflow = 0 鐵律維持
