# 政大棒球征服世界 底層邏輯書

版本：v1.0
日期：2026-05-25
適用版本：遊戲 v3.25、引擎校準 v2.1、規則書 v4.0

## 0. 這份文件在講什麼

從你抓 CPBL 球員資料、到遊戲裡實際投出一球、再到球被打出去之後守備跑壘判定結束，中間總共經過三條鏈：

1. **資料鏈**：CPBL JSON → stat-mapper → `data.js` → 遊戲讀取 → `player.abilities`
2. **投打對決鏈**：投手出手 → 物理位置 → 打者判讀 → 揮空/界外/進場分流
3. **擊球後鏈**：EV/LA/SA → 物理飛行 → 守備員處理 → 跑壘推進 → 狀態套用

每一條鏈都有一群參數在控制。這份文件把這三條鏈展開，告訴你每一步的輸入、輸出、公式、可調的參數在哪個檔案哪一行。

---

# 第一部 資料鏈：CPBL 數據如何變成 abilities

## 1.1 整體資料流

```
CPBL 官方頁面（rebas）
    │  ← 爬蟲
    ▼
cpbl_players_2024-2026.json
    │  ← tools/stat-mapper.html 上傳 + data.js 上傳
    ▼
新版 data.js（CPBL_BATTER_STATS_2025 / CPBL_PITCHER_STATS_2025）
    │  ← 遊戲啟動時讀取
    ▼
game.js StatMapper.createPlayerFromStats()
    │
    ▼
player.abilities { contact, power, ... }     ← 引擎讀取的最終欄位
player.advancedStats { heatMap, plateDiscipline, ... }
player.talents [], traits []
```

關鍵原則：**v3.24 起，stat-mapper 已預先計算好 `abilities` 物件存進 data.js**，遊戲讀取時優先用 `stats.abilities`，沒有才動態 fallback 到舊公式。所以調整能力值校準只要改 stat-mapper 的 `piecewise()` 參數重跑即可，不必動 game.js。

## 1.2 三季加權

`tools/stat-mapper.html:77`

```js
const RECENCY = { '2024': 0.60, '2025': 1.00, '2026': 0.80 };
```

每位球員的三季資料按以下權重合併：

- **打者**用 PA（打席數）加權：`weight[yr] = PA[yr] × RECENCY[yr] / Σ`
- **投手**用 BF（面對打者數）加權：同樣邏輯
- 2025 整年資料權重最高、2024 老資料降權、2026 還在跑所以也略降

合併方式（`stat-mapper.html:295` wAvg）：對 AVG / OPS+ / ERA / WHIP 等每一個欄位都跑一次加權平均。`covered` 機制處理缺值：如果某季沒這個欄位就跳過，剩下的權重正規化。

## 1.3 piecewise 校準（v3.24 NBA 2K 式評分標尺）

這是把「CPBL 真實數據」轉成「45–82 的能力值」的核心公式（`stat-mapper.html:117`）：

```js
function piecewise(x, lo, mid, hi, lo_s=45, mid_s=60, hi_s=82) {
  if (x <= lo) return lo_s;          //  最差 → 45
  if (x >= hi) return hi_s;          //  CPBL 頂級 → 82
  if (x <= mid) return lo_s + (x - lo) / (mid - lo) * (mid_s - lo_s);   // lo→mid 段
  return mid_s + (x - mid) / (hi - mid) * (hi_s - mid_s);               // mid→hi 段
}
```

設計理念：

- **中職平均 → 60**（mid_s）
- **中職頂級 → ~82**（hi_s）
- **0–45** 和 **82–100** 都保留：低於 45 表示「比聯盟最差還差」、高於 82 留給升級系統與更強聯盟（WBC、MLB）

數值越低越好的（ERA、BB%、WHIP）用 `piecewiseInv`，把 hi/mid/lo 反轉。

最後 `pw()` 把結果 clamp 在 45–82 區間並四捨五入。**所以從資料端進來的能力值天花板是 82**，82+ 只能透過升級系統突破。

## 1.4 打者九大能力公式

`stat-mapper.html:149` `computeBatterAbilities()`：

| 能力 | 公式 | 主要資料來源 |
|---|---|---|
| **contact** | `0.65 × piecewise(AVG, .230, .275, .340) + 0.35 × piecewise(OPS+, 65, 100, 165)` | 打擊率為主、OPS+ 為輔 |
| **power** | `0.60 × piecewise(ISO, .040, .110, .230) + 0.40 × piecewise(HR, 0, 8, 28)` | ISO（純長打率）為主 |
| **speed** | `piecewise(SB, 0, 12, 45) + 守位加成` | CF +7、SS/OF/LF/RF +4、2B/3B +2 |
| **discipline** | `0.50 × piecewise(BB%, 3, 7.5, 14) + 0.50 × piecewiseInv(K%, 30, 19, 9)` | 保送率、三振率 |
| **fielding** | 守位固定值 | C 68、SS/2B 66、3B 65、OF 63、1B 58 |
| **arm** | 守位固定值 | C 70、SS/3B 65、OF/RF 63、2B 62、1B 57 |
| **clutch** | `piecewise(OPS+, 65, 100, 165)` | 暫時等同 OPS+，未來會接 RISP |
| **vsLeft** | `contact × 0.55 + discipline × 0.25 + (右打 +4 / 左打 -3)` | 推算 |
| **vsRight** | `contact × 0.55 + power × 0.20 + (左打 +4 / 右打 +2)` | 推算 |
| **scoringPosition** | `clutch` | 暫等同 |
| **pinchHitter** | `power × 0.40 + discipline × 0.35 + contact × 0.25` | 代打綜合分 |

注意：**fielding / arm 目前是守位固定值，沒有用個人 FLD%/E/A**。修正書 §10 已列為待補項。

## 1.5 投手九大能力公式

`stat-mapper.html:188` `computePitcherAbilities()`：

| 能力 | 公式 |
|---|---|
| **velocity** | `0.55 × piecewise(K%, 12, 20, 32) + 0.45 × piecewise(K/9, 4, 7, 11)` |
| **control** | `0.45 × piecewiseInv(BB%, 14, 8, 3) + 0.55 × piecewiseInv(WHIP, 1.55, 1.22, 0.85)` |
| **breaking** | `0.55 × piecewise(ERA+, 60, 100, 170) + 0.45 × piecewiseInv(FIP, 5.5, 3.8, 2.0)` |
| **stamina** | SP: `piecewise(IP, 85, 150, 210)`、RP: `piecewise(IP, 25, 55, 80)` |
| **fielding** | `68 + clamp(starts/4, 0, 7)`（先發投手略高） |
| **stuff** | `velocity × 0.48 + breaking × 0.36 + control × 0.16` |
| **discipline** | `control × 0.70 + breaking × 0.30`（不送保送 + 變化球控制） |
| **vsLeft** | `breaking × 0.45 + control × 0.35 + (左投 +8 / 右投 +2)` |
| **crisis** | `control × 0.40 + breaking × 0.35 + ctrl2 × 0.25`（壘上有人時的修正） |
| **pickoff** | `control × 0.45 + fielding × 0.35 + starts × 0.30` |
| **quickDelivery** | `control × 0.35 + velocity × 0.25 + fielding × 0.40` |
| **recovery** | `(RP 72 / SP 57) + piecewise(IP, 25, 80, 180) × 0.14`（先發/中繼基底不同） |

注意 **velocity 用 K% 推算**，不是球速本身。原因是 rebas 沒有球速資料，K% 是球質壓制力的代理指標。所以球速不快但 K% 高的投手（變化球派）會被算高。下勾/側投投手另外用 `armSlotBonus` 補 stuff（見 §2.7.2）。

## 1.6 advancedStats（進階資料層）

除了 11 個 abilities 數字，每位球員還有一包 `advancedStats`，這些在投打對決中會被引擎讀取：

**打者：**
- `heatMap` — 3×3 好球帶熱區，每格 `{contactMod, powerMod, eyeMod}`（PA 加權合併三季）
- `gbRate / ldRate / fbRate / popupRate` — 擊球型態分布（滾/平/飛/popup）
- `avgLaunchAngle` — 預估平均仰角（從 battedBall 推算）
- `plateDiscipline` — 四區（core/edge/chase/invalid）的 swingRate/whiffRate/inPlayRate
- `swingByCount` — 球數情境（0-0、0-1 ... 3-2）的攻擊率
- `timing` — fastLatePct/fastEarlyPct/slowLatePct/slowEarlyPct
- `pitchTypeMatchup` — FF/SI/FC/SL/CU/CH/FS 七種球種的對應表現
- `foulSplit / situational / splits / spray / ballQuality / highlights`

**投手：**
- `pitchArsenal` — 球種武器庫（球種分布與使用率）
- `pitchSequenceModel` — 配球傾向
- `velocity / rpm / evAllowed / zoneProfile / splits`

這些不是必填，引擎都有 fallback。但有資料時會以混合比率（30%、25% 等）影響判定（見 §2.4）。

## 1.7 評級系統（兩套，不要混淆）

**rating（球員整體評等）**：S/A/B/C，依 OPS+ 或 ERA+。S ≥ 165、A ≥ 130、B ≥ 100、其他 C。用途：升級 XP 倍率（S 1.5×、A 1.3×、B 1.1×、C 1.0×、D 0.6×）。

**abilityGrade（個別能力字母）**：S/A/B/C/D/E，依分數區間（`game-params.js:232`）：

| 區間 | 字母 | 顏色 | 標籤 |
|---|---|---|---|
| 90+ | S | 金黃 | 超凡 |
| 80–89 | A | 紫色 | 頂尖 |
| 70–79 | B | 藍色 | 優良 |
| 60–69 | C | 綠色 | 中庸 |
| 50–59 | D | 灰色 | 尚可 |
| 0–49 | E | 深灰 | 不足 |

這是 v3.25 引進的 UI 顯示，每個能力旁邊都有自己的字母 chip。

## 1.8 從資料 → 升級系統的銜接

`player.abilities` 是「初始值」。遊戲中升級後的 ceiling 由品階決定：

```
ceiling = 95 + playerRank × 5      // playerRank 0~6（凡人→宗師）
```

也就是新球員初始 ceiling=95，每升一階 +5，最高品階 6 階 ceiling=125。

升級流水線（v3.25 統合後）：
```
game.js: awardPlayerXP(player, baseXP, category, game)
  → 計算 xpBonus（教練）+ RATING_GROWTH_MULTIPLIER
  → PlayerGrowth.gainXP(player, gain, category)
      → 累積 XP，達門檻 → applyLevelUpBonus
      → 依 category 升對應屬性（batting → contact/power/discipline/clutch）
      → +1 受限於 getAbilityCeiling
```

每次 `upgradeRank` 還會呼叫 `unlockTraitsByRank()` 檢查是否解鎖特質（見 §2.7.3）。

---

# 第二部 投打對決鏈：一球如何決勝

## 2.1 流程總覽

`pitch-engine.js` 的 `PitchEngineOrchestrator.resolveSinglePitch()` 是統一入口。一球依序跑：

```
Step 1  PitchPhysicsModule.generate()       — 投球位置生成（§17.4）
Step 2  BatterJudgmentModule.decide()       — 打者出棒決策（§17.6, §17.7）
Step 3  若不揮棒：判定壞/好球/暴投 → 計入球數
        若揮棒：
Step 4  ContactResolutionModule.resolve()   — Contact 分數 + 揮空/界外/進場分流（§17.8, §17.9）
Step 5  若進場：InPlayOutputModule.compute() — 產出 EV/LA/SA（§17.11）
```

四個子模組全部是**純函式**，不直接改 `game` 狀態。狀態變更（log、球數、跑者）由 `pitch-result-applier.js`（未進場）與 `in-play-result-applier.js`（進場）接手。

## 2.2 Step 1 — 投球位置生成

`pitch-engine.js:174 PitchPhysicsModule.generate(pitcherStats, pitchConfig)`

### 輸入

- **`pitcherStats`**：`{ control, velocity, breaking, stuffScore, fatigue, pitchTypes, throws }`
  - 這些是「**有效能力**」——已經套過體力 mods、armSlot bonus、特質加成的結果
- **`pitchConfig`**：`{ aimCellIndex, aimPosition, effortKey, pitchType }`
  - `aimPosition`：v3.25.3 後支援連續 cm 座標（拖拉 UI 輸出）
  - `aimCellIndex`：舊版 5×5 格中心（對手 AI 還是用這個）
  - `effortKey`：`full` / `normal` / `easy`
  - `pitchType`：`{ name, speed, movement, moveXMin, ... }`

### 公式鏈

**(1) 原始位置 (originalTarget)**

```js
originalTarget = aimPosition || getGridCenter(aimCellIndex, 5)
```

5×5 格中心座標：`x, y ∈ {-30, -15, 0, +15, +30}` cm。

**(2) 控球隨機偏差 (missOffset，§17.4 截斷高斯)**

```js
missRadius = clamp(36 - control × 0.34, 3, 28)   // cm
sigma = missRadius / 2.2
// 從圓內截斷高斯抽樣 (dx, dy)
```

control=100 → missRadius=3cm（神準）；control=50 → missRadius=19cm；control=0 → missRadius=28cm。**這就是控球能力的物理意義**。

**(3) 球路變化縮放 (movementScale)**

```js
gradeScale   = GRADE_MOVE_SCALE[stuffGrade]    // S 1.15, A 1.08, B 1.03, C 1.00, D 0.94, E 0.88
effortScale  = EFFORT_MOVE_SCALE[effortKey]    // full 1.05, normal 1.0, easy 0.94
fatigueScale = clamp(1 - fatigue × 0.004, 0.78, 1)
movementScale = gradeScale × effortScale × fatigueScale
```

- stuffGrade 由 stuffScore 分級（S ≥93、A ≥85、B ≥75、C ≥65、D ≥55、E）
- 體力 100→fatigueScale=0.6→clamp 到 0.78，**體力越差變化球越扁**

**(4) 變化量 (moveX, moveY)**

`_getPitchMovementRange()` 依球種名（中文）對應球種變化範圍，再乘 movementScale。例如曲球：
```js
spread = clamp((movement - 45) × 0.45, 2, 24)
xMin = 0.18×spread×hand   xMax = 0.45×spread×hand   // 橫向偏移（hand: 左投 -1 / 右投 +1）
yMin = -0.95×spread       yMax = -0.62×spread       // 下墜量
```

**(5) finalPosition**

```js
finalPosition.x = originalTarget.x + missOffset.dx + moveX
finalPosition.y = originalTarget.y + missOffset.dy + moveY
```

**(6) 暴投 / 好球判定**

```js
isWildPitch = |x| > 67.5  || |y| > 67.5     // 9×9 接球區外
// 好球帶 ±22.5cm，球半徑 3.6cm
isStrike = max(|x|-22.5, 0)² + max(|y|-22.5, 0)²  ≤  3.6²
```

球邊緣擦到好球帶就算好球（規則書 §4）。

**(7) 球速**

```js
speedKmh = clamp(112 + velocity × 0.6 + N(0, 1.8), 100, 170)
```

velocity=82（CPBL 頂級）→ 平均 161 km/h。velocity=60（中位）→ 平均 148 km/h。

### 輸出

`PitchState { originalTarget, postMiss, finalPosition, isWildPitch, isStrike, moveX, moveY, movementMag, movementScale, missOffset, stuffScore, stuffGrade, speedKmh, effortKey, aimCellIndex }`

## 2.3 Step 2 — 打者判斷與出棒

`pitch-engine.js:310 BatterJudgmentModule.decide(batterStats, pitchState, battingConfig)`

### 輸入

- **`batterStats`**：`{ eye, contact, power, bats, advancedStats }`
  - eye 就是 discipline，已經套過特質（綠繡眼 +7）、timing、策略 eyeMod
- **`battingConfig`**：`{ strategyKey, targetZoneIndex, velocityLock, pitchSpeedGroup, strikes, balls }`
  - `strategyKey`：6 種策略之一（見 §2.6）
  - `targetZoneIndex`：打者瞄準的 3×3 好球帶格子（0–8）
  - `velocityLock`：`fast` / `slow` / `none`
  - `pitchSpeedGroup`：實際投球被分為 `fast`（直球系）或 `slow`

### 公式鏈

**(1) 球速鎖定修正 (timingMod)**

```js
matched = (lockMode === pitchSpeedGroup)
contact: matched ? +8 : -14    // 鎖對 +8、鎖錯 -14
power:   matched ? +4 : -8
eye:     matched ? 0 : -3
```

再加上歷史 timing 資料修正：
```js
badPct = latePct + earlyPct                       // 從 advancedStats.timing 取
timingAdj = -(badPct - 0.50) × 12                 // 基準 0.50，每偏 0.10 ±1.2 分
timingMod.contact = clamp(baseContact + timingAdj, -18, 12)
```

**(2) effectiveEye**

```js
effectiveEye = clamp(eye + timingMod.eye + strategy.eyeMod, 0, 100)
```

**(3) 正確判斷機率 (pCorrectRead，§17.6)**

```js
edgePenalty = clamp((centerDist - 18) / 35, 0, 0.22)
pCorrectRead = clamp(
  0.66 + (effectiveEye - 50) / 130
  - movementMag / 230
  - edgePenalty × 0.65,
  0.18, 0.94
)
```

eye=70 → 約 0.81、eye=82 → 0.90、eye=50 → 0.66、eye=92 → 0.94。**這控制了打者誤判好壞球的機率**。movementMag 越大（變化球變化越強）越容易誤判。

**(4) perceivedStrike（打者「感知」的好壞）**

```js
actualInStrike = |x| ≤ 22.5  && |y| ≤ 22.5         // 物理上是不是好球
perceivedStrike = actualInStrike
  ? Math.random() < pCorrectRead                    // 真好球 → pCR 機率判斷正確
  : Math.random() < (1 - pCorrectRead)              // 真壞球 → 1-pCR 機率誤判為好球
```

**(5) 有效出棒半徑**

```js
effectiveSwingRadius = 24 + strategy.radiusDelta + standardZoneCoverage + twoStrikeCoverage
```

basic 24 cm。各策略修正在 §2.6。standard 策略對好球加 +14、兩好球後額外 +10/+2。

**(6) 追打率 (chaseProb，壞球出棒)**

```js
chaseProb = clamp(
  0.13 - (effectiveEye - 82) × 0.011    // P50 eye=82 為基準
  + movementMag / 280                    // 變化球誘出棒
  + strategy.chaseBonus,
  0.04, 0.42
)
// 兩好球保護策略 +0.08，standard +0.05
// swingByCount 加成：(swingRate - 0.47) × 0.30
```

eye=70 → 0.26、eye=82 → 0.13、eye=92 → 0.02 → clamp 至 0.04。

**(7) 出棒決策**

最關鍵的分支：

```js
if (perceivedStrike) {
  if (targetDist ≤ effectiveSwingRadius) {
    // 感知好球且距 target 近 → 機率出棒
    distRatio = targetDist / effectiveSwingRadius
    zoneSwingProb = clamp(0.88 - distRatio × 0.40 + chaseBonus × 0.3
      + (strikes === 2 ? 0.12 : 0) - (eye - 82) / 250, 0.30, 0.95)
    swings = random() < zoneSwingProb
  } else if (策略 ∈ {tightZone, patient, power}) {
    swings = false                                   // 嚴格策略不揮
  } else {
    // misread chase：感知好球但遠離 target
    swings = random() < clamp(0.42 + chaseBonus × 0.5 - eye/300, 0.05, 0.55)
  }
} else {
  // 感知壞球 → 統一用 chaseProb
  swings = random() < chaseProb
}
// 兩好球 standard 強制保護
if (!swings && twoStrikeStandard && actualInStrike) {
  swings = random() < 0.70
}
```

### 輸出

`SwingDecision { swings, perceivedStrike, pCorrectRead, chaseProb, timingMod, effectiveSwingRadius, targetZoneCenter, targetDist, edgePenalty, strategy }`

## 2.4 Step 4 — Contact 結果分流（若揮棒）

`pitch-engine.js:467 ContactResolutionModule.resolve(batterStats, pitchState, swingDecision, battingConfig)`

### 公式鏈

**(1) pitchStuffPenalty**

```js
pitchStuffPenalty = clamp((stuffScore - 50) / 2.4, -8, 22)
```

stuffScore=50 → 0、stuffScore=90 → 16.67、stuffScore=100 → 20.83。**球質越強，contact 被扣越多**。

**(2) targetContactMod**

```js
targetDistanceCm = |finalPosition - targetZoneCenter|
targetContactMod = clamp(12 - targetDistanceCm / 3, -12, 12)
```

球到打者預期位置就 +12、偏 6cm +10、偏 15cm +7、偏 30cm +2、偏 60cm -8。

**(3) hotZoneMod**

從打者的 `advancedStats.heatMap` 取對應格子的 `{contactMod, powerMod, eyeMod}`。一名「外角熱區」打者在右下格會吃到 contact +5、power +5 之類的真實資料加成。

**(4) finalContactScore**

```js
finalContactScore = clamp(
  contact
  + hotZoneMod.contact
  + targetContactMod
  + timingMod.contact
  + strategy.contactMod
  - pitchStuffPenalty
  + situationalContactMod,
  0, 100
)
```

**(5) contactQuality**

```js
contactQuality = clamp((finalContactScore - 45) / 50, 0, 1)
```

finalContactScore=45 → 0、70 → 0.5、95 → 1.0。

**(6) 三路機率**

```js
whiffProb  = clamp(0.27 - quality × 0.20 + stuffPenalty / 250, 0.03, 0.45) + strategy.whiffDelta
inPlayProb = clamp(0.18 + quality × 0.46, 0.10, 0.72)                       + strategy.inPlayDelta
foulProb   = (1 - whiffProb - inPlayProb)                                   + strategy.foulDelta
```

**(7) plateDiscipline 四區歷史混合（30%，需 ≥20 球樣本）**

```js
plateZone = classifyPlateZone(finalPosition)    // core/edge/chase/invalid
if (pdZone.pitches ≥ 20) {
  whiffProb  = whiffProb × 0.70  + pdZone.whiffRate × 0.30
  inPlayProb = inPlayProb × 0.70 + pdZone.inPlayRate × 0.30
}
```

四區定義（pitch-engine.js:147）：
- **core**：好球帶內半（d ≤ 11.25 cm）
- **edge**：好球帶外緣（11.25 < d ≤ 22.5）
- **chase**：追打區（22.5 < d ≤ 33.75）
- **invalid**：遠離區（d > 33.75）

**(8) pitchTypeMatchup 球種歷史混合（25%，需 ≥15 球樣本）**

```js
ptCode = classifyPitchTypeCode(pitchName)    // FF/SI/FC/SL/CU/CH/FS
if (ptMatch.seen ≥ 15) {
  whiffProb  = whiffProb × 0.75 + ptMatch.whiffRate × 0.25
  inPlayProb = inPlayProb × 0.75 + ptMatch.inPlayRate × 0.25
}
```

**(9) 兩好球修正**

```js
if (strikes === 2) {
  whiffProb -= 0.13
  foulProb  += 0.08
  inPlayProb = 1 - whiffProb - foulProb
}
```

**(10) 正規化與抽樣**

```js
正規化 → whiff + foul + inPlay = 1
roll = Math.random()
roll < whiff → swing_miss（+1 strike）
roll < whiff + foul → foul（strike<2 才+1）/ foul_with_two_strikes
否則 → in_play
```

**(11) effectivePower（給進場球用）**

```js
effectivePower = clamp(
  power + hotZoneMod.power + timingMod.power + strategy.powerMod - stuffPenalty × 0.35,
  0, 100
)
```

stuff 對 power 的懲罰比 contact 輕（×0.35）。

## 2.5 Step 5 — 進場球輸出（EV / LA / SA）

`pitch-engine.js:628 InPlayOutputModule.compute()`

**(1) EV（v2.1 校準後）**

```js
evKmh = clamp(
  92
  + effectivePower × 0.68          // ← v2.0 是 0.62，v2.1 升到 0.68（修正書 §11）
  + (finalContactScore - 50) × 0.28
  + N(0, 6),
  40, 190
)
```

power=70（mid-tier）→ 平均 146 km/h；power=82（top）→ 平均 154 km/h；power=60（avg）→ 平均 140 km/h。

**(2) 擊球型態抽樣**

```js
mix = { ground: gbRate, liner: ldRate, fly: fbRate, popup: popupRate }    // 從 advancedStats
ballType = pickWeighted(mix)        // ground / liner / fly / popup
```

如果打者沒有 advancedStats，fallback 是 `{ ground: 0.42, liner: 0.22, fly: 0.30, popup: 0.06 }`。

**(3) 仰角**

```js
typeRanges = {
  ground: [-15, 9],
  liner:  [10, 24],
  fly:    [25, 39],
  popup:  [40, 60]
}
baseLaunch = randomBetween(...typeRanges[ballType])
launch = baseLaunch × 0.7 + avgLaunchAngle × 0.3    // 與打者歷史均值混合
```

**(4) 噴射角**

```js
pullBias = (bats === 'L' ? +6 : -6)        // 左打拉打偏右、右打拉打偏左
spray = clamp(N(pullBias + finalPosition.x × 0.25, 14), -55, 55)
```

球進來偏內角會增加拉打傾向（`x × 0.25`）。

### 輸出

`InPlayContext { evKmh, launchAngleDeg, sprayAngleDeg, contactPointQuality, battedBallTypeHint, finalPositionAtPlate, pitchContext }`

## 2.6 打者策略 6 種

`pitch-engine.js:85 STRATEGY_PROFILES`：

| Key | 中文 | radiusΔ | contactMod | powerMod | eyeMod | chaseBonus | whiffΔ | foulΔ | inPlayΔ |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| standard | 標準 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| power | 強力揮擊 | -6 | -8 | +10 | -6 | -0.01 | +0.03 | -0.02 | -0.01 |
| tightZone | 縮小好球帶 | -8 | +3 | +8 | +3 | -0.05 | +0.01 | -0.02 | +0.01 |
| protect | 保護好球帶 | +10 | +8 | -8 | +2 | +0.12 | -0.04 | +0.07 | -0.03 |
| patient | 耐心選球 | -10 | -2 | -4 | +8 | -0.08 | -0.02 | +0.01 | +0.01 |
| aggressive | 積極攻擊 | +8 | -3 | +4 | -5 | +0.15 | +0.03 | -0.05 | +0.02 |

- **radiusDelta**：直接改 `effectiveSwingRadius`
- **chaseBonus**：直接加進 chaseProb 和 misread chase
- **whiffΔ/foulΔ/inPlayΔ**：直接加進三路機率

設計理念：power 換 contact、protect 換 chase（兩好球求自保）、patient 換 chase（拚保送）、aggressive 反向操作（積極追打）。

## 2.7 投手與打者 buff/debuff 鏈

四個子模組吃的 `pitcherStats` / `batterStats` 是「**有效能力**」，game.js 在呼叫引擎前已經把以下修正都套好。

### 2.7.1 投手體力 5 級狀態

`game-params.js:221 staminaStates`：

| 階段 | 範圍 | 名稱 | 圖示 | 能力修正 | 每球扣血倍率 |
|---|---|---|---|---|---|
| 5 | 80–100 | 龍精虎猛 | 🐉 | vel+2 / ctrl+2 / break+2 | 0.50× |
| 4 | 60–80 | 微微出汗 | 💧 | 不變 | 0.85× |
| 3 | 35–60 | 略顯疲憊 | 😮‍💨 | vel-3 / ctrl-2 / break-2 | 1.20× |
| 2 | 15–35 | 累死了 | 😩 | vel-8 / ctrl-6 / break-4 | 1.70× |
| 1 | 0–15 | 懷疑人生 | 💀 | vel-15 / ctrl-12 / break-10 | 2.20× |

`Player.consumeStamina()` 每球用對應的 `decayMul` 扣，所以越累每球扣越多（雪崩效應）。`resolveAtBatWithContext()` 在計算 vel/ctrl/break 時自動套 `state.mods`。

### 2.7.2 下勾/側投投手特化

`game-params.js:184 specialPitcherArmSlot`（7 位手動維護）：

| 球員 | armSlot |
|---|---|
| 黃子鵬、陳鴻文、倪福德、鄭凱文 | sidearm |
| 宋家豪、林岳谷、江國謙 | submarine |

`game-params.js:195 armSlotBonus`：

| 出手點 | stuffScore | movementScale | vsRightContact | vsLeftContact |
|---|---:|---:|---:|---:|
| submarine | +6 | +0.05 | -4 | +2 |
| sidearm | +4 | +0.03 | -3 | +1 |

設計動機：黃子鵬等 ERA+ 偏高但 K%/K9 推算的 velocity 偏低，加 stuffScore 把球質拉回頂級。

### 2.7.3 特質（透過品階解鎖、實際加成）

`game-params.js:272 traits`，需要 `abilities[unlocker] ≥ min` 且 `playerRank ≥ minRank`：

**打者：**
- **神之巧手**（contact≥80） → finalContactScore +6
- **巨人之力**（power≥80） → effectivePower +7
- **綠繡眼**（discipline≥80） → eye +7
- **閃電俠**（speed≥80） → speed +8（跑壘 +12%、盜壘 +10%）
- **絕對守備**（fielding≥80） → catchRange +8%
- **神鵰俠侶**（arm≥80） → throwVelocity +10%
- **最終兵器**（clutch≥80） → 得點圈 contact +8、power +5
- **剋左狂魔**（vsLeft≥80） → 對左投 contact +7、power +5

**投手：**
- **光速球**（velocity≥82） → velocity +6、stuffScore +6
- **縫紉機**（control≥82） → missRadius -25%（更精準）
- **魔球師**（breaking≥82） → breaking +5、movementScale +8%
- **無限發動機**（stamina≥82） → staminaDecayMul -15%
- **絕對王牌**（stuff≥86 & crisis≥82，需鑽石階） → 高 leverage ctrl+5、break+4
- **覺醒**（任何能力，需宗師階） → 全能力 +2

### 2.7.4 天賦（純顯示，不二次加成）

`game-params.js:245 talents`：從 abilities 推出的標籤（重砲手、選球眼、火球男…），告訴玩家球員特色，**不再加成**（因為數值已經反映在 abilities）。負面天賦（控球不穩、恐左）也只是顯示，不再扣分。

---

# 第三部 擊球後鏈：從 EV/LA/SA 到結果

## 3.1 流程總覽

```
InPlayOutputModule 輸出 { evKmh, launchAngleDeg, sprayAngleDeg, battedBallTypeHint }
    ▼
BaseballPhysicsEngine.calcBattedBall()       — 物理飛行（距離、滯空、HR 判定）
    ▼
DefenseStateBuilder.buildDefenseState()       — 守備員配置 + 佈陣偏移
    ▼
FieldingEngine.resolveInPlay()                — 守備員選擇、處理結果、安打分類、雙殺
    ▼
BaserunningEngine.resolveAdvance()            — 跑者推進、send/hold 決策、得分
    ▼
InPlayResultApplier.apply()                   — 套用 game 狀態（出局/得分/log/XP）
```

## 3.2 BaseballPhysicsEngine：物理飛行

`baseball-physics-engine.js`，model version 1.1。

### 輸入

```js
calcBattedBall({ evKmh, launchAngleDeg, sprayAngleDeg, stadium, allowPhysicalFoul, surfaceRegistry })
```

### 球型分類

```js
launchDeg < 10  → ground
launchDeg < 25  → liner
launchDeg < 40  → fly
otherwise       → popup
```

### 滾地球路徑（buildGroundProfile）

```js
horizontalMps = max(8, evMps × cos(launch) × 0.94)
verticalMps   = max(0, evMps × sin(launch))
firstBounceTimeSec = clamp(2 × vMps / 9.81 × 0.86, 0, 0.85)
firstBounceDistanceM = horizontalMps × firstBounceTime × airDragLoss
restitution = clamp(surface.restitution - launch × 0.012, 0.52, 0.86)
rollStartSpeedMps = clamp(horizontalMps × restitution × surfaceScale, 5, 38)
// 先用 dirtFriction=5.25 在內野（前 58m）減速
// 出內野後改用 grassFriction=3.65 在外野繼續減速
stopDistance = firstBounce + dirt + grass
```

可調參數在 `FIELD_SURFACE_PHYSICS`（天然草、人工草、紅土混合、室內）：

| key | restitution | dirtFriction | grassFriction | airDrag |
|---|---:|---:|---:|---:|
| natural（天然草） | 0.72 | 5.25 | 3.65 | 0.030 |
| artificial（人工草） | 較高 | 較高 | 較低 | 較低 |
| mixed（紅土混合） | 中 | 中 | 中 | 中 |
| dome（室內） | 中 | 中 | 中 | 最低 |

### 空中球路徑

```js
horizontalMps = evMps × cos(launch)
verticalMps   = evMps × sin(launch)
vacuumDistanceM = (evMps² × sin(2×launch)) / 9.81           // 真空中拋體距離
dragCarryScale = clamp(0.65 - (airDrag - 0.03) × 1.4, 0.56, 0.72)
windCarryScale = clamp(1 + windHelp × 0.04, 0.96, 1.06)
altitudeScale  = 1 + altitude / 10000
parkCarryScale = stadium.hrFactor                            // 球場 HR 係數
distM = vacuumDist × dragCarryScale × windCarryScale × altitudeScale × parkCarryScale
hangTimeSec = clamp(2 × vMps / 9.81, [liner 0.65, fly 1.2], 6.8)
```

`dragCarryScale ≈ 0.65` 是經驗校準值，把真空拋體距離壓到實際距離。

### HR / hitNet / Barrel 判定

```js
wallDist = wallDistance(stadium, sprayDeg)        // 依噴射角線性插值 LF/CF/RF
trajectoryHeightAtWall = (vMps × wallTime - 0.5 × g × wallTime²) × 0.85
fenceHeight = stadium.fenceHeight || 3

isHR    = 非滾地非popup && distM ≥ wallDist && heightAtWall > fenceHeight
hitNet  = 非滾地非popup && distM ≥ wallDist && heightAtWall ≤ fenceHeight   // 牆面二壘安打
isBarrel = ev_mph ≥ 98 && launch ∈ [26 - shift, 30 + shift]    // 完美擊球
```

### 輸出（給守備引擎用）

```js
{
  ballType,
  preciseDistM, dist_m, preciseWallDistM,
  hangTimeSec,
  landingPoint { x, y },           // 落點座標
  firstBouncePoint,                // 滾地球第一彈跳
  groundProfile,                   // 滾地球完整路徑
  flightProfile,                   // 空中球速度/carry/apex
  trajectorySamples,               // 動畫用連續樣本
  surface,                         // 場地物理常數
  isHR, hitNet, isBarrel, isFoul
}
```

## 3.3 FieldingEngine：守備員選擇

`fielding-engine.js`，整檔 1500+ 行的核心引擎。

### 守備員預設站位（v2.0 校準後）

```js
POSITIONS = {
  P:  (0, 18),    C:  (0, -2),
  1B: (30, 30),   2B: (18, 48),   3B: (-30, 30),   SS: (-18, 48),
  LF: (-50, 72),  CF: (0, 86),    RF: (50, 72)        // ← v2.0 從 80/94/80 前移
}
```

加上佈陣偏移（v3.20 守備佈陣系統）：`infield_in`、`double_play`、`outfield_deep`、`outfield_in`、`bunt_defense`、`standard` 六種。

### 候選守備員（candidatePositions）

```js
ballType === ground  → ['P', '1B', '2B', '3B', 'SS', 'LF', 'CF', 'RF']
ballType === popup
  preciseDistM < 38  → ['C', 'P', 1B, 2B, 3B, SS]
  else               → 全部
ballType === fly/liner
  preciseDistM < 62  → 全部
  else               → ['LF', 'CF', 'RF']
```

### 守備員評分 (evaluateCandidate)

關鍵公式（`fielding-engine.js:416`）：

```js
fielding = ability(player, 'fielding')
speed    = ability(player, 'speed')
arm      = ability(player, 'arm')
penalty  = player.getPositionPenalty(position)      // 非本守位扣分

reactionSec = clamp(0.7 - fielding/230 - speed/280 + penalty/80, 0.18, 0.78)
speedMps    = clamp(4.7 + speed/42 + fielding/180, 4.8, 8.3)

routeDistanceM = |playPoint - start|
arrivalSec     = arrivalTimeFor(playPoint, ballInfo)
rangeMarginM   = speedMps × max(0, arrivalSec - reactionSec) - routeDistanceM

reachChance = clamp(0.08 + (rangeMargin + 3.5) / 10, 0.02, 0.98)
```

**fly 球額外加成（v2.0 修正後）：**

```js
wallRatio = preciseDistM / preciseWallDistM
routineAirBonus = wallRatio < 0.78 ? 0.22 : wallRatio < 0.9 ? 0.14 : 0.04

if (hangTime < 3)  hangAdjust = -(3 - hang) × 0.15      // 球太快、追不上
elif (hangTime > 4) hangAdjust = clamp((hang - 4) × 0.14, 0, 0.20)   // routine pop fly
else hangAdjust = 0

reachChance += routineAirBonus + hangAdjust
```

**難度（difficulty）：**
- isBarrel +0.08
- liner +0.10
- ground 且 ball速度 >27 m/s +0.20
- 反應不足 (arrival < reaction + 0.16) +0.24
- 投手前強襲 +0.34
- 長 spray (>35°) +0.04

**handleChance：**

```js
handleChance = clamp(0.985 + (fielding-75)/720 - penalty/280 - difficulty, minHandle, 0.998)
successScore = reachChance × handleChance
```

`chooseFielder` 依 `successScore` 排序選最佳。**內野滾地球優先選內野手**（successScore ≥ 0.08 即可），否則才讓外野手撿。

### 處理結果（rollFielding）

```js
reachRoll = random()
if (reachRoll > reachChance) {
  // 沒接到
  hardGrounder = ballType==='ground' && (ballArrivalSpeedKmh > 72 || arrival < reaction + 0.28)
  routineEnough = reachChance > 0.72 && rangeMargin > -1 && !hardGrounder
  return { error: routineEnough ? 'field' : null }    // 是 routine play 沒接到 → 失誤
}
handleRoll = random()
if (handleRoll ≤ handleChance) return { success: true }    // 接到
// 接到但失手
throwErrChance = clamp(0.42 - (arm-70)/250, 0.12, 0.58)
fieldErrChance = clamp(0.38 - (fielding-70)/250, 0.12, 0.58)
error = throw / field / mental（依抽樣）
```

### 安打分類（classifyHit）

```js
if (preciseDistM ≥ wallDist × 0.92)
  return isBarrel && random()<0.3 ? 'triple' : 'double'
if (preciseDistM ≥ wallDist × 0.65 || battedBallTypeHint === 'liner')
  return 'double'
return 'single'
```

修正書 §12.8 已標記這套分類過簡，liner 一律 double 是 issue #7。

### 雙殺機率（doublePlayChance）

```js
DP = clamp(
  0.42
  + (ev_mph - 90) / 200
  + (fielding - 70) / 250
  + (arm - 70) / 250
  - (batterSpeed - 70) / 200,
  0.15, 0.7
)
```

ev 越強、守備臂力越好、打者腳程越慢 → 雙殺率越高。

### 傳球與封殺（throwDecision）

外野安打可能產生 `throwDecision`，包含直傳 vs 中繼選擇：

```js
runnerTravelSec = baseSteps × 27.4 / runnerSpeedMps
throwTravelSec = distance / throwMps    // throwMps = clamp(24 + (arm-70)/2.9, 18, 35) × longThrowDecay
margin = runnerTravelSec - (catchSec + exchangeSec + throwTravelSec)
margin > 0 → safe / margin ≤ 0 → out
```

`longThrowDecay = clamp(1 - max(0, dist-38) × 0.0028, 0.82, 1)` — 超過 38m 開始衰減。中繼員選擇：靠近落球點到目標壘的中間區域，總時間（catch + exchange + relay1 + relay2）vs 直傳取較短。

## 3.4 BaserunningEngine：跑壘推進

`baserunning-engine.js`。所有跑壘共用同一份 movement plan。

### 跑者速度

```js
speed = ability(runner, 'speed')
speedMps = clamp(6.35 + (speed-70)/35 + acceleration, 5.45, 8.35)
acceleration = max(0, baseSteps-1) × 0.12         // 連跑多壘有加速
runnerTravelSec = (27.4 × baseSteps) / speedMps
```

speed=70 → 6.35 m/s（一壘約 4.31s）；speed=82 → 6.69 m/s（4.10s）；speed=50 → 5.78 m/s（4.74s）。

### Send/Hold 決策（decideExtraBase）

```js
chance = clamp(
  baseChance                          // 情境基準
  + advanceBonus                      // 教練/特質
  + (speed - 70) / 220                // 腳程
  + (aggression - 50) / 260           // 跑壘積極度
  + styleBias,                        // 激進/保守風格
  0.06, 0.94
)
action = random() < chance ? 'send' : 'hold'
```

### 各情境的 baseChance

| 情境 | runnerId | from → target | baseChance |
|---|---|---|---:|
| Single | R2 | 2B → home | 0.38 |
| Double | R1 | 1B → home | 0.45 |
| Walk | R1/R2/R3 | 強制推進 | — |
| Triple | 所有跑者 | 清壘 | 100% |
| HR | 所有跑者 | 清壘 | 100% |
| Air-out tag-up（depth flyout） | R3 | 3B → home | 由飛球深度 + 守備員 arm 動態算 |
| Error | 所有跑者 | 強制推進 | — |

### 高飛犧牲打（tag-up）

`fielding-engine.js airOutDecision`：
- R3 → home：飛球深度 ≥ 78m 且接殺位置遠 → 高機率 send
- R2 → 3B：類似邏輯，但 baseChance 較低

外野手接殺後立刻產生 `throwDecision`，比較跑者衝壘時間 vs 球到本壘時間，決定 safe/out。

## 3.5 InPlayResultApplier：套用結果

`in-play-result-applier.js apply(context)` 按 `playResult.code` 分支：

| code | 動作 |
|---|---|
| `foul` | strike +1（若 <2）、log |
| `home_run` | `advanceRunners('全壘打')`、resetCount、shake effect、batter XP +50 |
| `net_out` | 牆面飛球出局、recordOut、resetCount |
| `net_double` | 牆面二壘安打、batter XP +22 |
| `error` | playerErrors++、advanceRunners('error')、log |
| `double_play` | 兩個 recordOut |
| `ground_out` | recordOut、resetCount |
| `popup_out` | recordOut |
| `fly_out` | advanceRunners('tag_up') 若有 advanceResult、recordOut |
| `hit`（single/double/triple） | advanceRunners、XP single+12/double+22/triple+30 |

### XP 表

| 事件 | XP | category |
|---|---:|---|
| 全壘打 | +50 | batting |
| 三壘安打 | +30 | batting |
| 二壘安打 | +22 | batting |
| 一壘安打 | +12 | batting |
| 保送（我方打者） | +8 | batting |
| 投手三振 | +12 | pitching |
| 投手保送對手 | -1 | pitching |

## 3.6 視覺時間軸（visualTimeline）

引擎產生的事件序列，battle-scene.js 只能讀取、不能自己重判定。事件型別：

```
contact         (擊球瞬間)
ball_arrives    (球到落點)
fielder_start   (守備員起跑)
fielder_arrives (守備員到位)
runner_start    (跑者起跑)
runner_slide    (近距離滑壘)
runner_arrives  (跑者到壘)
runner_out      (跑者出局)
throw_start     (傳球起點)
throw_arrives   (傳球到達)
```

雙殺事件會有 force + relay 兩段傳球。Tag-up 跑者的 `startAtSec` 從接殺瞬間算（不是擊球瞬間）。

---

# 第四部 整合速查表

## 4.1 關鍵常數

| 常數 | 值 | 說明 |
|---|---:|---|
| GEOMETRY.gridHalf | 67.5 cm | 9×9 接球區半徑 |
| GEOMETRY.strikeHalf | 22.5 cm | 好球帶半徑 |
| GEOMETRY.cellSizeCm | 15 cm | 每格寬高 |
| GEOMETRY.ballRadiusCm | 3.6 cm | 棒球半徑 |
| infieldLimitM | 58 m | 內野/外野分界 |
| dirtFrictionMps2 | 5.25 | 紅土摩擦 |
| grassFrictionMps2 | 3.65 | 草地摩擦 |
| dragCarryScale | ~0.65 | 真空距離 → 實際距離 |
| GRAVITY | 9.81 m/s² | — |
| EV power 係數 | 0.68 | v2.1，原 0.62 |
| pCorrectRead baseline | 0.66 | v1.5 |
| chaseProb baseline | 0.13 | v1.2 |
| whiffProb baseline | 0.27 | v1.4 |

## 4.2 一球完整 trace 範例

假設：
- 投手：control=72、velocity=75、breaking=70、stamina state=fresh（mods +2/+2/+2）
- 球種：曲球，effort=normal
- 打者：contact=73、power=82、discipline=75、bats=L
- 打者策略：standard、瞄 3×3 中央格、velocityLock=none、球數 1-1
- 投手瞄左下角 (-30, -30)

**Step 1 投球生成：**
- 有效 control = 72+2 = 74、stuffScore ≈ 75（B）
- missRadius = 36 - 74×0.34 = 10.84 cm，sigma ≈ 4.93
- 假設 missOffset = (+3, -2)
- movementScale = 1.03 (B) × 1.0 × 1.0 = 1.03
- 曲球 spread = (70 - 45) × 0.45 = 11.25
- moveX ≈ -3、moveY ≈ -10（典型曲球下墜）
- finalPosition = (-30+3-3, -30-2-10) = (-30, -42) → 在 9×9 內、不在好球帶（y=-42 超 22.5）→ **真壞球**
- speedKmh = 112 + 75×0.6 + N(0,1.8) ≈ 158 km/h（曲球實際更慢，這裡只是引擎簡化）

**Step 2 打者判斷：**
- effectiveEye = 75 + 0 + 0 = 75
- movementMag = √(3² + 10²) ≈ 10.4
- edgePenalty = (39 - 18) / 35 = 0.6 → clamp 0.22
- pCorrectRead = 0.66 + (75-50)/130 - 10.4/230 - 0.22×0.65 ≈ 0.66 + 0.192 - 0.045 - 0.143 = 0.664
- actualInStrike = false → perceivedStrike = random() < (1-0.664) = 33.6%
- 假設 perceivedStrike = false（66% 機率）
- chaseProb = 0.13 - (75-82)×0.011 + 10.4/280 + 0 ≈ 0.244
- swings = random() < 0.244 → 假設 false → **打者沒揮**

**Step 3a 不揮棒：**
- isStrike=false、isWildPitch=false → ball
- 球數 1-1 → 2-1

如果 Step 2 打者誤判為好球並揮棒：

**Step 4 Contact：**
- stuffScore=75 → pitchStuffPenalty = (75-50)/2.4 = 10.4
- targetDist = |(-30,-42) - (0,0)| = 51.6cm → targetContactMod = 12 - 51.6/3 = -5.2
- hotZoneMod 看打者熱區（假設下角 -2）
- finalContactScore = 73 + (-2) + (-5.2) + 0 + 0 - 10.4 + 0 = 55.4
- contactQuality = (55.4 - 45) / 50 = 0.208
- whiffProb = 0.27 - 0.208×0.20 + 10.4/250 ≈ 0.27 - 0.042 + 0.042 = 0.27
- inPlayProb = 0.18 + 0.208×0.46 = 0.276
- foulProb = 1 - 0.27 - 0.276 = 0.454
- 1-1 球數，不套兩好球修正
- roll = random()，假設 0.6 → 落在 foul 區（0.27 ~ 0.27+0.454=0.724）→ **界外**

如果 roll 落 in_play：

**Step 5 EV/LA/SA：**
- effectivePower = 82 + (-2hot) + 0timing + 0strategy - 10.4×0.35 = 76.4
- evKmh = 92 + 76.4×0.68 + (55.4-50)×0.28 + N(0,6) ≈ 145.5
- 假設打者 gbRate=0.45, ldRate=0.20, fbRate=0.30, popupRate=0.05 → 抽到 fly
- launch = randomBetween(25,39) × 0.7 + avgLaunchAngle × 0.3
- 假設 launch=30、spray=N(+6+(-30)×0.25, 14) = N(-1.5, 14) → 假設 spray=-12

**Step 6 物理：**
- ballType=fly、evMps=40.4
- vacuumDist = 40.4² × sin(60°) / 9.81 = 144 m
- distM = 144 × 0.65 × 1.0 × 1.0 × 1.0 = 93.6 m
- wallDist(spray=-12) ≈ 滿球場左中外野 ≈ 110 m → 不夠 → 非 HR
- hangTime = 2 × 40.4×sin(30°) / 9.81 × 1.0 ≈ 4.12s

**Step 7 守備：**
- candidatePositions：fly + dist 93.6 ≥ 62 → ['LF','CF','RF']
- LF start=(-50, 72)、playPoint=landing≈(-19, 92)
- routeDistance = √((50-19)² + (92-72)²) ≈ 36.7m
- LF fielding=75、speed=70 → reactionSec=0.7-0.326-0.25=0.124（clamp 0.18）
- speedMps = 4.7 + 70/42 + 75/180 = 6.78
- rangeMargin = 6.78 × (4.12 - 0.18) - 36.7 = 26.71 - 36.7 = -10
- reachChance = 0.08 + (-10+3.5)/10 = 0.015 → clamp 0.02
- routineAirBonus（wallRatio=93.6/110=0.85） = 0.14、hangAdjust (4.12>4) = 0.017
- reachChance 最終 = 0.02 + 0.14 + 0.017 = 0.177
- 不太可能接到 → **大概是 hit**
- distM 93.6 ≥ wallDist×0.65=71.5 → classifyHit = 'double'

**Step 8 跑壘：**
- 假設 R1 在一壘，speed=72 → decideExtraBase chance = 0.45 + (72-70)/220 + … ≈ 0.48
- 假設 send → R1 score、batter 站二壘 → +1 run

**Step 9 套用：**
- in-play applier：advanceRunners('二壘安打')、log、batter XP+22

整個鏈走完約 ms 級。

## 4.3 修改數值平衡的入口指南

| 想改什麼 | 改哪裡 |
|---|---|
| CPBL 真實數據 → abilities 校準 | `tools/stat-mapper.html` 的 `piecewise()` 呼叫參數 → 重跑匯出 data.js |
| 控球偏差/變化球範圍 | `pitch-engine.js _sampleMissOffset` / `_getPitchMovementRange` |
| 打者誤判機率（pCorrectRead） | `pitch-engine.js:340` |
| 追打率（chaseProb） | `pitch-engine.js:362` |
| 揮空率 baseline | `pitch-engine.js:506` |
| EV 公式（HR 機率） | `pitch-engine.js:633` |
| 守備員預設站位 | `fielding-engine.js:4` + `defense-state-builder.js:15`（兩處必須同步）|
| reachChance 公式 | `fielding-engine.js:433-446` |
| 安打分類門檻 | `fielding-engine.js classifyHit` |
| 雙殺機率 | `fielding-engine.js doublePlayChance` |
| 跑者送/擋 baseChance | `baserunning-engine.js resolveAdvance` 內各情境 |
| 體力 5 級狀態 | `game-params.js:221 staminaStates` |
| 下勾/側投名單 | `game-params.js:184 specialPitcherArmSlot` |
| 天賦/特質定義 | `game-params.js:245 talents` / `game-params.js:272 traits` |
| 字母評級門檻 | `game-params.js:232 abilityGrades` |
| XP 數值 | `pitch-result-applier.js`、`in-play-result-applier.js`、`game-params.js experience` |
| 升級規則 | `player-growth-system.js` |
| 球場物理（草/紅土/室內） | `data.js FIELD_SURFACE_PHYSICS` |

## 4.4 已知殘留偏差速查（v2.1 後）

引用自 `投打對決修正書v1.1.md §12`，按優先級：

| Issue | 症狀 | 主修檔案 |
|---|---|---|
| #1 HR 雙向失準（高） | top tier HR 過量、mid tier 不足 | `pitch-engine.js:633` EV 公式 |
| #2 弱打 BABIP 偏高（中） | 短飛球落在無人區 | `fielding-engine.js:11-13` 外野位置 |
| #3 強打 BABIP 偏低（中） | reachChance 上限飽和 | `fielding-engine.js:433` |
| #4 弱打 K% 過高（中） | 林哲瑄等 contact 低分過度懲罰 | `pitch-engine.js` whiff 公式 |
| #5 AVG R² 退步（低） | #1 副作用 | 跟 #1 一起修 |
| #6 視覺層差距（低） | battle-scene 座標未同步 | `battle-scene.js:945` |
| #7 liner doubles 分類過簡（低） | 所有 liner 一律 double | `fielding-engine.js classifyHit` |
| #8 popup 候選保守（低） | SS/2B 對 50-65m 球未追 | 待設計新 ballType 子分類 |

每條 issue 都附建議改法與風險，未來繼續校準從這裡接續。

---

# 附錄 A：檔案地圖

```
資料層
├── data.js                       球員資料（CPBL_BATTER_STATS_2025 / CPBL_PITCHER_STATS_2025）
└── tools/
    ├── stat-mapper.html         CPBL JSON → data.js 整合工具（瀏覽器版）
    ├── stat-mapper.js           Node.js 版（邏輯相同）
    └── sim-tester.html          投打模擬批次測試

投打對決層
├── pitch-engine.js              四子模組（Physics/Judgment/Contact/InPlay）
├── pitch-result-applier.js      未進場結果套用（ball/strike/walk/strikeout/foul）
└── game.js                      協調器、resolveAtBatWithContext()

擊球後層
├── baseball-physics-engine.js   純物理（飛行、滾動、HR 判定）
├── defense-state-builder.js     守備員配置 + 佈陣
├── fielding-engine.js           守備選擇、處理、安打分類、雙殺、傳球
├── baserunning-engine.js        跑者推進、send/hold 決策
└── in-play-result-applier.js    場內球結果套用（出局/得分/XP/log）

UI / 動畫層（不在邏輯鏈內）
├── battle-scene.js              Canvas 動畫（只讀 visualTimeline）
├── game-renderer.js             HTML UI
└── animation-assets.js          Sprite hook

參數設定層
└── game-params.js               所有可調數值（traits、staminaStates、armSlot、abilityGrades、experience...）

養成/管理層
├── player-growth-system.js      升級系統、品階、特質解鎖
├── logistics-system.js          後勤管理（教練、訓練、行銷）
├── rewards-system.js            賽後獎勵
├── newspaper-summary.js         旺來體育報紙
├── story-data.js / story-intro.js  劇情
└── commentary.js / commentary-data.js  旁白
```

# 附錄 B：閱讀建議順序

1. 第一部 §1.3 piecewise 公式（**理解能力值從哪來**）
2. 第一部 §1.4–1.5 打者/投手能力公式（**理解 abilities 的每個欄位代表什麼**）
3. 第二部 §2.1 投打流程總覽
4. 第二部 §2.7 buff/debuff 鏈（**理解 abilities 在進入引擎前的修正**）
5. 第三部 §3.1 擊球後流程總覽
6. 第四部 §4.2 一球完整 trace 範例（**把所有公式串起來看一次**）
7. 第四部 §4.3 修改數值平衡的入口指南（**改東西的時候查這裡**）

# 附錄 C：規則書 vs 底層邏輯書

| 文件 | 用途 |
|---|---|
| **規則書（投打對決模型規則書.md）** | 設計藍圖 — 描述「機制應該是什麼樣」（§17 最終定版）|
| **底層邏輯書（本檔）** | 實作對照 — 描述「程式碼實際在算什麼」、參數在哪個檔案哪一行 |
| **修正書（投打對決修正書v1.1.md）** | 校準歷史 — 描述「為什麼改、改了什麼、效果如何、issue tracker」|
| **更新書（v3.XX-更新書.md）** | 版本日誌 — 描述「v3.X 加了哪些新玩法」|

四份文件互補，建議搭配查閱。
