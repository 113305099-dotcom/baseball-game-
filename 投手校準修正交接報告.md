# 投手校準修正交接報告

日期：2026-06-01  
對象：下一位接手 AI / 遊戲工程師  
範圍：投手模擬校準、投打對決體系檢討、後續平衡路線

---

## 0. 一句話結論

目前投手校準最嚴重的問題不是「投手能力值完全沒用」，而是 **球種層級 `pitchTypes[].stuff` 的資料映射把頂級投手的總體 `abilities.stuff` 覆蓋掉了**。

這造成強投在模擬中被降格成低球威投手：

- 徐若熙總體能力：`velocity=90`, `stuff=87`
- 徐若熙四縫線球種：`stuff=40`
- sim-tester 與 game.js 都優先使用球種 `stuff`
- 結果：真實 `K9=9.12`，模擬 `K9=4.54`

CSV 整體結果也支持這個結論：

| 指標 | R2 | 真實平均 | 模擬平均 | 判讀 |
|---|---:|---:|---:|---|
| ERA | 0.002 | 3.31 | 4.25 | 幾乎無排序能力 |
| OPS-against | 0.011 | .590 | .763 | 強投被打太慘 |
| K9 | 0.019 | 6.54 | 5.45 | 三振通道失效 |
| BB9 | 0.562 | 2.60 | 2.26 | 控球通道仍有效 |
| H9 | 0.013 | 8.69 | 10.72 | 被安打通道失效 |
| HR9 | 0.000 | 0.50 | 0.62 | 幾乎全雜訊 |
| WHIP | 0.151 | 1.26 | 1.44 | 主要靠 BB9 撐住 |
| Whiff% | 0.066 | 19.13% | 11.49% | 球威/揮空訊號斷裂 |

所以第一優先不是再調 BB9，也不是先重寫整套引擎，而是修正「球種 stuff 壓扁總體 stuff」這條資料鏈。

---

## 1. 目前架構速讀

### 1.1 資料鏈

目前資料流程大致是：

```text
CPBL JSON
  -> tools/stat-mapper.html
  -> data.js
  -> Player / sim-tester
  -> pitch-engine.js
  -> FieldingEngine / BaserunningEngine
  -> 模擬結果
```

重要原則：

- `data.js` 已經存好 `abilities`。
- 遊戲本體和 sim-tester 都直接讀 `abilities` 與 `pitchTypes`。
- 投手總體能力包含 `velocity/control/breaking/stuff/stamina/discipline/crisis`。
- 球種資料 `pitchTypes[]` 包含 `speed/movement/control/stuff/slugRisk/moveXMin...`。

### 1.2 投手結果如何產生

投手結果目前不是單一公式，而是多段通道：

```text
control
  -> resolvePitchAimCell / controlBallRateMul
  -> 壞球率
  -> BB9

velocity
  -> velContactPenalty
  -> contact score 下降
  -> K9 / whiff / in-play rate

stuffScore
  -> pitchStuffPenalty
  -> contact score 下降
  -> whiff / foul / in-play 分流

stuffScore
  -> plateDiscipline 混合倍率
  -> 打者歷史 whiff/inPlay 比例依投手強弱修正

stuffScore / speed / deception / location
  -> effectivePower 下降
  -> EV / 被安打 / HR

fielding engine
  -> 進場球是否安打
  -> H9 / WHIP / ERA
```

目前 CSV 顯示：

- `control -> BB9` 這條還活著。
- `velocity/stuff -> K9/Whiff` 這條幾乎斷掉。
- `stuff/quality -> H9/ERA/OPS` 這條也不夠強。

---

## 2. 這次 CSV 的直接診斷

來源：

```text
C:\Users\user\Downloads\sim-tester-pitcher-batch-2026-05-31 (1).csv
```

樣本：

- 111 名投手
- 每位投手 vs 合格打者池隨機抽樣
- 輸出 K9 / BB9 / H9 / HR9 / WHIP / ERA / OPS / Whiff%

### 2.1 指標分層

#### BB9：目前是唯一還算成功的通道

`BB9 R2 = 0.562`。  
這代表 `controlBallRateMul(control)` 和 sim-tester 的 aim cell 壞球率機制仍然有效。

不要第一步去動 BB9。  
如果先動 BB9，很容易把唯一可用的投手排序也打壞。

#### K9 / Whiff%：目前幾乎失效

`K9 R2 = 0.019`，`Whiffp R2 = 0.066`。  
這不是單純係數太小，而是排序訊號被覆蓋或反轉。

症狀：

- 頂級三振投手模擬 K9 大幅偏低。
- 真實高 Whiff 投手沒有在 sim 中產生足夠揮空。
- 模擬 Whiff% 平均只有 11.49%，真實平均 19.13%。

#### H9 / OPS / ERA：投手壓制被打品質的通道仍不足

`H9 R2 = 0.013`、`OPS R2 = 0.011`、`ERA R2 = 0.002`。  
這代表進場後結果主要由打者與守備決定，投手「讓對方打不好」的獨立訊號不足。

但這一層要等 K9/Whiff 修好後再動。  
否則會混淆：現在強投被安打多，部分原因其實是 `stuffScore` 被低估，導致 contact/in-play 品質都偏高。

### 2.2 最嚴重案例

#### 徐若熙

```text
realK9 = 9.12
simK9  = 4.54
dK9    = -4.58
realWhiffp = 25.48%
sim 走出低 K / 低 whiff
```

data.js 中總體能力：

```js
abilities: {
  velocity: 90,
  control: 85,
  breaking: 84,
  stuff: 87
}
```

但球種層級：

```js
pitchTypes: [
  { name: "四縫線", speed: 150.5, movement: 84, control: 35, stuff: 40 },
  { name: "卡特球", speed: 142.6, movement: 84, control: 45, stuff: 48 },
  { name: "滑球",   speed: 137.0, movement: 84, control: 40, stuff: 55 },
  { name: "曲球",   speed: 124.3, movement: 88, control: 37, stuff: 40 },
  { name: "變速球", speed: 134.9, movement: 84, control: 35, stuff: 69 }
]
```

結果：

- 他明明是 `stuff=87` 的頂級投手。
- 實際每球卻多數用 `stuff=40~55`。
- `pitchStuffPenalty = (stuffScore - 50) / 2.4` 會把 40 當成弱球威，甚至給打者變相加分。

#### 勝騎士、威能帝、古林睿煬、艾速特

這些投手都是真實高 K / 高壓制，但 sim K9 明顯偏低：

| 球員 | realK9 | simK9 | 差距 |
|---|---:|---:|---:|
| 徐若熙 | 9.12 | 4.54 | -4.58 |
| 勝騎士 | 8.51 | 4.60 | -3.91 |
| 威能帝 | 8.13 | 4.45 | -3.68 |
| 古林睿煬 | 9.37 | 6.22 | -3.15 |
| 艾速特 | 7.91 | 4.91 | -3.00 |

這不是單一球員資料異常，而是系統性：強投的球威訊號沒有進入投打引擎。

---

## 3. 根因：球種 stuff 的單位與標尺錯誤

### 3.1 來源程式

在 `tools/stat-mapper.html`：

```js
const whiffR = (typeof data.whiffRate === 'number') ? data.whiffRate : 0;
const realStuff = (cnt >= 30)
  ? Math.round(clampInt(whiffR * 100 * 1.5, 40, 95))
  : null;
```

問題在於 `whiffRate` 是小數，例如：

```text
0.208 = 20.8%
0.318 = 31.8%
0.457 = 45.7%
```

目前公式會算成：

```text
0.208 * 100 * 1.5 = 31.2 -> clamp 到 40
0.318 * 100 * 1.5 = 47.7 -> 48
0.457 * 100 * 1.5 = 68.55 -> 69
```

這導致：

- 普通直球幾乎全部被壓到 `40`。
- 好的 breaking pitch 也常只有 `50~60`。
- 只有極端高 whiff 球種才會接近 70。
- 總體 `abilities.stuff=80+` 幾乎沒有用。

### 3.2 覆蓋點

在 `tools/sim-tester.html`：

```js
function estimateStuffScoreGameLike(pitcher, pitch) {
  if (Number.isFinite(pitch?.stuff)) return clampNumber(pitch.stuff, 0, 100);
  ...
}
```

在 `game.js`：

```js
function estimateStuffScore(pitcher, pitch) {
  let base;
  if (Number.isFinite(pitch?.stuff)) base = clampNumber(pitch.stuff, 0, 100);
  else {
    ...
  }
}
```

這兩處都把球種 `stuff` 視為最終值。  
所以只要 `pitchTypes[].stuff` 低，總體 `abilities.stuff` 就被蓋掉。

### 3.3 為什麼以前 baseline 還比較好

舊 baseline JSON 顯示，Wave B/C 後曾經有：

```text
K9 R2    約 0.52
BB9 R2   約 0.49~0.60
Whiff R2 約 0.75
```

這份 CSV 卻變成：

```text
K9 R2    0.019
Whiff R2 0.066
```

合理推論：

1. 後續導入真實 `pitchTypes` 後，球種 `stuff` 開始覆蓋總體 `stuff`。
2. `realStuff = whiffRate * 100 * 1.5` 的標尺太低，導致頂級投手失去壓制。
3. sim-tester 修掉了 moveX/Y 單位問題，但仍保留球種 `stuff`。
4. game.js 本體則同時保留球種 `stuff` 與 moveX/Y，風險更高。

---

## 4. P0 修正建議：先修球種 stuff，不動 BB9

### 4.1 改 stat-mapper：球種 stuff 用 piecewise，不用線性乘法

把 `tools/stat-mapper.html` 的：

```js
const realStuff = (cnt >= 30)
  ? Math.round(clampInt(whiffR * 100 * 1.5, 40, 95))
  : null;
```

改成類似：

```js
const realStuff = (cnt >= 30)
  ? pw(piecewise(whiffR, 0.15, 0.25, 0.40))
  : null;
```

建議初始標尺：

| whiffRate | stuff |
|---:|---:|
| 0.15 | 50 |
| 0.25 | 70 |
| 0.40 | 90 |

理由：

- CPBL 投手球種 whiffRate 大多落在 0.15~0.40。
- `piecewise` 已經是全專案能力轉換標尺。
- 可讓普通球種在 50~70，優秀球種在 75~90。
- 不會讓 20.8% whiff 的四縫線被硬壓到 40。

更保守版本：

```js
const realStuff = (cnt >= 30)
  ? pw(piecewise(whiffR, 0.12, 0.22, 0.36))
  : null;
```

更激進版本：

```js
const realStuff = (cnt >= 30)
  ? pw(piecewise(whiffR, 0.14, 0.24, 0.34))
  : null;
```

建議先用保守中間值 `0.15 / 0.25 / 0.40`，跑完 baseline 再微調。

### 4.2 球種 stuff 不應 100% 覆蓋總體 stuff

即使 stat-mapper 修好，也不建議讓 `pitch.stuff` 完全覆蓋投手總體 `abilities.stuff`。

建議改成混合：

```js
function estimateStuffScoreGameLike(pitcher, pitch) {
  const pitcherStuff = clampNumber(pitcher?.abilities?.stuff ?? 70, 0, 100);
  if (Number.isFinite(pitch?.stuff)) {
    const pitchStuff = clampNumber(pitch.stuff, 0, 100);
    return clampNumber(pitcherStuff * 0.65 + pitchStuff * 0.35, 0, 100);
  }
  ...
}
```

game.js 同步：

```js
function estimateStuffScore(pitcher, pitch) {
  const pitcherStuff = clampNumber(pitcher?.abilities?.stuff ?? 70, 0, 100);
  let base;
  if (Number.isFinite(pitch?.stuff)) {
    const pitchStuff = clampNumber(pitch.stuff, 0, 100);
    base = clampNumber(pitcherStuff * 0.65 + pitchStuff * 0.35, 0, 100);
  } else {
    const velocity = clampNumber((pitch?.speed ?? pitcher.abilities?.velocity ?? 70), 0, 100);
    const movement = clampNumber((pitch?.movement ?? pitcher.abilities?.breaking ?? 70), 0, 100);
    const control = clampNumber((pitch?.control ?? pitcher.abilities?.control ?? 70), 0, 100);
    base = clampNumber(
      pitcherStuff * 0.55 + (velocity * 0.30 + movement * 0.25 + control * 0.10),
      0,
      100
    );
  }
  const armSlot = pitcher?.armSlot || window.GAME_PARAMS?.specialPitcherArmSlot?.[pitcher?.name];
  const armBonus = armSlot ? (window.GAME_PARAMS?.armSlotBonus?.[armSlot]?.stuffScore || 0) : 0;
  return clampNumber(base + armBonus, 0, 100);
}
```

理由：

- `abilities.stuff` 是投手整體壓制力，應該是主訊號。
- `pitch.stuff` 是該球種相對品質，應該只做微調。
- 若完全用球種 whiffRate，會把打者群、球數情境、使用位置、配球意圖混進單球球威。

建議權重：

| 方案 | 總體 stuff | 球種 stuff | 風險 |
|---|---:|---:|---|
| 保守 | 75% | 25% | 球種差異較小 |
| 推薦 | 65% | 35% | 平衡 |
| 激進 | 55% | 45% | 球種差異大，較可能製造噪音 |

先用 `65 / 35`。

### 4.3 不要再讓球種 control 大幅拖累總體 control

現在真實 `pitchTypes[].control` 也很低，例如徐若熙四縫線 `control=35`。  
sim-tester 中目前：

```js
control += ((selectedPitch.control || 70) - 70) / 16;
```

這一段還算小，`35` 只會讓 control 降約 `2.2`，不是主因。  
但 game.js 和未來 AI 不應把球種 control 放大。

建議：

- P0 不動球種 control。
- P1 才重新定義 `pitchTypes[].control`。
- 如果要防呆，可以 clamp 球種對總體 control 的影響：

```js
const pitchControlDelta = clampNumber((selectedPitch.control - 70) / 16, -3, 3);
control += pitchControlDelta;
```

### 4.4 moveX / moveY 單位問題要同步進 game.js

sim-tester 已經有註解說：

```js
// stat-mapper 存的是 rebas 原始座標（-235~+238），不是 cm。
// 引擎 _getPitchMovementRange 碰到 moveXMin 會直接用（當 cm），導致位移 4 公尺。
// 解法：不傳 coord ranges，讓引擎從 movement 能力值 + 球種名推算。
```

sim-tester 的 `buildGameLikePitchTypes()` 已經不傳 `moveXMin/Max`。  
但是 game.js 的 `Player.generatePitchTypes()` 仍保留：

```js
moveXMin: pt.moveXMin,
moveXMax: pt.moveXMax,
moveYMin: pt.moveYMin,
moveYMax: pt.moveYMax
```

這代表遊戲本體仍可能吃到錯誤單位。  
建議 P1 同步修正：

- 先不要把 `moveXMin/Max` 傳入 pitch-engine。
- 或在 stat-mapper 寫入前把 raw coordinate 正規化成 cm。

最保守做法：

```js
// 不傳真實 move range，避免單位錯用
// moveXMin: pt.moveXMin,
// moveXMax: pt.moveXMax,
// moveYMin: pt.moveYMin,
// moveYMax: pt.moveYMax,
```

這會讓本體與 sim-tester 行為一致。

---

## 5. P0 修正後的驗證門檻

### 5.1 先只跑投手 baseline

使用：

```bash
node tools/run-pitcher-baseline.js --pa=1000 --minbf=100 --out=tools/_baseline-pitcher-after-stuff-fix.json
```

目標：

| 指標 | 修正前 CSV | 第一階段目標 |
|---|---:|---:|
| K9 R2 | 0.019 | >= 0.35 |
| Whiffp R2 | 0.066 | >= 0.50 |
| BB9 R2 | 0.562 | 不低於 0.45 |
| mean sim K9 | 5.45 | 6.3~7.5 |
| mean sim Whiff% | 11.49% | 15%~19% |
| mean sim BB9 | 2.26 | 2.2~2.8 |

如果 K9/Whiff 沒回來，代表球種 stuff 還不是唯一問題，要繼續追 `pitchTypeMatchup` 混合或 swing/whiff 機率。

如果 K9 過頭：

- 先把總體/球種 stuff 權重從 `65/35` 改成 `75/25`。
- 或把 `pitchStuffPenalty` 斜率從 `/2.4` 改弱到 `/2.8`。
- 不要第一時間降 `velContactCoef`，因為那會影響速度派排序。

### 5.2 再跑打者 baseline

因為 stuff 會影響打者整體 AVG/K/SLG，所以投手 baseline 過後要跑打者：

```bash
node tools/run-batter-baseline.js --pa=1000 --minpa=100 --out=tools/_baseline-batter-after-stuff-fix.json
```

目標：

| 指標 | 目標 |
|---|---|
| mean AVG | 不比前一版下降超過 .010 |
| AVG R2 | 不明顯下降 |
| SLG R2 | 不明顯下降 |
| mean K% | 不上升超過 2 個百分點 |
| HR/PA | 不爆炸、不塌陷 |

如果打者整體被壓太低：

- 優先調 `pitchStuffPenalty` 斜率。
- 次優先調 `plateDiscipline` 混合權重。
- 不要直接加打者 contact，否則會破壞打者能力校準。

### 5.3 最後用人工代表球員檢查

建議固定看 8 類投手：

| 類型 | 代表 |
|---|---|
| 高 K 火球先發 | 徐若熙、古林睿煬 |
| 洋將王牌 | 勝騎士、威能帝、艾速特、羅戈 |
| 控球型先發 | 魔神龍、布坎南 |
| 側投/特殊投法 | 黃子鵬、陳鴻文 |
| 高保送投手 | 魏碩成、蒙德茲 |
| 後援高 K | 曾峻岳、陳冠偉 |
| 低 K 吃局數 | 王維中類型 |
| 小樣本異常 | BF 100~150 的投手 |

每類看：

- real/sim K9
- real/sim BB9
- real/sim H9
- real/sim WHIP
- sim Whiff%
- sim BABIP
- pitch mix 是否合理

---

## 6. P1 校準方向：H9 / WHIP / ERA

P0 修完後，K9/Whiff 應該會回復。  
下一個大問題會是 H9 / WHIP / ERA。

### 6.1 為什麼 H9 目前很難準

H9 不是單純投手能力，而是混合：

```text
投手球威
  -> contact quality
  -> EV / LA / spray
  -> 物理落點
  -> 守備可及性
  -> 安打分類
  -> 跑壘與補傳
```

目前投手壓制 in-play quality 的通道仍太弱。  
Wave F 已經加入：

- speedQualityPenalty
- lateBreakQualityPenalty
- deceptionQualityPenalty
- locationQualityPenalty

但 CSV 顯示 H9 R2 仍很低。  
合理原因：

1. `stuffScore` 被球種 stuff 壓低，Wave F 沒吃到正確強投訊號。
2. 進場後結果仍太依賴打者 `ballQuality`。
3. 投手真實 `evAllowed.avg` 尚未進入 EV 公式。
4. 守備與 BABIP 噪音掩蓋投手差異。

### 6.2 建議新增「投手 EV allowed 通道」

data.js 已有：

```js
advancedStats.evAllowed.avg
```

例如徐若熙：

```js
evAllowed: { avg: 156.4 }
```

這是投手允許擊球初速，應該是 H9/OPS/ERA 的核心過程指標。

建議在 `InPlayOutputModule.compute()` 中加入投手側 quality modifier。  
目前函式只拿：

```js
compute(batterStats, contactResult, pitchState)
```

但 `pitchState` 沒有投手 `advancedStats.evAllowed`。  
建議新增：

```js
pitcherStats.evAllowedAvg
```

然後放入 `pitchState`：

```js
evAllowedAvg: pitcherStats.evAllowedAvg
```

再在 EV 公式中加：

```js
const evAllowedAvg = Number(pitchState.evAllowedAvg);
const pitcherEvAdj = Number.isFinite(evAllowedAvg)
  ? clamp((evAllowedAvg - leagueEvAllowedAvg) * 0.20, -4, 4)
  : 0;

evKmh = evKmh + pitcherEvAdj;
```

注意方向：

- `evAllowedAvg` 越低，投手越能壓低 EV。
- `evAllowedAvg` 越高，投手越容易被強擊。

初始參數：

```js
leagueEvAllowedAvg = 150
coef = 0.20
clamp = [-4, +4]
```

驗證：

- H9 R2 是否提升。
- OPS R2 是否提升。
- HR9 是否爆炸或塌陷。
- 打者 SLG baseline 是否被破壞。

### 6.3 BABIP allowed 不要直接硬套

不建議直接用真實 H9 或 BABIP allowed 來扣安打率，因為那會把守備、球場、運氣、樣本都混進投手能力。

可以做的：

- 用 `evAllowed.avg` 壓 EV。
- 用 `zoneProfile` / location quality 調整 sweet spot 機率。
- 用 `pitchArsenal` 的 inPlayRate 微調進場率。

不建議做的：

```js
if (pitcher.good) hitChance -= 0.05;
```

這會讓引擎從物理模型退回黑箱機率。

### 6.4 H9 的目標不要一開始設太高

投手 H9 本來就比 K9/BB9 難校準，因為安打受守備和隨機性大。  
建議分階段目標：

| 階段 | H9 R2 | WHIP R2 | OPS R2 |
|---|---:|---:|---:|
| 修完 stuff | 0.05~0.15 | 0.15~0.25 | 0.05~0.15 |
| 加 evAllowed | 0.15~0.30 | 0.25~0.40 | 0.20~0.35 |
| 加 location/sweet spot | 0.25~0.40 | 0.35~0.50 | 0.30~0.50 |

不要要求第一波就 H9 R2 0.7。  
那很可能代表過度擬合。

---

## 7. P2 校準方向：配球、球種與真實感

### 7.1 目前球種選擇太粗

sim-tester 的 fallback：

```js
if (fastball) return 第一個速球
if (breaking) return movement 最高
if (waste) return control 最高
return pool[0]
```

這會讓真實球種使用率失真。  
data.js 已有 `pitchArsenal[].usageRate` 與 `pitchSequenceModel`。  
長期應該讓 sim-tester 與 game.js 都使用同一套 PitchSequencingEngine。

建議：

1. 每個球種保留 `usageRate`。
2. `selectGameLikePitch()` 用 usageRate 加權，而不是永遠 `pool[0]`。
3. 球數情境再用 `pitchSequenceModel` 調整。
4. `waste` 不應選 control 最高球種，而應選「最適合誘打/掉出好球帶」的球種。

### 7.2 球種 name 目前疑似 encoding 錯亂

data.js 中出現：

```js
"name": "å››ç¸«ç·š"
"name": "æ»‘çƒ"
```

這是 mojibake。  
如果 `pitch-engine.js` 用中文名稱判斷球種：

```js
PITCH_SPEED_RATIO[pitch.name]
PITCH_LATE_BREAK[pitch.name]
PITCH_DECEPTION_WINDOW[pitch.name]
classifyPitchTypeCode(pitchName)
```

那 mojibake 名稱會導致：

- 球速 ratio fallback 到 1.0。
- deceptionWindow fallback 到 0.30。
- pitchTypeCode 可能失敗。
- pitchTypeMatchup 混合失效。

這是另一個高風險點。  
下一位 AI 必須檢查：

- data.js 是否整檔 encoding 壞掉。
- sim-tester 是否因為瀏覽器 charset 仍顯示正確。
- `pitch.name` 在 runtime 進 PitchEngine 時到底是中文還是 mojibake。

如果 runtime 真的是 mojibake，請優先修 data.js 產出 encoding 或在讀取時 normalize。

建議建立映射：

```js
const PITCH_NAME_MOJIBAKE_FIX = {
  'å››ç¸«ç·š': '四縫線',
  'äºŒç¸«ç·š': '二縫線',
  'å¡ç‰¹çƒ': '卡特球',
  'æ»‘çƒ': '滑球',
  'æ›²çƒ': '曲球',
  'è®Šé€Ÿçƒ': '變速球',
  'æŒ‡å‰': '指叉',
  'ä¼¸å¡çƒ': '伸卡球'
};
```

但最好的解法是修輸出 encoding，不是在各處補丁。

### 7.3 球種速度的單位混用

目前 `pitchTypes[].speed` 可能是真實 km/h，例如 `150.5`。  
但 `buildPitcherStatsForPitch()` 這段：

```js
velocity += ((selectedPitch.speed || 75) - 75) / 18;
```

以前假設 `selectedPitch.speed` 是能力值 scale，現在卻可能是 km/h。

以徐若熙四縫線：

```text
(150.5 - 75) / 18 = +4.19
```

這不算巨大，但語義混亂。  
而 fallback 球種速度如果是 ability scale，例如 `velocity=90`，同一公式：

```text
(90 - 75) / 18 = +0.83
```

兩種資料單位不同，效果不同。

建議：

- `pitch.speedKmh` 存真實 km/h。
- `pitch.speedAbilityDelta` 存相對投手能力的修正。
- `PitchEngine._estimateSpeedKmh()` 應明確決定用哪一個。
- 不要在同一欄位混用 km/h 和 ability scale。

### 7.4 球種 control 的意義也要重定義

目前 `realControl` 用座標散布：

```js
100 - avgRangeCm * 0.5
```

但資料的 `coordRangeX/Y` 可能不是 cm，或至少不是 engine 的 cm。  
這導致很多球種 control 被壓到 `35`。

長期應該改成：

```text
球種 control = 總體 control + 該球種相對控球難度
```

例如：

| 球種 | control offset |
|---|---:|
| 四縫線 | +0 |
| 二縫線 | -1 |
| 卡特球 | -2 |
| 滑球 | -3 |
| 曲球 | -5 |
| 指叉 | -6 |
| 變速球 | -2 |

真實座標散布可以作為微調，但不要直接覆蓋。

---

## 8. 對目前投打對決體系的檢討

### 8.1 優點

這套系統已經比單純「打者分數 vs 投手分數」完整很多：

- 有逐球結果，不是直接產生打席結果。
- 有瞄準位置、投球偏差、球種移動、好壞球判定。
- 有打者判斷、追打、揮空、界外、進場分流。
- 有 EV / LA / spray。
- 有物理飛行與守備引擎。
- 有跑壘與得分模擬。
- 有 sim-tester 與 baseline JSON，已具備校準工具雛形。

架構方向是對的：  
它在模擬「過程」，而不是只反推「結果」。

### 8.2 最大問題：資料語義沒有被鎖住

目前最危險的不是公式本身，而是欄位語義混亂：

| 欄位 | 現況問題 |
|---|---|
| `pitchTypes[].speed` | 有時像 ability scale，有時是真實 km/h |
| `pitchTypes[].movement` | 有時是 ability scale，有時是由座標 range 推出 |
| `pitchTypes[].control` | 座標散布可能被當成 engine control |
| `pitchTypes[].stuff` | whiffRate 線性轉換後覆蓋總體 stuff |
| `moveXMin/Max` | raw coordinate 被當 cm |
| `pitch.name` | 可能有 encoding 問題 |

建議建立資料 schema 文件，明確定義：

```js
pitchTypes: [{
  name: string,              // canonical Chinese name
  code: 'FF'|'SI'|...,
  usageRate: number,         // 0~1
  speedKmh: number,          // 真實 km/h
  speedAbilityDelta: number, // ability 修正
  movementAbility: number,   // 0~100
  controlOffset: number,     // -10~+10
  stuffAbility: number,      // 0~100，但不可覆蓋 pitcher stuff
  slugRisk: number
}]
```

不要再讓同一欄位承擔兩種單位。

### 8.3 第二大問題：投手能力被拆出來，但不是每個能力都有 outcome 通道

目前比較有效：

- `control -> BB9`
- `velocity -> contact/K9`
- `stuff -> whiff/inPlay`，但被資料 bug 破壞

較弱或未完成：

- `breaking -> 實際球路難打`
- `discipline -> 邊角/配球穩定`
- `crisis -> 真實 leverage`，sim 量不到
- `vsLeft/vsRight -> 手性壓制`，暫緩
- `stamina -> 長局數衰退`，sim-tester 未模擬
- `evAllowed -> 被擊球品質`，尚未接入

建議每個投手能力都要有明確通道：

| 能力 | 應影響 |
|---|---|
| velocity | 反應時間、contact penalty、EV 抑制一小部分 |
| control | 壞球率、邊角率、失投率、walk |
| breaking | 軌跡後段偏移、deception、chase、弱擊球 |
| stuff | whiff、foul/in-play 分流、sweet spot 抑制 |
| stamina | velocity/control/breaking 隨投球數衰退 |
| discipline | 不只是控球，應影響投球計畫品質、edge/corner 命中率 |
| crisis | 壘上有人/高 leverage 的 control 與 weak contact |
| vsLeft/vsRight | 對不同打者手性調整 contact/chase |
| pickoff/quickDelivery | 盜壘、跑者起步、短打/戰術 |

### 8.4 第三大問題：sim-tester 與 game.js 邏輯不完全一致

sim-tester 目前有一些 game.js 沒同步的防呆：

- sim-tester 不傳 `moveXMin/Max`
- game.js 仍保留真實 move ranges

game.js 也有一些 sim-tester 量不到的東西：

- 體力衰退
- 特質
- crisis leverage
- 真實跑者局面
- 玩家操作策略
- 對手 AI sequencing

建議：

1. 把共用邏輯抽成真正共用 helper。
2. sim-tester 不應複製一份 game-like 邏輯後慢慢分岔。
3. 至少建立「校準模式」與「實戰模式」兩個明確 entry。

理想架構：

```text
duel-model/
  buildPitcherStats.js
  buildBatterStats.js
  selectPitch.js
  selectAimCell.js
  applyFatigue.js
  runPitch.js

game.js
  -> 呼叫 duel-model

sim-tester.html
  -> 呼叫 duel-model
```

### 8.5 第四大問題：用 R2 當唯一成功標準不夠

R2 能看排序，但不能看整體水位。  
例如：

- K9 R2 高，但 mean K9 高 2.0，仍不好玩。
- BB9 R2 高，但 mean BB9 只有 1.0，聯盟不真實。
- ERA R2 低，但如果 ERA 隨機性高，不能直接判死刑。

建議每次 baseline 同時看：

| 類型 | 指標 |
|---|---|
| 排序 | R2、Spearman rank |
| 水位 | mean real vs mean sim |
| 分散度 | SD real vs SD sim |
| 極端值 | top/bottom 10% 誤差 |
| 個案 | 代表球員表 |
| 遊戲性 | 三振、保送、安打、HR 是否好玩 |

目前 CSV 的典型問題是：

- K9 R2 低
- mean K9 也偏低
- sim K9 SD 比真實小

這代表不是只有水位錯，而是排序也失效。

### 8.6 第五大問題：打者校準與投手校準互相拉扯

同一個 `PitchEngine` 同時服務：

- 打者能力回歸
- 投手能力回歸
- 實戰遊戲手感

每次提高投手壓制，都可能：

- 打者 AVG 下降
- 打者 K% 上升
- HR 下降
- SLG R2 改變

所以要固定雙 baseline：

1. 投手 baseline 看 pitcher sorting。
2. 打者 baseline 看 hitter sorting 是否被破壞。

任何只看投手 baseline 的修正都不應直接合入。

---

## 9. 建議的未來校準路線圖

### Phase 0：建立可重複驗證

目標：讓每次改動可比較。

任務：

- 固定 PA，例如投手 `1000 PA`、打者 `1000 PA`。
- 若可行，加入 seed random，避免每次 R2 因雜訊亂跳。
- baseline JSON 命名包含日期與改動：

```text
tools/_baseline-pitcher-2026-06-01-stufffix-v1.json
tools/_baseline-batter-2026-06-01-stufffix-v1.json
```

建議新增：

```js
seededRandom(seed)
```

讓 sim-tester 可以輸入 seed：

```text
seed = 20260601
```

### Phase 1：修 stuff 資料鏈

目標：

- K9 R2 回到 0.35~0.55。
- Whiff R2 回到 0.50~0.75。
- BB9 不壞。

任務：

1. `stat-mapper.html` 改球種 stuff。
2. `sim-tester.html` 改總體/球種 stuff 混合。
3. `game.js` 同步。
4. 重新產 data.js 或至少手動 patch 現有球種 stuff。
5. 跑投手 + 打者 baseline。

### Phase 2：修 pitch name / speed / move schema

目標：消除單位混用。

任務：

1. 確認 encoding。
2. pitch name 改 canonical。
3. `speed` 拆成 `speedKmh` 與 ability delta。
4. `moveXMin/Max` 不再直接傳 engine。
5. `control` 改 offset，而不是 raw range 反推。

### Phase 3：H9 / OPS / ERA 通道

目標：

- H9 R2 至少 0.15~0.30。
- OPS R2 至少 0.20~0.35。
- WHIP R2 至少 0.25~0.40。

任務：

1. 接入 `evAllowed.avg`。
2. 加 weak contact / sweet spot suppression。
3. 檢查 FieldingEngine 是否過度吃落點隨機。
4. 檢查 sim BABIP 分布。

### Phase 4：配球與球數策略

目標：

- 讓真實投手特色更明顯。
- 高速球派、變化球派、控球派在結果上有不同形狀。

任務：

1. 使用 `usageRate` 選球。
2. 使用 `pitchSequenceModel`。
3. 球數影響球種與位置。
4. waste/putaway/getAhead/mustStrike 明確化。

### Phase 5：體力與角色

目標：

- SP / RP 在長期 sim 中有差異。
- 體力不是只在實戰 UI 顯示。

任務：

1. sim-tester 投手批次加 pitch count。
2. SP 模擬 5~7 局疲勞曲線。
3. RP 模擬 1~2 局高強度。
4. stamina 影響 velocity/control/breaking。
5. 不要把 RP 和 SP 直接放同一條校準線上比較 ERA。

### Phase 6：手性與特殊投法

目標：

- 下勾/側投、左投右投、左右打 split 更真實。

任務：

1. stat-mapper 補 `vsRight` 投手能力。
2. sim-tester 打者池依 bats 分布抽樣。
3. 加入 `vsLHB/vsRHB` 對 contact/chase 的修正。
4. 特殊投法不只加 stuff，也影響 deception / release angle。

---

## 10. 建議另一個 AI 的實作順序

### Step 1：不要碰 BB9

原因：

- 目前 BB9 是唯一 R2 過 0.5 的通道。
- 先動 BB9 會掩蓋真正的 K9/Whiff 問題。

### Step 2：修球種 stuff 標尺

修改：

```text
tools/stat-mapper.html
```

將 `whiffR * 100 * 1.5` 改為 piecewise。

### Step 3：修 stuff 混合

修改：

```text
tools/sim-tester.html
game.js
```

把：

```js
if (Number.isFinite(pitch?.stuff)) return pitch.stuff;
```

改為：

```js
return pitcherStuff * 0.65 + pitchStuff * 0.35;
```

### Step 4：重新產生或修補 data.js

如果有原始 CPBL JSON：

1. 用 stat-mapper 重跑。
2. 覆蓋 data.js。
3. 確認 pitchTypes stuff 不再大量 40。

如果暫時沒有原始 JSON：

- 可先在讀取時混合總體 stuff，降低現有 data.js 的傷害。
- 這樣不必立即重產 data.js，也能驗證根因。

### Step 5：跑 baseline

先跑：

```bash
node tools/run-pitcher-baseline.js --pa=1000 --minbf=100 --out=tools/_baseline-pitcher-after-stuff-fix.json
```

再跑：

```bash
node tools/run-batter-baseline.js --pa=1000 --minpa=100 --out=tools/_baseline-batter-after-stuff-fix.json
```

### Step 6：只根據結果做一個小調整

如果 K9 太低：

- 提高球種 stuff 權重到 45%。
- 或加強 `pitchStuffPenalty`。

如果 K9 太高：

- 降球種 stuff 權重到 25%。
- 或弱化 `pitchStuffPenalty`。

如果 BB9 壞掉：

- 檢查是否 accidentally 動到 `controlBallRateMul`。
- 不要用 whiff 修正補 BB9。

---

## 11. 校準時的紅線

### 不要把結果指標直接塞進結果

不要做：

```js
if (pitcher.eraPlus > 150) outChance += 0.05;
```

這會讓引擎失去可解釋性。

可以做：

```js
if (pitcher.evAllowedAvg < leagueAvg) evKmh -= ...
```

因為 EV allowed 是過程指標。

### 不要一次改多條通道

每次只動一個主機制：

- 一次只動 stuff。
- 或一次只動 EV allowed。
- 或一次只動 pitch selection。

不要同時改 stuff、EV、fielding、BABIP。  
否則 baseline 好了也不知道誰有效。

### 不要只看 R2

每次都看：

- R2
- mean
- SD
- top error
- 打者 baseline
- 代表球員

### 不要讓 sim-tester 和 game.js 分岔

sim-tester 是校準工具，不應該變成另一套遊戲。  
任何 game-like helper 若修了 sim-tester，也要同步檢查 game.js。

---

## 12. 快速檢查清單

下一個 AI 開工前，請先檢查：

- [ ] `data.js` 的 `pitchTypes[].name` runtime 是否是正確中文。
- [ ] `pitchTypes[].stuff` 是否大量等於 40。
- [ ] `game.js estimateStuffScore()` 是否 100% 使用 `pitch.stuff`。
- [ ] `tools/sim-tester.html estimateStuffScoreGameLike()` 是否 100% 使用 `pitch.stuff`。
- [ ] `game.js Player.generatePitchTypes()` 是否仍傳 `moveXMin/Max`。
- [ ] `tools/stat-mapper.html buildPitchTypesFromRealData()` 是否仍用 `whiffR * 100 * 1.5`。
- [ ] 修完後投手 baseline 的 K9 / Whiff 是否回升。
- [ ] 打者 baseline 是否沒有被壓垮。

---

## 13. 最小可行修正版本

如果只允許做最小修正，不重產 data.js，建議改兩處：

### sim-tester.html

```js
function estimateStuffScoreGameLike(pitcher, pitch) {
  const pitcherStuff = clampNumber(pitcher?.abilities?.stuff ?? 70, 0, 100);
  if (Number.isFinite(pitch?.stuff)) {
    const pitchStuff = clampNumber(pitch.stuff, 0, 100);
    return clampNumber(pitcherStuff * 0.70 + pitchStuff * 0.30, 0, 100);
  }
  const a = pitcher?.abilities || {};
  const velocity = clampNumber(pitch?.speed ?? a.velocity ?? 70, 0, 100);
  const movement = clampNumber(pitch?.movement ?? a.breaking ?? 70, 0, 100);
  const control = clampNumber(pitch?.control ?? a.control ?? 70, 0, 100);
  return clampNumber(
    pitcherStuff * 0.60 + velocity * 0.18 + movement * 0.16 + control * 0.06,
    0,
    100
  );
}
```

### game.js

```js
function estimateStuffScore(pitcher, pitch) {
  const pitcherStuff = clampNumber(pitcher?.abilities?.stuff ?? 70, 0, 100);
  let base;
  if (Number.isFinite(pitch?.stuff)) {
    const pitchStuff = clampNumber(pitch.stuff, 0, 100);
    base = clampNumber(pitcherStuff * 0.70 + pitchStuff * 0.30, 0, 100);
  } else {
    const velocity = clampNumber((pitch?.speed ?? pitcher.abilities?.velocity ?? 70), 0, 100);
    const movement = clampNumber((pitch?.movement ?? pitcher.abilities?.breaking ?? 70), 0, 100);
    const control = clampNumber((pitch?.control ?? pitcher.abilities?.control ?? 70), 0, 100);
    base = clampNumber(
      pitcherStuff * 0.60 + velocity * 0.18 + movement * 0.16 + control * 0.06,
      0,
      100
    );
  }

  const armSlot = pitcher?.armSlot || window.GAME_PARAMS?.specialPitcherArmSlot?.[pitcher?.name];
  const armBonus = armSlot ? (window.GAME_PARAMS?.armSlotBonus?.[armSlot]?.stuffScore || 0) : 0;
  return clampNumber(base + armBonus, 0, 100);
}
```

這個最小版能先救回總體 stuff，不必立刻重產 data.js。

---

## 14. 更完整修正版本

完整版本應包含：

1. stat-mapper 修球種 stuff 公式。
2. stat-mapper 輸出 canonical pitch code/name。
3. stat-mapper 不再輸出 raw moveXMin/Max，或明確標記單位。
4. game.js 與 sim-tester 混合總體 stuff + 球種 stuff。
5. game.js 與 sim-tester 對 pitchTypes 使用同一個 normalize helper。
6. pitch-engine 接收 `pitch.code`，不要靠中文名稱猜球種。
7. baseline runner 加 seed。
8. 報告同時輸出 R2/mean/SD/top errors。

---

## 15. 最終建議

請下一個 AI 不要從「整體投手很爛」開始大改。  
目前其實已經有幾條通道是經過多輪校準的：

- `control -> BB9` 可以保留。
- `velocity -> K9` 方向可以保留。
- `stuff -> whiff` 機制可以保留，但資料輸入壞掉。
- `Wave F` 的被擊球品質通道可以保留，但要等 stuff 修好後再評估。

真正的第一刀是：

```text
修正球種 stuff 標尺 + 不讓球種 stuff 完全覆蓋總體 stuff
```

這是低風險、高收益、可驗證的修正。  
做完這步再談 H9/ERA/OPS，不然所有後續校準都會建立在錯誤的投手壓制力上。

