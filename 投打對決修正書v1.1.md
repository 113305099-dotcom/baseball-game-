# 投打對決修正書 v1.1

版本：v1.1  
日期：2026-05-21  
目的：把投打對決從「混在守備判定中的簡化球路估算」推進到「可獨立測試、可校準、可被守備引擎消費的物理核心」。

## 1. 目前診斷

本次依據 `tools/sim-tester.html` 匯出的 `sim-tester-batch-2026-05-21 (2).csv`、現有 `pitch-engine.js`、`fielding-engine.js` 與規則書檢查後，判斷失真不是單一資料欄位錯誤，而是三層問題疊加。

### 1.1 批次測試觀察值

99 位打者、每人 300 PA、合計 29,700 PA 的測試結果：

| 指標 | 真實平均 | 模擬平均 | 問題 |
|---|---:|---:|---|
| AVG | .255 | .355 | 模擬高約 .100 |
| K% | 17.6% | 36.9% | 模擬約 2.1 倍 |
| BB% | 7.3% | 1.0% | 保送幾乎消失 |
| SLG | .352 | .443 | 長打率偏高 |

### 1.2 主要失真來源

1. `PitchEngine` 的出棒決策目前過度鼓勵追打，導致 `BB%` 被壓到不合理低點。
2. `FieldingEngine` 的空中球處理太常把可接飛球推進安打分支，導致 `AVG/BABIP` 明顯偏高。
3. `popup` 與 `liner` 的物理/守備分類不穩定，特別是深遠 popup 安打率過高、liner 安打一律偏向長打。
4. `sim-tester.html` 使用同一位中位數投手、固定空壘、固定中立守備，適合抓引擎偏差，但不能直接代表完整賽季。

## 2. v1.1 已新增模組

新增檔案：

- `baseball-physics-engine.js`

此模組是純函式物理核心，不直接決定安打、出局、失誤或跑壘。它只負責把擊球輸入轉成可供守備引擎使用的球路資料。

### 2.1 載入順序

瀏覽器主遊戲載入順序已調整為：

1. `pitch-engine.js`
2. `baseball-physics-engine.js`
3. `fielding-engine.js`
4. `baserunning-engine.js`

`tools/sim-tester.html` 也使用相同順序，避免測試器與主遊戲吃到不同物理模型。

### 2.2 FieldingEngine 接線方式

`fielding-engine.js` 目前保留舊函式作為 fallback，但若 `window.BaseballPhysicsEngine` 存在，會優先呼叫新模組：

- `calcBattedBall()`
- `surfacePhysicsFor()`
- `wallDistance()`
- `classifyBallType()`
- `pointAtDistance()`
- `groundStateAtDistance()`

這樣做的好處是可以先替換物理層，不一次大改守備結果判定。

## 3. 物理核心 API

### 3.1 主要入口

```js
BaseballPhysicsEngine.calcBattedBall({
  evKmh,
  launchAngleDeg,
  sprayAngleDeg,
  stadium,
  allowPhysicalFoul,
  surfaceRegistry
});
```

### 3.2 輸入定義

| 欄位 | 說明 |
|---|---|
| `evKmh` | 擊球初速，km/h |
| `launchAngleDeg` | 仰角，度 |
| `sprayAngleDeg` | 擊球方向，度；負值偏左外野，正值偏右外野 |
| `stadium` | 球場牆距、牆高、海拔、風、HR factor、場地材質 |
| `allowPhysicalFoul` | 是否允許物理界外，不允許時 spray 會限制在場內角度 |
| `surfaceRegistry` | 場地材質物理表，例如天然草、人工草、紅土混合 |

### 3.3 輸出重點

| 欄位 | 說明 |
|---|---|
| `physicsModelVersion` | 目前為 `1.1` |
| `ballType` | `ground / liner / fly / popup` |
| `preciseDistM` | 最終距離，公尺 |
| `hangTimeSec` | 滯空或到達處理點時間 |
| `landingPoint` | 落點座標 |
| `firstBouncePoint` | 滾地球第一彈跳點 |
| `groundProfile` | 滾地球彈跳、摩擦、停止距離、停止時間 |
| `flightProfile` | 空中球水平/垂直速度、阻力、風、球場 carry factor、apex |
| `trajectorySamples` | 供動畫、debug、測試使用的連續軌跡樣本 |
| `isHR` | 物理上是否越牆且高於牆 |
| `hitNet` | 是否打到牆/網而未越過 |

## 4. 物理模型內容

### 4.1 滾地球

滾地球現在由以下因素決定：

- EV 轉換成水平速度與垂直速度。
- 依仰角計算第一彈跳時間與第一彈跳距離。
- 依場地 `restitution` 計算彈跳後滾動初速。
- 先用內野 dirt friction 減速，再用外野 grass friction 減速。
- `trajectorySamples` 標記 `air / dirt / grass / stopped` 階段。

### 4.2 空中球

空中球現在由以下因素決定：

- EV、仰角與方向拆成水平速度、垂直速度。
- 以重力、空氣阻力、風、海拔、球場 HR factor 計算 carry。
- 以牆距與牆高判定 `isHR` 或 `hitNet`。
- 輸出 apex、hang time、trajectory samples，供守備追球與動畫使用。

### 4.3 邊界

物理模組不處理：

- 守備員是否追到球。
- 是否失誤。
- 跑者是否推進。
- 安打是一壘安打、二壘安打或三壘安打。
- XP、旁白、UI。

這些仍由 `FieldingEngine`、`BaserunningEngine`、`InPlayResultApplier` 負責。

## 5. 校準目標

v1.1 先完成物理層抽離，不直接把所有機率調到完美。後續校準建議依序處理：

| 階段 | 目標 |
|---|---|
| v1.2 | 修正出棒模型，讓 `BB%` 回到約 6-9%，`K%` 回到約 15-22% |
| v1.3 | 修正空中球與 popup 守備，使整體 `BABIP/AVG` 回到合理區間 |
| v1.4 | 修正 liner 安打長打分布，避免平飛球安打過度變成二壘安打 |
| v1.5 | sim-tester 新增投手加權抽樣、左右投分布、壘上狀態與球場分布 |

## 6. 測試策略

已在 `tools/smoke-test-engines.js` 加入物理核心 smoke test：

1. 確認 `BaseballPhysicsEngine` 存在。
2. 確認 `calcBattedBall()` 輸出 `physicsModelVersion = 1.1`。
3. 確認飛球有 `trajectorySamples`。
4. 確認滾地球有 `groundProfile` 與滾動階段。
5. 確認 `FieldingEngine.calcBattedBall()` 會吃到新物理模型輸出。

## 7. 下一步修正建議

下一個最有效的修正不是再改資料，而是改 `PitchEngine` 的出棒模型：

1. 用 `advancedStats.plateDiscipline[zone].swingRate` 作為出棒率基準。
2. 用 `swingByCount[count].swingRate` 修正球數情境。
3. 將 `chaseRate` 目標鎖在真實打者資料附近，而不是由 `aggressive/protect` 策略硬推。
4. 保留玩家策略影響，但把策略影響縮小為「偏移」而不是「主決策」。

等 `BB%` 和 `K%` 回到合理區間後，再校準 `FieldingEngine` 的飛球、popup、liner BABIP。

## 8. 2026-05-21 校準補記

這一輪不是資料本身失真，而是「測試器已經開始接近主遊戲流程後」，暴露出投球 AI、打者判斷與空中球守備三個引擎偏差。

### 8.1 第二輪批次問題

使用 `sim-tester-batch-2026-05-21 (3).csv` 重跑 99 位打者、每人 300 PA 後，原始偏差如下：

| 指標 | 真實平均 | 模擬平均 | 判讀 |
|---|---:|---:|---|
| AVG | .255 | .288 | 已比前一輪下降，但仍偏高 |
| K% | 17.6% | 48.5% | 三振率嚴重過高 |
| BB% | 7.3% | 約 0.5% | 保送幾乎不存在 |
| BABIP | - | .560 | 守備空中球與場內球處理嚴重失真 |

診斷結果：

1. `sim-tester.html` 原本已更接近主遊戲流程，因此問題不再只是測試器設計。
2. `PitchEngine` 的打者判斷用原始瞄準點判斷好壞球，沒有充分使用投球變化後的最終位置。
3. AI 投球計畫太常把球塞進好球帶，造成打者很少走到保送情境。
4. 兩好球後揮空率過高，界外球與進場比例不足，導致 K% 被放大。
5. `FieldingEngine` 對 fly、popup、liner 的候選守備員、滯空時間與接殺條件過嚴，造成 BABIP 明顯膨脹。

### 8.2 已實作修正

本輪已完成以下修正：

| 模組 | 修正 |
|---|---|
| `pitch-engine.js` | 打者判斷改以 `finalPosition` 為主，讓實際進壘點影響是否出棒 |
| `pitch-engine.js` | 降低基礎揮空率，兩好球時提高保護性接觸與界外機率 |
| `game.js` | 中立 AI 投球計畫加入 `waste`，並降低純好球帶塞球傾向 |
| `tools/sim-tester.html` | 同步主遊戲的中立 AI 投球計畫與瞄準權重 |
| `fielding-engine.js` | 修正 popup 外野候選、fly 接殺條件、liner 到達時間與外野站位 |
| `defense-state-builder.js` | 同步外野基礎站位，避免主遊戲與守備引擎位置不一致 |
| `baseball-physics-engine.js` | 空中球滯空時間回到較自然的物理尺度，讓守備追球有合理時間 |

### 8.3 最新驗證數字

修正後以相同條件重跑 99 位打者、每人 300 PA：

| 指標 | 真實平均 | 模擬平均 | 狀態 |
|---|---:|---:|---|
| AVG | .255 | .253 | 已貼近 |
| K% | 17.6% | 24.3% | 仍偏高，但已大幅改善 |
| BB% | 7.3% | 11.4% | 仍偏高，需要下一輪微調 |
| BABIP | - | .348 | 回到可校準範圍 |
| AVG R² | - | .199 | 球員間排序仍需改善 |
| K% R² | - | .559 | 三振能力排序已有可用訊號 |
| BB% R² | - | .078 | 保送能力排序仍不足 |

診斷指標：

| 指標 | 數值 | 判讀 |
|---|---:|---|
| Strike Rate | 59.2% | 合理偏低一點 |
| Swing Rate | 43.6% | 接近合理範圍 |
| Zone Swing Rate | 61.9% | 稍保守 |
| Chase Rate | 17.0% | 偏低，解釋 BB% 偏高 |
| Whiff / Swing | 12.4% | 已從過高修回 |
| Foul / Swing | 50.8% | 仍偏高，是 K% 偏高的主因之一 |
| In Play / Swing | 36.8% | 可再提高一點 |
| In-play Hit Rate | 34.9% | 合理 |

### 8.4 下一輪校準方向

接下來不要先改真實球員資料，應優先微調引擎：

1. 將兩好球後 `foulProb` 再略降，並把部分比例轉成 `inPlayProb`，目標是把 `K%` 從 24% 拉回 18-21%。
2. 略提高壞球邊緣的追打率，或降低 `waste` 投球計畫權重，目標是把 `BB%` 從 11% 拉回 7-9%。
3. 改善 `BB% R²`：讓 `eye`、真實 `BB%`、zone discipline 對選球更有差異化，不要所有打者都被同一套中立策略壓成近似結果。
4. 暫時不要大改守備 BABIP，因為目前 `.348` 已進入可接受校準帶；若再改太多，AVG 會再次失真。

## 9. 2026-05-24 校準工作（v1.2 ~ v1.5）

這一輪以 `tools/sim-tester.html` 為主要驗證工具，跑 99 位打者 × 每人 300 PA（vs 中位數投手），共五輪迭代。每一輪的修正都立即驗證、依結果調整下一步。

### 9.1 起點數據（修正前）

從 `sim-tester-batch-2026-05-24.csv` 開始：

| 指標 | 真實 | 起點 | 偏差 |
|---|---:|---:|---|
| AVG | .255 | .251 | OK（巧合） |
| K% | 17.6 | 24.4 | 偏高 |
| BB% | 7.3 | 11.5 | 偏高 |
| Chase Rate | ~28 | 17.2 | 偏低 |
| BB% R² | - | 0.14 | 排序訊號不足 |

### 9.2 引擎修正記錄

#### v1.2（CSV (1)）：`chaseProb` 公式重寫 + standard 策略允許主動追打

- 發現 `pitch-engine.js:361` 的舊公式 `0.22 - eye/180 + move/220` 在 eye=80 算出 -0.18，被 floor 0.03 鉗死 → 99 個打者 chaseProb 全相同 → BB% R²=0.14。
- 同時發現 `pitch-engine.js:386` 的 standard 策略對「感知為壞球」直接 `swings=false`，永不主動追打，所有 chase 都來自誤判。
- 確認真實 discipline 分布平均 80.4（P10=72, P50=82, P90=92），不是記憶中的 60。

修正：
```js
chaseProb = clamp(
  0.18 - (effectiveEye - 82) * 0.0085 + movementMag / 280 + strategy.chaseBonus,
  0.04, 0.42
);
// 並讓所有策略對「感知壞球」都套用 chaseProb，差異由 strategy.chaseBonus 體現
```

結果：BB% 從 11.5 → 5.1（過頭），Chase Rate 從 17 → 35（過頭）。但 BB% R² 升到 0.31。

#### v1.3（CSV (2)）：chaseProb 微調 + zone swing 機率化 + whiff 重校

公式微調：baseline 0.18 → 0.13，斜率 0.0085 → 0.011（拉開頂級眼與差眼）。
Zone swing 機率化（`pitch-engine.js:378`）：standard 策略對感知好球不再 hard true，改為依 `targetDist / effectiveSwingRadius` 機率出棒：
```js
zoneSwingProb = clamp(
  0.88 - distRatio * 0.40 + strategy.chaseBonus * 0.3
  + (strikes === 2 ? 0.12 : 0) - (effectiveEye - 82) / 250,
  0.30, 0.95
);
```
Whiff 重校：baseline 0.23 → 0.32、stuff 影響 /300 → /180、兩好球 -0.16 → -0.08。

結果：K% 從 18.6 → **28.4（炸開）**，發現 P4 揮空率拉太高、Chase 仍卡 35%（誤判才是主要 chase 來源，P2 沒解到 chase）。

#### v1.4（CSV (4) → (5)）：whiff 退一半 + misread chase 分支修正

退一半 whiff：baseline 0.32 → 0.27、stuff /180 → /250、兩好球 -0.08 → -0.13。
修正 misread chase（`pitch-engine.js:394` 的 else 分支）：
```js
// 舊：0.58 - eye/260（eye=80 算出 0.27，太高）
// 新：0.42 - eye/300（eye=80 算出 0.15）
swings = Math.random() < clamp(0.42 + strategy.chaseBonus * 0.5 - effectiveEye / 300, 0.05, 0.55);
```

結果：K% 從 28.4 → 24.2，Chase 從 35 → **29.6（達標）**，BB% 從 5.0 → **6.8（接近真實 7.3）**，AVG 從 .245 → **.256（精準）**。

#### v1.5（CSV 待測）：pCorrectRead 提升 + 2K 保護機率提高

從 (5) 的指標推算，每球有 21.5% 是「站著被叫好球」，是 K% 多出來那 6.6% 的主因。原因是 pCorrectRead 偏低（eye=80 算出 0.73），27% 機率打者把好球誤判為壞球。

修正（`pitch-engine.js:339`）：
```js
// 舊：0.62 + (eye-50)/140，上限 0.92
// 新：0.66 + (eye-50)/130，上限 0.94
```
並把兩好球 standard 保護揮棒從 0.55 提升到 0.70。

### 9.3 五輪後的指標總覽

| 指標 | 真實 | (0) 起點 | (5) 校準後 | 達標? |
|---|---:|---:|---:|---|
| AVG（平均） | .255 | .251 | **.256** | ✅ |
| BB%（平均） | 7.3 | 11.5 | **6.8** | ✅（含 IBB 差距合理） |
| Chase Rate | ~28 | 17.2 | **29.6** | ✅ |
| Swing Rate | ~46 | 43.4 | **49.6** | ✅ |
| BB% R² | - | 0.14 | **0.37** | 大幅改善 |
| K%（平均） | 17.6 | 24.4 | **24.2** | ⚠️ 仍偏高（v1.5 預期降至 18-19）|
| BABIP（平均） | ~.295 | .347 | .345 | ⚠️ 守備層問題 |

### 9.4 關鍵發現：個別球員偏差揭露守備引擎反轉 bug

平均達標掩蓋了個別球員的嚴重失真。`(5).csv` 殘差分析：

- AVG RMSE = 0.041、MAE = 0.033
- 24/99 球員 AVG 偏差 ≥ 0.05
- AVG R² 僅 0.18、SLG R² 僅 0.17

**系統性偏誤模式：強打者被低估、弱打者被高估，且方向集中**

| 球員 | abilities | real AVG | sim AVG | sim BABIP |
|---|---|---:|---:|---:|
| 張育成 | contact=73, power=82 | .299 | .204 | **.225** |
| 林安可 | contact=71, power=82 | .291 | .189 | **.219** |
| 林立 | S 級 | .348 | .252 | .308 |
| 林泓弦 | contact=45, power=46 | .207 | .301 | **.383** |
| 許哲晏 | contact=48, power=59 | .230 | .306 | **.400** |
| 蔡佳諺 | contact=47, power=62 | .212 | .283 | **.400** |

平均 BABIP 0.345 看起來「過高但可接受」，實際是「強打者 .22 + 弱打者 .40」互相抵消的結果。**個別球員的 BABIP 跟現實完全反轉**。

### 9.5 守備引擎反轉 bug 的幾何根因

追完 `fielding-engine.js`，確認問題出在守備員預設站位產生的「無人區」：

```
P:(0,18)    1B:(30,30)   2B:(18,48)
C:(0,-2)    3B:(-30,30)  SS:(-18,48)
LF:(-50,80) CF:(0,94)    RF:(50,80)
```

- 內野手處理範圍 ≤ y=52（`infieldLimitM=52`）
- 外野手預設站位 y=80（LF/RF）
- **52 ~ 80m 是「無人區」**

#### 弱打者的「短飛球」吃這個空間
林泓弦 EV 116, LA 18° → 落點 ≈ 60m
- 內野追不上、外野要前衝 20m、hangTime 短
- `rangeMargin = 6 × (1.3 − 0.4) − 20 = −14.6` → `reachChance ≈ 0.02`
- → **98% 是 bloop hit → BABIP 飆高**

#### 強打者的硬飛球被深守備員等到
張育成 EV 148, LA 28° → 落點 ≈ 95m
- 外野手在 y=80m，後退 15m，hangTime ≈ 5s
- `rangeMargin = 6 × (5 − 0.4) − 15 = +12.6` → `reachChance` 飽和 0.98
- 還加上 `routineAirBonus +0.22`（`fielding-engine.js:436`）
- → **強打者硬飛球被接殺 → BABIP 低**

#### 影響範圍：fly ball 球型獨大
| 球路型態 | 真實 CPBL | 引擎現況 |
|---|---|---|
| 強打硬飛球（EV 145+, LA 25-30）| 過牆 HR 或落點接殺各半 | 過度接殺 |
| 弱打短飛球（EV 110-, LA 18-25）| 多半 IF popup 或 OF 衝接 | 變 bloop 安打 |
| 強打硬平飛（liner）| 守備穿越 → 二壘安打 | OK |
| 弱打地滾球 | 內野處理 → 出局 | OK |

**這個 bug 在實際遊玩時也會發生**：強打者表現像 .200 打者、替補弱打者表現像 .300 打者。跟 sim-tester 使用 mock 78/78/78 守備員的關係不大（即使用真實守備員也只能緩解，無法解決幾何根因）。

### 9.6 下一步：守備引擎修復方向

從「P5 暫緩」升級為 **P0 最高優先**。可行的修法：

#### 選項 A：縮小無人區（最簡單）
LF/RF 從 y=80 改為 y=72，CF 從 94 改 86。
- ✅ 一行 code 改完
- ⚠️ 會略增 HR 率，需要重校

#### 選項 B：依 hangTime 給外野手反應懲罰（物理正確）
hang time < 3s 的 fly ball 給外野手 reach 懲罰（球太快趕不上）。
hang time > 4s 的給更大獎勵（routine play）。
重新調 `routineAirBonus`（line 436）讓它依 hangTime 動態變動。
- ✅ 物理直覺正確
- ⚠️ 需要重校 routineAirBonus

#### 選項 C：IF/OF 互補機制（最真實）
讓 SS/2B 對 50-65m 的 popup 也能 attempt（往後追），讓 OF 對 90-100m 的硬飛球更頻繁地放棄。
- ✅ 最接近真實守備
- ⚠️ 改動大、需要新邏輯，留到 v1.5+

**建議組合：A + B 一起做**，A 是立即止血、B 修根本問題。修完後重跑 sim-tester，預期：
- BABIP 整體從 .345 降到 ~.30
- 強打者 BABIP 升到 .30-.35
- 弱打者 BABIP 降到 .25-.28
- AVG R² 從 0.18 跳到 0.4+
- SLG R² 從 0.17 跳到 0.4+

### 9.7 v1.2 校準工程的教訓

- **平均達標不等於對**：強弱誤差會互相抵消。永遠看殘差分布跟 R²，不要只看平均。
- **改一個參數要先預測它如何影響其他指標**：v1.3 改 whiff 一次改三個地方（baseline、stuff、2K），結果 K% 炸 +10。應該一次改一個參數。
- **誤判（misread）才是 chase 的主要來源**：在 pCorrectRead ~0.7 的條件下，調 chaseProb 的效果有限，要先處理 misread 路徑。
- **物理引擎跟守備引擎要當作一個系統看**：EV 變強會讓球落點變遠、hangTime 變長，這對守備是有利還是不利取決於守備員位置。「pitch-engine 對了」不代表「整體 AVG 對了」。

## 10. 2026-05-24 守備引擎反轉 BABIP 修復（v2.0）

依 §9.5 / §9.6 的診斷，本輪一次完成「無人區縮小」+「hangTime 動態調整」+「佈陣同步」三件事。

### 10.1 修改範圍

**檔案 1：`fielding-engine.js`**

POSITIONS（line 4-14）外野手前移：

```diff
-  LF: { x: -50, y: 80 },
-  CF: { x: 0,   y: 94 },
-  RF: { x: 50,  y: 80 }
+  LF: { x: -50, y: 72 },
+  CF: { x: 0,   y: 86 },
+  RF: { x: 50,  y: 72 }
```

`evaluateCandidate` 內的 fly ball 加成（line 434-439）改寫：

```diff
   if (ballInfo.ballType === "fly") {
     const wallRatio = ballInfo.preciseWallDistM > 0 ? ballInfo.preciseDistM / ballInfo.preciseWallDistM : 0.8;
-    const routineAirBonus = wallRatio < 0.78 ? 0.34 : wallRatio < 0.9 ? 0.22 : 0.08;
-    const hangBonus = clamp((ballInfo.hangTimeSec - 3) * 0.12, 0, 0.18);
-    reachChance = clamp(reachChance + routineAirBonus + hangBonus, 0.04, 0.98);
+    const routineAirBonus = wallRatio < 0.78 ? 0.22 : wallRatio < 0.9 ? 0.14 : 0.04;
+    let hangAdjust;
+    if (ballInfo.hangTimeSec < 3) {
+      hangAdjust = -(3 - ballInfo.hangTimeSec) * 0.15;
+    } else if (ballInfo.hangTimeSec > 4) {
+      hangAdjust = clamp((ballInfo.hangTimeSec - 4) * 0.14, 0, 0.20);
+    } else {
+      hangAdjust = 0;
+    }
+    reachChance = clamp(reachChance + routineAirBonus + hangAdjust, 0.04, 0.98);
   }
```

**檔案 2：`defense-state-builder.js`**（必須同步，否則主遊戲走 `buildDefenseState()` 仍會吃舊座標）

BASE_POSITIONS（line 15-17）同步：

```diff
-  LF:   { x: -50,  y: 80  },
-  CF:   { x: 0,    y: 94  },
-  RF:   { x: 50,   y: 80  }
+  LF:   { x: -50,  y: 72  },
+  CF:   { x: 0,    y: 86  },
+  RF:   { x: 50,   y: 72  }
```

`DEFENSIVE_ALIGNMENTS.outfield_in`（line 71-75）的偏移縮小，避免外野手前移到內野範圍：

```diff
   outfield_in: {
     offsets: {
-      "LF": { dx: 0, dy: -18 },
-      "CF": { dx: 0, dy: -18 },
-      "RF": { dx: 0, dy: -18 }
+      "LF": { dx: 0, dy: -10 },
+      "CF": { dx: 0, dy: -10 },
+      "RF": { dx: 0, dy: -10 }
     }
   }
```

### 10.2 為什麼這樣選

- **外野手前移 8m**：把 52-80m 的無人區縮成 52-72m，弱打者短飛球（落點 ~60m）進入外野手 reach 範圍；CF 也同步前移 8m 維持外野手間距均勻。
- **`routineAirBonus` 縮減**：原本對所有 fly 都給 0.08-0.34 的正獎勵，沒有區分硬飛球與一般高飛球。縮減後仍然保留「往場內飛的球容易接」這個直覺，但不會壓死所有硬飛球。
- **`hangAdjust` 取代 `hangBonus`**：以 hangTime 3-4 秒為中性區。< 3s 給線性懲罰（球太快、守備員反應跟追球時間不夠）；> 4s 給線性獎勵（routine pop fly）。物理上正確。
- **`outfield_in` 偏移 -18 → -10**：外野手前移後 y=72，原本 -18 偏移會落到 y=54，與內野限制 y=52 重疊；改為 -10 後落在 y=62，仍在外野範圍。

### 10.3 視覺層差距（已知議題，本輪不修）

`battle-scene.js:945-947` 仍寫死 LF y=86 / RF y=86 / CF（未列出）。修完引擎後：

- LF/RF：引擎 y=72 vs 視覺 y=86 → **14m 視覺差距**（修前是 6m）
- CF：引擎 y=86 vs 視覺（待確認）→ 一致或略差

由於 v3.22 之後動畫只讀 `visualTimeline.events`，守備員的實際跑動點位來自引擎 `playPoint`，不會直接吃 battle-scene 的 POSITIONS 常數。`battle-scene.js:945` 的座標只影響「靜止站位視覺」。可以留到美術 PNG sprite 接入時一起修。

### 10.4 待驗證

執行步驟：
1. 強制刷新 `tools/sim-tester.html`（Ctrl+F5），上傳 data.js。
2. A 批次回歸測試：99 打者 × 300 PA vs 中位投手。
3. 匯出 CSV，比對下表。

| 指標 | (5) 修前 | 預期修後 | 接受範圍 |
|---|---:|---:|---|
| AVG（平均） | .256 | .250 - .270 | ±0.015 |
| K%（平均） | 24.2 | 不變 | ±0.5 |
| BB%（平均） | 6.8 | 不變 | ±0.5 |
| Chase Rate | 29.6 | 不變 | ±1.0 |
| BABIP（平均） | .345 | **.30 - .32** | 必須下降 |
| AVG R² | 0.18 | **≥ 0.35** | 必須改善 |
| SLG R² | 0.17 | **≥ 0.35** | 必須改善 |
| 張育成 BABIP | .225 | **≥ .30** | 強打者要回升 |
| 林泓弦 BABIP | .383 | **≤ .30** | 弱打者要下降 |
| 林安可 BABIP | .219 | **≥ .30** | 強打者要回升 |
| 許哲晏 BABIP | .400 | **≤ .30** | 弱打者要下降 |

不達標時的判讀：
- 個別球員仍偏差大 → 先回報哪幾位，由人工判斷下一步，不要堆參數補丁。
- HR 率明顯上升（> +0.5%）→ 外野手前移太多，下次把 y=72 改回 75。
- 弱打者 BABIP 仍 > .30 → routineAirBonus 還要再縮減一點。
- 強打者 BABIP 仍 < .30 → hangAdjust 的 < 3s 懲罰係數加大（0.15 → 0.18）。

### 10.5 未來工作（保留）

§9.6 提到的選項 C（IF/OF 互補機制：SS/2B 對 50-65m popup 也可 attempt、OF 對 90-100m 硬飛球更頻繁放棄）暫不實作。如本輪 A+B 無法達標再啟用。

### 10.6 v2.0 實測驗證（`sim-tester-batch-2026-05-24 (6).csv`）

99 打者 × 1000 PA = 99,000 PA 完整驗證結果：

| 指標 | 真實 | (5) 修前 | (6) v2.0 修後 | §10.4 目標 | 狀態 |
|---|---:|---:|---:|---:|---|
| AVG 平均 | .255 | .256 | **.246** | .250-.270 | ⚠️ 略低於下緣 |
| BABIP 平均 | - | .345 | **.321** | .30-.32 | ✅ |
| AVG R² | - | 0.18 | **0.368** | ≥ 0.35 | ✅ |
| SLG R² | - | 0.17 | **0.238** | ≥ 0.35 | ❌ 仍不足 |
| OBP R² | - | - | 0.209 | - | - |
| K% R² | - | - | 0.608 | - | - |
| BB% R² | - | 0.37 | 0.395 | - | - |
| K% 平均 | 17.6 | 24.2 | 22.2 | - | 略偏高 |

**個別指標球員 BABIP**：

| 球員 | 修前 | 修後 | §10.4 目標 |
|---|---:|---:|---|
| 張育成 (強打) | .225 | .238 | ≥ .30 ❌ |
| 林安可 (強打) | .219 | .251 | ≥ .30 ❌ |
| 林泓弦 (弱打) | .383 | .331 | ≤ .30 ⚠️ |
| 許哲晏 (弱打) | .400 | .334 | ≤ .30 ⚠️ |
| 蔡佳諺 (弱打) | .400 | .321 | ≤ .30 ⚠️ |

**結論：v2.0 守備修復目標部分達成，宣告 v2.0 完成。**

達成：BABIP 平均、AVG R²、反轉模式緩解（弱打者 BABIP 大幅下降）。
未達成：SLG R²、強打者 BABIP 仍偏低（殘留 0.05-0.07 落差）、四位指標球員仍未全部歸位。

### 10.7 殘留問題的根因（移到 §11 v2.1 處理）

殘留的 SLG R² 與強打者 BABIP 偏低，**根因不在守備引擎**，而是 EV 生成公式造成 **「HR 斷崖」**：

| 球員 | realHR | simHRrate | 落差 |
|---|---:|---:|---|
| 林安可 | 32 | 2.6% | ~合理 |
| 張育成 | 29 | 2.0% | 偏低 |
| **朱育賢** | 21 | **0.3%** | 嚴重偏低 |
| **許基宏** | 21 | **0.5%** | 嚴重偏低 |
| **林立** | 12 | **0.0%** | 完全 0 |
| **林泓育** | 12 | **0.0%** | 完全 0 |
| **胡金龍** | 6 | **0.0%** | 完全 0 |

95 / 99 位打者 simHRrate = 0.0%，只有頂級 4 位（EV 突破 150 km/h）能 HR。中等力量打者（power 65-75，real HR 6-21）的飛球無法越牆，全變 fly out。連鎖影響：
- SLG R² 失準（中等打者 SLG 全面壓低）
- 強打者部分 HR 變 fly out（拉低 BABIP）
- AVG 略低（HR 算命中，少了 10-20 支/1000 PA）

處理方向：v2.1 校準 EV 公式（見 §11）。

## 11. 2026-05-24 EV 公式校準（v2.1）

### 11.1 修改內容

**檔案**：`pitch-engine.js:633-640`（InPlayOutputModule.compute → EV 公式）

```diff
       // EV（§17.11）
       const evKmh = MathUtils.clamp(
         92
-        + effectivePower * 0.62
+        + effectivePower * 0.68
         + (finalContactScore - 50) * 0.28
         + MathUtils.gaussianRandom(0, 6),
         40, 190
       );
```

單一參數變動：`effectivePower` 係數 0.62 → 0.68。

### 11.2 為什麼是 0.68 而不是 0.78

物理計算的 HR 閾值（中性場 100m 牆 + LA 30° + dragCarryScale 0.65）約是 EV ≈ 150 km/h。要讓中等力量打者（power 70）的飛球**偶爾**達到 HR 機率，需要把他的平均 EV 從 142 提到 ~146-148，讓上 σ tail 跨過 150。

| Power 值 | 0.62 (原) | 0.68 (新) | 0.78 (初提議) | HR 機率估算（flies） |
|---:|---:|---:|---:|---|
| 60 (avg) | 136 | 140 | 145 | 0.68 → ~4% (合理) ；0.78 → ~14% (過高) |
| 70 (mid) | 142 | 146 | 154 | 0.68 → ~25% (合理) ；0.78 → ~75% (過高) |
| 75 (strong) | 145 | 149 | 158 | 0.68 → ~40% (合理) ；0.78 → ~85% (太誇張) |
| 82 (top) | 150 | 154 | 163 | 0.68 → ~70% (合理) ；0.78 → EV 上限 (失真) |

選 0.68 而非 0.78 的理由：
1. 0.78 讓 power 82 的平均 EV 達 163 km/h，超過 CPBL 球員實測上限（160-170 km/h 是頂級峰值，不是平均）。
2. 0.78 讓 power 70 的飛球 75% 變 HR，會把中等打者 SLG 推到不合理的高位。
3. 0.68 給予 power 60-75 區間「合理但有限」的 HR 機率，符合 CPBL 中等打者 5-20 HR/季的實際分布。

### 11.3 預期效果

| 球員 | 修前 simHRrate | 預期 v2.1 |
|---|---:|---:|
| 林安可 | 2.6% | 2.5-3.5% (持平到略升) |
| 朱育賢 | 0.3% | 1.5-2.5% |
| 許基宏 | 0.5% | 1.5-2.5% |
| 林立 | 0.0% | 0.5-1.2% |
| 林泓育 | 0.0% | 0.5-1.0% |
| 胡金龍 | 0.0% | 0.3-0.8% |

連鎖效應（預估）：
- SLG R²：0.238 → **0.35-0.45**（HR 取代 fly out 讓中等打者 SLG 提升）
- 強打者 BABIP：張育成 .238 → **.27-.30**（部分 fly out 變 HR，但 HR 不計 BABIP，所以剩下 BIP 的命中率上升）
- AVG 平均：.246 → **.250-.260**

### 11.4 驗證指標

| 指標 | (6) v2.0 | 預期 v2.1 | 接受範圍 |
|---|---:|---:|---|
| AVG 平均 | .246 | .250-.260 | ±0.010 |
| BABIP 平均 | .321 | .30-.33 | ±0.015 |
| AVG R² | 0.368 | ≥ 0.38 | 不能退步 |
| **SLG R²** | 0.238 | **≥ 0.35** | 必須改善 |
| HR rate 平均 | ~0.3% | 0.7-1.2% | 不要超過 1.5% |
| 朱育賢 simHRrate | 0.3% | **≥ 1.0%** | 必須提升 |
| 林立 simHRrate | 0.0% | **> 0%** | 從零變正 |

### 11.5 不達標時的調整方向

- HR 過多（平均 > 1.5%、強打者 simHRrate > 4%）→ 係數退回 0.66。
- HR 仍不足（朱育賢 < 0.8%、林立 = 0%）→ 係數推至 0.72。
- SLG R² 仍 < 0.30 → 問題可能不在 EV，而在 liner doubles 分類（§5 v1.4 排程），不要在這條 PR 再加參數。

### 11.6 未動範圍

- `baseball-physics-engine.js` 的 dragCarryScale、wallDist、HR 物理判定：不動。
- 守備引擎：v2.0 修完，不動。
- `(finalContactScore - 50) * 0.28` 係數：不動（contact 對 EV 的關係是合理的）。
- Gaussian noise σ=6：不動（變異度合理）。

### 11.7 v2.1 實測驗證（`sim-tester-batch-2026-05-24 (7).csv`）

99 打者 × 1000 PA = 99,000 PA 完整驗證結果：

| 指標 | 真實 | (6) v2.0 | (7) v2.1 | §11.4 目標 | 狀態 |
|---|---:|---:|---:|---:|---|
| AVG 平均 | .255 | .246 | **.242** | .250-.260 | ❌ 略低 |
| BABIP 平均 | - | .321 | **.314** | .30-.33 | ✅ |
| HR rate 平均 | - | 0.10% | **0.28%** | 0.7-1.2% | ⚠️ 仍偏低 |
| **SLG Pearson r²** | - | 0.238 | **0.430** | **≥ 0.35** | ✅ **達標** |
| AVG Pearson r² | - | 0.367 | 0.325 | 不能退步 | ❌ 退步 0.042 |
| OBP Pearson r² | - | 0.209 | 0.240 | - | ✓ 略升 |
| K% Pearson r² | - | 0.608 | 0.550 | - | ⚠️ 略退 |
| BB% Pearson r² | - | 0.395 | 0.371 | - | ✓ 持平 |

**個別 HR 指標達標狀況：**

| 球員 | realHR | v2.0 | v2.1 | §11.3 預期 | 達標? |
|---|---:|---:|---:|---|---|
| 林安可 | 32 | 2.6% | **6.8%** | 2.5-3.5% | ❌ 過量 |
| 魔鷹 | 34 | 2.0% | 5.5% | 2.5-3.5% | ❌ 過量 |
| 張育成 | 29 | 2.0% | 5.6% | 2.5-3.5% | ❌ 過量 |
| 吉力吉撈 | 28 | 1.8% | 4.6% | - | ⚠️ 略多 |
| 朱育賢 | 21 | 0.3% | **0.6%** | 1.5-2.5% | ❌ 仍低 |
| 許基宏 | 21 | 0.5% | 1.0% | 1.5-2.5% | ⚠️ 接近下緣 |
| 林立 | 12 | 0.0% | **0.7%** | 0.5-1.2% | ✅ 達標 |
| 林泓育 | 12 | 0.0% | 0.1% | 0.5-1.0% | ❌ 仍低 |
| 胡金龍 | 6 | 0.0% | 0.1% | 0.3-0.8% | ⚠️ 仍低 |

**結論：v2.1 主目標（SLG R²）達成，但 HR 機制呈現「雙向失準」：**
- Top tier (real HR 28-34) HR rate 過量 ~70%
- Mid tier (real HR 12-21) HR rate 仍不足 ~30%

### 11.8 v2.1 trade-off 紀錄與決議

v2.1 是一個明確的 trade-off：
- **拿到的**：SLG R² 0.238 → 0.430（主目標達成）、HR 系統不再單調 0%
- **失去的**：AVG R² 略退 0.04、K% R² 略退、強打 HR rate 過量

**根因（不在這條 PR 修）**：EV 公式是線性，但 HR 機制是「跨過 150 km/h 步階函數」。線性 boost 對 power 80+ 過度、對 power 70 以下不足。**這是公式形態的問題，不是參數沒調好**。

**決議：接受 v2.1 收尾**。理由：
1. SLG R² 是 v2.1 唯一明確目標，已超越
2. HR 過量對遊戲體感是正面（強打者該打多 HR）
3. 持續校準邊際效益下降，§9.7 教訓 #1 反向適用
4. 殘留問題另立 §12 完整紀錄，供未來修改

## 12. 已知殘留問題與調整指南（v2.1 後）

本段是 v2.0/v2.1 收尾後的 issue tracker。每個問題附**檔案、行號、建議改法、預期效果、風險**，方便未來逐條處理。優先級從高到低。

### 12.1 整體狀態（截至 2026-05-24）

| 引擎 | 狀態 | 評語 |
|---|---|---|
| `pitch-engine.js` | 校準完畢（v2.1） | 出棒、揮空、界外、進場分流大致合理，HR 系統仍粗糙 |
| `baseball-physics-engine.js` | 未動 | 物理層正確 |
| `fielding-engine.js` | 校準完畢（v2.0） | 反轉 BABIP 修復，殘留弱打者輕度偏高 |
| `defense-state-builder.js` | 校準完畢（v2.0） | 已同步、佈陣 outfield_in 已重校 |
| `baserunning-engine.js` | 未動 | 簡化版，未發現顯著問題 |
| `battle-scene.js` | **視覺層待同步** | 與引擎座標差 14m，不影響判定 |

### 12.2 Issue #1：HR 雙向失準（高，主要殘留）

**症狀**：
- Top tier (real HR 28-34) sim HR rate 4.6-6.8%，**比真實過量 ~70%**
- Mid tier (real HR 12-21) sim HR rate 0.1-1.0%，**比真實不足 ~30%**

**根因**：EV 公式（`pitch-engine.js:633-640`）是線性 `92 + power * 0.68 + ...`，但 HR 判定是 EV > 150 km/h 的步階函數。線性 boost 對不同 power 區間效果不均勻。

**檔案**：`pitch-engine.js:633-640` (`InPlayOutputModule.compute` → EV 公式)

**建議改法**（從輕到重）：

**選項 A — 微調係數退到 0.66**
```diff
-  + effectivePower * 0.68
+  + effectivePower * 0.66
```
- 預期：SLG R² 0.43 → ~0.38、強打 HR 6.8% → ~5.5%、mid-tier HR 退一點
- 風險：mid-tier 仍不足，治標不治本

**選項 B — piecewise knee**（推薦）
```js
const powerLow = Math.min(effectivePower, 75);
const powerHigh = Math.max(0, effectivePower - 75);
const evKmh = MathUtils.clamp(
  92 + powerLow * 0.78 + powerHigh * 0.35
  + (finalContactScore - 50) * 0.28 + MathUtils.gaussianRandom(0, 6),
  40, 190
);
```
- 預期：power 60→139.8、power 70→147.6、power 75→150.5、power 82→152.95、power 90→155.75
- 效果：mid-tier 大幅 boost、top tier 平緩 → 兩端都接近真實
- 風險：要重跑 sim-tester 校準，可能要 2-3 輪微調

**選項 C — 改 HR 機制（v2.2 級別工程）**
- 修改 `baseball-physics-engine.js` 的 HR 判定，加入 barrel score 加成讓 EV 略低也有機會 HR
- 工程大，留到下次

### 12.3 Issue #2：弱打者 BABIP 仍偏高（中）

**症狀**：
- 林泓弦 sim BABIP .386 (real ~.26) — 偏高 0.13
- 蔡佳諺 sim BABIP .289 (real ~.27) — 偏高 0.02
- 許哲晏 sim BABIP .294 (real ~.27) — 偏高 0.02
- 陳品捷 sim BABIP .351 (real ~.25) — 偏高 0.10

**根因**：v2.0 縮小無人區後仍未完全消除。弱打者短飛球（EV ~115、LA ~20）落點 60m，仍可能落在外野手前 reach 邊緣。

**檔案**：`fielding-engine.js:11-13`（外野手位置）/ `fielding-engine.js:434-446`（fly 球 reach）

**建議改法**：

**選項 A — 再前移 4m**（保守）
```diff
-  LF: { x: -50, y: 72 },
-  CF: { x: 0,   y: 86 },
-  RF: { x: 50,  y: 72 }
+  LF: { x: -50, y: 68 },
+  CF: { x: 0,   y: 82 },
+  RF: { x: 50,  y: 68 }
```
- 同步更新 `defense-state-builder.js:15-17`
- 風險：強打者深飛球更容易被等到，BABIP 強打可能再下降；HR 率可能略降

**選項 B — 降低短飛球 routineAirBonus**
- `fielding-engine.js:436`：`wallRatio < 0.78` 條件改成 `wallRatio < 0.65`，把短飛球從 routine 拆出來
- 風險：守備引擎邏輯更複雜，可能引入新 bug

**注意**：林泓弦個案要先確認他的 abilities 是否真實。可能他真實 BABIP 確實偏低（因為打硬度不夠），但模擬假設打到球就有同樣分布。

### 12.4 Issue #3：強打者 BABIP 仍偏低（中）

**症狀**：
- 張育成 sim BABIP .290 (real ~.31) — 偏低 0.02（v2.1 比 v2.0 .238 已大幅改善）
- 林立 sim BABIP .313 (real ~.36) — 偏低 0.05
- 林安可 sim BABIP .287 (real ~.31) — 偏低 0.02
- 朱育賢 sim BABIP .263 (real ~.30) — 偏低 0.04

**根因**：強打的硬飛球變 HR 機率上升後 BABIP 改善，但仍有部分本該是「外野手後追接不到的硬飛球安打」被接殺。`reachChance` 公式對長 hangTime 球種仍會飽和到 0.98。

**檔案**：`fielding-engine.js:433`（reach 上限）

**建議改法**：
```diff
-  let reachChance = clamp(0.08 + (rangeMarginM + 3.5) / 10, 0.02, 0.98);
+  let reachChance = clamp(0.08 + (rangeMarginM + 3.5) / 10, 0.02, 0.92);
```
- 把 reach 上限從 0.98 → 0.92，讓 routine fly 也有 8% 機率掉球
- 預期：強打深飛球 BABIP 升 0.02-0.04
- 風險：所有飛球安打率上升，整體 BABIP 可能 .314 → .33+

### 12.5 Issue #4：弱打者 K% 仍偏高（中）

**症狀**：
- 林哲瑄 sim K% 36% (real 20%) — 高 16 pts ⚠️
- 拿莫.伊漾 sim K% 39% (real 33%) — 高 6 pts
- 高聖恩 sim K% 32% (real 25%) — 高 7 pts
- 王念好 sim K% 36% (real 28%) — 高 8 pts
- 張志豪 sim K% 34% (real 30%) — 高 4 pts

**根因**：未診斷。可能是 pitch-engine 的 whiff 公式對 contact 值很低的打者（contact 45-55）過度懲罰。

**檔案**：`pitch-engine.js`（contact/whiff 相關，需先 grep `whiffProb` / `swingMissProb`）

**建議**：先做單球員 sim-tester（B 模式）跑林哲瑄 3000 PA，看 zone whiff rate / chase whiff rate / two-strike whiff rate 是否都偏高。**不要在沒搞清楚前就調參數**。

### 12.6 Issue #5：AVG R² 略退（低）

**症狀**：v2.0 0.367 → v2.1 0.325，退步 0.04

**根因**：v2.1 強打 HR 過量讓他們 simAVG 飆升（張育成 .211 → .278），與真實 AVG 排序更不一致。這是 HR 過量的副作用。

**處置**：跟 Issue #1 一起修；獨立調無解。

### 12.7 Issue #6：視覺層與引擎座標差距（低）

**症狀**：`battle-scene.js:945-947` 仍寫死 LF y=86 / RF y=86，與引擎 y=72 差 14m。

**影響**：守備員「站位視覺」位置與「實際判定」不一致，看動畫會覺得守備員站得比實際遠。**不影響判定結果**（v3.22 後動畫只讀 visualTimeline 的 playPoint）。

**檔案**：`battle-scene.js:945-947`

**建議改法**：
```diff
-  LF: { x: -58, y: 86 },
+  LF: { x: -58, y: 72 },
   CF: { x: 0,   y: 86 },
-  RF: { x: 58,  y: 86 }
+  RF: { x: 58,  y: 72 }
```
- CF 不變（剛好已是 y=86）
- 等接入 PNG sprite 時順手改，現在不急

### 12.8 Issue #7：liner doubles 分類過簡（低）

**症狀**：未量化，但 §5 v1.4 預警過「liner 安打一律偏向長打」。可能 strong hitter 的所有 liner 都變 double 而不是 single。

**檔案**：`fielding-engine.js:530-538` (`classifyHit`)

```js
function classifyHit(ballInfo, battedBallTypeHint, random) {
  if (ballInfo.preciseDistM >= ballInfo.preciseWallDistM * 0.92) {
    return ballInfo.isBarrel && random() < 0.3 ? "triple" : "double";
  }
  if (ballInfo.preciseDistM >= ballInfo.preciseWallDistM * 0.65 || battedBallTypeHint === "liner") {
    return "double";
  }
  return "single";
}
```

**建議改法**：第二個 if 把 `battedBallTypeHint === "liner"` 條件拆出來，依 EV/sprayAngle 機率化選 single 或 double：
```js
if (battedBallTypeHint === "liner") {
  const isGap = Math.abs(ballInfo.sa_deg) > 12 && Math.abs(ballInfo.sa_deg) < 38;
  const isHardLiner = ballInfo.ev_mph >= 95;
  if (isGap && isHardLiner) return "double";
  return random() < 0.25 ? "double" : "single";
}
```

**風險**：可能讓 SLG 略降；要重跑 sim-tester 校準。

### 12.9 Issue #8：popup 守備候選保守（低）

**症狀**：未量化，但 §9.6 選項 C 預留的工作。SS/2B 對 50-65m 飛球應該也可 attempt（往後追），現在只在 popup（< 38m）會被選為候選。

**檔案**：`fielding-engine.js:374-382` (`candidatePositions`)

**建議改法**：略，需要設計新 ballType 子分類，工程大，不推薦現在做。

### 12.10 修改流程建議

如果未來要重啟校準工作，建議按以下順序：

1. **先讀本檔（§9-12）跟 `PROJECT_OVERVIEW.md`**。
2. **跑 sim-tester baseline** 對照當前狀態。
3. **一次只改一個檔案的一個參數**（§9.7 教訓 #2）。
4. **改完立刻跑 sim-tester 比對**，看 Pearson r²、平均值、四位指標球員。
5. **不達標就退回，不要堆參數**（§9.7 教訓 #3）。
6. **改完寫入新 §（§13、§14...）**，保留歷史。

### 12.11 v2.1 後不要做的事

- ❌ 不要再動 `routineAirBonus` / `hangAdjust`（已校準）
- ❌ 不要動 `baseball-physics-engine.js` 物理層
- ❌ 不要動 `data.js` 球員資料（用 stat-mapper 重跑才動）
- ❌ 不要為了 AVG R² 高一點就調 chaseProb / whiff baseline（容易破壞 K%/BB%）
- ❌ 不要在沒跑 sim-tester 驗證就 commit 參數變動

### 12.12 架構不變式（必須遵守）

- **`fielding-engine.js` POSITIONS 必須與 `defense-state-builder.js` BASE_POSITIONS 完全同步**（兩個是同一個概念的兩份副本，改一邊另一邊也要改）。v2.0 修復時這是 §9.6 沒提的關鍵同步點。
- **守備動畫只能讀 `visualTimeline.events`**，不能自己重判定（v3.22 規則）。
- **物理層 `baseball-physics-engine.js` 是 single source of truth**，hangTime / distance / HR 物理判定不要在守備層重複實作。

## 13. 2026-05-27 EV 分布化 + 仰角耦合（v1.5「深」改動）

這一輪不再校準參數，而是**改 EV 的生成模型本身**。源頭是使用者兩個洞察：
1. 「平均擊球初速只是平均，真實擊球是有範圍區間的」
2. 「魔鷹 power 82 → EV 152 的對應其實是對的（他 core.avgEV 149 確實這麼強），問題會不會在仰角？」

### 13.1 失敗的前置嘗試（v1.5 G，已退回）

先試過動引擎 EV 線性係數：`92 + power×0.68` → `110 + power×0.40`（CSV `2026-05-26 (2)`）。
**NET 失敗**：頂級 HR% 從 2x 過頭壓到 0.4x 不足（魔鷹 11.33→2.33），中強打 BABIP 全面崩盤（吳念庭 -.148、陳鏞基 -.125）。
**教訓**：EV 公式是線性、HR 是步階函數，動係數會「全聯盟一起平移」，無法只壓頂級不傷中段（與 §11.8 一致）。已退回 `0.68`。

### 13.2 根因（比 §12.2 Issue #1 更深的診斷）

舊 EV 公式 `92 + power×0.68 + (contact-50)×0.28 + N(0,6)` 有兩個結構缺陷：

**缺陷一：EV 被壓成「點」而非「分布」。** σ 只有 6，但真實 in-play EV 從 ~100（削到/頂到）到 maxEV（183）跨度 80+ km/h，實測 σ ≈ 13-16（從 maxEV−avgEV 反推）。魔鷹每球都吃 ~153（barrel 級），但真實 ballQuality.avgEV 只有 142——引擎讓**每一球都打在峰值上**。

**缺陷二：EV 與仰角獨立抽樣**（`pitch-engine.js` 舊 compute：EV 從 power、LA 從 ballType，互不相關）。真實世界 EV 與 LA 負相關——要打到 40° 高飛必須擊中球心下緣（削擊）→ 動量傳遞少 → EV 低；只有 LA 10-30° 才可能高 EV（barrel）。引擎讓 38° 高飛拿到跟 26° barrel 一樣的 EV，飛球 LA[25,39] 又與 HR 窗口[25-35]高度重疊 → 大量廉價 HR。

兩個缺陷是同一件事：**引擎沒把「擊球品質」當成有分布的隱變數**。

關鍵佐證（真實 CPBL，2024-2026）：

| 球員 | core.avgEV | ballQuality.avgEV | 差距 | maxEV |
|---|---:|---:|---:|---:|
| 魔鷹 | 149.1 | 142.2 | 6.9 | 183 |
| 吉力吉撈 | 159.2 | 150.7 | 8.5 | 174 |
| 林立 | 145.9 | 139.8 | 6.1 | 169 |
| 林泓弦（弱打）| — | 130.1 | — | 164（top_ev 164 後斷崖到 147，右偏明顯）|

### 13.3 修正內容（`pitch-engine.js` InPlayOutputModule.compute）

把「power 平均 + N(0,6) 點估計」改成**擊球品質驅動的分布**，五原則：

1. `ceilingEv = 92 + power×0.68 + (contact-50)×0.28`（power 驅動的咬死潛力 ≈ core.avgEV，**保留升級成長性**）
2. `anchorMean = 0.55×(ceilingEv−7) + 0.45×realAvg`（靠攏真實 `advancedStats.ballQuality.avgEV`，魔鷹 153→~146 落到 HR 閾值下；升級時 ceiling 上移整條曲線跟著上移）
3. 寬度 `spread = clamp(maxEV−avgEV, 18, 45)`，`evNoise` σ ≈ spread×0.34（≈12-15，取代假的 σ=6）
4. 右偏：`evNoise = z>=0 ? z×spread×0.42 : z×spread×0.30`（上尾長 = barrel 罕見，HR 只從尾巴出；下尾緊 = 弱擊球團塊）
5. 仰角耦合（均值≈0 的重分配）：`laShave = clamp(|LA−14|×0.42, 0, 16)`、`laAdj = −(laShave−6)`（LA 接近 14° 加成、偏離越多扣越多，扣回平均 6 使整體均值不偏移）

**並把擊球型態/仰角的計算移到 EV 之前**（因為 EV 現在依賴 LA）。
無 `ballQuality` 的新球員/卡片有 fallback（`realAvg = ceilingEv−7`、`realMax = realAvg+36`）。

### 13.4 驗證結果（CSV `sim-tester-batch-2026-05-27`，99 打者 × 300 PA）

| 指標 | v1.4 baseline | v1.5 深 | 變化 |
|---|---:|---:|---|
| AVG R² | 0.228 | **0.260** | ✓ +14% |
| SLG R² | 0.445 | 0.435 | 持平 |
| mean\|dAVG\| | 0.0348 | **0.0292** | ✓ −16% |
| RMSE | 0.0425 | **0.0369** | ✓ −13% |
| \|dAVG\|≥.05 球員數 | 26 | **17** | ✓ −35% |
| simAVG 平均 | — | .2566（real .2545）| ✓ |
| simBB% 平均 | — | 7.27（real 7.27）| ✓ |

三大問題收斂：魔鷹 HR% 11.33→**5.33**（real 5.8）、林安可 8.33→**4.67**、陳傑憲 dAVG −.101→**+.012**、余德龍 +.092→**+.006**、林泓弦 +.103→**+.039**。
張育成單測（3000 PA）AVG .298 vs real .303、平均 EV 155.4→150.2。

### 13.5 殘留問題（issue tracker，未來接手從這裡續）

**Issue #9：林立等 contact 型極端值 BABIP 偏低（中）**
- 症狀：林立 sim AVG .261 vs real .348（dAVG −.087，比 v1.4 −.049 略差）。真實 BABIP ~.36 來自頂級棒控/落點，加寬 EV 分布反而給他更多弱擊球。
- 根因：噴射角用高斯 σ=14 + pull bias，沒讀打者真實 `advancedStats.spray` 分布（檢討書 §10.4 #8）。
- 檔案：`pitch-engine.js` `_generateAngles`（spray 計算）
- 建議：spray 改用 `batter.advancedStats.spray.{left,center,right}` categorical 抽樣，重現落點刁鑽。風險：要重校，可能影響其他球員 BABIP。

**Issue #10：超弱打（OPS+ <60）仍高估（中）**
- 症狀：葉子霆 +.091、馬傑森 +.086、林政華 +.083。
- 根因：anchorMean 含 `0.55×(ceilingEv−7)`，power 驅動部分對弱打仍偏高；且 plateDiscipline 混合 30% 把弱打 inPlayRate 拉高（檢討書 §10.4 #3）。
- 檔案：`pitch-engine.js:526-549`（plateDiscipline 混合）
- 建議：plateDiscipline 混合 30%→15%，或 anchorMean 對低 ceiling 球員加重 realAvg 權重。一次一個，跑 sim 驗證。

**Issue #11：張育成 BB% 個案低估（低）**
- 症狀：sim BB% 7-8% vs real 14.51%。
- 根因：張育成真實 chase.swingRate 0.339（中等偏高）但 BB% 聯盟第一——投手怕長打誘投 chase 區、他追打也選到。stat-mapper discipline 公式 chase rate 反向 40% 權重把他算到 74。**全聯盟 BB% 已對齊（7.27=7.27），這是個案。**
- 檔案：`tools/stat-mapper.html` discipline 公式
- 建議：暫不動（個案，動了可能傷整體 BB% R²）。

**Issue #12：sim-tester batch vs 單測不一致（測試工具，非引擎）**
- 症狀：張育成 batch .221 vs 單測 .298。
- 根因：batch 用中位投手、單測對 鈴木駿輔，兩者投手/守備設定不同。
- 建議：以單測（3000 PA 更穩定）為個別球員可信值；batch 看聯盟整體趨勢與排序。

### 13.6 v1.5 深 不要做的事

- ❌ 不要回退到「EV = 點估計」（σ=6）——已證實是 HR 過量根因
- ❌ 不要再動引擎 EV 線性係數壓頂級（v1.5 G 已證實 NET 失敗）
- ❌ 不要為了林立一人調 spray 就 commit——先確認不傷其他人
- ✅ EV-仰角耦合的 `laShave×0.42`、右偏 `0.42/0.30`、anchor `0.55/0.45` 已校準，動前先跑 sim baseline

## 14. 2026-05-27 平飛球時間制守備 + 雜訊天花板（v1.5.1，打者校準收尾）

### 14.1 起點問題
v1.5 深後殘留：`corr(simBABIP, OPS+) ≈ 0` → BABIP 不隨打者強度變化，是 AVG R²（0.26）卡關主因。診斷分兩半：1a 弱打 BABIP 太高、1b contact 型強打（吳念庭/林立）BABIP 太低。spray 路線因 CPBL 管線 `location_coord_x` 壞掉（全判 right，要重抓）暫不走（記 cpbl-pbp-pipeline 記憶）。

### 14.2 物理根因（使用者提出時間制思路）
`arrivalTimeFor`（`fielding-engine.js:410`）平飛球用**真空式 hangTime**（`2·v/g`），但 `preciseDistM` 是 drag 衰減後距離。結果平飛球「只飛到 ~57m 卻給守備員 2.2s」→ 守備時間被高估 → 硬平飛該穿越空檔卻被接殺 → contact 型強打 BABIP 偏低。

### 14.3 修正（已落地）
`fielding-engine.js:410-422` liner 分支：攔截時間改用**水平飛行時間** `水平距離 / flightProfile.horizontalMps`，再乘 `LINER_FIELD_TIME_SCALE`。資料齊全已驗證：`flightProfile.horizontalMps`（水平球速 m/s）存在、`ballInfo.ev_mph` 是 MPH。

### 14.4 關鍵發現：level↔slope 耦合 + 99×300 雜訊天花板

調 `LINER_FIELD_TIME_SCALE` 跑了三輪（全 99×300 PA）：

| scale | corr(BABIP,OPS+) | AVG R² | BABIP | AVG | meanAbsDAVG |
|---:|---:|---:|---:|---:|---:|
| 1.0 | +.163 | .461 | .361 | .294 | .043 |
| 1.2 | **−.079** | .302 | .340 | .278 | .035 |
| 1.35 | +.036 | .310 | .315 | .258 | .029 |

**兩個教訓（最值錢的部分）：**
1. **level 與 slope 是同一個時間旋鈕耦合的**——時間給太多（高 scale），守備員連硬平飛空檔都追得到 → 斜率塌掉；時間給太少（低 scale），全平飛變好打 → level 爆走。單一旋鈕無法兩全。
2. **99×300 PA 的 corr/R² 是雜訊主導**：corr 在 +.163 / −.079 / +.036 之間亂跳（標準誤 ±0.1）。scale=1.0 的「corr +.163 / R² .461」**不可重現**，是被 level 爆走（+.047 BABIP）夾帶。**差點被單次數字騙了**（呼應 §9.7 教訓）。

**可信 vs 不可信**：level（平均 AVG/BABIP，29700 PA 平均）可信且單調；corr/R²（單次相關係數）不可信。

### 14.5 決定：scale=1.35 定案
選 scale=1.35 因為它的 **level 最準**（AVG .258 ≈ 真實 .254、BABIP .315、meanAbsDAVG .029 最低）。平飛時間制的價值是**物理更正確**（水平時間取代真空 hangTime），level 中性、無害；對 R² 的幫助落在雜訊內，**不宣稱為勝利**。

### 14.6 打者校準收尾狀態
大失真都修好了（可重現的真實成果）：
- HR 雙向失準（魔鷹 11.3%→5.3%，v1.5 深 §13）✅
- 強弱 BABIP 大反轉（v2.0 守備 §10）✅
- 中強打埋沒（資料 bug 修復 §9-10、EV 分布化 §13）✅

剩餘殘留**都在雜訊內、邊際遞減**，列為低優先：
- 超弱打 OPS+<60 仍略高估（葉子霆/馬傑森 +.04~.08）
- contact 型極端值（林立 BABIP .36 引擎達不到）— 需真實 spray（管線要修+重抓）
- 低 K% 好 contact 球員 sim K% 偏高（whiff 地板）

### 14.7 要繼續打者校準的人必讀
**先解決雜訊再動手**，否則是跟雜訊打架：
- 跑 **99×1000**（不是 300）讓 corr/R² 標準誤減半，或固定 RNG seed，或多跑平均
- 只信能在更大樣本重現的差異；< 0.1 的 corr/R² 變動一律當雜訊
- level（平均值）可信，個別球員看單測 3000 PA（比 batch 穩）

### 14.8 下一步是投手校準（不是打者）
打者已到雜訊邊際。高 ROI 的是**投手**——9 項能力**從沒用 sim 驗證過**，且 `能力值體系檢討書v1.md §3` 診斷出 velocity（用 K% 不是球速）、breaking（用 ERA+ 跟變化球無關）、control（用 WHIP 含被打安打）都用錯指標。那是**大失真**（不怕雜訊），公式已在 §3 寫好，待實作 + sim 驗證。下個對話從這裡開始。

## 15. 2026-05-28 投手校準完整工作 + 引擎架構失衡發現

### 15.1 已落地：stat-mapper 投手公式修正
四項並動（`tools/stat-mapper.html`）：
1. **velocity 公式**：對齊引擎 `maxSpeed = 112 + velocity × 0.6` 概念。改用 vMax 為主（75%）+ K9 輔（25%）、vAvg 缺值用作 fallback。原本曾被改成 vAvg 為主、方向錯（vAvg 是全球種混合、不對齊「直球速度」）。outlier vMax>160 排除。
2. **stuff bug**：原 s1 = velocity×0.40 + breaking×0.35 + control×0.10 加總 0.85，讓所有投手 stuff 系統性偏低 ~10 分。normalize 到 1.0（0.47/0.41/0.12）。
3. **vsLeft bug**：原 `v1 × confidence × 0.60 + v2 × (1 - confidence × 0.40)` 在 confidence=1 時權重 1.2 超過 1.0，高 vsLBF 樣本投手 vsLeft 系統性偏高（威能帝/艾速特/古林睿煬 都 87+）。改標準線性混合。
4. **piecewise 區間全面對齊 CPBL 實測**（N=108-111，BF≥100）：
   - vMax (144, 149, 156)、vAvg (132, 137, 143)、K9 (4.3, 6.3, 9.3)
   - BB9 (4.7, 2.7, 1.2) inv、whiffp (15, 19, 27)
   - zoneRate (0.45, 0.49, 0.54)、edgeRate (0.21, 0.23, 0.26)
   - breakingWhiffRate (0.17, 0.25, 0.38)、breakingUsage (0.29, 0.46, 0.61)、chaseWhiff (0.22, 0.31, 0.44)
5. **stat-mapper 輸出補欄位**：bb9 / h9 / hr9 / whiffp / bf（投手 sim 對比真實要這些）

### 15.2 已落地：sim-tester 投手批次模式（v2.3）
- 新 Tab C「投手批次回歸」：全合格投手 × N PA × 中位數打者 → K9/BB9/H9/HR9/WHIP/Whiff% R²
- 載入 data.js 時自動找 MEDIAN_BATTER（OPS+ 最接近 100）
- 後改為 **打者池抽樣**（VALID_BATTERS 隨機抽）避免單一打者吸收差異
- 後加入 **control 敏感的瞄準邏輯**（`resolvePitchAimCellGameLike(plan, control)`）：強控敢瞄 zoneCorner 擦邊好球、弱控集中 center
- CSV / JSON 下載

### 15.3 stat-mapper 對齊真實 R² 漂亮
join data.js abilities + 真實 summary：

| abilities → real | R² |
|---|---:|
| stuff → K9 | **0.716** |
| **stuff → Whiffp** | **0.865** |
| velocity → K9 | 0.566 |
| breaking → K9 | 0.423 |
| **control → BB9** | **0.789** |

**stat-mapper 投手公式校準大成功**。問題不在這層。

### 15.4 失敗：sim 結果遠不如 abilities R²
四輪 sim-tester 嘗試（單一中位、打者池、aim 看 control v1/v2/v3）：

| 指標 | abilities→real | sim 最佳 | 訊號保留率 |
|---|---:|---:|---:|
| Whiffp | 0.865 | 0.748 | 86% |
| K9 | 0.716 | 0.323 | 45% |
| **control→BB9** | **0.789** | **0.097** | **12%** |
| H9 | — | 0.020 | 接近 0 |
| WHIP | — | 0.001 | 純雜訊 |

### 15.5 失真根因 1：plateDiscipline / pitchTypeMatchup 攤稀（53%）
`pitch-engine.js:526-550` v3.23 加入的兩層混合：
```js
whiffProb  = whiffProb  * 0.70 + pdW * 0.30;      // 打者 plateDiscipline 30%
whiffProb  = whiffProb  * 0.75 + ptW * 0.25;      // 打者 pitchTypeMatchup 25%
```
`pdW` / `ptW` 是該打者 PA 平均（含面對所有強弱投手）。對打者校準 OK，對投手 sim **把投手強弱訊號吸收 50%**：
- 純引擎 stuff 50→90 whiff 差距 13.4 pp
- 混入後 7.1 pp（CPBL 真實 12 pp 的 60%）

同樣失真也吃 inPlay → 拖累 H9 / WHIP。

### 15.6 失真根因 2：missOffset sigma 被 spread 淹沒
`pitch-engine.js:260-271` missOffset sigma：control 30→100 → 11.7 cm → 1.4 cm（差 8.4 cm）。但 `_getPitchMovementRange` 的 spread 對中位變化球已是 11-20 cm — **完全淹沒 sigma 訊號**。
- 強控弱 break 的滑球：spread 2 + sigma 2.5 → 球落瞄準位置
- 弱控強 break 的滑球：spread 20 + sigma 11 → 球到處飛
- 結果「強控 vs 弱控」對 finalPosition 的影響在球路變化前是噪音

### 15.7 失真根因 3（**架構級**）：投手 9 大能力擠在 stuffScore 一條通道

#### 打者方（v1.5.1 校準成功的原因）

| 能力 | 直接通道 | 影響範圍 |
|---|---|---:|
| contact | finalContactScore 主項 | ±25 分 |
| power | effectivePower 主項、EV 公式 | ±25 分 |
| eye/discipline | effectiveEye、pCorrectRead、chaseProb | ±25 分 |
| vsLeft/Right | buildBatterStats contact 修正 | ±5 分 |

**每個打者能力都有專屬獨立通道**，能力 50→90 → outcome 30%-50% 差距。

#### 投手方（校準失敗的根因）

| 能力 | 直接通道 | 影響範圍 | 問題 |
|---|---|---:|---|
| velocity | maxSpeed = 112 + vel×0.6、進球速 → timing matching | 22 km/h 球速差 | sim batter `velocityLock='none'` → **完全不用 velocity** |
| control | _sampleMissOffset sigma 2.5~11.7 cm | 9 cm 差 | 被 spread 20 cm 淹沒 |
| breaking | _getPitchMovementRange spread 2~24 cm | spread 22 cm 差 | 跟 stuff 通道重疊 |
| stuff | pitchStuffPenalty → finalContactScore | -8~+22 分 | **9 大能力擠這條** |
| stamina | sim 中 fatigue=0 | 0 | 沒接入 |
| **discipline** | **沒接入引擎** | 0 | — |
| **crisis** | **沒接入**（RISP 才有）| 0 | — |
| **vsLeft** | sim batter 不分左右 | 0 | — |
| fielding | FieldingEngine 用 | 小 | — |

換句話說：**打者有 3 條大水管、投手只有 stuffScore 1 條小水管 + missOffset 1 個小水龍頭**。stat-mapper 不管把投手 abilities 校準到 R² 0.79、進入引擎就被擠扁。

### 15.8 三層修法

#### 短期（單一函式級、不傷打者）
**方案 B**：讓 `pdW` / `ptW` 對投手 stuffScore 動態化
```js
const stuffMul = 1 + (stuffScore - 70) * 0.008;  // stuff 70=1.0、50=0.84、90=1.16
const pdW_adj = clamp(pdW * stuffMul, 0.01, 0.95);
const pdI_adj = clamp(pdI / stuffMul, 0.01, 0.95);   // 反向：強投讓打者進場率↓
```
- 預期：Whiffp R² 0.665 → 0.78、H9 R² 0.02 → 0.15、K9 R² 0.32 → 0.42
- BB9 微升（主因不在此層）
- 風險：可能傷打者校準，要重跑 v1.5.1 baseline

#### 中期（單一檔級、需重跑打者 sim）
**選 2-3 條最關鍵通道接入**：
1. **velocity → 打者揮棒判斷**：球速越快、pCorrectRead↓（取代 timingMod 對中位 batter 無效的問題）
2. **control → 球路精準度（不只 missOffset）**：弱控變化球更亂（spread 變大）、強控變化球更精準
3. **discipline → 投手 AI 瞄準策略**：放進引擎（取代我做的 sim-tester aim 修正）

#### 長期（架構級重設計）
**對稱化「投手能力 → 引擎影響」表**，每個 ability 都有獨立通道：

| 能力 | 新通道（架構 v2 提案） |
|---|---|
| velocity | 直接影響 contactQuality 的 timingPenalty（不依賴 velocityLock） |
| control | (a) missOffset sigma（現有，放大），(b) 球路精準度 spread 修正 |
| breaking | (a) movement spread（現有），(b) 變化球揮空率加成 |
| **stuff** | 整合 velocity+breaking+control 的「球的綜合難度」(現有，保留) |
| **discipline** | 投手 AI 瞄準策略（敢瞄 edge / 誘揮）→ 整合到引擎 OpponentAI |
| **crisis** | 壘上有人時 stuff +X、ctrl +Y |
| **stamina** | sim 中要能模擬疲勞累積（投球數 → fatigue 上升） |
| **vsLeft** | 左右打者對決加成（contact -X、whiff +Y） |
| fielding | 強襲滾地球攔截範圍 |

預期能達到打者校準同等的 R² 水準（0.4-0.6 主要、頂級指標 0.6-0.8）。

### 15.9 接手者必讀

#### 已知好的（不要動）
- stat-mapper 投手公式（§15.1 四項已落地）
- data.js 投手 abilities（control→real BB9 R² 0.79）
- sim-tester 投手批次模式（§15.2）

#### 已知壞的（需動）
- pitch-engine.js ContactResolutionModule 兩層混合攤稀
- pitch-engine.js _sampleMissOffset sigma 範圍被 spread 淹沒
- discipline / crisis / stamina / vsLeft 沒接入引擎

#### 動引擎前一定要
1. 先跑打者批次 sim 拿 v1.5.1 baseline（AVG R² ~0.26、SLG R² ~0.43）
2. 改動引擎後重跑打者 sim 驗證沒崩
3. 改動引擎後重跑投手 sim 驗證有改善
4. 確認沒有兩邊互相矛盾的調整（例如 stuffMul 拉強投手 whiff、會否讓強投手對打者 contact 也異常）

#### 不要做的
- 動 stat-mapper 投手公式（已成功、再動沒意義）
- 在 sim-tester 層繼續加 aim 邏輯（已試 v1/v2/v3、瓶頸在引擎）
- 用方案 A（aim 看 control）+ 方案 B 之外的「微調修正」（小參數調動無法解決架構失衡）

### 15.10 待動清單（接手用）
- [ ] 方案 B 動 ContactResolutionModule stuffMul（pitch-engine.js:526-550）
- [ ] 重跑打者 sim 確認沒崩
- [ ] 重跑投手 sim 確認 Whiffp/H9/K9 R² 提升
- [ ] velocity 通道：在 BatterJudgmentModule.decide 加入 speedKmh 對 pCorrectRead/chaseProb 影響
- [ ] control 通道：spread 受 control 反向影響（弱控變化球更難控）
- [ ] discipline 通道：把 sim-tester 的 control 敏感 aim 邏輯搬進引擎 OpponentAI
- [ ] stamina 通道：sim 中模擬疲勞累積
- [ ] crisis / vsLeft 通道：壘上有人 / 左右打加成

> **§15.10 已被 §16 取代為更細粒度的 Wave A→E 清單**。下次接手請從 §16 開始讀。本待動清單留作歷史紀錄。

---

## 16. 2026-05-28 長期架構重設計：投手 9 通道對稱化（研究設計，未落地）

> 本章是 §15.8「長期（架構級重設計）」的詳細設計藍圖。讀完整段才動手。
> **狀態：設計階段、尚未落地任何程式碼。** 落地依 §16.5 Wave A→E 順序進行。

### 16.1 為什麼要做架構級對稱化

§15 的診斷已確認三層失真：plateDiscipline/pitchTypeMatchup 攤稀（53%）、missOffset sigma 被 spread 淹沒、9 大投手能力擠 stuffScore 一條通道。短期方案 B（§15.8）能修第一層，但**第二、三層在引擎結構不對稱前修不掉**——sim R² 永遠在 0.3-0.4 的天花板。

實地 audit `pitch-engine.js` + `game.js:resolveAtBatWithContext`（line 4720-4900）確認了具體現狀：

#### 16.1.1 打者方（三條獨立大水管，已可校準到 R²≈0.4-0.6）

| 能力 | 進場位置 | 影響 |
|---|---|---:|
| contact | `ContactResolutionModule.resolve` → `finalContactScore` 主項 | ±25 分 |
| power | `ContactResolutionModule` → `effectivePower` 主項 → `InPlayOutput` EV | ±25 分 |
| eye/discipline | `BatterJudgmentModule.decide` → `effectiveEye` → `pCorrectRead` & `chaseProb` | ±25 分 |
| 副通道 | vsLeft/Right、scoringPosition、heatMap、bats、advancedStats (plateDiscipline / timing / pitchTypeMatchup / spray / avgLaunchAngle) | 已就位 |

#### 16.1.2 投手方（一條主水管 stuffScore + 三條已死/半死通道）

| 能力 | game.js 接點 | 引擎內路徑 | 實際強度 |
|---|---|---|---|
| **velocity** | `vel` 線性疊加（trait、stamina mods、特殊配球） | (a) `_estimateSpeedKmh` → `speedKmh`（**未進 contactQuality 公式**）<br>(b) `estimateStuffScore × 0.45` → `stuffScore` → `pitchStuffPenalty` | 唯一進場通道是 stuff（共用）；sim 中 batter velocityLock='none' → 直通 timingMod=0 → **velocity 對 sim 中位打者完全無感** |
| **control** | `ctrl` 線性疊加 | (a) `_sampleMissOffset` sigma = `(36 - ctrl × 0.34) / 2.2`（**訊號 2.5～11.7 cm，被 spread 11-20 cm 淹沒**）<br>(b) `estimateStuffScore × 0.15` → `stuffScore` | sigma 訊號弱 + stuff 權重最小，這是「control→BB9 R² 0.79 abilities→0.097 sim」12% 訊號保留率的主因 |
| **breaking** | `breaking` 線性疊加 | (a) `_getPitchMovementRange` spread → `movementMag` → `pCorrectRead` / `chaseProb` ✓（**有效**）<br>(b) `estimateStuffScore × 0.40` → `stuffScore` | 唯一有獨立通道的投手能力，但對 whiff 還是要靠 stuff |
| **stuff** | `estimateStuffScore` 計算 | `pitchStuffPenalty = clamp((stuffScore - 50) / 2.4, -8, 22)` → finalContactScore | 9 大能力擠這條 |
| **stamina** | `staminaState.mods` 加減 vel/ctrl/break（v3.25.2）+ `fatigue` → `movementScale 0.78~1.0` | 引擎內間接（已套在 vel/ctrl/break 上）+ movementScale | **但 sim-tester 每 PA 重設 → fatigue 永遠 0** |
| **crisis** | `game.js:4830` 壘上有人時 `ctrl += (crisis-70)/5` | 唯一通道、只在壘上有人時有效 | 邊緣 |
| **discipline (投手)** | **無** | **無**（line 3288/4039 只是 UI 顯示） | 死通道 |
| **vsLeft (投手)** | **無**（line 4813 讀的是「打者」的 vsLeft） | **無** | 死通道 |
| **fielding** | 給 FieldingEngine 用 | 不在 PitchEngine | 不算 |
| **pickoff / quickDelivery** | game.js:2789 牽制成功率 | 不在 PitchEngine | 不算 |
| **recovery** | **無**（只在 line 2655 教練加成有提到） | **無** | 死通道 |

**結論**：打者 3 條直接、獨立、可校準的通道；投手只有 stuff 1 條主通道 + breaking 半條（movement spread）+ control/velocity 兩條被淹沒的細支管。**這就是 sim R² 永遠追不上 abilities R² 的結構原因。**

### 16.2 設計原則（四個硬約束）

1. **不傷打者校準**：每一波改動後重跑打者 sim，R² 不能低於 v1.5.1 baseline（AVG ~0.26、SLG ~0.43）。
2. **不破壞 stat-mapper abilities R²**：不重新校準 abilities 公式，能力的物理意義保持一致。新通道只是「讓 abilities 進得了引擎」。
3. **可獨立校準**：每條通道都有自己的係數常數，可單獨開關、A/B 測試。建議全部寫進 `game-params.js` 的新區塊 `pitcherChannelCoefficients`。
4. **可從 sim 觀察**：sim-tester 跑出來「能力 X +10 → outcome Y 變動 Z」要能被測到。如果改完跑 sim 沒差，這條通道就還沒接上。

### 16.3 9 通道對稱化架構表（核心設計）

| # | 能力 | 新通道名 | 進場位置 | 主要影響 outcome |
|---|---|---|---|---|
| 1 | velocity | **時間壓迫通道** | `BatterJudgmentModule.decide` 內 pCorrectRead 修正 + `ContactResolutionModule.resolve` 內 finalContactScore 直接扣分 | contactQuality↓、whiff↑、K9↑ |
| 2 | control | **精準度通道（兩段）** | `_sampleMissOffset` sigma 公式拉大 + `_getPitchMovementRange` spread 反向放大 | BB9↓、wild pitch↓、被 hit 降低 |
| 3 | breaking | **變化球揮空通道** | `ContactResolutionModule.resolve` 內 whiffProb 對「變化球種」獨立加成 | 變化球 whiff↑、K9↑ |
| 4 | stuff | （保留現狀為總管） | `pitchStuffPenalty` 不動，但因為 1/2/3 各自開通道、stuff 相對權重自然下降 | 整體 contactQuality 修正 |
| 5 | stamina | **疲勞累積通道** | sim-tester 模擬投球數→fatigue 上升（引擎本身已就位） | 後段 K9↓、BB9↑、HR9↑ |
| 6 | crisis | **leverage 通道（擴大）** | `game.js:4830` 壘上有人時 ctrl/break/stuff 同時加成 | RISP_AVG_allowed↓ |
| 7 | discipline | **AI 瞄準通道** | `resolvePitchAimCell` 依 discipline 調整 edge vs center 權重 | BB9↓ + K9↑（誘揮） |
| 8 | vsLeft / vsRight | **左右打對決通道** | `game.js:4813` 附近加入 pitcher.abilities.vsLeft / vsRight 修正 contact / ctrl | 左右打 sim AVG 分流 |
| 9 | fielding | （保留 FieldingEngine 內） | 不動 | BABIP_allowed↓ |

副通道（不在 9 條主表內，但配套要做）：
- **stuffScore 對 pdW/ptW 動態化**（§15.8 短期方案 B）：強投手能擊穿打者 plateDiscipline 平均、不被攤稀
- **stuffScore 公式 normalize**（已在 §15.1 完成）

### 16.4 各通道詳細公式設計

每段都先給「現狀」、「新公式」、「預期效果」、「對打者的副作用」。所有係數都建議集中在 `game-params.js` 新區塊 `pitcherChannels`，可獨立調校。

#### 通道 1：velocity → 時間壓迫

**現狀**：velocity 進 stuffScore（0.45 權重）+ 進 speedKmh（但 sim batter velocityLock='none' → timingMod=0 → 完全無感）

**新公式**（兩段）：

```js
// (1A) pitch-engine.js BatterJudgmentModule.decide 在 pCorrectRead 計算後
//      speedKmh 對誤判機率的影響（取代 velocityLock 對中位 batter 失效的問題）
const velReadAdj = clamp((speedKmh - 142) * pcoef.velReadCoef, -0.06, 0.06);
//                          ^^ CPBL 中位球速 142 km/h
pCorrectRead -= velReadAdj;     // 球速越快、判斷越容易出錯

// (1B) pitch-engine.js ContactResolutionModule.resolve 在 finalContactScore 計算
//      速球派直接扣 contact（不靠 stuff）
const velContactPenalty = clamp((speedKmh - 142) * pcoef.velContactCoef, -4, 10);
finalContactScore -= velContactPenalty;
```

建議係數：`velReadCoef = 0.005`、`velContactCoef = 0.18`。

**校準範圍**：
- velocity 50 → speedKmh ~142 → velReadAdj=0、velContactPenalty=0（基準）
- velocity 70 → speedKmh ~154 → velReadAdj=−0.06、velContactPenalty=−2.2
- velocity 90 → speedKmh ~166 → velReadAdj=+0.06、velContactPenalty=+4.3
- velocity 100 → speedKmh ~172 → velReadAdj=+0.06（clamp）、velContactPenalty=+5.4

**預期效果**：火球派投手（鋼龍、威能帝）K% +3-5pp，sim K9 R² 從 0.32 → 0.50。

**對打者的副作用**：velocity 通道是「全打者均勻扣分」，對打者間相對 R² 無影響、只會整體下調 contact baseline 1-2 分。可用 finalContactScore baseline 反向補 +1.5 抵消（如果跑 sim 發現整體 AVG 偏低 0.01-0.015）。

#### 通道 2：control → 精準度（兩段）

**現狀**：sigma 2.5～11.7 cm 被 spread 11-20 cm 淹沒、只進 stuff 0.15 權重

**新公式**（兩段）：

```js
// (2A) pitch-engine.js PitchPhysicsModule._sampleMissOffset 拉大訊號
//      舊：missRadius = clamp(36 - ctrl * 0.34, 3, 28)  → 差距 8.4 cm
//      新：missRadius = clamp(42 - ctrl * 0.45, 4, 38)  → 差距 18 cm
const missRadius = clamp(42 - ctrl * pcoef.ctrlSigmaCoef, 4, 38);
const sigma = missRadius / 2.2;
//   control 30 → missRadius 28.5、sigma 13.0（高離散）
//   control 70 → missRadius 10.5、sigma 4.8（中位）
//   control 90 → missRadius 1.5、sigma 0.7（神準，clamp 到 4 → sigma 1.8）
//   差距：13.0 - 1.8 = 11.2 cm，是原本 9 cm 的 1.25 倍 — 仍可能不夠
//   若不夠：可進一步用 ctrlSigmaCoef = 0.50（差距 14 cm）

// (2B) pitch-engine.js PitchPhysicsModule._getPitchMovementRange 反向修正
//      弱控變化球 spread 變大、強控變化球 spread 變小
const ctrlSpreadMul = clamp(1 + (70 - ctrl) * pcoef.ctrlSpreadCoef, 0.80, 1.35);
//   control 50 → ctrlSpreadMul 1.24（球路更亂）
//   control 70 → ctrlSpreadMul 1.00（基準）
//   control 90 → ctrlSpreadMul 0.76（球路更準）
// 在現有 spread 計算後乘上：
spread = clamp((movement - 45) * 0.45, 2, 24) * ctrlSpreadMul;
```

建議係數：`ctrlSigmaCoef = 0.45`、`ctrlSpreadCoef = 0.012`。

**預期效果**：
- 弱控投手（control 50）missOffset 變大 + 變化球 spread 變大 → BB9 大幅升高、wild pitch 機率自然出現
- 強控投手（control 90）相反 → 球落瞄準位置、變化球控在 edge → BB9 低、誘揮多
- 預期 control→BB9 R² 從 0.097 → 0.40+，是這次架構修法**最大收益點**

**對打者的副作用**：
- 弱控投手的球更常進壞球區 → 打者 chaseProb 公式被觸發更多次（advancedStats.plateDiscipline.chase 區）
- 但這與真實情境一致：對弱控投手就是會多選球
- 風險：若 spread 拉太大、所有變化球都偏到外角、打者揮空率反而下降（球根本沒進好球帶）
- 補救：先用 `ctrlSpreadCoef = 0.010`（更保守）跑 sim，看看是不是矯枉過正

#### 通道 3：breaking → 變化球揮空加成

**現狀**：breaking 進 spread（有效）+ 進 stuff 0.40 權重，但對「揮空率」的直接加成沒有獨立通道

**新公式**：

```js
// pitch-engine.js ContactResolutionModule.resolve 在 whiffProb 正規化前
const isBreakingPitch = pitchTypeCode && !['FF', 'SI'].includes(pitchTypeCode);
//                       ^^ 直球系（四縫、伸卡）不適用、其他全是變化球
if (isBreakingPitch) {
  const brkWhiffAdj = clamp((breaking - 70) * pcoef.brkWhiffCoef, -0.05, 0.12);
  whiffProb += brkWhiffAdj;
  // 同時相應降低 inPlayProb（保持 whiff+foul+inPlay=1）
  inPlayProb -= brkWhiffAdj * 0.6;
  foulProb   -= brkWhiffAdj * 0.4;
}
```

建議係數：`brkWhiffCoef = 0.0050`。

**校準範圍**：
- breaking 50 → brkWhiffAdj = −0.05（clamp）
- breaking 70 → brkWhiffAdj = 0
- breaking 90 → brkWhiffAdj = +0.10
- breaking 95（魔球師加成）→ brkWhiffAdj = +0.12（clamp）

**預期效果**：變化球派投手（黃子鵬、艾速特）變化球揮空率 +5-10pp。Whiffp R² 從 0.665 → 0.78。

**對打者的副作用**：低 contact 打者面對強變化球時揮空更頻繁，但這與真實情境一致。可能讓低 contact + 高 fly% 的打者（強拉派但 contact 中等）sim K% 略升。需要跑 sim 確認。

#### 通道 4：stuff → 保留 + 微調權重

**不動主公式**：`pitchStuffPenalty = clamp((stuffScore - 50) / 2.4, -8, 22)` 保留。

**配套微調**（§15.8 方案 B）：

```js
// pitch-engine.js ContactResolutionModule.resolve 在 plateDiscipline 混合段
const stuffMul = 1 + (stuffScore - 70) * pcoef.stuffMulCoef;  // 0.008
const pdW_adj  = clamp(pdW * stuffMul,            0.01, 0.95);
const pdI_adj  = clamp(pdI / Math.max(0.5, stuffMul), 0.01, 0.95);  // 反向：強投讓打者進場率↓
whiffProb  = whiffProb  * 0.70 + pdW_adj * 0.30;
inPlayProb = inPlayProb * 0.70 + pdI_adj * 0.30;
```

**理由**：v3.23 加的 plateDiscipline 30% 混合對打者校準成功、對投手 sim 失敗，因為 `pdW` 是該打者面對「所有投手」的平均。stuffMul 把強投對應的 pdW 放大、弱投縮小，等於告訴混合公式「這個打者面對的不是平均投手」。

**設計動機**：這條改動是 Wave A，要先做完才動 1/2/3。**因為它能解開 plateDiscipline 攤稀的瓶頸，後面通道才看得到 sim 上的訊號**。

#### 通道 5：stamina → sim-tester 累積（不動引擎）

**現狀**：引擎已就位（`getStaminaState(pitcher)` + `Player.consumeStamina(amount)`），但 sim-tester 每 PA 重設 pitcher.state.stamina → fatigue 永遠 0 → mods 不啟用。

**新做法**：在 sim-tester 投手批次模式內模擬「投球數累積」：

```js
// tools/sim-tester.html 投手批次 loop 內
let pitchCount = 0;
for (let pa = 0; pa < totalPA; pa++) {
  // 模擬一個 PA 平均 3.8 球
  const paPitches = 3 + Math.random() * 1.6;
  pitchCount += paPitches;
  // 每球消耗 0.85 base × decayMul（同 game.js:4938）
  const ceiling = pitcher.getStaminaCeiling();
  const decayMul = getStaminaState(pitcher).decayMul;
  pitcher.state.stamina = Math.max(0, ceiling - pitchCount * 0.85 * decayMul);
  // 接著走原本 sim PA 流程（這時 fatigue 會自動透過 getFatigueScore() 累積到 vel/ctrl/break mods）
}
```

**預期效果**：stamina 高的投手後段（pitch 70+）能力保持、stamina 低的崩盤。對「先發 vs 救援」的 sim 結果會分化。

**對打者的副作用**：無（sim batter 沒接體力累積邏輯）。

#### 通道 6：crisis → leverage 擴大

**現狀**（`game.js:4830`）：
```js
if (game.getCurrentRunners().some((r, idx) => r && idx >= 1)) {
  contact += ((batter.abilities.scoringPosition || contact) - 70) / 5;
  ctrl    += ((pitcher.abilities.crisis         || ctrl)    - 70) / 5;
}
```

**新公式**（壘上有人時三能力同時加）：
```js
if (game.getCurrentRunners().some((r, idx) => r && idx >= 1)) {
  contact += ((batter.abilities.scoringPosition || contact) - 70) / 5;
  const crisisAdj = (pitcher.abilities.crisis || 70) - 70;
  ctrl     += crisisAdj * pcoef.crisisCtrlCoef;      // 0.20（同舊 /5）
  breaking += crisisAdj * pcoef.crisisBreakCoef;     // 0.10（新）
  vel      += crisisAdj * pcoef.crisisVelCoef;       // 0.05（新，小幅）
  // 注意：vel/ctrl/break 改完，stuffScore 在後面才算（line 4779），所以會自動傳入
}
```

**預期效果**：crisis 95 投手 RISP_AVG_allowed 比 crisis 50 低 0.030-0.040。

**對打者的副作用**：與 scoringPosition 通道對稱。打者 scoringPosition 也是 /5 加 contact、強度相當。若兩邊都很強，相互抵消、回到 baseline。風險不大。

#### 通道 7：discipline → AI 瞄準

**現狀**：`resolvePitchAimCell` 是純靜態權重表，不讀 pitcher.abilities.discipline。

**新公式**（在 `resolvePitchAimCell` 內加入 discipline 修正）：

```js
function resolvePitchAimCell(game, battingTeam, plan, pitcher) {
  // 玩家投手保留拖拉介面，這條只影響對手 AI
  if (battingTeam === 'opponent') {
    return normalizeZoneIndex(game.pitchAimCell, 5, 12);
  }
  // 對手 AI 邏輯
  const disc = pitcher?.abilities?.discipline ?? 70;
  const edgeBias = clamp((disc - 70) / 40, -0.5, 0.6);
  //   disc 50 → edgeBias = -0.5（保守、瞄中央）
  //   disc 70 → edgeBias =  0   （基準）
  //   disc 85 → edgeBias = +0.38（敢瞄邊角）
  //   disc 95 → edgeBias = +0.60（誘揮專家）

  // 對 pickWeighted 的 weight 表動態調整
  let weights = getBaseWeights(plan);
  weights = weights.map(w => {
    const isEdge = isEdgeCell(w.value);     // index 1,3,5,9,15,19,21,23 等
    const isCenter = isCenterCell(w.value); // index 6,7,8,11,12,13,16,17,18
    if (isEdge)   return { ...w, weight: w.weight * (1 + edgeBias) };
    if (isCenter) return { ...w, weight: w.weight * (1 - edgeBias * 0.4) };
    return w;
  });
  return pickWeighted(weights);
}
```

需要新加兩個 helper：`isEdgeCell(idx)` / `isCenterCell(idx)`（基於 5×5 格的位置）。

**預期效果**：高 discipline 投手（捕手配球聰明 + 投手自己有 setup 能力）BB9 ↓ + K9 ↑（敢瞄 edge 但球進好球帶）。

**對打者的副作用**：高 discipline 投手對打者誘揮率↑、打者 chaseProb 通道被觸發更多次。這與真實情境一致——對「強投捕配球」就是會被多誘揮。

#### 通道 8：vsLeft / vsRight（投手）

**現狀**：`game.js:4813-4814` 只讀打者的 vsLeft/vsRight，**沒讀投手的**。

**新公式**（與打者 vsLeft 對稱）：

```js
// game.js resolveAtBatWithContext line 4814 後加
// 投手對左右打修正（對稱於打者 vsLeft / vsRight）
const pitcherVsBatHand = batter.bats === 'L'
  ? pitcher.abilities?.vsLeft
  : batter.bats === 'R'
    ? pitcher.abilities?.vsRight
    : null;
if (Number.isFinite(pitcherVsBatHand)) {
  const diff = pitcherVsBatHand - 70;
  contact -= diff * pcoef.pitcherVsBatContactCoef;   // 0.15（打者扣分）
  ctrl    += diff * pcoef.pitcherVsBatCtrlCoef;      // 0.10（投手 ctrl 加分）
}
```

**預期效果**：
- vsLeft 85 的左投對左打、打者 contact -2.3、投手 ctrl +1.5
- vsLeft 60 的左投對左打、打者 contact +1.5、投手 ctrl -1
- 「平台優勢」自然出現，sim 中左投對左打 K% +3-5pp

**對打者的副作用**：與打者 vsLeft（line 4813）對稱，**兩邊同時生效**。若打者 vsLeft 85 + 投手 vsLeft 85，相互抵消、回到 baseline。風險不大。

**注意**：`abilities.vsRight` 投手版需要 stat-mapper 補（目前 §1.5 投手公式只算 vsLeft）。可在 Wave C 落地前先補 stat-mapper：`vsRight = breaking × 0.45 + control × 0.35 + (右投 +8 / 左投 +2)`。

#### 通道 9：fielding → 不動

FieldingEngine 已用，不在 PitchEngine 範圍。

### 16.5 實作 Wave 順序

按「動工小、效益大、風險小」排序。每個 Wave 結束都要跑兩套 sim（打者 + 投手）確認。

| Wave | 內容 | 預估工時 | 主要修哪幾條通道 | 預期 R² 提升 |
|---|---|---:|---|---|
| **A** | §15.8 短期方案 B：`stuffMul` 動態化 | 1 小時 | 通道 4 配套 | Whiffp 0.67→0.78、K9 0.32→0.42 |
| **B** | 通道 1（velocity）+ 通道 2（control） | 3-4 小時 | 1, 2 | BB9 0.10→**0.40**、K9 0.42→0.55 |
| **C** | 通道 3（breaking）+ 通道 6（crisis）+ 通道 8（vsLeft/Right） | 2-3 小時 | 3, 6, 8 | Whiffp 0.78→0.80、H9 0.15→0.30 |
| **D** | 通道 7（AI 層） | 2 小時 | 7 | K9 0.55→0.60、BB9 0.40→0.50 |
| **E** | 通道 5（sim-tester 體力累積） | 1 小時 | 5 | 後段體力效應顯現、可分先發/救援 |

#### Wave 內細部步驟

每個 Wave 都遵循同一節奏：
1. **跑 baseline**：先用 v1.5.1 引擎跑一次打者 sim（99×1000 PA）+ 投手 sim（全合格投手 × 1000 PA），存 CSV 當對照組
2. **改動**：只動該 Wave 列出的程式碼
3. **跑兩套 sim**：打者 sim 確認沒崩、投手 sim 確認有改善
4. **判斷分支**：
   - 打者 R² 退步 > 0.02 → **回退**，係數縮一半再試
   - 投手 R² 沒提升 → 檢查通道是否真的接上（sim batter velocityLock？打者 advancedStats 有沒有觸發 30% 混合？）
   - 兩邊都過 → 進下一 Wave

**Wave A 細節**（最重要、修最大瓶頸）：
- 修檔：`pitch-engine.js:528-549`
- 新增 `game-params.js:pitcherChannels.stuffMulCoef = 0.008`
- 寫法見 §16.4 通道 4
- 驗證：強投手（魔神龍、艾速特）面對中位打者 Whiff% 從 ~20% → 25%+

**Wave B 細節**：
- 修檔：`pitch-engine.js:260-271`（_sampleMissOffset）、`pitch-engine.js:285-298`（_getPitchMovementRange）、`pitch-engine.js:340-394`（BatterJudgment）、`pitch-engine.js:494-518`（ContactResolution）
- 新增 `game-params.js:pitcherChannels.{velReadCoef, velContactCoef, ctrlSigmaCoef, ctrlSpreadCoef}`
- 驗證關鍵：弱控投手（黃恩賜、汪俊良）sim BB9 對齊真實 BB9（R² 從 0.097 拉到 0.40+）。火球派（鋼龍、威能帝）K9 +2-3pp。

**Wave C/D/E** 細節照表執行。

### 16.6 預期 R² 與驗證表

| 指標 | v1.5.1 baseline | Wave A 後 | Wave B 後 | Wave C 後 | Wave D+E 後 |
|---|---:|---:|---:|---:|---:|
| 投手 Whiffp R² | 0.665 | 0.78 | 0.78 | 0.80 | 0.80 |
| 投手 K9 R² | 0.32 | 0.42 | 0.55 | 0.58 | **0.62** |
| 投手 BB9 R² | 0.097 | 0.10 | **0.40** | 0.42 | **0.50** |
| 投手 H9 R² | 0.02 | 0.15 | 0.25 | **0.35** | 0.40 |
| 投手 WHIP R² | 0.001 | 0.10 | 0.25 | 0.35 | 0.45 |
| 打者 AVG R² | 0.26 | 0.25-0.27 | 0.25-0.27 | 0.25-0.27 | 0.25-0.27 |
| 打者 SLG R² | 0.43 | 0.42-0.44 | 0.42-0.44 | 0.42-0.44 | 0.42-0.44 |

**達標條件**：打者 R² 不退步 + 投手三大指標（K9/BB9/H9）R² 都 ≥ 0.35。

### 16.7 風險與回退條件

| Wave | 主要風險 | 偵測指標 | 回退做法 |
|---|---|---|---|
| A | stuffMul 拉強投對打者 whiff 過頭 → 打者 K% baseline 飆 | 打者 sim K% 上升 > 2pp | `stuffMulCoef 0.008 → 0.005` |
| B | velocity contact penalty 全聯盟 contact baseline 下移 → 打者 AVG R² 退步 | 打者 sim AVG 整體下降 > 0.015 | `velContactCoef 0.18 → 0.12`，或在 ContactResolution 補 +1.5 baseline |
| B | control sigma 拉太大、弱控投手球路全跑壞球區 → 打者 chaseProb 反向變高、揮空率反而下降 | 弱控投手 sim Whiff% 降低 | `ctrlSigmaCoef 0.45 → 0.40` 或 `ctrlSpreadCoef 0.012 → 0.010` |
| C | breaking 通道 + stuff 通道重複計算 → 強變化球投手 K9 過高 | 強投手 sim K9 > 真實 +2 | `brkWhiffCoef 0.0050 → 0.0035` |
| C | crisis 與打者 scoringPosition 兩邊都強 → RISP 統計失真 | sim RISP_AVG 偏離真實 ±0.030 | 兩邊係數同除 1.5 |
| D | AI 瞄準改寫造成 opponent 投球異常（球路全跑邊角） | sim 中 opponent BB9 飆 | 確認 isEdgeCell/isCenterCell 邏輯、edgeBias clamp 收緊 |
| E | sim-tester 體力累積讓救援投手前 1 局沒差但後續打不到 | 救援 sim 後段不正常 | 確認 paPitches 抽樣公式、ceiling 對 RP 設定 |

每 Wave 結束保留 baseline CSV，若回退仍無法達標，**停止該 Wave**、進下一 Wave、回頭再修。

### 16.8 與打者校準的相容性說明

四條設計約束中最關鍵的是「不傷打者校準」。本架構提案的設計策略：

1. **通道 1（velocity）整體下調 contact baseline**：對所有打者均勻、相對排序不變。若整體 sim AVG 下降，補正 baseline +1.5 即可。
2. **通道 2（control）影響投手球路位置**：打者只是被動接收，contact 公式不變。
3. **通道 3（breaking）只影響「變化球」這個小子集的 whiff**：對「打速球見長」的打者影響小、對「打變化球見長」的打者反向（這是真實的）。
4. **通道 6（crisis）與打者 scoringPosition 對稱**：兩條都 /5，相互抵消設計。
5. **通道 7（AI）只改 opponent 投球分布**：玩家投手保留拖拉介面、不變。
6. **通道 8（vsLeft/Right）與打者 vsLeft/Right 對稱**：強遇強、平衡。

**整體預期**：打者 sim R² 上下浮動 ±0.02 內、不退步。

**驗證方式**：
- 跑 baseline → 跑 Wave A → 跑 Wave B → ... 每步存 CSV
- 比對打者 AVG / OPS / K% / BB% 的全聯盟平均（level）+ 個別 R²（slope）
- level 變動 > 0.010 或 R² 變動 > 0.02 → 對應通道係數縮（依 §16.7 回退表）

### 16.9 規則書與底層邏輯書同步清單

實作完成後需同步以下文件（一起改、避免散落）：

- **規則書 §17.4 / §17.6 / §17.8**：補上「投手能力 → 引擎影響」的對稱表
- **規則書新增 §17.15**：「投手 9 通道對稱化架構（v4.1）」
- **底層邏輯書 §2.7**：把 buff/debuff 鏈擴展為「投手 9 通道」的對應表（現有 §2.7.1-2.7.4 主要寫打者）
- **底層邏輯書 §4.3**：修改數值平衡入口指南新增 `game-params.js pitcherChannels`
- **底層邏輯書 §4.4**：把 §15.7 投手通道擠 stuff 那條 issue 從 issue tracker 移除

### 16.10 給未來接手者的 quick reference

| 你想做什麼 | 看哪段 |
|---|---|
| 從頭理解問題鏈 | §15.4-15.7 → §16.1 |
| 看每條通道公式 | §16.4 |
| 看實作順序 | §16.5 |
| 看 Wave 預期 R² | §16.6 |
| 看回退條件 | §16.7 |
| 動之前必讀（不傷打者） | §16.8 |
| 動之後要同步什麼文件 | §16.9 |

### 16.11 不要做的事（從 §15.9 延伸）

- **不要動 stat-mapper 投手公式**（§15.1 已完成、再動沒意義）
- **不要一次動多條通道**（失敗難歸因，§14.4 教訓）
- **不要動 EV 線性係數壓頂級**（§13.1 試過、NET 失敗）
- **不要用 99×300 PA 的 corr/R² 判斷成敗**（§14.4 雜訊主導）→ 必須跑 99×1000 PA 或固定 seed
- **不要把 sim-tester aim 邏輯保留**（應該搬進引擎，§15.9 + 通道 7）
- **不要在通道 7 改玩家投手**（玩家保留拖拉介面，AI 改寫只動 opponent 分支）

### 16.12 待動清單（取代 §15.10）

#### Wave A — §15.8 短期落地　✅ 已落地（2026-05-30，見 §16.13）
- [x] 新增 `game-params.js:pitcherChannels.stuffMulCoef = 0.008`（Section 10）
- [x] 改 `pitch-engine.js`（plateDiscipline 30% 混合段，現 line ~558-568）：加 stuffMul，pdW×stuffMul / pdI÷stuffMul
- [x] 跑打者 sim 99×1000 PA → R² 不退步（AVG 0.528→0.544、SLG 0.576→0.582、mean K% +0.22pp）
- [x] 跑投手 sim → Whiffp R² 0.706→0.774（達標）、K9 0.291→0.384、BB9 0.061→0.146
- [x] 記錄 baseline JSON（`tools/_baseline-{batter,pitcher}-preWaveA.json` + `_postWaveA-*.json`）

#### Wave B — velocity + control 通道　✅ 已落地（2026-05-30，見 §16.14 / §16.14.1）
- [x] ~~missOffset/spread 路線~~ → **改道**：control→BB9 真正機制是 aim 壞球率，非投球落點（§16.14）
- [x] control 通道：`controlBallRateMul()` 單一來源 + game.js/sim-tester 兩處 aim 套用 → BB9 R² 0.146→0.566
- [x] velocity 通道：`velContactPenalty`（樞紐 velocity=70）→ K9 R² 0.415→0.520
- [x] 跑兩套 sim：投手 K9 0.52 / BB9 ~0.53 達標；打者 AVG/SLG/mean 雜訊內不退步
- [x] 打者 baseline 未跌（重跑確認首跑低數字為雜訊），不需補 +1.5 baseline
- [n/a] velReadCoef(1A)、ctrlSigmaCoef/ctrlSpreadCoef（missOffset 路線已棄）

#### Wave C — breaking + crisis + vsLeft/Right 通道　⚠️ 部分（2026-05-30，見 §16.15）
- [x] ~~通道 3 breaking whiff~~ → **退回**：breaking 與 stuff 共線、R² 不升只墊高 K 率（§16.15 C-1）
- [x] 通道 6 crisis：`game.js` 得點圈段擴大到 ctrl/break/vel（+ game-params crisisCtrl/Break/VelCoef）。browser smoke 過。⚠️ sim 量不到（不模擬跑者）
- [~] 通道 8 vsLeft/Right：**暫緩**——需 stat-mapper 補 vsRight，與 §16.11「不要動 stat-mapper」衝突，待使用者定
- [x] 跑兩套 sim：通道 3 退回後投手 sim 回 Wave B 終態（K9 0.52/BB9 0.51）；打者未崩
- [→] **H9/WHIP 真正修法 = 新「命中抑制通道」（建議 Wave F，碰 InPlayOutputModule，獨立謹慎做）**

#### Wave D — AI 瞄準通道
- [ ] 改 `game.js:4270 resolvePitchAimCell`：接 pitcher.abilities.discipline + edgeBias 邏輯
- [ ] 新增 helper `isEdgeCell(idx)` / `isCenterCell(idx)`
- [ ] 確認玩家投手仍走拖拉 UI（battingTeam === 'opponent' branch 不改）
- [ ] 跑兩套 sim

#### Wave E — sim-tester 體力累積
- [ ] 改 `tools/sim-tester.html` 投手批次模式：加 pitchCount 累積 + stamina 衰減
- [ ] 驗證救援投手後段體力效應
- [ ] 確認對打者 sim 無影響（打者跑 vs 中位投手、不應觸發此邏輯）

#### 收尾
- [ ] 同步規則書 §17 + §17.15（新增）
- [ ] 同步底層邏輯書 §2.7 + §4.3 + §4.4
- [ ] 更新 PROJECT_OVERVIEW.md「目前建議接手順序」
- [ ] 移除 §15.7 投手擠 stuff issue（已解）
- [ ] 把 §15.10 / §16.12 待動清單清空、改為「已落地（v4.1）」

---

## 16.13 Wave A 落地紀錄（2026-05-30）

**改動（單一參數，符合「不堆參數」工作流）：**
1. `game-params.js` Section 10 新增 `pitcherChannels.stuffMulCoef = 0.008`。
2. `pitch-engine.js`：
   - 模組頂部加 `PITCHER_CHANNELS`（內建預設＝校準來源；`GAME_PARAMS.pitcherChannels` 若載入則覆寫。⚠️ sim-tester.html 不載 game-params，故兩處數值須一致）。
   - `ContactResolutionModule.resolve` plateDiscipline 30% 混合段：`stuffMul = 1 + (stuffScore-70)×0.008`，`pdW_adj = clamp(pdW×stuffMul, .01, .95)`、`pdI_adj = clamp(pdI÷max(.5,stuffMul), .01, .95)`、`pdF_adj` 重算。

**新增工具：** `tools/run-pitcher-baseline.js`（比照 run-batter-baseline.js，無頭驅動 sim-tester.html Tab C，算 K9/BB9/H9/HR9/WHIP/Whiffp R²）。

**驗證（99/106 × 1000 PA，本機 Chrome via CDP，無 seed）：**

| 指標 | pre-Wave-A | post-Wave-A | §16.6 目標 | 判定 |
|---|---:|---:|---:|---|
| 投手 Whiffp R² | 0.706 | **0.774** | 0.78 | ✅ 達標 |
| 投手 K9 R² | 0.291 | **0.384** | 0.42 | ✅ 大幅升、略低目標 |
| 投手 BB9 R² | 0.061 | **0.146** | 0.10 | ✅ 翻倍 |
| 投手 H9 R² | 0.022 | 0.000 | 0.15 | ⏳ 待 Wave B（control 通道） |
| 投手 HR9 / WHIP R² | 0.080 / 0.000 | 0.003 / 0.000 | — | ⏳ 雜訊，待 Wave B/C |
| 打者 AVG R² | 0.528 | 0.544 | 不退步 | ✅ |
| 打者 SLG R² | 0.576 | 0.582 | 不退步 | ✅ |
| 打者 mean K% | 20.63pp | 20.85pp | 升 <2pp | ✅ +0.22pp |
| 打者 mean AVG | sim .254 | sim .255 | 不退步 | ✅ |

**結論：** Wave A 解開「打者 PA 平均攤稀投手強弱訊號」的瓶頸，whiff 路徑（Whiffp/K9/BB9）顯著改善、打者零傷害（batter sim 走中位投手 鈴木駿輔 stuff≈70 → stuffMul≈1.0，故不受影響——設計如預期）。H9/WHIP 仍趴地，符合預期（需 Wave B 的 control 精準度通道才會動）。Wave A 保留。

**下一步：Wave B（velocity 通道 + control 通道）**，§16.4 通道 1/2、§16.12 Wave B 清單。預期 BB9 0.146→0.40、K9 0.384→0.55。⚠️ Wave B 是對打者風險最高的一波（velocity contact penalty 會整體下移 contact baseline），動前重跑 baseline、動後嚴格比對打者 AVG mean（§16.7 回退表）。

> baseline 對照組 JSON 已存 `tools/_baseline-{batter,pitcher}-preWaveA.json` 與 `_postWaveA-*.json`。Wave B 的 pre-baseline = 本次 post-Wave-A。

---

## 16.14 Wave B control→BB9 通道（2026-05-30）— §16 通道 2 改道成功

### 失敗路線（已退回，不要再試）：missOffset/spread 放大（§16.4 通道 2 原設計）
- 改 `_sampleMissOffset` missRadius 公式（42-ctrl×0.45）+ `_getPitchMovementRange` spread×ctrlSpreadMul。
- 結果 **NET 失敗**：投手 BB9 R² 反崩 **0.146→0.012**、K9 0.384→0.283。
- **根因診斷（逐球員資料）**：弱控投手 sim BB9 沒系統性上升、強控反而上升，sim BB9 離散度 SD 僅 0.42（真實 0.99）。
- **架構級發現**：**這個引擎的保送不是由投球落點（missOffset）決定的**。保送 = 投球瞄準格落在「好球帶外」（aim cell ∈ outEdge/chase）+ 打者沒追打。而 aim 函式（sim-tester `resolvePitchAimCellGameLike`、game.js `resolvePitchAimCell`）的**壞球率與控球無關**（前者 aggression 只重分配好球帶內部、outEdge/chase 鎖死；後者連 control 參數都沒有）。所以 control→BB9 訊號根本進不了引擎。

### 成功路線（已落地）：壞球率隨控球變、樞紐 control=70
- **單一來源** `pitch-engine.js controlBallRateMul(control)`：`mul = clamp(1 - (ctrl-70)×ctrlBallRateCoef, 0.40, 1.60)`，`ctrlBallRateCoef=0.012`（control 70→×1.0、40→×1.36、100→×0.64）。樞紐 70 保住聯盟平均 BB9 不塌陷（避開 sim-tester 註解記載的 v2 塌陷教訓）。
- **兩處 aim 函式都呼叫它**（方案 A，使用者選定）：
  - `tools/sim-tester.html resolvePitchAimCellGameLike`：outEdge/chase 權重 ×mul。
  - `game.js resolvePitchAimCell`：加 `control` 參數（呼叫點傳 `ctrl`），好球帶外格子（非 inner 3×3）權重 ×mul；waste 計畫不套（故意投壞球）。
- 係數同步 `game-params.js pitcherChannels.ctrlBallRateCoef` + `pitch-engine.js PITCHER_CHANNELS`（sim-tester 不載 game-params，引擎內建為準）。

### 驗證（1000 PA）
| 指標 | post-Wave-A (pre-B) | post-Wave-B | §16 目標 | 判定 |
|---|---:|---:|---:|---|
| 投手 **BB9** R² | 0.146 | **0.566** | 0.40 | ✅ 超標 |
| 投手 **WHIP** R² | 0.000 | **0.141** | — | ✅ 附帶（BB 是成分） |
| 投手 K9 R² | 0.384 | 0.415 | 0.55 | ⏳ 待 B-2 velocity |
| 投手 Whiffp R² | 0.774 | 0.727 | — | 雜訊內 |
| 投手 mean BB9 | sim 2.04 | sim 2.11 | 不塌陷（真實 2.75）| ✅ |
| 打者 AVG R² | 0.544 | 0.504 / 重跑 0.539 | 不退步 | ✅ 雜訊內 |
| 打者 SLG R² | 0.582 | 0.529 / 重跑 0.554 | 不退步 | ✅ 雜訊內 |
| 打者 mean AVG | sim .255 | sim .251 / .253 | 不退步 | ✅ |

**教訓（寫給未來接手）**：§16 的「通道」設計是**假設**，落地前未驗證引擎實際機制。通道 2 假設「control→missOffset→出好球帶→保送」，但實機保送由 **aim 瞄準格**決定，missOffset 只是次級擾動。**動引擎通道前，先確認該 outcome 在引擎裡到底由哪條路徑產生**（用逐球員 sim 資料反推，不要照抄設計文件）。

> baseline JSON：`_postWaveA-*`（=pre-B）、`_postWaveB-*`、`_postWaveB-batter-rerun`。B-2 velocity 的 pre = post-Wave-B(control)。

### 16.14.1 Wave B-2 velocity 通道（通道 1）— 已落地
- **改道**：§16 原設計用 speedKmh 樞紐 142km/h（=velocity 50），但本引擎能力平均=70 → 多數投手 >142 → 會整體下壓聯盟 AVG。**改用 velocity 能力樞紐 70**（平均投手 0 penalty，與 control 通道一致），避免聯盟位移。
- **落地**：`pitchState` 加 `pitcherVelocity`；`ContactResolutionModule.resolve` 加 `velContactPenalty = clamp((velocity-70)×velContactCoef, -10, 4)`，從 finalContactScore 扣除（快速球派 contact↓→whiff↑→K9↑）。係數 `velContactCoef=0.18`（PITCHER_CHANNELS + game-params）。只動 1B（contact），未動 1A(pCorrectRead)——1B 已足夠。
- **驗證（1000 PA）**：
  | 指標 | post-B(control) | post-B2 | §16 目標 | 判定 |
  |---|---:|---:|---:|---|
  | 投手 K9 R² | 0.415 | **0.520** | 0.55 | ✅ 逼近 |
  | 投手 Whiffp R² | 0.727 | 0.759 | — | ✅ |
  | 投手 BB9 R² | 0.566 | 0.494 | 0.40 | ✅（雜訊，兩次均~0.53）|
  | 打者 AVG R² | ~0.52 | 0.510 / 重跑 0.555 | 不退步 | ✅ 雜訊內 |
  | 打者 SLG R² | ~0.54 | 0.471 / 重跑 0.579 | 不退步 | ✅ 雜訊內（首跑 0.471 是雜訊）|
  | 打者 mean AVG | sim .25 | .247 / 重跑 .252 | 不退步 | ✅ |
- **教訓**：首跑打者 SLG R² 0.471 / mean AVG .247 看似 B-2 壓到打者，重跑回 0.579 / .252 證明是 run-to-run 雜訊。**單跑數字會騙人，R² 退步必須重跑確認再決定退係數**（呼應 §14.4）。

### 16.14.2 Wave B 總結（A+B 兩波合計，2026-05-30）
| 指標 | pre-Wave-A | post-Wave-B（A+B 合計）| §16.6 終極目標 |
|---|---:|---:|---:|
| 投手 K9 R² | 0.291 | **0.520** | 0.62 |
| 投手 BB9 R² | 0.061 | **~0.53**（0.566/0.494）| 0.50 ✅ |
| 投手 Whiffp R² | 0.706 | **0.759** | 0.80 |
| 投手 WHIP R² | 0.000 | **~0.10**（0.141/0.066）| 0.45 |
| 投手 H9 R² | 0.022 | ~0.05 | 0.40 |
| 打者 AVG / SLG R² | 0.528 / 0.576 | ~0.54 / ~0.56 | 不退步 ✅ |

**剩餘缺口**：H9 / HR9 / WHIP 仍偏低 → 命中抑制（被安打）尚無強通道，待 **Wave C**（通道 3 breaking 變化球揮空 + 通道 6 crisis + 通道 8 vsLeft/Right）與更長期的 BABIP_allowed 通道。K9/BB9（投手兩大主指標）已達標。

**Wave B 落地清單**：`game-params.js pitcherChannels` = {stuffMulCoef, ctrlBallRateCoef, velContactCoef}；`pitch-engine.js` PITCHER_CHANNELS 同步 + controlBallRateMul()(export) + pitcherVelocity(pitchState) + resolve velContactPenalty；`game.js` resolvePitchAimCell(+control 參數、好球帶外×ballMul、呼叫點傳 ctrl)；`sim-tester.html` resolvePitchAimCellGameLike(呼叫 controlBallRateMul)。新工具 `tools/run-pitcher-baseline.js`。

---

## 16.15 Wave C（2026-05-30）— 通道 3 退回、通道 6 落地、通道 8 暫緩

使用者選「先做通道 3 + 通道 6，vsRight(通道 8) 跳過避開 stat-mapper 衝突」。

### C-1 通道 3（breaking → 變化球揮空）— 已試並退回
- **做法**：pitchState 加 pitcherBreaking；resolve() 對非 FF/SI 球種加 `brkWhiffAdj=clamp((breaking-70)×0.0050, -0.05, 0.12)` 到 whiffProb。
- **結果 NET 失敗**：Whiffp R² 0.759→0.752、K9 R² 0.520→0.514（**持平**），但 **mean K9 sim 7.38→7.74 過頭惡化**（真實 6.47）、H9 R² 沒動（目標落空）、打者 mean AVG .252→.246。
- **根因**：**breaking 與 stuff 共線**（幾乎所有投手都投變化球、breaking 又與 stuff 相關），加揮空不分投手強弱（R² 不升）只整體墊高 K 率。正是 §16.7 自己預警的「breaking 通道與 stuff 通道重複計算」。**退回。**

### C-2 通道 6（crisis → leverage 擴大）— 已落地（game.js，sim 量不到）
- **做法**：`game.js` 得點圈加成段，crisis 從只加 ctrl(/5) 擴大到同時加 ctrl(×0.20=舊/5,相容)/breaking(×0.10)/vel(×0.05)，壘上有人才生效，與打者 scoringPosition 對稱。係數在 `game-params.js pitcherChannels.{crisisCtrlCoef,crisisBreakCoef,crisisVelCoef}`。
- **驗證**：browser smoke test 通過（真實遊戲正常）。⚠️ **sim-tester 不模擬壘上跑者 → 此通道在投手 sim 完全 inert、量不到 R²**。屬真實對局的 RISP 表現改進，非 sim 可校準項。

### Wave C 結論：H9/WHIP 的命中抑制需要「新通道」，不在 §16 既有設計內
- 通道 3（breaking）共線無效、通道 6（crisis）不可量測、通道 8（vsRight）有 stat-mapper 依賴衝突暫緩。
- **H9 R²≈0.02 卡關的真因**：sim 的被安打（BABIP）主要由打者 + 守備引擎決定，**投手側沒有「擊球品質壓制」的獨立通道**（stuff 只進 contactQuality，影響 whiff 多於 in-play 的落點品質）。
- **未來真正的 H9 修法（建議當新 §17 通道）**：投手 stuff/control → 壓低**進場球的擊球品質**（exit velo / 理想接觸點機率），讓強投手的被安打 BABIP 真的較低。這要碰 `InPlayOutputModule` 的 EV/launch 生成，屬「動打者已校準的 in-play 物理」——風險高、需重跑打者 baseline，**建議獨立一波（Wave F）謹慎處理**，不要併進 Wave C 隨手做。

### Wave C 最終狀態（投手 sim，與 Wave B 終態相同——通道 3 已退、通道 6 不影響 sim）
| 指標 | pre-Wave-A | 現在（A+B+C） | §16.6 終極目標 |
|---|---:|---:|---:|
| 投手 K9 R² | 0.291 | **~0.52** | 0.62 |
| 投手 BB9 R² | 0.061 | **~0.53** | 0.50 ✅ |
| 投手 Whiffp R² | 0.706 | **~0.73** | 0.80 |
| 投手 H9 / WHIP R² | 0.02 / 0.00 | ~0.02 / ~0.08 | 0.40 / 0.45（待 Wave F 命中抑制通道）|

**已達標**：K9、BB9（投手兩大主指標，從趴地到 0.5+）。**未達標**：H9/WHIP/Whiffp（需 Wave F 新通道，已寫明方向）。
