# 能力值體系檢討書 v1

版本：v1.4 (含 v1.5 工作清單)
日期：2026-05-26
目的：診斷現行 abilities 公式對真實能力的失真問題，提出以「過程指標」取代「結果指標」的修正方案

> **v1.1（同日追加 §4）**：§2.1 contact 建議公式（揮空率主導 75%）在實作 + sim-tester 驗證後出現「強打高 K% 群嚴重低估」的反轉失真，已在 `tools/stat-mapper.html` 落實修正公式（AVG + OPS+ anchor 主導 65%、揮空率縮為 35%），詳見 §4。
>
> **v1.2（同日追加 §5/§6/§7）**：v1.1 contact 修正後使用者反映「聯盟最強的 abilities 仍只到 65-72，沒到 80-90」。改 stat-mapper piecewise 標尺為 lo=40/mid=60/hi=90、pw clamp [35,95]，跑 sim-tester 第二輪驗證後出現「U 型誤差曲線」失衡（頂級炸彈過頭、中強打被埋沒、弱打仍高估）。**§5** 紀錄 v1.2 驗證結果與根因；**§6** 應使用者點名做引擎物理 audit（velocityHandlingPenalty / 飛行物理 / launchAngle / 全壘打牆 / 模擬器時間 vs 機率混合模式 / routineAirBonus 階梯效應）；**§7** 討論「能力值平移」三方案，推薦顯示/引擎尺度分離的方案 B 為長期架構修正。
>
> **v1.3（2026-05-26，§8）**：重新 audit 引擎內 anchor，發現引擎普遍假設「平均球員 = 70」（contactQuality (X-45)/50、handleChance (fielding-75)/720、fallback=70），但 v1.0~v1.2 stat-mapper 把 CPBL 平均算到 60，整個聯盟被當成「低於平均」處理。v1.3 採「**A+B+F 小集合**」改動：A) piecewise 標尺對齊引擎 anchor（lo=50/mid=70/hi=90、pw [40, 99]）；B) power piecewise 改用 CPBL EV 實測區間（125/143/155）；F) reachChance routineAirBonus 平滑階梯。velocityHandlingPenalty 時間制改造延後到 v1.4 視結果再動。詳見 **§8**。
>
> **v1.4（2026-05-26，§9-§10）**：v1.3 sim 驗證後出現新失衡（頂級炸彈 HR% 10%、中強打仍 sim AVG -.10），追查發現**CPBL JSON summary 區塊有合計 bug** — Colab 管線的 `get_slice_data()` 用 `sums[-1]` 只取最後一季 summary，但 advancedStats/ballQuality 有正確合計，造成同球員的 contact 與 power 基於不同樣本量混合。修 Colab 管線（`merge_summaries_weighted()` 加權合計 + `MIN_PA_FOR_MULTI_SEASON_INCLUSION = 30`）後重跑：**中強打 sim AVG 大幅改善**（林立 .239→.299、吳念庭 .206→.246、胡金龍 .236→.293），廖健富完美對齊（sim .264 vs 真實 .271）。**§9** 紀錄資料 bug 修正細節，**§10** 紀錄 v1.4 sim 結果與**完整未完成工作清單**（含 P0 三大殘留問題 #1 B' / #2 G / #3 降混合權重，P1-P5 共 16 項待辦）。下一輪建議從 P0 #1 開始。

## 0. 核心問題

使用者點名的問題（以選球為例）：

> 目前 discipline 用 `0.5×BB% + 0.5×inv(K%)` 推算。但 BB% 是「結果」、K% 又跟 contact 能力混在一起。**真正反映選球能力的應該是「追打壞球率（chase rate）」與「好球出棒率（zone swing rate）」**。

這是個系統性問題，不只 discipline：

| 能力 | 現行公式用什麼 | 那是什麼性質 |
|---|---|---|
| contact | AVG + OPS+ | 結果（含 BABIP 運氣 + power）|
| power | ISO + HR rate | 結果（部分重疊）|
| speed | SB | 結果（受意願影響）|
| discipline | BB% + K% | 結果（K% 還混了 contact）|
| velocity（投手）| K% + K/9 | 結果（不是球速）|
| control（投手）| BB% + WHIP | 結果（WHIP 含被打的安打）|
| breaking（投手）| ERA+ + FIP | **跟變化球完全無關** |
| clutch | OPS+ | 結果（不分情境）|
| vsLeft / vsRight | contact × 0.55 + ... | 推算（不用真實 splits）|

**結果指標**告訴你「這人最後表現如何」；**過程指標**告訴你「這人是怎麼造成這個結果的」。要建一個能在引擎中模擬投打對決的能力值體系，**必須用過程指標**——因為引擎本身就是在模擬過程。

好消息：rebas 抓回來的 JSON 裡，過程指標的料**完全夠**。

## 1. CPBL JSON 真正可用的欄位盤點

`cpbl_players_2024-2026.json` 每位球員的 `seasons.{year}` 都有以下欄位（這是底層邏輯書沒展開的細節）：

### 1.1 打者可用欄位

```
summary
  PA / games / AVG / OBP / SLG / OPS / OPSplus / wOBA / ISO / BABIP
  BBp / Kp / Whiffp / GBp / BIPp / SBp / BBK
  RC / WPA / PosWPA / NegWPA / RE24 / PH

plateDiscipline                      ← 四區選球資料（核心！）
  core / edge / chase / invalid
    .pitches  .swingRate  .whiffRate  .foulRate  .inPlayRate  .avgEV

swingByCount                          ← 12 種球數的攻擊傾向
  "0-0" ... "3-2"
    .pitches  .swingRate  .chaseRate  .contactRate

timing
  fastLatePct  fastEarlyPct  slowLatePct  slowEarlyPct

battedBall                            ← 擊球型態
  GB / LD / FB / PU

spray                                 ← 拉/推打傾向
  left / center / right

situational                           ← 情境表現
  RISP_AVG  highLI_AVG  pinch_AVG  lateClose_AVG

splits                                ← 左右投對戰（重要！）
  vsLHP: { pa, AVG, HR }
  vsRHP: { pa, AVG, HR }
  ballQuality: { avgEV, maxEV, inPlay }
  highlights: { home_runs[], top_ev[], clutch_pas[] }

heatMap                               ← 3×3 好球帶熱區
  "0,0" ... "2,2": { contactMod, powerMod, eyeMod }

pitchTypeMatchup                      ← 對各球種表現
  FF/SI/FC/SL/CU/CH/FS:
    .seen  .swingRate  .whiffRate  .foulRate  .inPlayRate  .avgEV

foulSplit
  preTwoStrikeRate  twoStrikeRate
```

### 1.2 投手可用欄位

```
summary
  AVG / OBP / SLG / OPSplus / BABIP（被打擊指標）
  BBp / Kp / Whiffp / GBp / SBp
  ERA / ERAplus / FIP / WHIP / SO9 / BB9 / H9 / HR9
  IPOut / BF / WPA / PosWPA / NegWPA

velocity                              ← 真實球速！（現行公式沒在用！）
  avg  max

rpm                                   ← 轉速！
  avg

evAllowed
  avg                                 ← 被擊出 EV

zoneProfile                           ← 各區 whiff（投手版）
  [{ zone: 'core'/'edge'/'chase'/'invalid', pitches, whiffRate }, ...]

pitchArsenal                          ← 球種武器庫（重要！）
  FF / SI / FC / SL / CU / CH / FS:
    .pitchCount  .usageRate  .whiffRate  .foulRate  .inPlayRate  .avgEVAllowed

pitchSequenceModel                    ← 配球傾向

splits                                ← 對左右打者表現
  vsLHB: { bf, kRate }
  vsRHB: { bf, kRate }
```

**現行 stat-mapper 用到的只是 summary 的一小撮欄位。** 大量過程指標被存進 advancedStats 給引擎用，但 abilities 計算本身完全沒讀。這是改進空間最大的地方。

## 2. 打者九大能力 — 逐項診斷與改進方案

### 2.1 contact（巧打）

**現行公式**
```js
c1 = piecewise(AVG, 0.230, 0.275, 0.340)
c2 = piecewise(OPS+, 65, 100, 165)
contact = pw(c1 × 0.65 + c2 × 0.35)
```

**問題**

| # | 問題 | 後果 |
|---|---|---|
| 1 | AVG 嚴重受 BABIP 運氣影響 | 同一個 contact 能力可能因 BABIP 差 0.05 而 contact 差 7 分 |
| 2 | OPS+ 包含 ISO（長打率），其實是 power 的一部分 | contact 與 power 高度相關，無法分離 |
| 3 | 沒用 Whiffp（揮空率） | Whiffp 才是「打到球的能力」最直接的指標 |
| 4 | 沒用 plateDiscipline.core.whiffRate（好球帶內揮空率） | 好球帶內揮空率是 contact 能力的關鍵 |

**建議公式**

```js
// 主指標：好球帶內接觸能力（最直接）
c1 = piecewiseInv(plateDiscipline.core.whiffRate, 0.15, 0.08, 0.03)
// 次指標：整體揮空率
c2 = piecewiseInv(Whiffp, 25, 15, 7)
// 結果指標（保留但降低權重）：BABIP 中和後的 AVG
adjAVG = AVG × (0.300 / max(BABIP, 0.220))   // BABIP 標準化到 .300
c3 = piecewise(adjAVG, 0.230, 0.275, 0.340)

contact = pw(c1 × 0.45 + c2 × 0.30 + c3 × 0.25)
```

**改進說明**

- 主指標換成 `plateDiscipline.core.whiffRate`（好球帶內揮空率）。這直接量化「球進好球帶你能不能打到」，是 contact 的物理意義。
- Whiffp 是全區揮空，作為次指標。
- AVG 仍保留，但做 BABIP normalization（除以個人 BABIP）抵消運氣。權重從 0.65 → 0.25。
- 公式回到「能力 → 結果」而不是「結果 → 倒推能力」。

### 2.2 power（長打）

**現行公式**
```js
p1 = piecewise(ISO, 0.040, 0.110, 0.230)
p2 = piecewise(HR, 0, 8, 28)
power = pw(p1 × 0.60 + p2 × 0.40)
```

**問題**

| # | 問題 | 後果 |
|---|---|---|
| 1 | ISO 和 HR rate 高度重疊（HR 是 ISO 的主要組成） | 等於 0.60 + 0.40 都在算 HR |
| 2 | 沒用 avgEV / maxEV（真實擊球初速） | EV 才是 power 的物理意義 |
| 3 | 沒用 plateDiscipline.core.avgEV（核心區擊球品質） | 沒用真實擊球品質 |
| 4 | 沒用 ballQuality.maxEV | 沒看球員的 power 天花板 |

**建議公式**

```js
// 主指標：好球帶內平均擊球初速
p1 = piecewise(plateDiscipline.core.avgEV, 110, 130, 150)   // km/h
// 次指標：最大 EV（power 天花板）
p2 = piecewise(ballQuality.maxEV, 140, 155, 170)
// 結果指標：ISO
p3 = piecewise(ISO, 0.040, 0.110, 0.230)
// HR rate（次要）
p4 = piecewise(HR / PA × 600, 0, 8, 28)

power = pw(p1 × 0.40 + p2 × 0.20 + p3 × 0.25 + p4 × 0.15)
```

**改進說明**

- 主指標換成 `plateDiscipline.core.avgEV`。這是物理上 power 最準的代理。Issue：core.avgEV 在 JSON 中是 km/h，門檻設 110/130/150 對應 CPBL 觀察（130 是中位、150 是頂級）。
- maxEV 反映「上限值」——有些球員 avgEV 一般但 maxEV 驚人（炮管型）。
- ISO 和 HR 從主指標降為輔助指標（總權重 0.40）。

**注意**：如果 maxEV 缺失（部分球員為 0），需 fallback。Highlights.top_ev 可作為替代來源。

### 2.3 speed（速度）

**現行公式**
```js
spd = piecewise(SB, 0, 12, 45)
posBonus = CF +7 / SS/OF +4 / 2B/3B +2 / 0
speed = pw(spd + posBonus)
```

**問題**

| # | 問題 | 後果 |
|---|---|---|
| 1 | SB（成功盜壘數）受意願影響 | 不偷壘的快腿被低估（如吳念庭、林子偉早期）|
| 2 | 沒考慮 SBp（盜壘成功率） | 高量低效率的 SB 跟低量高效率的 SB 同分 |
| 3 | 沒考慮 3B（三壘安打數） | 三壘安打是腳程最純的指標（球同樣強，腳程快才上得了三壘）|
| 4 | 守位加成過粗 | 同樣守 SS 的不同球員速度可能差 20 分 |

**建議公式**

```js
// 主指標：盜壘嘗試率（SB / opp）+ 成功率
sbAttemptRate = (SB + CS) / (1B + BB)        // 上壘後試圖盜的比率
s1 = piecewise(sbAttemptRate × 100, 5, 15, 35)   // % 形式
s2 = piecewise(SBp / 100, 0.55, 0.72, 0.85)
// 三壘安打率（純腳程指標）
s3 = piecewise(3B / PA × 600, 0, 2, 7)
// 守位 baseline（防止 DH/1B 被高估）
posBaseline = CF 55 / OF 52 / SS 50 / 2B 48 / 3B 47 / 1B 42 / C 40 / DH 45

speed = pw(s1 × 0.40 + s2 × 0.20 + s3 × 0.25 + posBaseline × 0.15)
```

**改進說明**

- 用 attempt rate 而不是 SB 絕對數，解掉「不偷壘的快腿」問題。
- SBp 衡量效率（光快不夠，要會跑）。
- 3B rate 是「跑壘能力」的純樣本（甚至比 SB 更純，因為 SB 還受對方捕手影響）。
- 守位從加成改為 baseline 混入（更柔性），避免 DH 被算得太低。

### 2.4 discipline（選球）— **使用者點名項**

**現行公式**
```js
e1 = piecewise(BBp, 3.0, 7.5, 14.0)
e2 = piecewiseInv(Kp, 30.0, 19.0, 9.0)
discipline = pw(e1 × 0.50 + e2 × 0.50)
```

**問題**

| # | 問題 | 後果 |
|---|---|---|
| 1 | BB% 是結果（投手投壞球 + 打者選到 + 沒揮的綜合） | 高 BB% 可能是因為打者讓投手不敢投好球（power 高），跟選球無關 |
| 2 | K% 嚴重受 contact 能力污染 | 林泓弦這種低 contact 球員 K% 偏高，但選球可能不差 |
| 3 | **完全沒用 chase rate / zone swing rate**（用戶點名） | 真正的選球能力指標沒進入計算 |
| 4 | 沒用 plateDiscipline.invalid.swingRate | 對「明顯壞球」的揮棒率最能區分高低選球眼 |

**建議公式**

```js
// 主指標：追打率（壞球揮棒）— 越低越好
e1 = piecewiseInv(plateDiscipline.chase.swingRate,  0.45, 0.28, 0.15)
// 次指標：對明顯壞球的揮棒率 — 頂級選球眼專屬
e2 = piecewiseInv(plateDiscipline.invalid.swingRate, 0.20, 0.08, 0.02)
// 第三指標：好球出棒率（積極度，太低也不好）
zoneRate = (plateDiscipline.core.swingRate + plateDiscipline.edge.swingRate) / 2
e3 = piecewise(zoneRate, 0.40, 0.60, 0.78)
// 結果指標：BB%（保留作弱輔助）
e4 = piecewise(BBp, 3.0, 7.5, 14.0)

discipline = pw(e1 × 0.40 + e2 × 0.25 + e3 × 0.15 + e4 × 0.20)
```

**改進說明**

- **e1 chase swing rate**：壞球揮棒率（22.5 < d ≤ 33.75 cm 的揮棒率）。越低代表越能忍住誘惑。CPBL 觀察：頂級選球眼如吳念庭、王威晨 ~0.15、一般 ~0.28、差的 ~0.45。
- **e2 invalid swing rate**：明顯壞球（d > 33.75 cm）的揮棒率。這對區分「頂級」與「頂尖」最有效——好球員只揮 2-3%，差球員揮 15-20%。
- **e3 zone swing rate**：好球出棒率。**過低**也不好（消極選球）——選球眼好的人對好球出棒積極（zoneRate 70%+），對壞球忍住。e3 是正向 piecewise（越高越好），上限 0.78 避免變得「太揮」。
- **e4 BB%**：只給 20% 權重，當作「最終驗證」。

**注意**：v3.25.x 引擎的 chaseProb 公式（`pitch-engine.js:362`）也用了「effectiveEye - 82」當樞紐，這跟新 discipline 是對齊的。新公式產生的 discipline 值與引擎期望的尺度一致。

### 2.5 fielding（守備）

**現行公式**
```js
fielding = posBaseDefense(position)   // 守位固定值
```

**問題**

| # | 問題 | 後果 |
|---|---|---|
| 1 | 同守位所有人同分 | 同樣是 SS 的林立 vs 林子偉同分 |
| 2 | 沒用 errors（已存在於 data.js）| 即使有 E 數也不算 |
| 3 | CPBL 沒爬 FLD% / UZR / DRS | 沒高品質守備資料 |

**短期建議公式**

```js
// 守位 baseline（同現行）
base = posBaseDefense(position)

// errors 修正：每 100 PA 失誤越少越好
errorRate = errors / max(games × 4, 1)        // 每場機會數估算
errorMod = clamp(-(errorRate - 0.04) × 60, -8, +6)

fielding = pw(base + errorMod)
```

**長期建議**：補爬 CPBL 守備頁，新增 `fldPct / errors / assists` 三欄；改用 `piecewise(fldPct, 0.940, 0.975, 0.995)` 為主。

### 2.6 arm（臂力）

**現行公式**：守位固定值

**問題**：與 fielding 完全相同——個人差異 0。

**短期建議**：維持守位 baseline，等補爬 A（助殺）資料後改用 piecewise。

**重要觀察**：對打者來說 arm 其實只在守備時使用（外野傳球），日常價值不高，所以這項暫時失真的影響比 fielding 小。優先級可以降低。

### 2.7 clutch（關鍵）

**現行公式**
```js
clutch = piecewise(OPSplus, 65, 100, 165)
```

**問題**

| # | 問題 | 後果 |
|---|---|---|
| 1 | clutch 直接 = OPS+，等同沒做差異化 | scoringPosition 又等同 clutch，三個欄位同一值 |
| 2 | 沒用 situational.RISP_AVG / highLI_AVG | 真正 clutch 資料沒進入 |
| 3 | 沒用 WPA（贏率貢獻值） | WPA 是 sabermetrics 公認的 clutch 指標 |

**建議公式**

```js
// 主指標：RISP 表現相對於整體 AVG 的提升
risp = situational.RISP_AVG || AVG
risbBoost = clamp((risp - AVG) / max(AVG, 0.20), -0.30, 0.40)
c1 = piecewise(risbBoost, -0.10, 0, 0.15)

// 次指標：highLI 表現
hli = situational.highLI_AVG || AVG
hliBoost = clamp((hli - AVG) / max(AVG, 0.20), -0.30, 0.40)
c2 = piecewise(hliBoost, -0.10, 0, 0.15)

// WPA per PA（單次貢獻贏率）
wpaPerPA = WPA / max(PA, 1)
c3 = piecewise(wpaPerPA, -0.005, 0.001, 0.010)

// 結果指標 OPS+ 作 baseline（避免極端值）
c4 = piecewise(OPSplus, 65, 100, 165)

clutch = pw(c1 × 0.30 + c2 × 0.25 + c3 × 0.20 + c4 × 0.25)
```

**改進說明**

- 用「RISP_AVG 相對自身 AVG 的提升」而不是 RISP_AVG 絕對值——這才是 clutch（情境下變強的能力），不是整體打擊能力。
- WPA per PA 抓住每打席對贏率的貢獻。
- OPS+ 保留 25% 作 anchor，避免樣本太小時極端。

### 2.8 vsLeft / vsRight（對左右投）

**現行公式**
```js
vsLeft  = pw(contact × 0.55 + discipline × 0.25 + (R打+4 / L打-3 / S打+1))
vsRight = pw(contact × 0.55 + power      × 0.20 + (L打+4 / R打+2 / S打+2))
```

**問題**

| # | 問題 | 後果 |
|---|---|---|
| 1 | 完全用 contact + discipline 推算 | 真實對左/右投表現完全沒用到 |
| 2 | **沒用 splits.vsLHP.AVG / vsRHP.AVG**（已有真實資料）| 浪費 |
| 3 | 沒區分 PA 樣本量 | 對某手別只有 30 PA 也算進去 |

**建議公式**

```js
// 主指標：真實 vs LHP 打擊率（相對個人 AVG）
vsLPA = splits.vsLHP.pa || 0
vsLAVG = splits.vsLHP.AVG || AVG
vsLBoost = clamp((vsLAVG - AVG) / max(AVG, 0.20), -0.40, 0.40)

// 信心度：PA 越多越可信，否則退回 contact-based 推估
confidence = clamp(vsLPA / 150, 0, 1)
v1 = piecewise(vsLBoost, -0.15, 0, 0.20)
v2 = pw(contact × 0.55 + discipline × 0.25 + handBonus)

vsLeft = pw(v1 × confidence × 0.60 + v2 × (1 - confidence × 0.40))

// vsRight 同理，用 splits.vsRHP
```

**改進說明**

- 用真實 splits 為主，PA < 150 時混合舊推算公式（信心度）。
- 「相對個人 AVG 的提升」抓住「對某手別比一般差/好」的能力，而不是「整體打擊率」。
- 慣用手 bonus 仍保留但縮小（已部分反映在真實 splits 中）。

### 2.9 scoringPosition（得點圈）與 pinchHitter（代打）

**現行公式**
```js
scoringPosition = pw(clutch)                                  // 等同 clutch
pinchHitter     = pw(power × 0.40 + discipline × 0.35 + contact × 0.25)
```

**問題**

- `scoringPosition` 與 clutch 完全一樣，是冗餘欄位。建議刪除或改用 `situational.RISP_AVG` 直接 piecewise。
- `pinchHitter` 是各能力加權，沒用 `summary.PH` 或 `situational.pinch_AVG`。

**建議公式**

```js
// scoringPosition：直接用 RISP 表現 + 安打能力
risp = situational.RISP_AVG || AVG
sp1 = piecewise(risp, 0.220, 0.275, 0.350)
sp2 = piecewise(contact, 50, 65, 80)         // 二次混合，用算出的 contact
scoringPosition = pw(sp1 × 0.65 + sp2 × 0.35)

// pinchHitter：用 pinch_AVG（如有 PA ≥ 20）
pinchPA = PH || 0
pinchAVG = situational.pinch_AVG || AVG
if (pinchPA ≥ 20) {
  ph1 = piecewise(pinchAVG, 0.180, 0.250, 0.350)
  pinchHitter = pw(ph1 × 0.60 + (power × 0.40 + discipline × 0.35 + contact × 0.25) × 0.40)
} else {
  pinchHitter = pw(power × 0.40 + discipline × 0.35 + contact × 0.25)   // 退回舊公式
}
```

## 3. 投手九大能力 — 逐項診斷與改進方案

### 3.1 velocity（球速）— 失真最嚴重

**現行公式**
```js
v1 = piecewise(Kp, 12, 20, 32)
v2 = piecewise(SO9, 4, 7, 11)
velocity = pw(v1 × 0.55 + v2 × 0.45)
```

**問題（重大）**

| # | 問題 | 後果 |
|---|---|---|
| 1 | **完全沒用 velocity.avg（JSON 有真實球速！）** | 變化球派投手（K% 高但球速慢）被算得跟火球男一樣 |
| 2 | K% / K9 是「結果指標」 | 等於用 stuff 推算 velocity，但 stuff 又是用 velocity 算的→循環 |
| 3 | 沒用 velocity.max | 球員速度天花板沒反映 |

**真實案例**

| 球員 | velocity.avg (km/h) | 真實狀況 | 現行 velocity 估算 |
|---|---:|---|---:|
| 上頂火球派（如鋼龍） | 152 km/h | 球速王 | K%/K9 → 約 78 |
| 變化球派（如黃子鵬） | 138 km/h | 球速不快但壓制力強 | K%/K9 → 約 76 |

兩位差只有 2 分，但實際球速差 14 km/h。這在引擎裡會讓變化球派投手的「快速球速度」被高估，物理位置生成失真。

**建議公式**

```js
// 主指標：真實平均球速
v1 = piecewise(velocity.avg, 130, 142, 155)
// 次指標：最大球速（天花板）
v2 = piecewise(velocity.max, 140, 150, 162)
// 輔助：K9（驗證一致性）
v3 = piecewise(SO9, 4, 7, 11)

if (velocity.avg is finite) {
  velocity = pw(v1 × 0.60 + v2 × 0.25 + v3 × 0.15)
} else {
  // 缺值 fallback：用舊公式
  velocity = pw(v1_old × 0.55 + v2_old × 0.45)
}
```

**改進說明**

- 直接用 JSON 的 `velocity.avg`，這是物理意義上的真實球速。
- max 提供天花板。
- K9 只給 15% 作為驗證。

### 3.2 control（控球）

**現行公式**
```js
ctrl1 = piecewiseInv(BBp, 14,   8,   3)
ctrl2 = piecewiseInv(WHIP, 1.55, 1.22, 0.85)
control = pw(ctrl1 × 0.45 + ctrl2 × 0.55)
```

**問題**

| # | 問題 | 後果 |
|---|---|---|
| 1 | WHIP 包含被打的安打（H 占大頭）| WHIP 高的投手可能是被打的能力差，跟控球無關 |
| 2 | 沒用 zoneProfile（球進入好球帶的比例） | 真正的控球指標沒用 |
| 3 | 沒考慮 wild pitch / hit batter | 暴投/觸身球是控球失誤的物理表現 |

**建議公式**

```js
// 主指標：保送率（直接反映控球）
c1 = piecewiseInv(BB9, 5.5, 3.2, 1.5)
// 次指標：好球帶內球數比率（zoneRate）
zoneTotal = zoneProfile[core].pitches + zoneProfile[edge].pitches
totalPitches = zoneProfile[*].pitches.sum()
zoneRate = zoneTotal / totalPitches
c2 = piecewise(zoneRate, 0.45, 0.60, 0.72)
// 結果指標：WHIP（弱化）
c3 = piecewiseInv(WHIP, 1.55, 1.22, 0.85)

control = pw(c1 × 0.45 + c2 × 0.35 + c3 × 0.20)
```

**改進說明**

- BB9 取代 BB% 作主指標（BB9 是每 9 局的保送，BF 較大時更穩定）。
- zoneRate（球進入 core+edge 的比率）是控球的物理意義。CPBL 觀察：頂級 ~0.68、中位 ~0.60、差 ~0.50。
- WHIP 降為 20%（含安打能力雜訊）。

### 3.3 breaking（變化球）— 失真最嚴重之一

**現行公式**
```js
b1 = piecewise(ERAplus, 60, 100, 170)
b2 = piecewiseInv(FIP,   5.5, 3.8, 2.0)
breaking = pw(b1 × 0.55 + b2 × 0.45)
```

**問題（重大）**

| # | 問題 | 後果 |
|---|---|---|
| 1 | **ERA+ 和 FIP 都是綜合結果，跟變化球無關** | 一個只投速球的火球男 ERA+ 也可以很高 |
| 2 | 完全沒用 pitchArsenal | 變化球種使用率、揮空率全沒用 |
| 3 | 沒用 zoneProfile.chase/invalid.whiffRate | 誘出棒揮空率（變化球招牌技）沒用 |

**真實案例**

只投速球的羅戈：ERA+ 高 → breaking 算 78
變化球大師（如威能帝）：ERA+ 高 → breaking 也算 78

但兩人 pitchArsenal 完全不同：羅戈 FF 70% / SL 30% / SL whiff 0.25；威能帝 FF 40% / SL 30% / CU 20% / CH 10% / 平均 whiff 0.32。新公式要區分。

**建議公式**

```js
// 主指標：所有變化球種的「揮空率加權平均」
breakingPitches = ['SL', 'CU', 'CH', 'FS', 'FC']
weightedWhiff = 0
weightedUsage = 0
for code in breakingPitches:
  if pitchArsenal[code]:
    weightedWhiff += pitchArsenal[code].whiffRate × pitchArsenal[code].pitchCount
    weightedUsage += pitchArsenal[code].pitchCount

breakingWhiffRate = weightedWhiff / max(weightedUsage, 1)
b1 = piecewise(breakingWhiffRate, 0.15, 0.25, 0.40)

// 次指標：變化球使用率（會投變化球 vs 只會投速球）
breakingUsage = weightedUsage / totalPitches
b2 = piecewise(breakingUsage, 0.20, 0.40, 0.60)

// 輔助：chase 區揮空率（變化球的招牌效果）
chaseWhiff = zoneProfile[chase].whiffRate
b3 = piecewise(chaseWhiff, 0.20, 0.35, 0.50)

// 結果指標 FIP 弱輔助
b4 = piecewiseInv(FIP, 5.5, 3.8, 2.0)

breaking = pw(b1 × 0.45 + b2 × 0.15 + b3 × 0.25 + b4 × 0.15)
```

**改進說明**

- 主指標 b1 是「所有變化球種的揮空率加權平均」，物理上對應「變化球能不能讓人揮空」。
- b2 變化球使用率：避免「速球派誤判為變化球派」——只投速球的人 b2 低。
- b3 chase 區揮空率：變化球的招牌效果（誘出棒後揮空）。
- FIP 從 45% 降為 15%。

### 3.4 stamina（耐力）

**現行公式**
```js
stamina = SP: pw(piecewise(IP, 85, 150, 210))
        : RP: pw(piecewise(IP, 25, 55, 80))
```

**問題**

- 用總 IP 算 stamina 有點不公平：某投手只先發 10 場（受傷）的 IP 自然少，但耐力可能很好。
- 沒考慮「平均單場 IP」（每場 IP/games）——這才是耐力指標。

**建議公式**

```js
ipPerGame = IPOut / 3 / max(games, 1)

if (position === 'SP') {
  s1 = piecewise(ipPerGame,    3.5, 5.5, 7.0)      // 主：每場 IP
  s2 = piecewise(IP / max(games, 1) × 28,  85, 150, 210)  // 投影 28 場全季
  stamina = pw(s1 × 0.65 + s2 × 0.35)
} else {
  s1 = piecewise(ipPerGame,    0.8, 1.2, 1.6)      // RP 每場 IP
  s2 = piecewise(games,        20,  45,  65)       // 出場頻率
  stamina = pw(s1 × 0.55 + s2 × 0.45)
}
```

**改進說明**

- ipPerGame 是耐力最直接的指標（先發 7 局的鐵人 vs 5 局退場）。
- 中繼則看出場頻率 + 每場 IP。

### 3.5 fielding（守備）— 投手版

維持「68 + starts/4 加成」的弱公式，等補爬 CPBL 投手守備資料再改。或直接用守位 baseline 68（不再給 SP 加成，避免假高分）。

### 3.6 stuff（球質綜合）

**現行公式**
```js
stuff = pw(velocity × 0.48 + breaking × 0.36 + control × 0.16)
```

**問題**

- 修好 velocity / breaking / control 後，stuff 自動連動修正——這層公式本身沒問題。
- 但可以額外加 `Whiffp` 作為交叉驗證（被打揮空率就是 stuff 的最終實證）。

**建議公式**

```js
// 三能力加權（同現行）
s1 = velocity × 0.40 + breaking × 0.35 + control × 0.10
// Whiffp 直接驗證
s2 = piecewise(Whiffp, 15, 23, 32)

stuff = pw(s1 × 0.70 + s2 × 0.30)
```

**改進說明**

- 把 Whiffp（揮空率）加入 30% 權重，作為「stuff 是否真的有用」的最終驗證。
- 球速、變化球、控球都頂尖但 Whiffp 低的投手，stuff 應略降；反之提升。

### 3.7 discipline（投手版選球）

現行公式：`pw(control × 0.70 + breaking × 0.30)`

問題：直接用 control + breaking 推算，沒用真實「不送保送 + 不被選到」資料。

**建議公式**

```js
// 主指標：BB9（少送保送）
d1 = piecewiseInv(BB9, 5.5, 3.2, 1.5)
// 次指標：edge 區 + chase 區的精度（會把球塞在邊邊）
edgeRate = zoneProfile[edge].pitches / totalPitches
d2 = piecewise(edgeRate, 0.20, 0.27, 0.35)

discipline = pw(d1 × 0.55 + d2 × 0.45)
```

### 3.8 vsLeft（對左打）

**現行公式**
```js
vsLeft = pw(breaking × 0.45 + control × 0.35 + (L投 +8 / R投 +2))
```

**問題**：沒用 splits.vsLHB.kRate（真實資料）。

**建議公式**

```js
vsLBF = splits.vsLHB.bf || 0
vsLK = splits.vsLHB.kRate || (Kp / 100)
confidence = clamp(vsLBF / 200, 0, 1)

v1 = piecewise(vsLK × 100, 12, 20, 32)     // 對左打 K% piecewise
v2 = pw(breaking × 0.45 + control × 0.35 + handBonus)

vsLeft = pw(v1 × confidence × 0.60 + v2 × (1 - confidence × 0.40))
```

### 3.9 crisis（危機處理）

**現行公式**
```js
crisis = pw(control × 0.40 + breaking × 0.35 + ctrl2 × 0.25)
```

**問題**：沒用 WPA / PosWPA 等 leverage 指標、沒用 RISP 被打擊率（投手版的 RISP）。

**建議公式**

```js
// 主指標：WPA 淨值 per BF（leverage 表現）
wpaNet = (PosWPA + NegWPA) / max(BF, 1)
c1 = piecewise(wpaNet, -0.008, 0.001, 0.010)

// 次指標：保送率（壓力下不送保送）
c2 = piecewiseInv(BBp, 14, 8, 3)

// 控球與變化球輔助
c3 = pw(control × 0.40 + breaking × 0.35)

crisis = pw(c1 × 0.35 + c2 × 0.30 + c3 × 0.35)
```

**注意**：PosWPA / NegWPA 在 JSON 中存在，但要確認單位。WPA per BF 大致範圍 -0.008 到 +0.012。

### 3.10 pickoff / quickDelivery / recovery

這三項 CPBL 沒有對應的進階資料，維持現行公式即可。

## 4. 預期改進效果（試算）

挑三位有代表性的球員試算（從之前修正書 §10.6 取得 baseline）：

### 4.1 林安可（強打左打）

| 能力 | 現行公式 | 新公式（推估） | 變化 |
|---|---:|---:|---|
| contact | 71 | 73-75 | 上升（好球帶內 whiff 低、Whiffp 低）|
| power | 82 | 82-85 | 持平/略升（avgEV、maxEV 都進入計算）|
| discipline | 75 | 78-80 | **上升**（chase rate 低、invalid 揮棒少）|
| clutch | 75 | 78-82 | 上升（RISP 表現好）|
| vsLeft | 65 | 60-65 | **下降**（左打對左投本就弱，splits.vsLHP.AVG 偏低）|

### 4.2 黃子鵬（變化球派投手）

| 能力 | 現行公式 | 新公式（推估） | 變化 |
|---|---:|---:|---|
| velocity | 76 | 60-65 | **大幅下降**（真實球速 ~138 km/h，piecewise 算出 60 出頭）|
| control | 72 | 78-82 | **大幅上升**（zone rate 高、BB9 低）|
| breaking | 74 | 80-83 | **大幅上升**（變化球揮空率頂級、使用率高）|
| stuff | 73 | 73-76 | 持平（vel 降但 break/whiff 升，互相抵消）|

引擎影響：黃子鵬投四縫線速球的 `speedKmh` 從 ~158 km/h 降至 ~135 km/h（更接近真實），但變化球的 `stuffScore` 大幅上升，符合「球速慢但球路鬼」的真實表現。`armSlotBonus` 還會再加 +4 stuffScore。

### 4.3 鋼龍（火球派洋投）

| 能力 | 現行公式 | 新公式（推估） | 變化 |
|---|---:|---:|---|
| velocity | 78 | 82-86 | **上升**（avg.155 km/h piecewise 算高）|
| control | 75 | 70-75 | 持平/略降（BB9 中等，zone rate 一般）|
| breaking | 76 | 65-70 | **下降**（pitchArsenal 偏速球派，變化球種揮空率與使用率都低）|
| stuff | 76 | 78-82 | 上升（vel 升 + Whiffp 高）|

## 5. 實作建議

### 5.1 開發路徑

**Phase 1：診斷與驗證（不改 code）**

1. 寫一個獨立 HTML 工具 `tools/ability-comparator.html`：
   - 讀取現有 data.js + cpbl_players JSON
   - 對每位球員同時跑舊公式和新公式
   - 輸出 CSV：球員名、舊 abilities、新 abilities、差異
2. 人工檢視 99 位球員，標記異常變動
3. 必要時微調 piecewise 門檻

**Phase 2：套用到 stat-mapper（最小改動）**

只改 `tools/stat-mapper.html` 的 `computeBatterAbilities()` 和 `computePitcherAbilities()` 兩個函式，其他不動。

風險：data.js 重新生成後，所有球員的 abilities 都變，遊戲內球員強度需重新平衡。

**Phase 3：sim-tester 驗證**

跑 99 打者 × 1000 PA 看：
- 整體 AVG / K% / BB% / BABIP 是否回到合理區間
- AVG R² / SLG R² 是否改善（理論上會大幅改善）
- 個別指標球員是否歸位

如果 R² 顯著上升、平均沒崩，即接受新公式。

### 5.2 預期風險

1. **球員強度大洗牌**：黃子鵬等變化球派投手會「變強」（breaking 升）、只投速球的會「變弱」（breaking 降）。可能影響遊戲平衡，需重調 reward。
2. **新球員 advancedStats 不足時**：所有公式都要寫 fallback。如果 `velocity.avg` 缺，退回舊公式。
3. **PA / BF 樣本量問題**：splits.vsLHP.pa 可能太小（< 50），要 confidence weighting。

### 5.3 不適合做的事

- **不要一次把所有 9 項都改**。建議分批：
  - Batch A：discipline / velocity / breaking（失真最嚴重）
  - Batch B：contact / power / control / clutch
  - Batch C：vsLeft / vsRight / fielding / arm / stamina / pinchHitter / scoringPosition / crisis
- 每批改完跑 sim-tester 驗證一次，避免複合失真。

### 5.4 配套修改

新公式生效後，下列參數可能需要重校：

| 檔案 | 參數 | 為何要重校 |
|---|---|---|
| `pitch-engine.js:340` | pCorrectRead baseline 0.66 | discipline 分布變化，可能影響 pCR 平均 |
| `pitch-engine.js:362` | chaseProb 公式中的「effectiveEye - 82」 | 82 是當前 P50，新公式 P50 可能不同 |
| `pitch-engine.js:633` | EV power 係數 0.68 | power 分布變化 |
| `game-params.js:245` traits | 各 trait 的 ability 門檻（80/82/86）| 新公式產生的能力值分布可能不同 |

需要先跑 sim-tester 看新分布，再決定怎麼校。

## 6. 不在這份檢討書處理的事

- **新增爬蟲欄位**：FLD% / 個人 A / 球員身高體重 — 這需要爬 CPBL 不同頁面，獨立工作項。
- **abilities 上限提升**：目前 piecewise 上限 82，新公式仍維持。若想讓真實巔峰球員突破 82，要改 `pw()` 上限與引擎多處 clamp，獨立議題。
- **修改 stuff 計算的權重**：新 stuff 公式只是把 Whiffp 加進來，更深入的改造（如 pitch-by-pitch tunneling 模型）超出範圍。
- **天賦/特質門檻調整**：新公式生效後 talents 的 ≥80 門檻可能要重設，留到下一輪。

---

## 4. v1.1 修正：sim-tester 驗證後的 contact 公式微調（2026-05-25）

§2.1 的「建議公式」已在 `tools/stat-mapper.html` 落地（揮空率主導 75% + AVG/BABIP 校正 25%），重跑出新版 data.js 後跑 `sim-tester-batch-2026-05-25.csv` 99 打者 × 300 PA 驗證，發現 **強打高 K% 群被嚴重低估、強弱反轉重現**，幅度比 v2.1 收尾還大。

### 4.1 驗證結果（個別指標球員）

| 球員 | realAVG | OPS+ | K% | abilities.contact | sim AVG | dAVG |
|---|---:|---:|---:|---:|---:|---:|
| 張育成 | .299 | 182 | 15.4 | 62 (C-) | .267 | -.032 |
| 魔鷹 | .298 | 174 | 17.7 | 52 (D) | .219 | -.080 |
| 林安可 | .291 | 167 | 18.9 | 55 (D) | .201 | -.091 |
| 林立 | .348 | 151 | 12.8 | 61 (C-) | .259 | -.089 |
| **許基宏** | .285 | 148 | 20.5 | **49 (E!)** | .164 | **-.121** |
| 劉子杰（純接觸型） | .328 | 147 | 7.1 | 76 (B-) | — | 接近 ✓ |
| 吉力吉撈 | .276 | 146 | 17.9 | 55 (D) | .194 | -.082 |
| 胡金龍 | .331 | 137 | 14.0 | 70 (B-) | .236 | -.095 |
| 朱育賢 | .286 | 133 | 20.7 | 51 (D) | .225 | -.061 |
| 陳鏞基 | .298 | 132 | 15.9 | 55 (D) | .184 | -.115 |
| 林泓弦（弱打） | .207 | 56 | 17.7 | (中) | .324 | **+.117** |
| 高聖恩（弱打） | .177 | 47 | 25.3 | (中) | .274 | +.097 |

**模式很乾淨**：contact ≥ 70（純接觸型）sim 接近真實；contact ≤ 55（強打但 K% 高）全面崩盤；弱打者因為公式對 K% 中等的他們「相對寬容」反而被高估。

### 4.2 §2.1 建議公式的失效根因

公式為 `c1×0.45 + c2×0.30 + c3×0.25`，三層都對強打不利：

1. **c1（core.whiffRate inv）+ c2（whiffp inv）佔 75%**：強打揮大棒，物理上 whiff 偏高（許基宏 K% 20.5%）是 swing aggressiveness 的副作用，不是 contact 能力差。把 whiff 當主指標等同把「揮棒風格」誤判為「打到球能力」。
2. **c3 的 BABIP normalization 反向懲罰**：`adjAVG = AVG × (0.300 / BABIP)`，假設高 BABIP = 運氣。但 CPBL 強打的 BABIP .33–.36 來自 hard contact（barrel rate），這是 skill。校正後許基宏 .285 → .244，連帶 c3 從 60+ 降到 50。
3. **c3 權重 0.25 太低**：即使 c3 算對了，AVG 在最終 contact 裡只貢獻 25%，無法抵銷 c1/c2 的雙重打擊。

### 4.3 v1.1 修正公式

`tools/stat-mapper.html` line 184–197：

```js
// v1.1 修正：AVG 為主、揮空率為輔，加 OPS+ anchor
const c1 = piecewiseInv(coreWhiff, 0.15, 0.08, 0.03);
const c2 = piecewiseInv(whiffp, 25, 15, 7);
const c3 = piecewise(Math.max(0.200, Math.min(0.400, ws.avg || 0.260)), 0.230, 0.275, 0.340);
const c4 = piecewise(ws.opsPlus || 0, 65, 100, 165);
contact = pw(c1 * 0.20 + c2 * 0.15 + c3 * 0.40 + c4 * 0.25);
```

**權重變動：**

| 元件 | v1.0（§2.1 原案） | v1.1 修正 | 變動理由 |
|---|---:|---:|---|
| c1 core whiff | 0.45 | **0.20** | 揮空是「揮棒風格」副作用，不是 contact 主指標 |
| c2 整體 whiff | 0.30 | **0.15** | 同上，且與 c1 高度相關 |
| c3 AVG | 0.25（含 BABIP校正） | **0.40（純 AVG）** | 移除 BABIP 校正、提權 |
| c4 OPS+ | — | **0.25**（新增） | 加總體打擊水準 anchor，避免揮空率主導 |

**為什麼移除 BABIP 校正**：CPBL 樣本量小，BABIP 個體差異 0.04–0.06 的物理意義難從「skill vs luck」可靠分離。對強打者（BABIP > .32）反向校正放大現有問題。下一版若要重啟，可改為「**反向式**校正」（只在 BABIP < .25 時往上拉），不要對高 BABIP 強打懲罰。

### 4.4 修正後預測（待 sim-tester 驗證）

樣本算式（許基宏：core_whiff ≈ 0.10、whiffp ≈ 20%、AVG .285、OPS+ 148）：
- c1 ≈ 50, c2 ≈ 50, c3 ≈ 63（.285 在 .275–.340 區間）, c4 ≈ 76（148 在 100–165 區間）
- contact ≈ 50×0.20 + 50×0.15 + 63×0.40 + 76×0.25 ≈ **62**（從 49 → 62）

| 球員 | 修前 contact | 預期修後 | 預期 sim AVG |
|---|---:|---:|---:|
| 許基宏 | 49 | ~62 | .220 → ~.270 |
| 朱育賢 | 51 | ~63 | .225 → ~.265 |
| 林安可 | 55 | ~67 | .201 → ~.260 |
| 林立 | 61 | ~76 | .259 → ~.310 |
| 陳鏞基 | 55 | ~65 | .184 → ~.250 |
| 林泓弦 | ~55 | ~49 | .324 → ~.230 |
| 高聖恩 | ~52 | ~47 | .274 → ~.205 |
| 劉子杰 | 76 | ~76 | 不變 |
| 胡金龍 | 70 | ~73 | 略升 |

整體預期：
- **強弱反轉解除**：強打 contact 上升、弱打 contact 下降
- **AVG R² 從 0.18-0.33 提升到 0.4+**（v2.1 修正書 §10.4 目標範圍）
- 平均 AVG 略升回 .250-.260
- **副作用警示**：power/discipline 公式未動，HR 雙向失準（修正書 §12.2 Issue #1）仍存在

### 4.5 操作步驟

1. 使用者打開 `tools/stat-mapper.html`（已修）。
2. 上傳 `cpbl_players_2024-2026.json` + 舊 `data.js`，點「開始整合」。
3. 下載新 `data.js` 覆蓋遊戲根目錄。
4. 強制刷新 `tools/sim-tester.html`，跑 99 打者 × 300 PA。
5. 匯出 CSV，對照 §4.4 的預期表格，確認反轉解除。

### 4.6 不達標時的調整方向（依使用者偏好「一次只動一個參數」）

- **強打仍偏低**（許基宏 sim AVG < .250）：c3 權重 0.40 → 0.45、c4 → 0.25
- **強打過頭**（許基宏 sim AVG > .280、林立 > .340）：c4 權重 0.25 → 0.15
- **弱打仍偏高**（林泓弦 sim AVG > .250）：c1/c2 權重略提到各 0.25 / 0.20
- **OPS+ anchor 讓平均型球員過高**：c4 piecewise 三閾值改為 70/105/170（門檻整體上移）

**不要做的事**：
- 不要重新引入 BABIP 校正（已驗證反向）
- 不要為了一兩位球員調 power/discipline（contact 是當前主因，先單一變項收斂）
- 不要動 game.js StatMapper（讀取優先邏輯沒問題，只需重跑 stat-mapper）

### 4.7 教訓（補入 §0 核心問題框架）

「結果指標 vs 過程指標」的洞察是對的，但**過程指標也不是絕對解**：
- 過程指標（whiff rate）量化的是「動作層」表現，不直接等於「能力結果」
- 強打的 whiff 高是 aggressive swing 的副作用，這是策略不是能力差
- **混合策略**：過程指標適合「分解」能力（例如 chase rate 分離 discipline 與 contact），但**最終結果指標（AVG/OPS+）仍是 anchor**
- 物理意義最重的指標應該佔最大權重——但要驗證它在當前樣本（CPBL N=99）有沒有可靠的訊號雜訊比

→ 這條會影響後續 power / discipline / clutch 等公式調整。建議**先驗證 v1.1 contact**，再決定其他能力是否要做同類「結果指標 anchor」回補。

---

---

## 5. v1.2 標尺擴展驗證結果（2026-05-25 第二輪）

§4 的 v1.1 contact 公式修正後，sim-tester 顯示 abilities 分布壓縮在 50-72 區間（張育成 OPS+ 182 / 全聯盟第一強，contact 只算到 69），使用者反映「最強應該到 80-90」，因此進一步動 stat-mapper 的 `pw()` 與 `piecewise` 標尺。

### 5.1 改動內容（已落地）

`tools/stat-mapper.html:142-153`：

```js
// 舊（v1.1）
function piecewise(x, lo, mid, hi, lo_s=45, mid_s=60, hi_s=82) { ... }
function piecewiseInv(x, hiRaw, midRaw, loRaw) { return piecewise(x, loRaw, midRaw, hiRaw, 82, 60, 45); }
function pw(v) { return Math.max(45, Math.min(82, Math.round(v))); }

// 新（v1.2）
function piecewise(x, lo, mid, hi, lo_s=40, mid_s=60, hi_s=90) { ... }
function piecewiseInv(x, hiRaw, midRaw, loRaw) { return piecewise(x, loRaw, midRaw, hiRaw, 90, 60, 40); }
function pw(v) { return Math.max(35, Math.min(95, Math.round(v))); }
```

設計意圖：把分布從 [45, 82] 拉開到 [35, 95]，讓聯盟最強的 abilities 進入 80+ 區間。

### 5.2 sim-tester 結果：U 型誤差曲線（CSV 路徑 sim-tester-batch-2026-05-25 (2).csv，99 打者 × 1000 PA）

**v1.2 沒解掉強弱反轉，反而製造了新的失衡**：誤差不是線性，而是依 OPS+ 形成 U 型分布。

| 群組 | 代表球員 | OPS+ | dAVG | sim HR% | 真實 HR% | 病徵 |
|---|---|---:|---:|---:|---:|---|
| **頂級炸彈過頭** | 林安可 | 167 | +.039 | 8.70% | 4.67% | sim SLG .745 vs 真實 .541 |
| 頂級炸彈過頭 | 魔鷹 | 174 | +.010 | 7.70% | 4.00% | sim SLG .704 vs 真實 .563 |
| 頂級炸彈過頭 | 張育成 | 182 | +.010 | 6.00% | 3.33% | sim SLG .669 vs 真實 .541 |
| 頂級炸彈過頭 | 吉力吉撈 | 146 | +.039 | 7.90% | 4.33% | sim SLG .696 vs 真實 .507 |
| **中強打被埋沒** | 林立 | 151 | **-.099** | 0.60% | 0.33% | sim AVG .249 vs 真實 .348 |
| 中強打被埋沒 | 陳鏞基 | 132 | -.082 | — | — | sim .216 / 真實 .298 |
| 中強打被埋沒 | 王政順 | 122 | -.079 | — | — | sim .207 / 真實 .286 |
| 中強打被埋沒 | 申皓瑋 | 117 | -.078 | — | — | sim .195 / 真實 .273 |
| 中強打被埋沒 | 朱育賢 | 133 | -.070 | — | — | sim .216 / 真實 .286 |
| **弱打仍高估** | 林泓弦 | 56 | **+.099** | — | — | BABIP .381 |
| 弱打仍高估 | 馬傑森 | 46 | +.080 | — | — | BABIP .345 |
| 弱打仍高估 | 陳品捷 | 59 | +.069 | — | — | BABIP .345 |
| 弱打仍高估 | 高聖恩 | 47 | +.057 | — | — | BABIP .339 |

聯盟整體擊球分布（圖：sim-tester 對決診斷）：
- 揮空/揮棒 10.86%（合理）
- 平飛安打率 49.75%（偏低，MLB 約 ~70%）
- **飛球安打率 40.81%**（v1.1 為 10.97%，**真實 ~12%**）
- **高飛安打率 50.00%**（v1.1 為 13.21%，**真實 ~5-8%**）
- 滾地安打率 25.81%（合理）

→ 飛球與高飛球被嚴重高估，**這是 v1.2 後頂級強打 HR 暴增的源頭**。

### 5.3 為什麼是 U 型？根因解構

v1.2 拉開 abilities 是統一公式（lo=40/mid=60/hi=90），但**每個 ability 對 sim AVG 的邊際貢獻不是線性**：

#### 區段 1：power 90+ 的「EV 失控區」
- power 90 → EV ≈ 92 + 90×0.68 = 153 km/h（平均）+ N(0,6) → 常飆 160+
- 飛球落地距離 ∝ EV²，160 km/h 飛球普遍超過 105m → 牆距內
- `fielding-engine.js` 沒對飛球/高飛球做 velocityHandlingPenalty（只滾地+平飛有）
- `reachChance` 的 routineAirBonus 階梯（wallRatio ≥ 0.9 時掉到 0.04）放大失分
- 結果：頂級炸彈飛球幾乎全變 HR 或長打

#### 區段 2：power 70-80 的「中強打卡卡區」
- power 75 → EV ≈ 143 km/h（平均），飛球距離 ~95m
- 落在外野手範圍邊緣，catchChance 仍 0.95+
- contact 65-72，inPlayProb 不特別高
- 結果：擊球品質沒到 HR 門檻，卻又敵不過守備 → 中強打 SLG/AVG 大幅失真

#### 區段 3：弱打 power 50-58 的「PBP 救援區」
- 引擎 abilities-driven inPlayProb 約 0.27-0.30
- 但 `pitch-engine.js:534-549` plateDiscipline 30% + pitchTypeMatchup 25% 混合，把弱打真實 PBP inPlayRate 拉高
- 守備引擎對弱擊球（EV 120-130）的 reachChance 仍高，但**弱平飛 90+ km/h 仍有 .50 BABIP**
- 結果：弱打少數進場球品質「夠用」→ BABIP .33-.38

### 5.4 對「contact 95 超級球員」的回答

使用者問：若加入 contact 95 的角色卡會否失衡？

**會。而且不只 contact**：
- 該球員 power 必然也用同公式算到 90+
- contact 95 → contactQuality (95-45)/50 = 1.0 → inPlayProb 0.64（封頂）
- 加上 power 90+ EV 失控 → 預估 HR% 12-15%（CPBL 真實頂級 ~5%）
- **遊戲平衡會崩潰**

更深層的問題：**stat-mapper 用統一公式 + 統一上限算所有能力，但引擎中每個能力對 sim 結果的「敏感度」截然不同**：
- power 對 EV 是「線性 + 飛球距離平方」
- contact 對 inPlay 是線性
- discipline 對 chaseProb 是 logistic
- speed 對 sb / 跑壘是線性

同樣把 abilities 拉到 90，power 90 比 contact 90 危險 5-10 倍。

---

## 6. 引擎物理 audit（使用者點名項目，2026-05-25）

### 6.1 velocityHandlingPenalty 的定義與漏洞

**規則書 §10.6.1** 定義：球速越快，守備員越難處理。

```
滾地球：penalty += clamp((evKmh - 142) / 4.8, 0, 12)
平飛球：penalty += clamp((evKmh - 148) / 4.0, 0, 18)
> 174 km/h：再 +6
```

引擎實作（`fielding-engine.js:459`）：
```js
handleChance = clamp(0.985 + (fielding - 75) / 720 - penalty / 280 - difficulty, minHandle, 0.998)
```

**漏洞**：規則書與引擎都只對滾地球與平飛球生效，**飛球與高飛球完全沒有 velocity 懲罰**。
v1.2 後強打 power 90+ 把飛球落地距離推到牆邊，但飛球只要守備員「追不到」就直接安打/HR，沒有「球太強守備員接不乾淨」的修正機制。

**修正建議**：飛球加 `penalty += clamp((evKmh - 155) / 5, 0, 10)`，但只進 handleChance（不進 reachChance，畢竟飛球追不到的本質是距離）。

### 6.2 飛行物理引擎的真實程度

`fielding-engine.js:244-360` `calcBattedBall`：

| 機制 | 真實程度 |
|---|---|
| 拋體距離公式 `v²·sin(2θ)/g` | ✅ 真空彈道方程 |
| 空氣阻力衰減 `dragCarryScale = 0.65 − ...` | ⚠️ **固定係數 0.56-0.72**，非 ODE 積分 |
| 高度時間 `vy·t − 0.5g·t²` × 0.85 | ⚠️ 拋體公式 + 手動 0.85 衰減 |
| 風 `windCarryScale = 1 + windHelp×0.04` | ⚠️ 線性係數 |
| 海拔 `1 + altitude/10000` | ⚠️ 線性係數 |
| 球場 HR 工廠 `stadium.hrFactor` | ✅ 球場特性 |

**沒有的真實物理**：
- Magnus 效應（球的旋轉影響飛行 ±5-10m）
- 真實雷諾數空氣阻力（縫線方向影響 drag coefficient）
- 溫度濕度影響空氣密度（夏天潮濕 → 距離 -3m）
- 球的反彈係數隨速度衰減（COR）
- 球棒材質與球體碰撞動力學

簡單說：拋體公式 + 經驗衰減係數，不是 MLB Statcast 用的剛體動力學模型。**對 EV 高的球誤差會放大**（拋體公式假設真空 → 高 EV 球距離被高估）。

### 6.3 launchAngle 計算限制（pitch-engine.js:697-717）

```js
typeRanges = { ground: [-15, 9], liner: [10, 24], fly: [25, 39], popup: [40, 60] }
// 步驟 1：依打者真實 GB/LD/FB/PU 比例抽 ballType
// 步驟 2：從該型態區間 uniform 抽 baseLaunch
// 步驟 3：若有 avgLaunchAngle，混合 70% 隨機 + 30% 平均
```

**沒考慮**：
- 球的高度位置（高球容易打高仰角、低球打低仰角）
- contactPoint 上下（球擊球棒上半 → 高仰角、下半 → 低仰角；MLB Statcast 核心輸入）
- swing path（uppercut vs flat swing，仰角偏好的核心決定因素）

實際結果：內角高球與外角低球打出來的仰角分布完全一樣 — 物理上不合理。**但對 sim 整體 AVG 影響有限**（因為打者自身分布已經內含 swing path 統計）。

### 6.4 全壘打牆判定（fielding-engine.js:69-75, 292-317）

```js
// 牆距離：依噴射角內插 LF/CF/RF 三個球場距離
function wallDistance(stadium, sprayDeg) { ... }

// 球到牆時間 + 拋體高度
wallTime = wallDist / horizontalSpeed
trajectoryHeightAtWall = (vy·wallTime - 0.5·g·wallTime²) × 0.85

isHR = distM >= wallDist && trajectoryHeightAtWall > fenceHeight
hitNet = distM >= wallDist && trajectoryHeightAtWall <= fenceHeight  // 撞牆變二壘安打
```

**有**：球到牆時間、到牆高度、圍欄高度（預設 3m）、各球場 LF/CF/RF 不同距離（線性內插）、海拔、windHelp、hrFactor。

**沒有**：
- 牆角偏移（CPBL 主場有「凹球場」，內角線與中外野非線性）
- 看台規則（ground rule double：球彈出場外 → 自動二壘安打）
- 沒收 HR 動畫（外野手跳上牆接 HR）
- 風隨高度變化

### 6.5 模擬器：時間 vs 機率混合模式

| 階段 | 真實時間/距離 | 機率推算 |
|---|---|---|
| 飛行距離、滯空時間 | ✅ 真實算 | — |
| 守備員 routeDistance / fielderSpeed | ✅ 真實算 | — |
| 跑者到壘時間 | ✅ 真實算 | — |
| 守備員「能不能追到」 | rangeMarginM 真實算 | 抽 `reachChance` |
| 接到後「能不能接乾淨」 | difficulty 真實算 | 抽 `handleChance` |
| 傳球時間 | ✅ 真實算 | — |
| 傳球準度 | — | 抽 `throwErrorChance` |
| 球與跑者「誰先到壘」 | timeMargin 真實算 | 接近時抽 `safeChance` |

物理量都真實計算，但「成功/失敗」的最終判定都是機率抽樣。這是 sabermetrics 模擬常用做法（MLB Statcast Expected Stats 也是基於 EV/LA 算機率，不是 100% 決定）。

### 6.6 reachChance 的 routineAirBonus 階梯效應（v1.2 失控加速器）

`fielding-engine.js:435-445`：
```js
const wallRatio = ballInfo.preciseWallDistM > 0 ? ballInfo.preciseDistM / ballInfo.preciseWallDistM : 0.8;
const routineAirBonus = wallRatio < 0.78 ? 0.22 : wallRatio < 0.9 ? 0.14 : 0.04;
```

**問題**：階梯式跳變（0.22 → 0.14 → 0.04）。當 v1.2 強打 power 升高，飛球落地距離往牆移：
- 舊 v1.1：wallRatio ~0.65，routineAirBonus = 0.22
- 新 v1.2：wallRatio ~0.85，routineAirBonus = 0.14（**-0.08**）
- 極端 power 90+：wallRatio ~0.92，routineAirBonus = 0.04（**-0.18**）

reachChance 直接掉 0.18 → 飛球大量變安打/HR。這是 v1.2 飛球安打率從 11% → 41% 的關鍵之一。

**修正建議**：改成平滑函數 `routineAirBonus = clamp(0.25 - wallRatio × 0.30, 0.02, 0.22)`，避免階梯跳變。

---

## 7. 能力值平移：三方案討論（使用者提案 2026-05-25）

使用者問：「可否把遊戲能力數值平移，例如原先 70 所代表的能力值改到 75 才擁有同樣的能力值」。

意圖解讀：讓玩家看到的數字符合直覺（張育成顯示 85，而不是 75）。

### 7.1 方案 A：硬平移（最直觀，但風險最大）

改 stat-mapper piecewise 標尺為 `lo→50, mid→70, hi→95`，pw clamp [40, 99]。

**致命缺陷**：引擎所有公式假設「mid = 60 或 70」這個 anchor：

| 引擎公式 | mid 平移後副作用 |
|---|---|
| `reactionTimeSec = 0.75 - fielding/250` | fielding 平均從 60 → 70，reactionTime 從 0.51s → 0.47s，**守備整體變強** |
| `contactQuality = (X-45)/50` | mid 平移後，平均球員 quality 0.30 → 0.50，**全聯盟 inPlayProb 上升** |
| `evKmh = 92 + power×0.68` | mid 平移後，平均球員 EV 從 133 → 140 km/h，**整體 HR 大爆發** |
| `reachChance` baseline | 不變但 fielding 升高 → handleChance 升高 → 守備整體更強 |

→ 平均球員變得「全能」，但**強打不再相對更強**（因為大家一起升）。**不能解問題，反而放大失衡**。

### 7.2 方案 B：分能力獨立平移（推薦的根本方案）

把「**顯示能力值（player.abilities）**」與「**引擎內部能力值**」解耦。

```js
// data.js 存「顯示能力值」（玩家看到、用於 UI / 字母評級）
abilities: { contact: 85, power: 78, ... }   // 平均=70、強打 85+、頂級 90+

// 引擎讀取時，透過 normalize helper 轉成「引擎內部能力值」
function toEngineScale(displayValue, abilityType) {
  switch (abilityType) {
    case 'power':
      // power 用「保守縮放」避免 EV 失控
      return 45 + (displayValue - 40) * 0.65;   // 顯示 90 → 引擎 78
    case 'contact':
      // contact 用「線性映射」（多少分=多少 quality）
      return 40 + (displayValue - 40) * 0.95;   // 顯示 90 → 引擎 87
    case 'velocity':
      // velocity 用「對 stuff 強敏感」設計，需保守
      return 45 + (displayValue - 40) * 0.70;
    default:
      return displayValue;
  }
}

// 引擎用 toEngineScale(ability, 'power') 替換現有的 player.abilities.power
```

**優點**：
1. **玩家看到的 abilities 符合 80-90 區間直覺**（張育成 contact 顯示 85）
2. **引擎仍用熟悉的公式**（不破壞已校準的 R²、不需重跑 sim-tester batch）
3. **每個能力可獨立調整轉換曲線**（power 縮 0.65 避免飛球失控、contact 縮 0.95 拉開差距）
4. **解掉「power 對 EV 過於敏感」這個結構問題**
5. **未來引入 contact 95 球員不會崩盤**（顯示 95 → 引擎內部仍是 ~92，可控）

**缺點**：
1. **兩套尺度需要記憶在文檔**（多一層 mental model，但 helper 集中管理可控）
2. **需要改 game.js、pitch-engine.js、fielding-engine.js 多處讀取點**（grep `\.abilities\.\w+` 估 20-30 處）
3. **traits 解鎖門檻（≥80）要重新校準**（顯示尺度上的 80 在新尺度可能是 75）
4. **能力升級系統要對應**（PlayerGrowth 加 +1 到顯示尺度後，引擎尺度只加 +0.65~0.95）

**實作位置**：
- 新增 `game-params.js` 中 `abilityScaleMap`（每個能力的轉換係數）
- 新增 helper `window.toEngineAbility(player, type)`，所有引擎用這個取代 `player.abilities.X`
- StatMapper 也改用此 helper

### 7.3 方案 C：純顯示層平移（最保守但本質欺瞞）

abilities 內部值不變（仍 45-82），**只在 UI 顯示時加 +10**：
- data.js 仍存 contact 70
- UI 顯示「80」
- 字母評級門檻同步上調

**優點**：零引擎風險、零 sim 重跑
**缺點**：本質欺瞞玩家、解不掉「contact 95 球員」的真實失衡、收尾後若要再改架構成本更高

### 7.4 推薦路徑

**P0 立刻處理**（短期：解 v1.2 失控）：
1. **方案 A1：stat-mapper 內 clamp power 上限回 82**（單行改動，犧牲頂級 power 區分度，但阻止 EV 失控）
2. 或 **方案 A2：飛球加 velocityHandlingPenalty + 平滑 routineAirBonus**（治本但動 fielding-engine 兩處）

**P1 長期架構修正**（方案 B）：
1. 設計 `abilityScaleMap`：power 0.65、velocity 0.70、contact 0.95、其他 1.0
2. 實作 `toEngineAbility(player, type)` helper
3. 全面替換 engine 內 `player.abilities.X` 引用
4. 重設字母評級門檻（S=90+ 改 S=85+；A=80-89 改 A=78-84）
5. 重設 traits 解鎖門檻（顯示尺度上 ≥80 對應引擎 ≥75）

P1 工作量約 1-2 天，但完成後可長期穩定、引入 95+ 球員不會崩。

### 7.5 不該做的事

- **不要回退 v1.2 piecewise 標尺到 v1.1**（會失去顯示直覺）
- **不要直接把 piecewise hi_s 拉到 100**（沒解決 power 對 EV 過敏問題）
- **不要動 fielding-engine 的 `reactionTimeSec / fielderSpeedMps` 公式 baseline**（守備整體校準 v2.0 才剛穩，動就要重跑 sim）

---

## 8. v1.3 改動（2026-05-26，A+B+F 小集合）

使用者選擇「動 A+B+F 小集合驗證」路徑。**未動引擎深層（議題 1 penalty 改時間制延後）**，先驗證 U 型誤差是否解除。

### 8.1 動機回顧（與 v1.2 的差異）

v1.2 把分布從 [45, 82] 拉開到 [35, 95] 後出現 U 型誤差。重新 audit 引擎內 anchor 假設：

| 引擎公式 | 假設「平均」anchor |
|---|---|
| `reactionTimeSec = 0.75 - fielding/250` | fielding 75 → 0.45s |
| `handleChance = 0.985 + (fielding-75)/720` | fielding **75** |
| `contactQuality = (X-45)/50` | contact **70** → quality 0.50 |
| `evKmh = 92 + power × 0.68` | power 70 → 139 km/h |
| `ability(player, key, fallback=70)` (fielding-engine.js:54) | **70** |

→ 引擎一致假設「平均 ≈ 70」，但 v1.0~v1.2 stat-mapper 把 CPBL 平均算到 60（mid_s=60）。
→ 整個 CPBL 在引擎內被當成「低於聯盟平均」處理，這才是失真根源。

### 8.2 A：piecewise 標尺對齊引擎 anchor（stat-mapper.html:142-153）

```js
// v1.3
function piecewise(x, lo, mid, hi, lo_s=50, mid_s=70, hi_s=90) { ... }
function piecewiseInv(x, hiRaw, midRaw, loRaw) { return piecewise(x, loRaw, midRaw, hiRaw, 90, 70, 50); }
function pw(v) { return Math.max(40, Math.min(99, Math.round(v))); }
```

| 預期分布 | 顯示值 | 物理對應 |
|---|---|---|
| 弱打底層 | 40-55 | CPBL 二軍邊緣 / 一軍替補 |
| 普通 | 55-65 | CPBL 一軍 role player |
| **中位平均** | **70** | **CPBL 一軍主力（OPS+ ~100）** |
| 強打 | 75-82 | CPBL 中強打（OPS+ 110-140） |
| **頂級** | **85-90** | **CPBL 真實頂級（OPS+ 150+）** |
| 升級空間 | 90-99 | 課金角色卡、養成上限 |

### 8.3 B：power piecewise 改用 CPBL EV 區間（stat-mapper.html:206-213）

從 `cpbl_players_2024-2026.json` 實測（2025-2026 雙季，N=38，PA≥100）：

| 百分位 | core.avgEV (km/h) |
|---|---:|
| p10 | 135.8 |
| p25 | 138.5 |
| **p50** | **146.1** |
| p75 | 149.4 |
| p90 | 152.0 |
| p95 | 156.8 |
| max | 162.0 (郭嚴文 outlier，OPS+ 63) |

舊（MLB 標尺）：`piecewise(coreAvgEV, 110, 130, 150)` → CPBL 平均 146 → mid 偏上 → 全聯盟 power 偏高
新（CPBL 標尺）：`piecewise(coreAvgEV, 125, 143, 155)` → CPBL 平均 146 → mid 偏上少量 → 對齊

```js
const p1 = isFinite(coreAvgEV) ? piecewise(coreAvgEV, 125, 143, 155) : 70;
const p2 = isFinite(maxEV) ? piecewise(maxEV, 140, 153, 168) : 70;
const p3 = piecewise(ws.iso || 0, 0.060, 0.130, 0.220);   // CPBL ISO 比 MLB 略低
const p4 = piecewise(hr || 0, 2, 10, 25);                  // CPBL HR 總數較少
```

### 8.4 F：reachChance routineAirBonus 平滑（fielding-engine.js:436）

舊：
```js
const routineAirBonus = wallRatio < 0.78 ? 0.22 : wallRatio < 0.9 ? 0.14 : 0.04;
```

新：
```js
const routineAirBonus = wallRatio < 0.70
  ? 0.22
  : clamp(0.22 - (wallRatio - 0.70) * 0.65, 0.02, 0.22);
```

| wallRatio | 舊（階梯） | 新（平滑） |
|---:|---:|---:|
| 0.50 | 0.22 | 0.22 |
| 0.70 | 0.22 | 0.22 |
| 0.78 | 0.14（**階梯跳變**） | 0.168 |
| 0.85 | 0.14 | 0.123 |
| 0.90 | 0.04（**第二跳變**） | 0.090 |
| 0.95 | 0.04 | 0.058 |
| 1.00 | 0.04 | 0.025 |

設計意圖：保留淺飛球（wallRatio ≤ 0.7）的 routine 加成，但在 0.70 後線性平滑遞減，避免 v1.2 飛球失控的階梯放大效應。

### 8.5 預期效果試算（待 sim 驗證）

| 球員 | OPS+ | v1.2 contact | v1.3 預估 contact | v1.2 power | v1.3 預估 power |
|---|---:|---:|---:|---:|---:|
| 張育成 | 182 | ~75 | ~78 | ~85 | ~82-84 |
| 林安可 | 167 | ~73 | ~78 | ~86 | ~80 |
| 林立 | 151 | ~71 | **82** | ~75 | ~78 |
| 廖健富 | 108 | ~64 | ~70 | ~68 | ~72 |
| 林泓弦 | 56 | ~55 | ~53 | ~58 | ~59 |
| 高聖恩 | 47 | ~52 | ~50 | ~55 | ~56 |

關鍵變化：
- **中強打**（OPS+ 100-150）contact 從 65-72 升到 70-82，解 v1.2 中段埋沒
- **頂級強打** power 從 85-90 降到 80-84，降低 EV 失控
- **弱打** 不變，仍需後續 P2 降低 plateDiscipline 混合或 P3 fielding 對弱擊球修正
- **平均** contact/power 從 60 升到 70，對齊引擎 anchor

### 8.6 未動的事（延後到 v1.4 或之後）

- **議題 1 velocityHandlingPenalty 改時間制**（fielding-engine handleChance）：延後驗證 A+B+F 效果後再決定
- **EV 公式 baseline 92 / 係數 0.68 校準**：延後，v1.3 power 已降，HR 失控應可緩解
- **其他打者 abilities（contact/discipline/clutch 等）CPBL 區間校準**：延後，看 v1.3 sim 結果再決定
- **投手 abilities CPBL 區間校準**：延後

### 8.7 操作步驟（使用者執行）

1. 打開 `tools/stat-mapper.html`（已改 A+B）
2. 上傳 `cpbl_players_2024-2026.json` + 舊 `data.js`
3. 點「開始整合」→ 下載新 data.js 覆蓋遊戲根目錄
4. **強制刷新** `tools/sim-tester.html`（Ctrl+F5，因為 fielding-engine.js 也改了）
5. 跑 99 打者 × 1000 PA，匯出 CSV
6. 對照 §8.5 預期表格驗證：
   - 中強打（林立、陳鏞基、王政順、申皓瑋、朱育賢）sim AVG 是否從 .20-.22 升到 .25-.30
   - 頂級強打 HR% 是否從 7-8% 降到 4-6%（接近真實）
   - 飛球安打率是否從 41% 降到 ~25-30%
   - 高飛安打率是否從 50% 降到 ~15-20%
   - U 型誤差曲線是否解除（dAVG 絕對值整體下降）

### 8.8 驗證後分支

- **若 U 型誤差解除**（中強打回升、頂級不過頭、弱打小幅改善）→ 收尾、寫入修正書、繼續 v1.4 看是否需要動 penalty / EV baseline
- **若中強打仍被埋沒**（如林立 sim < .27）→ 動議題 1 penalty 改時間制（補飛球盲區）
- **若頂級炸彈仍過頭**（HR% > 6%）→ 動 EV 公式 baseline 92 → 88、係數 0.68 → 0.62
- **若弱打仍高估**（如林泓弦 sim > .27）→ 動 plateDiscipline 混合權重 30% → 15%

---

## 9. v1.3 sim 驗證結果與 v1.4 規劃（2026-05-26）

### 9.1 v1.3 sim 結果摘要（CSV: sim-tester-batch-2026-05-26.csv）

A+B+F 落地後跑 99 打者 × 1000 PA：

**修對了**：
- 高聖恩 sim AVG .179 vs 真實 .177（完美對齊）
- 林泓弦 sim AVG .260 vs 真實 .207（+.052，從 v1.2 +.099 顯著改善）
- 廖健富 sim AVG .282 vs 真實 .221（+.061，從 v1.2 嚴重失準回正）
- 林安可 sim AVG .297 vs 真實 .291（接近真實）

**新失衡（U 型曲線變形）**：
- **頂級炸彈過頭加重**：魔鷹 sim AVG .376（HR% **10.10%** vs 真實 4.00%）、吉力吉撈 sim AVG .371（HR% 10.00% vs 4.33%）、張育成 HR% 7.20% vs 真實 3.33%
- **中強打仍埋沒**：林立 sim AVG .239（差 -.109）、吳念庭 .206（差 -.100）、胡金龍 .236（差 -.095）

### 9.2 根因：CPBL JSON summary 區塊資料合計 bug（**新發現**）

從 PowerShell 抽張育成各切片：
```
2024:       PA=182  games=47   AVG=0.27   ballQuality.inPlay=98
2025:       PA=270  games=65   AVG=0.292  ballQuality.inPlay=200
2026:       PA=116  games=27   AVG=0.351  ballQuality.inPlay=80
2025-2026:  PA=116  games=27   AVG=0.351  ballQuality.inPlay=280  ← summary=2026 單季、ball=雙季合計
2024-2026:  PA=116  games=27   AVG=0.351  ballQuality.inPlay=378  ← summary=2026 單季、ball=三季合計
```

**Bug**：Colab notebook 的 `get_slice_data()` 使用 `summary_from_dict(sums[-1])`，只取多季 list 的最後一項（即 2026 單季 summary），沒做加權合計。

但 advancedStats（plateDiscipline / heatMap / ballQuality / spray 等）有正確合計，造成**同一球員的 contact 是用 2026 小樣本（116 PA）算的、power 是用雙季加權（280 inPlay）算的** — 不同樣本量混合進同一球員的 abilities。

這對 2026 有打的球員（張育成、魔鷹、廖健富、吉力吉撈等）造成嚴重失真：
- 引擎用 AVG .351（2026 小樣本，可能高估）+ avgEV 137（雙季加權，含 2025 弱季）
- contact 算高、power 算中等 → sim AVG 可能過頭、power 與真實狀態不符

### 9.3 CPBL ballQuality.avgEV 全聯盟分布（實測，N=40, PA≥100）

```
p10:  130.4 km/h
p25:  136.3
p50:  139.2  ← CPBL 中位
p75:  143.5
p90:  145.7
p95:  147.9
max:  154.3 (朱迦恩)
聯盟加權平均：138.4 km/h

maxEV: p50=168 / p90=177 / p95=183 / max=186 (林安可)
```

### 9.4 CPBL 強打有兩種型態（深入發現）

從 PowerShell 抽 sim 失準球員的 ballQuality.avgEV：

| 球員 | OPS+ | 真實 AVG | **真實 avgEV** | 型態 |
|---|---:|---:|---:|---|
| 廖健富 | 73 | .221 | **153.0** | 強拉型（avgEV 高、contact 差） |
| 朱育賢 | 125 | .240 | 147.9 | 強拉型 |
| 魔鷹 | 151 | .287 | 145.8 | 強拉型 |
| 林安可 | 192 | .318 | 143.3 | 平衡強打 |
| 張育成 2026 單季 | 217 | .351 | **144.2** | 強打 |
| 張育成 2025-2026 合計 | — | .310 | **137.1** | 含 2025 弱季拉低 |
| **林立** | 155 | **.344** | **129.8** | **contact 型強打**（低 EV 高 AVG） |
| 吳念庭 2026 | 123 | .283 | **134.1** | contact 型 |
| 胡金龍 | 142 | .323 | 139.5 | 平衡 |

**關鍵洞察**：林立、吳念庭真實 ballQuality.avgEV 只 CPBL p10-p25（130-135），但 AVG/OPS+ 高 — 靠落點刁鑽、平飛穿越拿安打。v1.3 power 公式用 coreEV 為主，把這群算成 power 70-78（中高），引擎用 power 70+ 推 EV → 飛球被外野手接到 → sim AVG 嚴重低估。

### 9.5 v1.4 規劃（按優先級）

**P0 必做：修 CPBL 管線 summary 合計 bug（待使用者重跑 Colab）**
- 修 `get_slice_data()` 與新增 `merge_summaries_weighted()`
- 加 `MIN_PA_FOR_MULTI_SEASON_INCLUSION = 30` 過濾小樣本季
- 規則：單季用單季；多季合計時排除 PA < 30 的季
- 修完重跑 cpbl_pbp 25-30 分鐘 → 重跑 stat-mapper → 重跑 sim-tester
- **預期**：張育成等「2026 有打」球員的 summary 改用三季合計（PA 568、AVG .297），sim 結果應明顯不同

**P1 候選：B' power 公式主指標改用 ballQuality.avgEV**
- 修 stat-mapper.html power piecewise
- 主指標換成 splits.ballQuality.avgEV（CPBL p10=130 / p50=139 / p95=148）
- 預期：林立、吳念庭 power 從 67-72 降到 55-62，飛球真的「強度不足」，sim AVG 應回升
- 但這要等 P0 修完才知道是否仍需做

**P2 候選：G EV 公式對齊 CPBL anchor**
- pitch-engine.js:650 改 `evKmh = 110 + power × 0.40 + ...`
- 對齊 CPBL p10-p90 實測，偏差 ≤ 2 km/h
- 但對「power 已對齊但飛球仍失控」的根因幫助有限

**P3 候選：E penalty 改時間制（含飛球加 penalty）**
- 補飛球盲區（規則書 §10.6.1 只滾地+平飛有 velocityHandlingPenalty）
- 工作量大，延後

### 9.6 v1.4 執行順序建議

1. 先 P0（修 Colab 管線 summary 合計 bug），重跑全鏈，看 sim 結果
2. 若失衡顯著改善 → 收尾、撰寫修正書 §13
3. 若中強打仍埋沒 → 加 P1（power 改用 ballQuality.avgEV）
4. 若頂級仍過頭 → 加 P2（EV 公式對齊）或 P3（penalty 改時間制）

---

## 10. v1.4 sim 結果與未完成工作清單（2026-05-26 收尾）

### 10.1 v1.4 sim 結果（CSV: sim-tester-batch-2026-05-26 (1).csv）

P0「summary 合計 bug」修對後，跑 99 打者 × 300 PA：

**大勝利：中強打埋沒大幅改善**
| 球員 | OPS+ | v1.2 | v1.3 | **v1.4** | 真實 |
|---|---:|---:|---:|---:|---:|
| 林立 | 151 | .249 | .239 | **.299** | .348 |
| 吳念庭 | 125 | .277 | .206 | **.246** | .307 |
| 胡金龍 | 137 | .275 | .236 | **.293** | .331 |
| 廖健富 | 108 | .211 | .282 | **.264** | .271（**完美對齊**）|
| 林安可 | 167 | .330 | .297 | **.277** | .292 |

**資料合計修對**：張育成 realOPS+ 從 v1.3 的 182（2026 單季）→ v1.4 的 187（兩季加權合計）；其他 2026 有打的球員 OPS+ 微幅變動，合計邏輯正確生效。

### 10.2 v1.4 殘留問題（三類）

**問題 A：OPS+ 100-130 中段強打 sim BABIP 偏低（系統性 -.06 ~ -.10）**
| 球員 | OPS+ | sim AVG | 真實 | dAVG | sim BABIP |
|---|---:|---:|---:|---:|---:|
| 陳傑憲 | 132 | .199 | .300 | **-.101** | .234 |
| 陳俊秀 | 121 | .174 | .262 | -.088 | .233 |
| 申皓瑋 | 118 | .187 | .274 | -.086 | .227 |
| 林政華 | 101 | .166 | .249 | -.084 | .205 |
| 王柏融 | 104 | .173 | .255 | -.082 | .217 |
| 陳晨威 | 127 | .223 | .298 | -.075 | .249 |
| 林泓育 | 122 | .229 | .297 | -.069 | .261 |

共通點：sim BABIP .20-.27 偏低 → 進場球被守備處理掉太多 → 中段擊球 EV 在引擎內過低。

**問題 B：頂級炸彈 HR% 仍約 2x 真實**
| 球員 | v1.3 HR% | **v1.4 HR%** | 真實 HR% |
|---|---:|---:|---:|
| 魔鷹 | 10.10% | **11.33%** | 4.00% |
| 吉力吉撈 | 10.00% | **9.67%** | 4.33% |
| 林安可 | 8.00% | **8.33%** | 4.67% |
| 張育成 | 7.20% | **6.67%** | 3.33% |

**問題 C：弱打 sim 回頭高估**
| 球員 | v1.3 sim | **v1.4 sim** | 真實 | dAVG |
|---|---:|---:|---:|---:|
| 林泓弦 | .260 | .303 | .200 | **+.103** |
| 余德龍 | .295 | .363 | .271 | +.092 |
| 葉子霆 | .234 | .275 | .209 | +.066 |
| 高聖恩 | .179 | .195 | .177 | +.018 |

### 10.3 已落地工作清單（按版本）

- ✅ **v1.0**：原始 contact/power/discipline 公式（用結果指標）
- ✅ **v1.1**（§4）：contact 公式從揮空率 75% 主導改成 AVG + OPS+ anchor 主導
- ✅ **v1.2**（§5）：piecewise lo=40/mid=60/hi=90、pw [35, 95] 拉開分布（被 v1.3 覆蓋）
- ✅ **v1.3 A**：piecewise 標尺對齊引擎 anchor（lo=50/mid=70/hi=90、pw [40, 99]）— `stat-mapper.html:142-153`
- ✅ **v1.3 B**：power piecewise 改 CPBL EV 區間（125/143/155、ISO 0.060/0.130/0.220、HR 2/10/25）— `stat-mapper.html:206-213`
- ✅ **v1.3 F**：reachChance routineAirBonus 平滑階梯 — `fielding-engine.js:436-441`
- ✅ **v1.4 P0**：CPBL_pbp Colab 管線 summary 合計 bug 修復（`merge_summaries_weighted` + `get_slice_data` 重寫 + `MIN_PA_FOR_MULTI_SEASON_INCLUSION=30`）

### 10.4 未完成工作清單（按優先級，未來接手從這裡讀）

#### P0：v1.4 後浮現的三大殘留（最高優先）

**[未完成 #1] B'：power 公式主指標改用 ballQuality.avgEV**
- **解什麼**：問題 A（中段強打 BABIP 偏低）
- **修改位置**：`tools/stat-mapper.html:206-213`
- **新公式**：
  ```js
  const ballAvgEV = bq.avgEV;  // splits.ballQuality.avgEV
  const p0 = isFinite(ballAvgEV) ? piecewise(ballAvgEV, 128, 139, 148) : 70;
  const p1 = isFinite(coreAvgEV) ? piecewise(coreAvgEV, 125, 143, 155) : 70;
  const p2 = isFinite(maxEV) ? piecewise(maxEV, 140, 153, 168) : 70;
  const p3 = piecewise(ws.iso, 0.060, 0.130, 0.220);
  const p4 = piecewise(hr, 2, 10, 25);
  power = pw(p0 * 0.40 + p1 * 0.15 + p2 * 0.10 + p3 * 0.20 + p4 * 0.15);
  ```
- **預期**：中強打 power 微調、sim AVG 提升 +.04 ~ +.08
- **CPBL ballQuality.avgEV 實測分布**：p10=130.4 / p25=136.3 / p50=139.2 / p75=143.5 / p90=145.7 / p95=147.9 / max=154.3
- **風險**：對「強拉但 contact 差」（如廖健富 avgEV 153）反向影響、需重跑 stat-mapper.html
- **驗證指標**：陳傑憲 / 陳俊秀 / 申皓瑋 sim AVG 預期升到 .24-.28

**[未完成 #2] G：EV 公式對齊 CPBL anchor**
- **解什麼**：問題 B（頂級炸彈 HR% 失控）
- **修改位置**：`pitch-engine.js:650-656`
- **新公式**：
  ```js
  evKmh = 110 + effectivePower * 0.40 + (finalContactScore - 50) * 0.28 + N(0, 6)
  ```
- **舊 vs 新**：power 70 EV 145→139（對齊 CPBL p50 138.4）、power 90 EV 159→147（對齊 p90 145.7）
- **預期**：魔鷹 / 吉力吉撈 HR% 從 10% 降到 4-6%
- **風險**：副作用是全聯盟 EV 整體下降，可能讓「中強打飛球」更難變安打（與 B' 反向）
- **驗證**：聯盟飛球安打率（v1.4 仍偏高）、頂級球員 SLG

**[未完成 #3] 降低 plateDiscipline / pitchTypeMatchup 混合權重**
- **解什麼**：問題 C（弱打回頭高估）
- **修改位置**：`pitch-engine.js:526-549`
- **改動**：plateDiscipline 30% → 15%、pitchTypeMatchup 25% → 12%
- **動機**：合計後 PA 變多但弱打 plateDiscipline 的真實比率（如 invalid 區的接觸率）被聯盟平均拉高，造成弱打 sim inPlay 偏多
- **預期**：林泓弦 sim AVG 從 .303 降到 ~.22-.24
- **風險**：對「真實 PBP 統計很可靠」的球員會失去個人化資料權重

#### P1：引擎深層改造（議題 1 系列，先看 P0 結果再做）

**[未完成 #4] velocityHandlingPenalty 改用「處理時間視窗」**
- **解什麼**：規則書 §10.6.1 的設計失真（用 EV 不合理）
- **修改位置**：`fielding-engine.js:447-459`（handleChance 區段）
- **新公式設計（已在 §6 與 v1.4 對話深入推導）**：
  ```
  滾地球：用 ballArrivalSpeedKmh 取代 EV
    penalty = clamp((ballArrivalSpeedKmh - 100) / 6, 0, 12)
  平飛球：用 verticalReactionTime（球到守備員位置時間）
    verticalReactionTime = routeDistanceM / lineDriveSpeedMps
    penalty = clamp((0.85 - verticalReactionTime) / 0.08, 0, 14)
  飛球：用 hangTimeSec（越長越好接，與現行相反）
    penalty = clamp((3.5 - hangTimeSec) / 0.5, 0, 8)
  高飛球（popup）：基本無 penalty（hangTime 通常 5+ 秒）
  ```
- **動機**：物理意義對齊「球給守備員的處理瞬間時間夠不夠」
- **副作用**：補飛球 penalty 漏洞（規則書原本只滾地+平飛有）

**[未完成 #5] 飛球加入 velocityHandlingPenalty（補規則書漏洞）**
- 包含在 #4 的飛球 hangTime 設計
- 進入 handleChance，不進 reachChance（避免重複扣 reachChance 已扣的時間問題）

#### P2：引擎物理優化（長期，影響顯著但工作量大）

**[未完成 #6] 飛行物理升級**
- **現狀**：拋體公式 `v²·sin(2θ)/g × 0.65（固定衰減）`，無真實 ODE / Magnus
- **可能改造**：
  - 加入 Magnus 效應（球的旋轉 spin → 影響飛行 ±5-10m）
  - 真實雷諾數空氣阻力（縫線方向影響 drag coefficient）
  - 溫度濕度影響空氣密度
  - 球的反彈係數隨速度衰減（COR）
- **位置**：`fielding-engine.js:244-360 calcBattedBall`

**[未完成 #7] launchAngle 計算改進**
- **現狀**：依打者 GB/LD/FB/PU 比例抽 ballType → 區間 uniform 抽，不考慮球的位置 / contact point
- **改造**：加入 swing path（高球易打高仰角、低球易打低仰角）
- **位置**：`pitch-engine.js:697-717 _generateAngles`

**[未完成 #8] sprayAngle 用打者真實分布**
- **現狀**：高斯分布 σ=14、pull bias ±6，不讀打者 advancedStats.spray
- **改造**：讀 batter.advancedStats.spray.{left, center, right} 用 categorical 抽
- **位置**：`pitch-engine.js:710-715`
- **影響**：林立 / 吳念庭等 contact 型強打的 BABIP 對應「落點刁鑽」會更準確

**[未完成 #9] 全壘打牆判定優化**
- 加入 ground rule double（球彈出場外 → 自動二壘安打）
- 加入沒收 HR（外野手跳上牆接 HR）
- 球場凹形（CPBL 主場非線性距離）

#### P3：abilities 公式進一步校準

**[未完成 #10] 其他打者 abilities CPBL 區間校準**
- contact（公式已部分對齊但仍有 v1.1 痕跡）
- discipline（chase swing rate 主導，目前 OK 但 piecewise 區間沒對齊 CPBL）
- clutch（RISP_AVG / WPA per PA，未實作）
- vsLeft / vsRight（splits 為主、confidence weighting，未實作）

**[未完成 #11] 投手 9 項 abilities CPBL 區間校準**
- velocity（用 velocity.avg，piecewise 區間需 CPBL 實測：應為 130/142/155 或實測值）
- breaking（用 pitchArsenal 揮空率加權，公式未實作）
- control（用 BB9 + zoneRate，部分實作）
- 其他（stamina / stuff / discipline / vsLeft / crisis）

**[未完成 #12] plateDiscipline / pitchTypeMatchup 混合權重深入驗證**
- 含 #3 但更廣：研究每個權重的影響
- 是否該對「PA 樣本量大」與「PA 樣本量小」的球員用不同權重

#### P4：資料補完（依賴爬蟲）

**[未完成 #13] CPBL 球員基本頁爬取**
- 守位 / bats / 身高體重 / 生日 / 照片 URL
- 用於 player-appearance.js / PLAYER_BIOS

**[未完成 #14] CPBL 守備統計爬取**
- FLD% / E / A
- 補 abilities.fielding / arm 個人差異（目前用守位 baseline）

**[未完成 #15] 跑壘速度資料**
- CPBL 沒 Sprint Speed
- 推導：SBp + 三壘安打數

#### P5：架構長期優化

**[未完成 #16] 顯示/引擎尺度分離（方案 B）**
- 已在 §7 詳細討論
- 新增 `game-params.js` 中 `abilityScaleMap`
- 新增 helper `window.toEngineAbility(player, type)`
- 引擎內 `player.abilities.X` 全面替換為 `toEngineAbility(player, X)`
- 重設字母評級門檻、traits 解鎖門檻
- **動機**：解掉「引入 contact 95 球員會崩盤」的長期問題

### 10.5 建議的下一輪執行順序（v1.5）

1. **先做 P0 #1 B'**（power 主指標換 ballQuality.avgEV）：影響最直接、修中強打、單行改動、不動引擎
2. 跑 sim-tester 驗證
3. 若中強打修對但頂級仍 HR 過頭 → **做 P0 #2 G**（EV 公式對齊）
4. 若弱打仍高估 → **做 P0 #3**（降混合權重）
5. 若三項都做完仍有殘留 → 進 P1（penalty 改時間制）

每輪只動一個地方、跑 sim-tester、看結果再決定下一步。**不要批次改三個** — 失敗難歸因。

### 10.6 給未來接手的 quick reference

| 你想做什麼 | 看哪段 |
|---|---|
| 從頭理解問題鏈 | §0 → §1 → §4 → §5 → §8 → §9 → §10 |
| 看當前最該動的下一步 | §10.5 |
| 看引擎物理 audit（velocityHandlingPenalty、飛行物理等） | §6 |
| 看能力值平移討論 | §7 |
| 看 v1.3 改動細節 | §8 |
| 看 v1.4 資料 bug 修正 | §9 |
| 看 CPBL ballQuality.avgEV 實測分布 | §9.3 |
| 看 CPBL 強打兩型態（強拉型 vs contact 型） | §9.4 |

---

## 附錄 A：可用資料的「能力對應表」速查

| 想算什麼能力 | 用什麼欄位 | 物理意義 |
|---|---|---|
| 打者 contact | plateDiscipline.core.whiffRate（反向）、Whiffp | 球進好球帶你能不能打到 |
| 打者 power | plateDiscipline.core.avgEV、ballQuality.maxEV、ISO | 擊球初速 |
| 打者 speed | SB attempts、SBp、3B rate | 跑壘能力 |
| 打者 discipline | plateDiscipline.chase.swingRate（反向）、plateDiscipline.invalid.swingRate（反向）、core+edge swingRate | 不揮壞球、會揮好球 |
| 打者 clutch | situational.RISP_AVG / AVG ratio、WPA per PA | 情境下變強 |
| 打者 vsLeft/Right | splits.vsLHP.AVG / vsRHP.AVG | 真實對戰表現 |
| 投手 velocity | velocity.avg、velocity.max | 球速本身 |
| 投手 control | BB9、zoneProfile core+edge ratio | 控球精準度 |
| 投手 breaking | pitchArsenal[SL/CU/CH/FS/FC].whiffRate（加權）、zoneProfile.chase.whiffRate | 變化球揮空能力 |
| 投手 stuff | 三能力 + Whiffp | 球質綜合 |
| 投手 crisis | WPA per BF | leverage 表現 |
| 投手 vsLeft | splits.vsLHB.kRate | 對左打 K 率 |

## 附錄 B：與底層邏輯書 / 修正書的關聯

- 本檔處理 **第一部資料鏈**（CPBL JSON → abilities）的失真問題
- 修正書 §1–11 處理的是 **第二、三部投打/守備引擎**的失真問題
- 兩條鏈獨立，但會交互：abilities 改了之後，引擎內所有讀 `player.abilities` 的地方都會受影響
- 改完 abilities，**修正書 §12 的 issue tracker 可能要重跑驗證**（特別是 #1 HR 雙向失準、#4 弱打 K% 過高）

## 附錄 C：實作優先級總結

| 項目 | 優先級 | 工作量 | 影響面 |
|---|---|---|---|
| 投手 velocity 改用 velocity.avg | **P0** | 小 | 變化球派 vs 速球派的引擎物理位置 |
| 投手 breaking 改用 pitchArsenal | **P0** | 中 | 變化球能力的本質定義 |
| 打者 discipline 改用 chase rate | **P0**（用戶點名）| 中 | 引擎 chaseProb 與 BB% R² |
| 打者 contact 改用 core.whiffRate | P1 | 中 | AVG R² 應顯著提升 |
| 打者 power 改用 avgEV / maxEV | P1 | 中 | HR rate 與 SLG R² |
| 投手 control 改用 zoneProfile | P1 | 中 | wild pitch 與 BB9 |
| 打者 vsLeft/Right 改用 splits | P2 | 小 | 左右投對戰 |
| 打者 clutch 改用 RISP/WPA | P2 | 小 | 得點圈表現差異化 |
| 投手 stuff 加入 Whiffp 驗證 | P2 | 小 | stuff 一致性 |
| 投手 crisis 改用 WPA per BF | P3 | 小 | leverage 表現 |
| 打者 / 投手 fielding-arm | P4 | 大（需爬 CPBL）| 守備能力差異化 |

P0 三項建議優先做。做完跑 sim-tester 驗證是否大幅改善 R²。
